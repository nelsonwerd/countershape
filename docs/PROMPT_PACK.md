# Countershape execution prompt pack

## Active U7 execution pack

The sole current operational cursor is the visible `countershape/u7-unit-paths/v3` capsule at the beginning of `## Current state` in `docs/HANDOFF_MODE_C.md`. Exact sealed U7C commit `cbc908d240a433b703827f6c5ae397be63588d1a`, tree `2dd1fb793d0cf118d23581598b497d3cce00b5c9`, is the parent. U7D is the active exact 15-path `SOURCE_FULL` cross-domain evidence boundary with subject `test: close U7 reference study evidence`; it has product authority `U7_REFERENCE_APPLICATION`, product behavior `CANDIDATE_UNRECEIPTED`, receipt `ABSENT`, and 13 `UNRECEIPTED` claims.

The amended execution chain is `C6B -> U7P -> U7M -> U7A -> U7B -> U7N -> U7C -> U7D -> U7R`. U7P sealed the original authority/checker/harness/runbook bootstrap. U7M owns only the successor-verifier repair. U7A supplies the CLI foundation; U7B sealed the HTTP falsification study. U7N owns only the second control amendment and has zero new product surface. U7C is narrowed to the independent CLI decision study plus one exact enrolled exit-0 official reference execution. U7D performs bounded cross-domain process/artifact closure whose mixed authority leaves HTTP finalized-run and classification absent. U7R alone reconciles sealed U7D source evidence into a three-path receipt projection. U7D receipt state is `ABSENT` through U7D and becomes `PRESENT` only at U7R. The U7P-origin/U7N-amended v2 harness observes direct subject processes, Git/worktree state, artifact bytes, and bounded repetition/resource facts; product-semantic equivalence, HTTP target/run/classification, generalized CLI execution, and whole-harness resource use remain outside that observation authority.

This split supersedes P08's older one-boundary execution wording without rewriting its historical premise. Every child has an exact parent, roster, subject, verification profile, product ceiling, receipt state, final root, ordered command manifest, explicit event index, and exact claim pathspecs. A child may satisfy that declaration but cannot add or widen its own authority. Do not begin a child until its exact parent has committed, sealed, exposed its note, passed explicit-commit strict verification, emitted an HTML witness, and moved its ledger into the commit-addressed archive.

Both topology amendments are `OWNER_OUT_OF_BAND`, `UNEVIDENCED`, unauthenticated, and unsigned; sealed P08 is bound only as `PROSE_ONLY` roadmap direction. U7M is classified `OWNER_AUTHORIZED_AUTHORITY_MIGRATION_DEFECT_REPAIR`; U7N is classified `OWNER_AUTHORIZED_CLI_AND_MILESTONE_AUTHORITY_RECONCILIATION`. Those authority records can select repository topology but cannot transfer grades, prove inherited behavior, authenticate the owner, or create product evidence. All frozen P07/C6 sections below are historical compatibility contracts and do not select the U7 cursor.

U7P retired two inherited historical replay entrypoints without rewriting sealed history, but its ordinary live C3P wrapper was still coupled to ambient C6R/C6B HEAD subjects. The first U7A cumulative event proved that every U7 descendant must fail that row. U7M retains all three checker entrypoints as historical-only and replaces their current enforcement with the exact successor-compatibility adapter. U7N preserves that repair unchanged and reruns it as claim 2. The failed U7A ledger and candidate checkpoint named in the U7M status remain permanent.

U7N also repairs two future-facing authority errors without changing runtime capability. The exact enrolled CLI reference stimulus selects normal exit `0`; the fixture's separate exit-`7` behavior is substrate coverage, not an enrolled official execution. A clean signal is an eligible completion whose missing selected exit code contradicts the normal-exit tuple; an invalid overlay is refused before spawn; runtime escaped-write behavior remains unproven. Sealed U7B produced direct HTTP bundle-process facts but explicitly did not produce target-bound HTTP finalized-run or classification authority. Therefore the only honest future milestone is `LOCAL_TWO_DOMAIN_PROCESS_ARTIFACT_GREEN_CLI_OFFICIAL_EXECUTION_HTTP_DIRECT_PROCESS_ONLY_FULL_STUDY_RESOURCE_UNRECEIPTED`, with fallback `ONE_DOMAIN_OFFICIAL_CONTRACT_EXECUTION_PLUS_HTTP_DIRECT_PROCESS_OBSERVATION_ONLY`.

Active U7D executes fresh same-ordinal HTTP and CLI workflows through the unchanged sealed code surfaces. Each installed route requires its package-owned handler to succeed; app then closes the retained Node runner, captures the checked bytes, and reopens them unchanged while closing the workspace before an opaque collector may publish that run. Callers cannot construct or submit snapshots. A collector retained across `3+3` application invocations establishes only package-handler validation plus two matching terminal artifact-byte projections. Event 8's unchanged v2 harness separately owns the actually executed six-process aggregate, cross-run freshness observation, exact bounded verdict, and honest fallback. No parent run, artifact, event, claim, or grade is reused. `internal/reference/reproduce` cannot classify HTTP or CLI outcomes, establish harness process/executable/stdout/environment/resource authority, reconstruct missing HTTP finalized-run/classification authority, generalize the enrolled CLI execution, or turn direct process/resource observation into a full-study resource claim. U7R alone may replace the exact pending-source blocks with the sealed source receipt.

Use only the generated U7 runbook for final-ledger lifecycle. Its prepare preflight runs from the stable private `.countershape` control root rather than the final root whose absence it proves; only after that check may the runbook explicitly create the unit parent and all six children as exact mode-`0700` directories. One root-owned writer owns repository, didrun, claim, Git, seal, recovery, and archive mutation for the whole attempt. Do not enter the next fence or mutate candidate/evidence/recovery state until every yielded didrun wrapper and relevant child is confirmed process-terminal; empty or yielded app output is not terminal evidence. If terminality or recorded interval non-overlap cannot be established, stop normal progression, make no mutation while a process may remain live, and after terminality use only the matching generated recovery fence. The runbook's success rotation is a restartable no-follow same-device transition and is valid only when no failed-attempt marker/ref exists for the candidate commit and the committed index plus tracked/untracked worktree remain exact before and after the move. A nonzero evidentiary command, claim, commit/preflight step, non-entropy seal failure, or later closure permanently ends that ledger. The sole exception is the generated manual checkpoint after a plain seal refuses solely on didrun's high-entropy heuristic: only a fresh claimed zero-finding scoped credential scan plus deliberate exact-inventory review may authorize uncommenting the redacted `--allow-secrets` command, whose note must disclose `secrets_override: true` and which never proves secret absence. Run the rendered precommit recovery for failures before commit, or the exact-full-commit recovery for every other seal/note/strict/HTML/archive failure; both preserve rather than delete evidence, and the postcommit path pins a failed ref before a compare-and-swap back to the declared parent. Never reuse the failed ledger or hand-author a cleanup.

**State:** phase-independent execution contract. The delimited capsule under `## Current state` in `docs/HANDOFF_MODE_C.md` is the sole live phase/receipt cursor; unit sections and pre-seal statuses here are bounded instructions or historical snapshots and may remain immutable after their owners seal. Every declared boundary through C6N is sealed. The permanent prerequisite edge is `C3B → C3D → C4V → C4M → C4N → C4P → C4 → C4H → C4I → C4K → C4J → C4L → C5 → C5V → C6A → C6F → C6G → C6M → C6N → C6R`; C6R was the historical exact-seven-path `SOURCE_FULL` note-consumer projection repair. Older active and blocked phrases are historical compatibility text. Prompt outcomes remain design requirements until the owning status file or a later independently sealed, delimited handoff/receipt descendant names the exact strict-clean commit and verbatim receipt grades; an intentionally pre-seal status cannot self-receipt.

Historical anchors remain explicit: C3P defines the pre-C3 runtime-epoch prerequisite, and sealed C3D directly followed sealed C3B commit `3b0b56d2bdd36531caf31e3dbad555aca061616d`. These statements preserve ancestry; they do not make either historical boundary active.

The next two dense inventory paragraphs preserve v19/C4N and v20/C4P wording required to replay sealed source contracts; every use of “active C4N” or “active C4” inside them is historical, not the operational cursor. The historical v30 P07 authority snapshot is `countershape/p07b-c-unit-paths/v30` with 44 ordered unit rows and 32 receipt-phase rows, exact sealed C6A followed by sealed C6F, sealed C6G, sealed C6M, sealed C6N, and sealed C6R. Its exact replay rulings include the source-era sentences “C6R is the live exact-seven-path `SOURCE_FULL` note-consumer projection repair” and “C6B remains blocked until C6R seals”; both quotations are retained solely for inherited P07 verification and neither describes current repository state or selects the live cursor.

# C6R — bind the observed sealed C6N note projection

C6R was the exact seven-path `SOURCE_FULL` defect repair with subject `fix: bind sealed C6N note redaction projection`; its owning contract is `docs/status/P07B-C-C6R-NOTE-CONSUMER-MAINTENANCE.md`, and its roster digest is `sha256:3f815ac91f817fba618f24d307919dd9e97aec0f142a5cee1c9e86b284e06169`. Its historical sealed tail was `C6A → C6F → C6G → C6M → C6N → C6R → C6B`. Its direct parent was sealed C6N commit `096842954c9e6ae72b66010fff81e90cf0bd7a22`, tree `69845d09f85abf76b275b16c0d941fbd568ee907`, note blob `b66c7b38bad6c8ea94288f2cd8afa87e76f0c831`, and note-body SHA-256 `68abfb23fe35e2d2844e0a1622f7c4566f08789a29d16dd170c02b0e7046ef11`.

Preserve the source-authored C6N prospective contract exactly as historical authority: `sha256:500955d69b63da43516312788befa6642ecbdcb6e7777ac014f5beeeb5beb41b` binds `LEGACY_DOUBLE`. Add the separately named observed sealed-C6N projection `sha256:cc851cc157036b09ae84739220db65bdc6593d0f78644d5a61fe789d6a93ffc4`, which binds `SUFFIX_PRESERVING` only after both checkers pin C6N's exact commit, tree, parent, subject, note blob, and note-body digest. The prospective C6R contract is `sha256:bc4dffd3b0a393eefbb7d3947a1ad9ca7a391a7a2a332113a893e0682a88cfde`. Cross-form substitution, a bare marker, a foreign suffix, positional drift, and note-identity drift must fail closed; no rule may reinterpret or reseal C6N.

The historical C6R runbook included `--verify-c6r-sealed-c6n-note` before the generic scope/verifier gates and ended with `--verify-c6r-preseal-ledger`. Receipt C6A remained `ABSENT`; the source-authority manifest was inherited byte-for-byte, no receipt projection was added, and all ten C6R grades remained `UNRECEIPTED` until its exact commit sealed and passed strict verification. Only then could C6B restore and reconcile the already-preserved receipt draft.

# C6N — bind the sealed C6M note-redaction epoch

Historical sealed snapshot: this section is frozen v29 source-era authority. C6N sealed at commit `096842954c9e6ae72b66010fff81e90cf0bd7a22`; its pre-seal tense does not supersede the later sealed C6R snapshot or the live U7 cursor.

C6N is the exact nine-path `SOURCE_FULL` defect repair with subject `fix: bind C6M note redaction epoch`; its owning contract is `docs/status/P07B-C-C6N-NOTE-REDACTION-MAINTENANCE.md`, and its roster digest is `sha256:012c7559efd28dce5dac7fb56881bbddd8f326f89eac4d32a6773ad8cfe856af`. Its frozen source-era tail is `C6A → C6F → C6G → C6M → C6N → C6B`. Its direct parent is sealed C6M commit `b0de83dca3510102af4d1cddae2b2023bc88a87d`, tree `3d80a02443209f5d5649fcba92b79b72a2a04a15`, note blob `d08739e2f5bb3d761d5962081242924ae9cb7f36`, and note-body SHA-256 `1bcf1b4563561d6a07db9394bb6b25b2e7d2ca126df18b98f0c8596cc960a487`.

Both independent checkers make the GOCACHE preview epoch an exact boundary input. C6A/C6F/C6G remain `SUFFIX_PRESERVING`; exact C6M and prospective C6N are `LEGACY_DOUBLE`. Neither form is a global fallback, raw exact previews remain admitted, and cross-epoch substitutions must refuse. The pinned sealed-source contract digests are `sha256:a7f3696229c63afe11911980d7b21873b21d5c06e50207f8d0883b2379e707ea` for C6M and `sha256:500955d69b63da43516312788befa6642ecbdcb6e7777ac014f5beeeb5beb41b` for C6N.

The C6N final-seal repair must preserve both permanent final reds plus the 39-event development red at `.didrun-history/p07b-c-c6n-development-red-plan-selftest-timeout-0f2ce2b787a9/.didrun/`, replace the sealed replay runner's 567-plus-567 per-blob Git subprocess fan-out with one exactly framed `cat-file --batch` extraction and one absolute-path `hash-object --stdin-paths --no-filters` parity batch, and retain in-process SHA-1, snapshot/path/mode checks, the 60-second Git deadline, the frozen sealed child, and no retries. `verify-current-selftest.mjs` must pin the batch commands, runner hostile oracle, and narrow C6F-derived Darwin diagnostic classifier. Command 6 proves that runner self-test through the cumulative verifier self-test; command 7 exercises both real sealed-C6A rows. Neither focused development evidence nor any failed final or development attempt can substitute for the fresh C6N ledger.

The plan-self-test repair is count-preserving. Keep its unchanged `1,200,000` ms outer bound and every existing case, diagnostic, console contract, claim, receipt transition, and verifier row. After one ordinary uncached baseline succeeds, an invocation-scoped authority snapshot may reuse only immutable real Git/note results guarded by exact canonical root, active boundary, receipt/C6-manifest bytes, Git and Node executable identities, `HEAD`, index tree, and notes tree; configured Git must resolve to the `/usr/bin/git` used by sealed-C6 derivation, explicit fixture injections win, mismatches follow the original path, and a complete terminal recapture is mandatory even after body failure. Parse the same qualification modules and exact entry fixture once per exact ordered path-to-byte input and Node identity, require full input equality after the typed digest lookup, and cache successes only. Limit the wrapper to the current C6M/C6N/C6B horizon. Do not add retry, deadline, case removal, case sharding, semantic relaxation, claim changes, receipt changes, or verifier-row changes.

The first repaired diagnostic ledger is preserved at `.didrun-history/p07b-c-c6n-plan-throughput-green-26a91ed4bb6c/.didrun/`: the complete plan self-test passed on staged tree `26a91ed4bb6ca1a13b75d7934975f7e6e75a4859` in `164.872573` seconds with its public markers unchanged, approximately 86.3% below the outer ceiling. It remains unclaimed because documenting the measurement creates a new candidate tree. Re-run the focused self-test and whole cumulative verifier on that final tree; never inherit the diagnostic pass.

Preserve the later operator-command red at `.didrun-history/p07b-c-c6n-development-red-bare-sealed-c6-tool-authority-c804f6ed8a00/.didrun/`. Its bare `--c6` invocation correctly refused absent `COUNTERSHAPE_NODE`; do not weaken that refusal or hand-approximate the hermetic environment. Discard the ledger's six earlier greens as non-reusable and run the complete cumulative verifier from command entry so its qualified runtime constructs and exercises both sealed-C6A rows.

The historical C6N runbook included `--verify-c6n-sealed-c6m-note` before the generic scope/verifier gates and ended with `--verify-c6n-preseal-ledger`. Receipt C6A remained `ABSENT`; the source-authority manifest was inherited byte-for-byte and no receipt projection was added. The frozen pre-seal sentence “C6B remains blocked until C6N seals” is retained only as source-era authority; C6N, the then-intervening C6R repair, and C6B are now sealed.

# C6G — isolate C3U historical C6 fixtures

This exact heading is frozen v28 source-era compatibility text. C6G, C6M, C6N, C6R, and C6B are sealed historical boundaries; the visible U7 capsule alone selects the live operational cursor.

`C6A → C6F → C6G → C6M → C6B` was the exact v28 source-era tail. C6G was a seven-path `SOURCE_FULL` defect-repair boundary with subject `fix: isolate C3U historical C6 fixtures`; its source-era authority, scope, ten-command manifest, nonclaims, and continuation remain in `docs/status/P07B-C-C6G-C3U-HISTORICAL-FIXTURE-MAINTENANCE.md`. It sealed at commit `3677b45d75204ce2ef7242abb5edd11cc049934e`, tree `520ad2cbf2d3da8354b02afd1cd2a7cd5df82d7d`, note blob `df26876b86d89689d9125531a816b92b7db97afb`, with 10/10 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`.

The blocked C6M plan self-test proved that `syntheticC3AbsentPlanFixture()` reapplied ambient `...current` state after historical C3U reconstruction. The repaired renderer installs the same explicit four-path absence authority already defined by C6F, whose ordered digest is `36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218` and whose 11-case defensive manifest is `983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`, after the ambient spread and validates it before use.

The ordinary full-plan path then runs a nine-case real-renderer oracle: one passing baseline, four omitted projections, and four reopened descendant values. All eight hostile cases refuse, and the ordered oracle digest is `c591e1874545674f0579db04d731ce5c366c408e99d3cd27eb63ffbef3ad4bf1`. C6F remains valid because its sealed claim was bounded to the C3Q installation and explicitly excluded other historical `checkPlan()` inputs.

Both independent checkers froze C6G's sealed-note contract at `sha256:3dfffad546f7fee36e2e51856cea98a754626f395f319b0c6264fe918df3ef00`. Its final ledger proved exact sealed C6F plus sealed C6A ancestry with `--verify-c6g-sealed-c6f-note` and closed after `--verify-c6g-preseal-ledger`; those modes did not receipt C6G before its own commit and seal.

## Sealed C6F historical authority-fixture maintenance — frozen v27 snapshot

Under its sealed source contract, C6F was a twelve-path `SOURCE_FULL` defect-repair boundary with subject `fix: isolate historical C6 authority fixtures`; its complete authority, scope, claim ceiling, ten-command manifest, and frozen continuation contract remain in `docs/status/P07B-C-C6F-HISTORICAL-AUTHORITY-FIXTURE-MAINTENANCE.md`.

The fixed C3Q/C3B historical regression must never read descendant C6 authority from the ambient worktree. Its checker-owned ordered absence roster is exactly `docs/captures/p07b-c/c6a-evidence-summary.json`, `docs/status/P07B-C-C6-EVIDENCE.md`, `spec/verification/p07b-c-c6a-receipt.json`, and `spec/verification/p07b-c-c6a-source-authority.json`, at `sha256:36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218`. The exact eleven-case catalog—baseline, four omissions, four non-sentinel substitutions, one foreign path, and one reorder—is pinned at `sha256:983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`; the actual C3Q override path must install that exact fixture before the historical plan check.

The live B checker must invoke exact C1 with `--c1`; it must never inherit the C checker's legacy no-argument full-C6A behavior. The verifier must label exactly `architecture-p07b-c-c6` and `architecture-p07b-c-c6-selftest` as `SEALED_C6A`, route them through `tools/check-sealed-c6a-architecture.mjs`, and keep every other row live. The runner pins sealed commit/tree/notes authority and raw 567-entry manifest `sha256:a6c028ab3302ba215d16c90f3a27cb0583e631584382f7a70bfaed59de8cfe52`; it may use only literal blob materialization, exact mode/OID checks, and a private detached Git control directory. Both independent checkers also derive the same future C6F sealed-source note contract at `sha256:84227e43b4534cc6977d5b5d274ee415f118af98c36e3e390c86d585d11aa5fb` and reject positional command-tail or redaction-shape drift. C6F validates exact sealed C6A with `--verify-c6f-sealed-c6a-note`, runs the full verifier roster once as live-current verification plus two explicitly labeled sealed replays, and closes its ten-event ledger with `--verify-c6f-preseal-ledger`. It introduces no manifest or receipt projection, changes no product/runtime source, and cannot relabel those two replay rows as current C6F architecture evidence or grade C6A or itself.

The first final ledger's command 8 exposed status-zero Git inventory carrying exact managed-Darwin Apple diagnostics after commands 1–7, including the full cumulative verifier, had passed. Preserve that attempt at `.didrun-history/p07b-c-c6f-final-f6c30a8f641a/.didrun/`; do not reuse its claims. The repaired scope checker admits only empty stderr or the bounded three-form Darwin grammar already pinned by the C6 evidence reader, with no leading UTF-8 BOM, otherwise-fatal UTF-8, and exact LF framing; every other stderr byte, nonzero status, signal, or spawn error remains fatal. C6F later closed through a separate repaired ledger.

# C5V — harden final verification hygiene

C5V is the sealed historical owner-authorized `SOURCE_FULL` repair between exact sealed C5 commit `240060f018d5b0e86914bd27361c5899ac9ba1c0`, tree `4906ff4af407fa4e48cbf569f7e456694660ca06`, and sealed C6A. Its exact subject is `fix: harden final verification hygiene`; the frozen authority, claim ceiling, live-era S6 incidents, and twelve-command manifest remain in `docs/status/P07B-C-C5V-VERIFIER-PREFLIGHT-HYGIENE.md`.

Before evidence mutation, the generated preparation preflight refuses any no-follow-visible `active.lock`, live `.didrun`, exact final-root residue, or nonignored `.DS_Store`. Before archival, the separate preflight still refuses `active.lock` and nonignored `.DS_Store`, requires the live `.didrun` and every final directory to be canonical private directories, and refuses an existing exact commit-addressed archive root. The repair also adds a terminal Finder-artifact row before verifier authority revalidation/finalization. Historical modes `--verify-c5v-sealed-c5-note` and `--verify-c5v-preseal-ledger` retain the exact sealed-C5 and twelve-event contracts. C5V subsequently closed; its earlier pre-seal `UNRECEIPTED` wording is frozen history and does not select the current cursor.

C4P sealed at commit `bbda662314fa3584859ad38f1baeb86ac1f09b5f`, tree `2819dfb90d23da0e89a17f92528338930b73f32c`, with note blob `0dbc8868d07eac3fa19a5cfab2a9b584d07209e8`, note-body SHA-256 `4a022906c3d4d4be4a6f32396537cd5c8fac0e2072f06b093b40f7fc8d0e3e75`, `10/10 TREE-EXACT`, strict exit `0`, and `secrets_override: true`. C4 later sealed at commit `c4089ab31181c08dad880bf9e5d40c622c8c91c4`, tree `f427e23b6fe297a49a36a99fa037073b637d82ca`, with 80/80 `TREE-EXACT` and strict exit `0`. C4H then sealed at commit `4b4686ea30e5ddc5043eee11ba24725bcce8bb94`, tree `2d02d220961f3fc41b2372df5eb8dc3aac47a529`, note blob `9532c545dd685046efd57e999613092f92e54e8d`, note-body SHA-256 `e94c4b8b48978f0208757f7f7707bde5c02b0cb7c91eebaa16873e9aaf1b5041`, with 11/11 `TREE-EXACT` and strict exit `0`. C4I sealed at commit `d784ace97dd03660c6c724e1cb8f838b869681ec`, tree `e38b702884b9df128b41bfa7710459dd92f901d7`, note blob `f1633147e2943e7272abf3b9b903ac72ab274494`, and note-body SHA-256 `f221a99aaa1c0037d0de7ebe8c311bfd0ff1653434c4042f30b00a67d9c4e033`, with 17/17 `TREE-EXACT` and strict exit `0`. C4K sealed at commit `8c1ee2df955055ee40eec1f14f0c4d96f97d5361`, tree `99fe7082d05c96647a144785e0ddc5af1a09f52f`, note blob `2d039109e269968e2314934fb4e2c4cccb31a097`, and note-body SHA-256 `cc276493b0d67bb9903d30591aaa99ed29df09d33154bed2f81faef717435766`, with 12/12 `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. C4J sealed at commit `c12c927d94e6c56529a2fb90148674b4b4e731a5`, tree `c99c1f394e02f17f8cf9de6b18f1a949bcaa07a8`, note blob `f76ca73665f8cd3f19fdb9c7d4537462390a2200`, and note-body SHA-256 `a6b6191547efccf168d18932d559cba5a896c8dcde1f64ede72a27844f495cf1`, with 13/13 `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. C4L and C5 subsequently sealed at commits `32911730afb7cc717b8e0a164b9ea0038d83f3fa` and `240060f018d5b0e86914bd27361c5899ac9ba1c0`, with 13/13 and 79/79 `TREE-EXACT` grades respectively and strict exit `0`. Those receipts bind their own boundaries only. C5V later sealed at commit `e7f51c0a8fbd2d6fbe8eacb4a7f0d46f010af7ba`, tree `65bdfc39b48c3b9b1b832a12cc9be5e0083ea5c4`, with 12/12 `TREE-EXACT` and strict exit `0`.

The sealed C4V machine scope is `countershape/p07b-c-unit-paths/v17`, with 19-row authority digest `sha256:11f96a7809d845ef21671d8f847bd3b9f324e84c539cd2f93ab2bc14929ecd46`. Sealed C4M advanced only the phase/checker authority to `countershape/p07b-c-unit-paths/v18`, with 20 rows at digest `sha256:dd134e870c11ed4e80eb8335680b59a0ed1553c45d04746fd2fdd722f3f975a4`. Active C4N advances that authority to `countershape/p07b-c-unit-paths/v19`, with 21 rows at digest `sha256:8f237c7d883c212167e5a04e5dd24d6efdacf4a9ce5492215fd06892a8237c4a`. Sealed C3 retains its exact forty-path roster `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`. Sealed C3R retains exactly seven paths at `sha256:fe14d9f9985771cce0525edfca47c001b5912dacc4c2d6fef182458d3b44f58e`. Sealed C3Q retains exactly seven paths at `sha256:f5f27e661c1ace6516aba4b9d5766f4c6ab50b6602dbcb101bff9b6a4ebfe6fc`; its historical machine scope was `countershape/p07b-c-unit-paths/v13`, and its sealed-tree refusal registry covered 42 plan-authority Markdown paths at `sha256:16d63155a00c9e87b7eec79707ad6d47d6f5d70205d18243b8bc687e401e4b66`. C3T owns exactly seven paths at `sha256:496788860db6c42f5acc50de0de02be8a63bcb8e11f99fb24d59d9c0e016477e`; its historical machine scope was `countershape/p07b-c-unit-paths/v14`, and its sealed-tree C3T/C3B self-receipt authority covered 43 paths at `sha256:52e78638d70f4e229e0b54c0a8311b6e0d1d2acc0d4e439cacf10cde65c62959`. C3U owns exactly seven paths at `sha256:7af7456929b0db08abc2a31bd1cb62d84002c1a6bcdda3f4a8b4896f6b8c30cd`; the exact C3U/C3B 44-path corpus at `sha256:1c5bf120f1ab948586bfa594e6ae634fc6acf7a5544febcccb2c4dc475a59a49` and its form/control manifest at `ef4b8250cb0fbb666598b1926d3d17349b48ef913adced41831776821e99703c` are frozen historical evidence. Sealed C3B remains frozen to three paths at `sha256:3ef17156a2f3051171da70983f9428de2b9bdb5a179602407a36f34200012a67`. Sealed C3D owns seven empty-prefix checker/schema/documentation paths at `sha256:ac4436680c612ad206d2786ddd3146af747e1f8ea2bdedf898afca67d46fcd63`; C4V authors the exact eleven-path maintenance boundary at `sha256:535a64fbb9f0d3c3672b4b8197fc7815da315117b1794a668e6565faa16df3f1` and may not absorb Go product source. C4M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4M-HISTORICAL-RECEIPT-FIXTURE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:ec49dc68ce8fcfe92720fd2e8d6afa6d19d6d1c26aa9d27173dc2df736d06a87`. Its intended subject was `fix: close descendant historical receipt fixtures`. C4N owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4N-SELF-RECEIPT-CARDINALITY-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:95de7e71ca2df4a57d0544c3197bb7d917359bb3d9ecfc51a2130973faf36e6e`. Its intended subject is `fix: derive live self-receipt cardinality from phase authority`.

C3D introduced a phase table and generic fixture generator for structural behavior, while four fixed semantic regressions preserve `C3R_NOTE_PREVIEW`, `C3Q_EXTERNAL_SELF_RECEIPT`, `C3T_VALIDATED_ABSENT_BASELINE`, and `C3U_TERMINAL_RECEIPT_LAYERING`. Sealed C4V extended that design without changing sealed C3D history. Sealed C4M repaired partial descendant-corpus transport; its historical plan matrix has 8 accepted and 45 rejected snapshots, while its historical independent scope matrix has 8 accepted and 28 rejected snapshots. Its pre-seal cursor said “C4 remains blocked until C4M seals”; that sentence is retained only as historical compatibility text. C4N accepted the remaining consolidation proposal: all forward edges, exhaustive invalid predecessors, specification ownership, and per-phase refusal cardinalities derive from the validated table and an independent code-owned forward allowlist. Its historical v19 plan matrix is 9 accepted and 232 rejected; the independent matrix is 9 accepted and 219 rejected. The historical generic self-receipt catalog has 840 cases at `a414d55463202a2edea6b132eb1259e83430ad10c0695ecbb729ac3441c3c19c`; six fixed semantic regressions make 846 executable cases at `0aa97e46bd3a24c9238b6bd46a574234ea9b798d72175a46d4190049a5910595`, with 447 accepted controls and 399 hostile rejections. The separate eight-row phase-extension cardinality catalog is pinned at `c5e5d421e0dab5709ef8356dc8c25421a0effff8f2698efea0da9e9d74d23761`. Its pre-seal cursor also said “C4 remains blocked until C4N seals”; that sentence is retained only as historical compatibility text. Retaining those semantic oracles is `INTENTIONAL_HYBRID`; replacing them with row-only tuples remains `DECLINED_WITH_REASON`.

Historical C4 preseal contract: Active C4 owns the exact 38-path roster plus three required prefixes declared below. Its implementation adds neutral copy-safe process mechanics, the sole official-target/admission runner, a detached spawn-adjacent reopen after owner acquisition, exact CLI process/scope closure, candidate-written bounded invocation evidence, multiplicity-preserving private manifests, durable-release revalidation, FCR publication, and classification-only recovery. The historical physical reference remains `CONTRADICTS`; a second real ruling over the same enrolled tree produces exact two-field `CONFORMS`. That contract is now sealed with all 80 C4 grades `TREE-EXACT`; its earlier `UNRECEIPTED` wording remains historical only.

The quoted `48-path active C4N` phrase in sealed history remains a v19 compatibility anchor. Historically, sealed C4P is the exact 49-path plan-authority surface at `sha256:d901a41a7b01febb9bf6f0f9d4823056e9fdb1109746a5adf132877b5ae791be`, with 765 rejections and 255 controls; C4 derived the 50-path surface at `sha256:3375ec014b2c977d8d27f052b0eb516b119e00b47c775afaf6c33ff757336c9e`, with 780 rejections and 260 controls. The v20 nine-phase aggregate was 7,020 rejections and 2,340 controls plus nine full-plan controls. Sealed C4H expanded the authority corpus to 59 paths with 915 rejections and 305 controls. Sealed C4I expanded it to 60 paths with 930 rejections and 310 controls; sealed C4K expanded it to 61 paths with 945 rejections and 315 controls; sealed C4J expanded it to 69 paths at `sha256:a53acdeef0e86b4865caab7822ea06cfa62742794ff1aaa5816023ef172cf901`, with 1065 rejections and 355 controls. Sealed C4L expanded it to 70 paths at `sha256:5894c9e05456cacff20e2f0095547b032befb13e7aea850583df3972f10fbc07`, with 1080 rejections and 360 controls. C4M remains frozen at 47 paths with 735 rejections and 245 controls, and neither C4N nor C4P leaks its status path backward. Historically, C4V retained 46 sorted Markdown paths at `sha256:b480bb8e93d9ddd6075a57bdf8dbe99cb663eb1f0a11228af31698b54a5dd2f2`.

The sealed C4V stability ruling remains explicit historical authority: its 68-claim replacement gate moved direct build, vet, and general-package jobs to `p=1` after the declared recurrence fired, while preserving `GOMAXPROCS=2`, test `-parallel=2`, sensitive/nested serialization, fresh caches, the single-verifier lock, every timeout, all tests, and ordering.

C4V predeclares both remaining product boundaries that otherwise could not safely authorize themselves. C4 owns 38 exact paths at `sha256:99bda0d3e5c2f2a2ee4f354945188bf2e5563716c9e26511211a4bfc9197bf56` plus three realized prefixes at `sha256:f5f7b8cdfeba3581e8632c0d6686287f5afa00aef224bb4babe39ee1fdc7f4b2`, and has an 80-command final runbook ending `--verify-c4-preseal-ledger`. Among C4's five exact additions, the new shared verification authorities are `spec/verification/p07b-b-future-surface-authority.json` and `tools/verify-runtime-authority.mjs`; the latter is the immutable Node execution-primitives layer used by both verifier entrypoints. C5 owns 12 exact paths at `sha256:d16ba74582e53dc5c791b8c34fbcd6200338dc066ec13230c2908eb6ba257bf5` plus three realized prefixes at `sha256:a9212680066fdbddc8f25eef04a41c264ac9148a506238dd92c39978e3e35f84`, and has a 79-command final runbook ending `--verify-c5-preseal-ledger`; its exact roster deliberately includes `docs/VERIFICATION.md`. C5V and C6A likewise own that manual whenever they evolve `tools/verify-current.mjs`. C5 owns its C architecture checker and self-test, but owns neither the B future-surface manifest, the B checker/self-test, the phase plan/scope checker, the phase specification, repetition helper, nor runtime authority; it may only satisfy the parent-sealed rows/topology.

C4 is also the final writer of the inherited B checker before C5. The named admitted states remain `C4-present/C5-absent` and `C4+C5-present`. C4V seals the independent future-surface schema/scanner at `sha256:91c4ca13a4b30a683b82066fdcdc1a7892de2679a2e5419a57142e36b22f5937`, its canonical baseline policy at `sha256:5a1c3e2f33969143765612f4934ce4ca41cc1e3df95383b8d7780354461e3399`, the exact live-and-strict-scanned 27-row sealed-C3D historical inventory at `sha256:901d7cb5ca65cb9cb1ca7932ece1741fbfc82df5f9e375d43fadd6e057615960`, and the allowed locations, but not C5's final row inventory. C4 closes that open instance decision: canonical `spec/verification/p07b-b-future-surface-authority.json` contains 13 observed C4 rows plus seven nonempty C5 rows at 2,248 bytes and `sha256:2e7c0e9fc530d75e5b8c6cd1cab5d270d5e8ce3e260cda37c4363b3e6c02f49a`. The C5 rows lock `http/evidence.go`, `http/runner.go`, `http/runner_darwin.go`, and `http/runner_unsupported.go` to the exact reserved-family identifiers required by the mirrored HTTP closure path. C5 preserves that file and the B/plan checkers unchanged and must match the combined real tree. Runner versus HTTP/scope topology is validated separately, so C5-without-C4, partial/mixed presence, missing/extra/relocated/lookalike rows or reserved tokens, and schema/digest/order drift fail. Checker-owned forward fixtures prove only validator/state-machine behavior, not future source existence or conformance. The one-known-row `sha256:4ebfb5ae2608f0cdaa2e316e087fd8c8e479a891e7e52d0a7aab7f9a45541807` is `TEST_VECTOR_ONLY/INCOMPLETE`.

C4 must seal its exact 54-case live catalog at `sha256:3be703fae10155c83b19be54ae7cd79cfc5b1bbaddfcf86bb2c9781ad93f853a` and the dormant exact 56-case C5 catalog at `sha256:08db7c338eb3ba900cf6df2545c04f27bed16889c81dd16e5fb18197c4d52958`. The helper exposes recursively frozen `qualificationMatrices`, the C4 identity aliases, exact digests, and stable `qualificationCaseForID(caseID)`. Its exported route is parse → canonicalize → validate → build → run: `parseRunArguments(argv)` directly selects an exact `--case <id>` and has a fail-closed ad-hoc preamble; `validateExecutionSpecification(specification)` reselects through the stable selector and rejects non-identical named objects; `buildGoTestArguments(specification)` returns the exact frozen Go argv; and `runRepetition(specification, dependencies = {})` begins with exact canonical validation and uses only `executionSpecification`. `main()` ends with `await runRepetition(parseRunArguments(process.argv.slice(2)))` before an exact Node-import-bound direct guard awaits it.

At live C4/C5, `qualificationStaticAuthority` decodes the recursively frozen literal catalog into a checker-owned projection and never imports or evaluates future candidate helper bytes. The static gate does not execute candidate parser, selector, validator, or builder exports; actual execution evidence is the separate helper self-test, per-case receipts, and preseal reconciliation. During C4, the helper self-test drives both dormant C5-only identities through the exported parser, selector, validator, exact argument builder, and dependency-injected run entry without runtime acquisition or absent-package execution; unknown IDs, wrong flags, extra arguments, copied specifications, raw alias lookup, and builder drift fail before child execution. The 54 C4 and 56 C5 cases run as separate `--case <id>` didrun events and immediate claims, so every receipt has its own lock, admitted authorities, fresh roots/environment, child result and Go JSON check, cleanup/finalization, and terminal evidence. Through C4V the lazy loader instead requires legacy helper SHA-256 `1321fb1381f1f74d0959b9ceb40b4dcc4abc637bb281ff2af759863ede3bfd5a` before import. Every generated final runbook sets `umask 077` and calls `--verify-final-runbook-parent-authority` through sterile `/usr/bin/env -i` before preparation and again before archival; C4I additionally sets `umask 077` inside every isolated fence, and sealed C4K, sealed C4J, sealed C4L, sealed C5V, sealed C6F, and sealed C6G preserve that per-fence rule. `--verify-c5-sealed-c4-note` keeps its byte-frozen argv and, when C5 is active, starts at its dynamic sealed C4L parent, reopens exact sealed C4J, C4K, C4I, and C4H before C4's exact source diff and 80-claim note, then independently validates `C4L → C4J → C4K → C4I → C4H → C4 → C4P → C4N → C4M → C4V → C3D` under one stable outer HEAD; C5 cannot rewrite that consumer.

At preseal reconciliation, the checker independently derives the exact ordered `[go,node,git,sh,cc,cxx]` roster from frozen `COUNTERSHAPE_GO`, `COUNTERSHAPE_NODE`, `COUNTERSHAPE_GIT`, `COUNTERSHAPE_SH`, `COUNTERSHAPE_CC`, and `COUNTERSHAPE_CXX`; it realpaths each, requires an executable regular non-symlink file, performs a bounded no-follow raw-byte read of at most 512 MiB with before/after identity, SHA-256 fingerprints it, and requires Node to equal the invoking checker. Every 54/56 stdout blob must match that roster; the terminal digest is SHA-256 of UTF-8 `JSON.stringify(roster)`; the roster is reopened after all blobs; and a self-consistent foreign stdout roster fails. This is bounded preseal-time admission plus post-blob re-admission and recorded output, not continuous executable-byte identity between execution and reconciliation, across events, or against same-uid swap-and-restore/TOCTOU.

The ancestry phrase `C4 → C4N → C4M → C4V → C3D` retained in sealed-v19 sections is historical. Sealed C4 reopens `C4 → C4P → C4N → C4M → C4V → C3D`; current C5 authority reopens `C4L → C4J → C4K → C4I → C4H → C4 → C4P → C4N → C4M → C4V → C3D`. The legacy CLI `--verify-c4-sealed-c4n-note` remains reachable only for historical v19 regression compatibility; sealed C4 used the C4P-specific predecessor mode, while C5 retains its byte-frozen argv and traverses through C4L, C4J, C4K, C4I, and C4H internally.

**System:** Countershape, a narrowed Darwin reference instrument for comparing exact Git candidates in declared fresh comparison envelopes, preserving an exact candidate-to-projection-fingerprint map, reducing one typed witness under named local rules, recording one locally caller-attributed exact-witness ruling record with human authenticity outside canonical proof, translating exact adapter projections into a portable selected-field ruling, and emitting a standalone Node contract only when a separate byte-complete source witness reconstructs that ruling's exact world.

**Operating persona:** Soren Vale—compiler and distributed-systems builder, creative-tools founder, allergic to hidden authority. Agents are speculative co-processors; disagreement is evidence of missing intent; a refusal with provenance is more useful than a confident fiction. Build something unusually coherent and tactile, but never spend correctness or trust language for spectacle.

## Sealed C4P maintenance insertion — historical contract

C4P owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4P-FUTURE-SURFACE-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:5dc083fadb60003b0ba96516d0a01f91daa88b7298b142018b5eda00e48ab59b`.

C4P's sealed subject is `fix: make B future-surface self-test phase-aware`. The current chain is `C3B → C3D → C4V → C4M → C4N → C4P → C4`. Its `countershape/p07b-c-unit-paths/v20` table has 22 rows at `sha256:6d7e41d5ff22fc73118ceb0ae9f27a2151e7121d3a91c8945a391b3f42868f7d`. The plan matrix has 10 accepted and 266 rejected cases; the independent matrix has 10 accepted and 253 rejected cases. A table-relative selector maps pre-C4V rows to `NONE`, C4V-through-pre-C4 rows to `BASELINE`, C4-through-pre-C5 rows to `C4`, and C5-or-later rows to `C5`. Both ambient and synthetic validation use it. The strict synthetic baseline and all 42 direct hostile cases stay intact, while routed controls cover every row, wrong-epoch state, unknown phase, and partial C5 topology. C4 remains blocked until C4P seals.

Future-note replay keeps the two observed didrun GOCACHE redaction epochs disjoint at hermetic position 6. C4V/C4M allow raw or their sealed legacy double marker; C4N/C4P/C4 allow raw or one marker plus the exact `.countershape/.../gocache` suffix. At GOCACHE position 6, the other epoch's form, a bare marker, or a foreign suffix fails; other prefix positions retain their own fixed projection rules, and command-tail redaction fails. New receipts therefore cannot use the older opaque projection to hide a wrong cache root.

The v20 generic structural catalog contains 902 cases at `d52bdf89855bb306c844103851452a439534e3042e4741bed8787386da529770`. Six fixed semantic regressions extend it to 908 executable cases at `5c2b8ecb64483ed999b817c74fe6751b2dbd689edf3c0740fbceb920c6f7f203`; the independent nine-row phase-extension catalog is `6ac375496ed3a160375e5f55258299daad807d9047bc2af4b9be46d2058c4e8c`.

The receipt-fixture consolidation is routed explicitly: `DEFECT_ALREADY_REPAIRED` for the generic structural generator already delivered by C3D/C4N, `INTENTIONAL_HYBRID` for generic structure plus fixed semantic regressions, and `DECLINED_WITH_REASON` for replacing those semantics with a four-field row tuple. C4P repairs only future-surface phase selection; it does not encode those semantics as shallow row flags or delete a hostile case. Its independent 22-row phase-to-mode oracle is pinned at `sha256:e042018eedc9ff7b78abc79361d6a824d0d4b4080e510f5b76336a3b39c8ff34`, and every `BASELINE` forward fixture carries an explicit frozen historical inventory instead of reading ambient C4 source.

## Sealed C4H verifier-hermeticity maintenance — historical contract

C4H is sealed at commit `4b4686ea30e5ddc5043eee11ba24725bcce8bb94`, tree `2d02d220961f3fc41b2372df5eb8dc3aac47a529`, with note blob `9532c545dd685046efd57e999613092f92e54e8d`, note-body SHA-256 `e94c4b8b48978f0208757f7f7707bde5c02b0cb7c91eebaa16873e9aaf1b5041`, 11/11 `TREE-EXACT`, and strict exit `0`. The owner-tense v21 text below is preserved as sealed pre-seal history.

C4H is the owner-authorized `SOURCE_FULL` maintenance boundary between exact sealed C4 and unchanged C5. Its subject is `fix: harden verifier execution boundary`. Its authority source is `OWNER_OUT_OF_BAND`; C4 did not predeclare the new edge, owner authorization is not cryptographically authenticated, and C4H inherits or rewrites no sealed grade.

C4H owns exactly these 18 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4H-VERIFIER-HERMETICITY-MAINTENANCE.md`, `research/deep-dive/p07b-c-maintenance-architecture/00-scope-and-method.md`, `research/deep-dive/p07b-c-maintenance-architecture/01-proof-semantics.md`, `research/deep-dive/p07b-c-maintenance-architecture/02-verifier-throughput.md`, `research/deep-dive/p07b-c-maintenance-architecture/03-maintenance-order.md`, `research/deep-dive/p07b-c-maintenance-architecture/04-synthesis.md`, `research/deep-dive/p07b-c-maintenance-architecture/05-follow-up-verification.md`, `research/deep-dive/p07b-c-maintenance-architecture/06-different-model-red-team.md`, `research/deep-dive/p07b-c-maintenance-architecture/07-executive-briefing.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/generate-p07-planning-example.mjs`, `tools/verify-current-selftest.mjs`, and `tools/verify-current.mjs`. Their sorted-newline roster digest is `sha256:1adc913036078ad4efdf418e78581d81369aa630d245a5b493c60a69a0478532`.

The `countershape/p07b-c-unit-paths/v21` authority has 23 ordered phase rows and preserves `C3B → C3D → C4V → C4M → C4N → C4P → C4 → C4H → C5`. Its plan transition matrix has 11 accepted and 302 rejected cases; the independent unit-scope matrix has 11 accepted and 289 rejected cases. C4H's current Markdown authority corpus has 59 paths, 915 hostile rejections, and 305 positive controls. Its 11-command runbook repairs bounded Git diagnostics, caller-umask-exact planning fixtures, canonical verifier entry refusal, and real subprocess lock/recovery coverage, then runs one cumulative verification pass plus the three exact scope/credential/preseal commands.

Proof sharding is `DECLINED_WITH_REASON`: C5's existing terminal claim is one exact 79-event ledger, and the current authority has no legal witness-reuse receipt transition. Composite Proof Protocol v2 is `DEFERRED_WITH_REASON` until after C6B. C5's 79 labels, types, argv, order, 56 cases, and three cumulative passes do not change. C5 remains blocked until C4H seals.

The owner-tense C4I section below is frozen v22 pre-seal history. It remains byte-oriented compatibility evidence and does not supersede the active C4L cursor in `docs/HANDOFF_MODE_C.md`.

## Sealed C4I verification-surface stability maintenance — historical snapshot

C4I was the owner-authorized `SOURCE_FULL` repair between exact sealed C4H and unchanged C5. Its subject was `fix: stabilize verification surfaces`. Its authority source was `OWNER_OUT_OF_BAND`; C4H did not predeclare the new edge, `predecessor-predeclared = false`, the owner instruction was not cryptographically authenticated, and C4I inherited or rewrote no sealed grade.

C4I owns exactly these 14 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4I-A2-AST-STABILITY-MAINTENANCE.md`, `internal/world/process_darwin_test.go`, `spec/verification/p07b-c-unit-paths.json`, `testkit/processfixture/main.go`, `tools/capture-p07b-a2-human-surface.mjs`, `tools/check-p07b-a2-architecture-selftest.mjs`, `tools/check-p07b-a2-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:522af4209bde3f769a12c3943f46bd83e566977ad8f275165310962c1e697585`.

The `countershape/p07b-c-unit-paths/v22` authority has 24 ordered phase rows at `3176e9a6764203afeed0ae836bd1a4974ae63887dbee8f0c1de1f66f2c93f580` and preserves `C3B → C3D → C4V → C4M → C4N → C4P → C4 → C4H → C4I → C5`. Its plan transition matrix has 12 accepted and 340 rejected cases; the independent unit-scope matrix has 12 accepted and 327 rejected cases. C4I's Markdown authority corpus has 60 paths, 930 hostile rejections, and 310 positive controls.

The checker still reads and hashes all six JavaScript profiles, but only drifted profiles enter the AST worker. Returned rows must match the requested paths exactly and in order. Clean and non-JavaScript cases require zero AST rows; each of the pinned 32 relevant mutants requires one exact singleton row; one hostile requires one exact ordered three-profile batch. `P07B_A2_JS_AST_QUERY_FAILED`, malformed/duplicate/out-of-order rows, and the synthetic precedence failure are fatal infrastructure failures before expected-hostile matching.

The second final attempt exposed a separate deterministic caller-context defect: the human-surface writer requested `0644`, inherited the mandated `umask 077`, created `0600`, and then correctly refused its own drift. C4I now admits that already-pinned A2 profile and proves exclusive create, exact `0644` normalization, collision refusal, caller-umask restoration, and private-root cleanup under `077` and `022`. This named test fixture does not claim hostile same-UID resistance, general materialization, or product security authority.

The third final attempt exposed a separate world-test causality defect after one complete cumulative pass: its second cumulative pass could terminate the original group before the nested `setpgid` child published its PID. The repaired test-only fixture atomically publishes canonical hash-bound `PREPARED`, `AUTHORIZED`, `READY`, `RELEASE`, and `RELEASED` records; cleanup authority plus an independent original-group observation precede escape authorization, exact stream markers plus independent post-escape PGID/SID checks prove readiness, and the child acknowledges the exact release before voluntary exit. Its self-lease begins before `PREPARED` is visible and avoids unsafe numeric-PID fallback. The unchanged `100 ms` timer starts before the callback and is never reset, although the callback can delay physical teardown while it establishes the test precondition. This is verification-fixture ordering, not a containment or wall-clock enforcement claim.

The final proof was deliberately post-load: one direct human-surface caller-umask self-test, three standalone A2 self-test passes, exact `world-lifecycle-readiness-20`, and three complete cumulative passes on one staged tree, along with candidate/scope/plan/ancestry/self-test/stage/credential/preseal gates in one 17-event ledger. Direct/general, sensitive, and nested Go work remain at `p=1`; `GOMAXPROCS=2`, test `-parallel=2`, fresh private caches, the exclusive lock, fixed verifier deadlines, and cumulative order remain exact. C4I changed Go test/test-fixture bytes but no production process implementation, cache policy, parallelism, package partition, production or verifier deadline, lock, assertion, or cumulative order. C5 remained blocked until C4I sealed; its 79 labels/types/argv/order, 56 qualification cases, and three cumulative passes remained unchanged.

## Sealed P07B-C C4K C3 Go-timeout maintenance — historical snapshot

C4K sealed at commit `8c1ee2df955055ee40eec1f14f0c4d96f97d5361`, tree `99fe7082d05c96647a144785e0ddc5af1a09f52f`, note blob `2d039109e269968e2314934fb4e2c4cccb31a097`, and note-body SHA-256 `cc276493b0d67bb9903d30591aaa99ed29df09d33154bed2f81faef717435766`, with `12/12 claims recorded-exact`, every grade `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. The owner-tense and `UNRECEIPTED` statements below are frozen v23 pre-seal history retained for compatibility; they do not supersede active C4L.

C4K is the owner-authorized `SOURCE_FULL` prerequisite after exact sealed C4I. Its subject is `fix: bound C3 architecture test timeout`. The v23 machine edge is `C4I → C4K → C5`, but owner direction parks the protected C4J payload before C5: C4J remains parked and C4J is not machine-declared until it restores itself in v24 after C4K closes.

C4K owns exactly these 11 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4K-C3-GO-TIMEOUT-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-architecture-selftest.mjs`, `tools/check-p07b-c-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:9ab25f722aeaae583d08df271d6162c873254c51799b36b03fc85ff64d78567e`.

The `countershape/p07b-c-unit-paths/v23` table contains 25 ordered rows. Only exact `c3-official-target` receives `-timeout=12m`; the other four C3 profiles and all six C4 profiles receive no inner timeout flag. The nested ceilings are `720 s → 900 s → 1080 s → 1200 s`: Go package, profile process, clean C3 self-test, and unchanged cumulative-verifier child. Every level has diagnostic margin, with no retry, package-partition, parallelism, assertion, cache, or cumulative-order change.

The final proof uses a 12-event ledger: nine test claims and three command claims. It runs the two candidate gates, plan self-test, exact sealed-C4I ancestry, unit-scope self-test, focused C3 checker, C3 defensive self-test, verifier self-test, one cumulative verifier, exact 11-path source gate, credential scan, and preseal reconciliation. Every C4K grade is `UNRECEIPTED` until the exact tree commits, seals, exposes a readable note, and passes strict verification.

## How to run this pack

Execute one prompt at a time, in order, in a fresh chat with the repository root as the working directory. A prompt is independently resumable but not independent of prior artifacts: it must read the brief, `docs/SEMANTICS.md`, `docs/PROJECTION_ALGEBRA.md`, latest handoff, owning research, current source, and prior sealed receipt. Those two semantic documents are mandatory reads for every downstream unit even if an older prompt's inline read list omits them. Do not paste all prompts into one context. Do not begin the next prompt until the current prompt's latest commit is sealed and `NO_COLOR=1 didrun verify --strict` exits 0.

The living authority order is:

1. `docs/CONCEPT_BRIEF.md` for the locked product and truth contract;
2. `research/deep-dive/11-p07-implementation-red-team.md`, then `08-RED_TEAM.md` and `07-SYNTHESIS.md`, for implementation-time corrections, cuts, and blocker reasoning;
3. the current prompt for unit ownership and acceptance;
4. `docs/HANDOFF_MODE_C.md` for observed repository state.

An implementation-time red-team correction section inside a prompt supersedes conflicting older prompt prose when that correction narrows authority, is covered by the planning validator, and is committed and receipted with the owning unit. Broadening authority or changing acceptance scope requires a separately receipted planning-contract change.

If these conflict, preserve the narrower truth claim and record the discrepancy before editing. Never silently broaden scope.

## Non-negotiable operating contract

`CandidateExecutionKey` is only a wire/display reference; opaque `CandidateExecutionBinding` plus the actual matching `WorldPlan` allocates structural `WorldInstance` identity. Runtime facts belong to `InstanceMeasurements` and edge receipts. `AssessComparison` persists one exact admission matrix per repetition or a pre-batch rejection. Candidate batches may share a map only with the same ordered admission-digest set. Reduction first checks full-map comparability and only then exact eligible labeled-map equality; a naked digest is never sufficient. U1 owns logical decisions, not reduction grades. P07B later-target execution must not reuse this 2–4-candidate chain: it uses a separate capability-only `ContractExecutionTarget` over an explicit directly materialized pinned/verified Git tree, fresh attempt, exact `DARWIN_KERN_BOOTSESSIONUUID_V1` capability, and admitted Node runtime; a store-private operational interlock plus intent-only StartClaim and process-local RunPermit; a target-bound `FinalizedContractRun` with separate process and `COMPLETE | PARTIAL | VIOLATED` scope axes; then a classifier-profile-bound `ContractExecution`. The boot owner reads `kern.bootsessionuuid` twice through a bounded fixed-name Darwin system-call edge, validates each 36-character hyphenated nonzero UUID, lowercases each sample, compares the normalized values, and exposes no raw-value constructor or clock/process/file/subprocess/PATH fallback. C4 remeasures before admission. Equality/change is only an OS-reported epoch fence, not reboot, child-absence, clone-resistance, VM non-rollback, kernel-trust, or safe-reset proof. Only the higher contract runner may consume official target/admission authority; low-level process mechanics never sees the permit. Generic CAS bytes, TAP, copied digests, caller-authored boot values, leases, or mutable semantic latest/status records confer no result authority. Any prompt language to the contrary is superseded.

### Product and novelty boundaries

Countershape candidates are trusted local code executed with the user's full permissions and host network access. Private roots, sparse environments, caps, and process groups improve repeatability and cleanup; they are not a sandbox. The golden path has no model-generated probe, label, ruling, explanation, or test. A real external model may be human-gated only for captured human-facing build artifacts: A2.2 generated README/CLI renderings, the U8 screenshot critic, and the P12 live-agent didrun audit. Pause immediately before any real provider call, obtain human authorization, and never simulate it. Critic opinion has no semantic authority.

Wake may produce immutable candidate refs and inert provenance. Countershape does not launch or recover agents, consume Wake events, manage fleets/effects/gates, replay/fork sessions, or become a friendlier Wake runtime. didrun remains an external command-receipt authority. Countershape never recreates its capture, grading, sealing, or freshness logic and never upgrades a didrun grade into product conformance.

Sealed C4 private evidence uses a deliberately shared persisted-body design and is pre-admitted: two 16 MiB channels, shared capture framing, projection framing, and bounded summaries produce a 48 MiB maximum-unique-body envelope before the run owner exists. Drain/Captured share one raw framed body; every actual draft is capped at 15 logical kinds/14 unique blobs and checked before persistence. Candidate-writable evidence/scope/attempt rosters are descriptor-bounded to exact cardinality plus one, and production cleanup removes only retained named leaves and proven-empty roots. Foreign residue is refused or marked ambiguous and remains unswept. Future prompts must not reintroduce base64 duplication, eager `ReadDir`, recursive cleanup, or claims of containment/secure deletion.

This is a repository-scale composition with acknowledged prior art, not a new disambiguation algorithm or semantic primitive. Prohibited claims include winner/best branch, deterministic or compatible worlds, semantic equivalence, smallest/root cause/global minimum, correctness/safety/security approval, anonymous/unbiased adjudication, safely shareable report, universal portability, or a grade summarized beyond its verbatim meaning.

### File and collaboration discipline

Inspect `git status --short` before every unit. Existing changes belong to the user or another active lane; preserve them. Respect the exact ownership in each prompt and record unavoidable cross-unit edits. Use `apply_patch` for hand edits. Do not use destructive Git commands, amend or erase sealed history, commit `.didrun/`, add AI co-author trailers, push, publish, release, or open a PR unless the user separately authorizes it.

Parallel agents are bounded read-only auditors and critics because they share the same filesystem and didrun ledger. The owning root is the sole repository writer and sole didrun, claim, Git, and seal operator. Parallel speed never relaxes the sequential unit gate, and parallel didrun writers are forbidden.

### didrun discipline—every shippable slice

Every load-bearing verification command runs as `didrun run -- <actual command>`: format, lint, unit/property/state/mutation tests, race tests, builds, fixtures, package smoke, Playwright, axe, Lighthouse, visual capture automation, security matrices, performance studies, external critic, and final audits. Read-only inspection need not become a product claim. An unwrapped check is `UNRECEIPTED`.

At each shippable boundary:

1. run the genuine final verification commands through didrun against the intended tree;
2. immediately after each final successful verification command, declare only the supported truthful claim without explicit event addressing; do not batch claims after later events, because didrun 0.1.0's observed default-event behavior can otherwise bind a claim to the wrong command;
3. stage the exact unit, review the diff and secret/private-artifact exposure, and commit;
4. seal that commit with didrun;
5. loop on `NO_COLOR=1 didrun verify --strict` until exit 0.

A nonzero strict exit means the unit is not done. Read the exact `failed`, `unknown`, `stale`, or coverage reason; repair the real cause; rerun affected verification through didrun; declare new honest claims; create a new commit; reseal; and reverify. Do not weaken or delete a test, claim, threshold, fixture, receipt, or old commit. Failed receipts on old commits are permanent history. `tree-exact` and `scope-exact` describe evidence/tree binding, not independent truth. Any capability without its own receipt is literally `UNRECEIPTED`.

### Build-loop and human surfaces

Unit tests do not substitute for exercising the real CLI, browser, generated contract, or local report. CLI copy, error states, documentation, studio, and exports are load-bearing. U8 requires repeated build → see → exercise → check → critique → rebuild passes, full 1440×900 and 375×812 state coverage, Playwright/axe/keyboard/zoom/forced-color/reduced-motion/injection checks, and a real different-provider screenshot critic. P11 separately inspects the packaged binary and exported report. Manual taste observations remain manual; they are never converted into a didrun grade.

### Continuation and honest cuts

Use files as memory. Update `docs/HANDOFF_MODE_C.md` at every sealed boundary and before any context-risk stop with: exact branch/commit/tree, completed prompt, changed files, commands/events/claims/verbatim grades, active processes, artifacts, findings, unresolved risks, next prompt, and the first exact command to run. Never make the user reconstruct state from chat.

If a stop condition fires, commit and receipt the narrowest honest fallback rather than inheriting the full claim. Allowed labels include truth-kernel prototype, observation-only instrument, one-domain instrument, DecisionRecord-only residue, CLI-only surface, or observation/approval prototype. Market demand, user comprehension, review compression, hostile-code security, cross-platform runtime, production operations, legal clearance, and maintainership cannot be produced by this pack.

## Ordered prompts

| Prompt | Shippable slice | Core outcome | Gate before advancing |
| --- | --- | --- | --- |
| [P00 — U0 controlling contracts](prompts/P00-U0-CONTROLLING-CONTRACTS.md) | U0 | schemas, examples, states, budgets, threat/fixture contracts, claim vocabulary | contract validators and examples strict-clean |
| [P01 — U1 truth kernel](prompts/P01-U1-TRUTH-KERNEL.md) | U1 | strict canonical authority, candidate binding, structural world/measurement/admission sums, tagged eligibility, comparable exact maps, ruling invariants, properties/mutants; no reduction grade | no subprocess until all required truth mutants die under the root-serialized gate |
| [P02 — U2 Git/world substrate](prompts/P02-U2-GIT-WORLD-SUBSTRATE.md) | U2 | filter-free regular/executable materialization and fresh Darwin process ownership | hostile Git/process fixtures strict-clean; no containment claim |
| [P03 — U3 CLI observation](prompts/P03-U3-CLI-OBSERVATION.md) | U3 | typed one-shot CLI stimulus/capture/projection, rotated repeats, control taxonomy | stable/flake/incomplete/control fixtures exact |
| [P04 — U4 HTTP observation](prompts/P04-U4-HTTP-OBSERVATION.md) | U4 | typed local HTTP readiness/request/capture/projection and contamination proof | same truth algebra; no HTTP coercion in generic kernel |
| [P05 — U5 bounded reducer](prompts/P05-U5-BOUNDED-REDUCER.md) | U5 | typed terminating neighbors, tri-valued new evaluation, comparability-first exact labeled-map preservation, store-issued durable sweep grade | shape/digest traps, changed basis or eligibility, unresolved, crash/corruption refusal, final sweep, both domains |
| [P06 — U6 Choicepoint store](prompts/P06-U6-CHOICEPOINT-STORE.md) | U6a | bounded semantic object store, fixed Darwin CAS spine, physical FreshConfirmation, strict Choicepoint/Decision codecs, blind identity/support omission, and noncompilable REFINE refusal | exact physical evidence, full-token CAS-before-publication, canonical body-field plus valid-example parity, blind leakage/alias/order attacks, and explicit successor/staleness nonclaims |
| [P07A — U6 portable ruling authority](prompts/P07A-U6-PORTABLE-RULING.md) | U6b | strict translation of unchanged CLI/HTTP projection bytes into adapter-bound portable tuples, real selectable/differing fields, selected-only custom expectations, and legacy nonportable preservation | binding/profile substitution, historical-wire malformed corpus, selected-field physical studies, restart compatibility, race/vet/architecture, and semantic mutation closure |
| [P07B — U6 standalone residue](prompts/P07-U6-STANDALONE-CONTRACT.md) | U6c umbrella — historical sealed chain | A1, A2.1, accepted corrected A2.2, complete B publication/materialization, and P07B-C through sealed C6R/C6B are historical authority; none selects the live U7 cursor | historical compatibility only; each subunit advanced only after its own seal and strict-clean closure |
| [P07B-A1 receipt](status/P07B-A1-SOURCE.md) | U6c/A1 — sealed | exact self-consistent PortableSource reconstruction, closed logical-Node runner profile, and portable child-bind HTTP Observation | source commit `1e56da3bfafc`, 15/15 verbatim `TREE-EXACT`; no ruling join or compiler |
| [P07B-A2 plan](prompts/P07B-A2-COMPILATION-PLAN.md) | U6c/A2 design lock | separates current-ruling compilation authority from the pure recoverable compiler | preserve all A1 ceilings and stop conditions |
| [P07B-A2.1 receipt](status/P07B-A2-1-AUTHORITY.md) | U6c/A2.1 — sealed | current-validated ruling snapshot, retained confirmation execution-binding roster, exact source/ruling/proof joins, and one sealed wrapper with privately retained original authority plus a separate authority-narrowed input; no generated files or store writes | exact receipt map and nonclaims; A2.2 is the sole next source boundary |
| [P07B-A2.2 — recoverable compiler](prompts/P07B-A2-2-RECOVERABLE-COMPILER.md) | U6c/A2.2 — sealed | pure deterministic compiler, strict recoverable six-file ContractBundle, fixed Node-core second implementation, manifest bootstrap, explicit ceiling proof, and shared Go/Node semantic corpus; no residue publication or product materializer | accepted corrected source `6bf33350944dcdd56a4ceaa70f1d0836203ffb1f` plus sealed receipt-document boundary; source-rewrite design remains `UNRECEIPTED` |
| [P07B-B — terminal publication/materialization](prompts/P07B-B-TERMINAL-PUBLICATION.md) | U6c/B — sealed | opaque current-ruling transition, durable terminal ContractBundle residue, full predecessor-chain restart reconstruction, and retryable exact six-file native materialization; no target or execution | source `eb06bdcf18f8e14db1257e73733afdc05cac045e` plus final handoff `464e47adbf7f4497dfafa89938a9539239ffd41b`; boundary is note-present and strict-clean |
| [P07B-B status](status/P07B-B-TERMINAL-PUBLICATION.md) | U6c/B receipt ledger — sealed | exact source commit/tree, verbatim grades, negative history, and explicit ceilings | historical receipt authority; C1 inherits B only through the current fail-closed checker chain |
| [P07B-C — target/run/execution](prompts/P07B-C-TARGET-RUN-EXECUTION.md) | U6c/C — historical sealed chain through C6R and C6B | C1/C2 inert semantics and persistence, C3 target authority, C4 CLI execution, C5 HTTP/full-scope execution, C5V verification hygiene, C6A cumulative evidence, C6F/C6G fixture isolation, C6M portable source authority, C6N replay hardening, and C6R's sealed-C6N note binding are historical sealed authority | C6R and its C6B continuation are sealed history; this row neither blocks continuation nor selects the live cursor |
| [P07B-C C1V throughput ruling](status/P07B-C-VERIFICATION-THROUGHPUT.md) | U6c/C1V — verifier maintenance | C1V was `SOURCE_FULL` and is sealed; it declines the unsafe Go-only persistent cache premise, conditionally tiers direct general work, adds fail-fast verifier ownership, and preserves sensitive/nested isolation | three cumulative passes, focused 50x/20x stability, material benchmark, exact 11-path scope, commit/seal/note/strict closure before C1E/C1B |
| [P07B-C C1E local-evidence maintenance](status/P07B-C-C1-LOCAL-EVIDENCE-MAINTENANCE.md) | U6c/C1E — checker maintenance | validates didrun's flat SHA-256 capture blobs and redirected Git loose objects as disjoint fail-closed namespaces; keeps C1B's four-path receipt authority unchanged | mixed-layout hostile self-test, real sealed-C1 archive match, full cumulative gate, exact 7-path scope, commit/seal/note/strict closure before restoring C1B |
| [P07B-C C2M receipt-phase checker maintenance](status/P07B-C-C2-RECEIPT-PHASE-MAINTENANCE.md) | U6c/C2M — sealed historical checker maintenance | preserves both C2 receipt phases in every plan self-test after the first valid C2B draft exposed phase-stale hostile mutations; C2B later reconciled the source evidence | full dual-phase hostile self-test, cumulative gate, exact 7-path `SOURCE_FULL` roster `sha256:75dcb6a88a9faec8aa89cfbe67c0870e88d95709a1bf07e2fb494e92362688cd`, commit/seal/note/strict closure before C2B |
| [P07B-C C3P runtime-epoch prerequisite](status/P07B-C-C3P-RUNTIME-EPOCH.md) | U6c/C3P/C3PB — sealed source and receipt | rejects the retired clock-derived profile, freezes exact `kern.bootsessionuuid` semantics plus direct single-target/C3 store-surface corrections, declares separate C3B receipt authority, and cumulatively enrolls the dedicated receipt checker | source exact 25-path `SOURCE_FULL` roster `sha256:8b047704df047cb96f1c5d19418c4cfe8502e4b4746bb4f24c0851dac8d018c0`, `12/12 TREE-EXACT`; C3PB commit `13369122ba7d5557eba1949095c1135a41843070`, `7/7 TREE-EXACT` |
| [P07B-C C3V throughput-proposal reconciliation](status/P07B-C-C3V-VERIFICATION-THROUGHPUT.md) | U6c/C3V — sealed no-delta maintenance; historical compatibility text: active no-delta maintenance | routed the stale observer proposal against sealed C1V: declined Go-only persistent cache and p8 escalation with reasons, retained qualified p2/sensitive tiering, acknowledged the existing fail-fast lock, and preserved the existing narrow receipt profile | commit `725933fa3c7826a281e84b51f729736cbcfac6ec`, `8/8 TREE-EXACT`, strict exit `0`; exact roster `sha256:6cc5d8fe929f0f1dfa6ffc223d2e8bcee5ba4596a09caa9dc032103bbb5bafbd`; verifier and repetition runner unchanged |
| [P07B-C C3M receipt-phase checker maintenance](status/P07B-C-C3P-RECEIPT-PHASE-MAINTENANCE.md) | U6c/C3M — sealed checker `DEFECT_REPAIR` | historical C3M boundary: kept the C3P absent-receipt fixture phase-stable after interposed descendants by reopening only the sealed C3P status and preserving that boundary's handoff minus at most one exact receipt block; C3PB remains unchanged in that historical contract, while the later sealed C3-era reconstruction admitted only C3-active state | exact seven-path `SOURCE_FULL` roster `sha256:58e1f6fba95cf3b4312136eea5bb5c64af89a6958711aca84181a1874aa03240`, `8/8 TREE-EXACT`, strict exit `0` |
| P07B-C C3PB source receipt reconciliation | U6c/C3PB — sealed exact receipt boundary | binds only the already-sealed C3P note and exact local snapshots; it claims no cumulative verification, runtime behavior, or target authority | exact three-path `RECEIPT_RECONCILIATION` roster `sha256:531cbe600cf2747752749890ea9a15c3d0126e498da6838d4f6faeb1a0d7e885`, commit `13369122ba7d5557eba1949095c1135a41843070`, `7/7 TREE-EXACT` |
| [P07B-C C3A cumulative-admission maintenance](status/P07B-C-C3A-CUMULATIVE-ADMISSION-MAINTENANCE.md) | U6c/C3A — sealed `SOURCE_FULL` `DEFECT_REPAIR` | admits only the exact C3 issuer path-symbol through inherited B, makes symlink invocation execute the checker, and aligns the Go model with the existing 128-byte schema/probe bound | commit `2b84d2841971568784d2ac955775b4a99ca7f0f6`, `11/11 TREE-EXACT`, strict exit `0`; exact thirteen-path roster `sha256:cc9bb98ceeb2f281336d3e235377d3cdb8920664f833baea830a84b847621188`; C3 remains frozen to forty paths |
| [P07B-C C3L legacy-lattice maintenance](status/P07B-C-C3L-LEGACY-LATTICE-MAINTENANCE.md) | U6c/C3L — sealed `SOURCE_FULL` `DEFECT_REPAIR` | preserves U6's historical store import lattice when no bridge exists and conditionally admits only the decoded-import, package-owned four-field/four-method C3 store-model bridge before later B/C3 checks | commit `efa918928246c7793f4ad7201c003020e3a42193`, `12/12 TREE-EXACT`, strict exit `0`; exact nine-path roster `sha256:b3624e1337681e0909e6c074246103e066118a5924e8f790e7d1f42359da9dc0`; C3 remains frozen to forty paths |
| [P07B-C C3F B future-surface admission maintenance](status/P07B-C-C3F-B-FUTURE-SURFACE-ADMISSION.md) | U6c/C3F — sealed `SOURCE_FULL` `DEFECT_REPAIR` | preserves B's historical zero-member C3 state and admits the future C3 target-codec surface only as one complete exact four-pair bundle; partial, relocated, renamed, Unicode-suffixed, and extra surfaces remain foreign | commit `63038644ba347d7b934a5557a490af95bb4428a4`, tree `3b4c39cfa907c2907d01dcd21399cafe52075872`, `10/10 TREE-EXACT`, strict exit `0`; exact ten-path roster `sha256:f64293895a1b824faafddf236757d381c813b59c40286225bb1c352d5c812c1a` |
| [P07B-C C3S cumulative U6 self-test maintenance](status/P07B-C-C3S-CUMULATIVE-SELFTEST-MAINTENANCE.md) | U6c/C3S — sealed `SOURCE_FULL` `DEFECT_REPAIR` | makes the checksum-pinned U6 fixture driver admit either the historical absent bridge or one exact already-present C3 bridge without changing production U6 policy or the frozen C3 source | commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, tree `fce8593ad1fe7af1474b4e113f03e82a9a052f79`, `10/10 TREE-EXACT`, strict exit `0`; exact eight-path roster `sha256:1d5f793e70caea0ad7379e92bbf781da50846276c28a2aca2d3e7e2ed8c17a26` |
| [P07B-C C3 exact-target publication](status/P07B-C-C3-TARGET.md) | U6c/C3 — sealed `SOURCE_FULL` | joins exact reopened residue, single-target Git materialization, fresh attempt, stable Darwin boot session, admitted Node runtime, and inert store witness into one structural `OfficialTarget`; C4 must freshly reopen at the physical boundary | commit `029c5cf43853eb3cb46520f6e0dea8431a3de5c0`, tree `de3e02a343f8ecfb344bdb28d014342f5b423959`, `18/18 TREE-EXACT`, strict exit `0`; source receipt remains a separate C3B concern |
| [P07B-C C3R note-preview checker maintenance](status/P07B-C-C3R-NOTE-PREVIEW-MAINTENANCE.md) | U6c/C3R — sealed `SOURCE_FULL` `DEFECT_REPAIR` | admits one exact sealed C3 note-preview projection for claim eight while retaining byte-exact raw local-ledger argv and phase-stable C3R/C3B fixtures | commit `424273b000f87925a87f0b3041b2e1e8c5750d39`, tree `0e4694362d0242a3b3da0ed2f8f78971ee595f6c`, `9/9 TREE-EXACT`, strict exit `0`; exact seven-path roster `sha256:fe14d9f9985771cce0525edfca47c001b5912dacc4c2d6fef182458d3b44f58e` |
| [P07B-C C3Q self-receipt checker maintenance](status/P07B-C-C3Q-SELF-RECEIPT-MAINTENANCE.md) | U6c/C3Q — sealed `SOURCE_FULL` `DEFECT_REPAIR` | closed positive-receipt refusal across the sealed-tree 42-path corpus; its production helper and direct C3Q/C3B renderer remain exact historical evidence, while later C3T corrects the broader outer-driver claim and C3U preserves terminal C3 composition | commit `818ed90ebbf99ae38916307d9f2342921d38ebbf`, `9/9 TREE-EXACT`, strict exit `0`; C3B remains frozen to three paths |
| [P07B-C C3T receipt-phase self-test maintenance](status/P07B-C-C3T-PHASE-SELFTEST-MAINTENANCE.md) | U6c/C3T — sealed `SOURCE_FULL` `DEFECT_REPAIR` | reconstructs a validated C3T-active absent fixture, runs every absent hostile mutation from it, and proves absent/present controlled-path round trips without weakening C3Q refusal policy | commit `9d8430aa70b1536a85dd7352a3d339667f2c1e88`, tree `3be4d7cde4d043b87438506483ffb8c256719df2`, `9/9 TREE-EXACT`, strict exit `0`; exact seven-path roster `sha256:496788860db6c42f5acc50de0de02be8a63bcb8e11f99fb24d59d9c0e016477e` |
| [P07B-C C3U cross-phase receipt-fixture maintenance](status/P07B-C-C3U-CROSS-PHASE-RECEIPT-FIXTURE-MAINTENANCE.md) | U6c/C3U — sealed `SOURCE_FULL` `DEFECT_REPAIR` | inserts synthetic C0/C1/C2/C3P legacy receipt blocks before an optional exact terminal C3 block, proves inverse recovery and byte-preservation, and keeps marker-free behavior stable | sealed after C3T commit `9d8430aa70b1536a85dd7352a3d339667f2c1e88`; historical machine authority `countershape/p07b-c-unit-paths/v15`, exact unit roster `sha256:7af7456929b0db08abc2a31bd1cb62d84002c1a6bcdda3f4a8b4896f6b8c30cd`, 44-path corpus `sha256:1c5bf120f1ab948586bfa594e6ae634fc6acf7a5544febcccb2c4dc475a59a49`, form/control manifest `ef4b8250cb0fbb666598b1926d3d17349b48ef913adced41831776821e99703c`; C3B remains frozen to three paths |
| [P07B-C C3D receipt-phase machinery consolidation](status/P07B-C-C3D-RECEIPT-PHASE-CONSOLIDATION.md) | U6c/C3D — sealed historical `SOURCE_FULL` `DEFECT_REPAIR`; the embedded “active” wording is a frozen pre-seal snapshot | makes one topology-only phase table and generic fixture generator the forward lifecycle authority, pins the complete 18-row table at `sha256:91ace265208883f3df976cc6a58d4a9d816d9c7c9fd2ba58887ccab13c3561fc`, makes C3D its sole specification owner, and selects the visible HANDOFF cursor through `--check-candidate-phase <unit>` using a strict full-index source snapshot plus mechanically coupled independent transition gate. Historical/live/future fixture inputs are phase-hermetic. It predeclares data-only C6M to create canonical `spec/verification/p07b-c-c6a-source-authority.json` schema `countershape/p07b-c-c6a-source-authority/v1`; C6M owns neither checker and supports preseal-parent plus sealed-current authority resolution. C6B adds `spec/verification/p07b-c-c6a-receipt.json`, supports preseal-parent HEAD=C6M plus sealed-current HEAD=C6B, reconciles the tracked sealed-C6A summary with explicitly declared local evidence, and preserves the exact two-error preactivation negative. Future sealed-source notes require full position-specific redaction-tolerant hermetic argv shape rather than suffix-only matching. C6A/C6M/C6B predeclare 11/10/9 claims ending `--verify-c6a-preseal-ledger`, `--verify-c6m-preseal-ledger`, and `--verify-c6b-preseal-ledger`; every future grade remains `UNRECEIPTED` until its own exact commit seals and verifies strictly. `--print-final-runbook <C3D|C6A|C6M|C6B>` prepares the HTML directory, wraps every fence in an isolated fail-fast subshell with cwd/commit self-containment, derives an exact first-12 commit key, shares one commit-addressed archive contract with the C6A receipt validator, renders those exact instruction contracts, and rejects topology-only C4/C5 | exact seven-path roster `sha256:ac4436680c612ad206d2786ddd3146af747e1f8ea2bdedf898afca67d46fcd63`; C6M exact five-path roster `sha256:5501a7e8c2d8f6d44392d101110edafe90a0348ef77f5ea24539f6a8ba969803`; C6B exact five-path roster `sha256:94501edc81cc170b220fb83873f70ba638a3f19a0a317e2e8df226057dc77a48`; 670-case combined catalog `a56fc2d71b79a06ba151ec454e7ed9280b20140e41c7006e027091eeddcf13e7`; four fixed C3R/C3Q/C3T/C3U regressions; unreceipted C4/C5/C6A/C6M and post-transition C6B full-plan controls; historical C4/C5 topology-only wording remains scoped to v16; schema `countershape/p07b-c-unit-paths/v16`; ten intended grades remain `UNRECEIPTED`; the historical phrase “C4 remains blocked until C3D” is not current authority |
| [P07B-C C4V execution-contract maintenance](status/P07B-C-C4V-EXECUTION-CONTRACT-MAINTENANCE.md) | U6c/C4V — sealed historical `SOURCE_FULL` `DEFECT_REPAIR`; its embedded active/unreceipted wording is a frozen pre-seal snapshot | preserves C3D's data-driven structural fixtures and four semantic regressions, inserts historical `C3D → C4V → C4`, freezes independent C4 and C5 source contracts, exact future Git-note consumers, immutable C4 qualification-runtime ownership, and deterministically requalifies the current verifier before either product boundary can edit source | sealed commit `861fd49a2b35b46c101dd6f18707f9c3525c6c7f`, tree `c2a560bf4c7bed6f93078af93714f979d06cf997`, 68/68 recorded-exact claims and strict exit `0`; exact eleven-path roster `sha256:535a64fbb9f0d3c3672b4b8197fc7815da315117b1794a668e6565faa16df3f1`; v17 authority `sha256:11f96a7809d845ef21671d8f847bd3b9f324e84c539cd2f93ab2bc14929ecd46`; C4 38 exact + 3 prefixes and 80 claims; C5 12 exact + 3 prefixes and 79 claims |
| [P07B-C C4M historical receipt-fixture maintenance](status/P07B-C-C4M-HISTORICAL-RECEIPT-FIXTURE-MAINTENANCE.md) | U6c/C4M — sealed historical `SOURCE_FULL` `DEFECT_REPAIR`; embedded active/unreceipted wording is a frozen pre-seal snapshot | captures one stable versioned descendant self-receipt corpus, rejects partial/foreign overrides, distinguishes omission from explicit absence, freezes the historical 44-path/form authority, and executes a fixed 31-subcase C4V descendant-corpus oracle | sealed commit `d87394799d2229bd6e6d342318f446272685a86f`, tree `afd0157303e9025a490b2b6ac991f5f15734137f`, 10/10 recorded-exact claims and strict exit `0`; exact seven-path roster `sha256:ec49dc68ce8fcfe92720fd2e8d6afa6d19d6d1c26aa9d27173dc2df736d06a87`; v18 authority `sha256:dd134e870c11ed4e80eb8335680b59a0ed1553c45d04746fd2fdd722f3f975a4`; historical 8/45 plan and 8/28 independent matrices |
| [P07B-C C4N self-receipt cardinality maintenance](status/P07B-C-C4N-SELF-RECEIPT-CARDINALITY-MAINTENANCE.md) | U6c/C4N — sealed historical `SOURCE_FULL` `DEFECT_REPAIR`; embedded active/unreceipted wording is a frozen pre-seal snapshot | derives valid edges, exhaustive invalid predecessors, specification ownership, and per-phase refusal cardinalities from v19 authority while preserving six fixed semantic regressions and frozen C4M history | sealed commit `b915d43cced850936c46b52620654f7506bdb993`, tree `3ea928443ecb32d6457babe2bce02ddec1d9632f`, note `949b8d2fbad1cce74e92971a30fdd31fae62f256`, 10/10 `TREE-EXACT`, strict exit `0`; exact seven-path roster `sha256:95de7e71ca2df4a57d0544c3197bb7d917359bb3d9ecfc51a2130973faf36e6e`; v19 authority `sha256:8f237c7d883c212167e5a04e5dd24d6efdacf4a9ce5492215fd06892a8237c4a`; 9/232 plan and 9/219 independent matrices; 840/846 catalogs |
| [P07B-C C4P future-surface phase maintenance](status/P07B-C-C4P-FUTURE-SURFACE-PHASE-MAINTENANCE.md) | U6c/C4P — sealed historical `SOURCE_FULL` `DEFECT_REPAIR` | routes B future-surface validation by independently pinned phase epoch, preserves an ambient-independent frozen baseline and all 42 direct hostiles, and keeps the generic-plus-semantic fixture hybrid | sealed commit `bbda662314fa3584859ad38f1baeb86ac1f09b5f`, tree `2819dfb90d23da0e89a17f92528338930b73f32c`, note `0dbc8868d07eac3fa19a5cfab2a9b584d07209e8`, `10/10 TREE-EXACT`, strict exit `0`; exact seven-path roster `sha256:5dc083fadb60003b0ba96516d0a01f91daa88b7298b142018b5eda00e48ab59b`; v20 authority `sha256:6d7e41d5ff22fc73118ceb0ae9f27a2151e7121d3a91c8945a391b3f42868f7d`; 10/266 plan and 10/253 independent matrices; 902/908 catalogs |
| [P07B-C C4 CLI profile](status/P07B-C-C4-CLI-PROFILE.md) | U6c/C4 — sealed historical `SOURCE_FULL` | neutral mechanics, sole runner admission, exact CLI physical closure, FCR/release/classification durability, real `CONTRADICTS` and `CONFORMS`, and C4-owned qualification authority | commit `c4089ab31181c08dad880bf9e5d40c622c8c91c4`, tree `f427e23b6fe297a49a36a99fa037073b637d82ca`, 80/80 `TREE-EXACT`, strict exit `0`; HTTP excluded |
| [P07B-C C4H verifier hermeticity](status/P07B-C-C4H-VERIFIER-HERMETICITY-MAINTENANCE.md) | U6c/C4H — sealed `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | bounded Git diagnostics, exact caller-mode fixtures, noncanonical verifier-entry refusal, and real subprocess lock/recovery coverage; execution profile unchanged | commit `4b4686ea30e5ddc5043eee11ba24725bcce8bb94`, tree `2d02d220961f3fc41b2372df5eb8dc3aac47a529`, 11/11 `TREE-EXACT`, strict exit `0` |
| [P07B-C C4I verification-surface stability](status/P07B-C-C4I-A2-AST-STABILITY-MAINTENANCE.md) | U6c/C4I — sealed `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | isolates drifted A2 AST work, constructs exact human-surface files under caller `077`/`022`, and coordinates Darwin escape-test readiness with a hash-bound four-record protocol plus self-lease | commit `d784ace97dd03660c6c724e1cb8f838b869681ec`, tree `e38b702884b9df128b41bfa7710459dd92f901d7`, 17/17 `TREE-EXACT`, strict exit `0`; frozen v22 history |
| [P07B-C C4K C3 Go-timeout maintenance](status/P07B-C-C4K-C3-GO-TIMEOUT-MAINTENANCE.md) | U6c/C4K — sealed historical `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | gives only exact `c3-official-target` an explicit `-timeout=12m`, preserving all other C3/C4 argv, package, retry, cache, parallelism, and cumulative-order authority | commit `8c1ee2df955055ee40eec1f14f0c4d96f97d5361`, tree `99fe7082d05c96647a144785e0ddc5af1a09f52f`, 12/12 `TREE-EXACT`, strict exit `0`; frozen v23 history |
| [P07B-C C4J bounded C5 verifier timeout](status/P07B-C-C4J-C5-VERIFIER-TIMEOUT-MAINTENANCE.md) | U6c/C4J — sealed `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | preserves C4K's timeout ladder and grants exactly 1,800,000 ms only to the two canonical C5 aggregate rows, with one spawn, no retry, and no inner semantic-deadline change | commit `c12c927d94e6c56529a2fb90148674b4b4e731a5`, tree `c99c1f394e02f17f8cf9de6b18f1a949bcaa07a8`, 13/13 `TREE-EXACT`, strict exit `0`; frozen v24 history |
| [P07B-C C4L non-product maintenance authority bootstrap](status/P07B-C-C4L-NON-PRODUCT-MAINTENANCE-BOOTSTRAP.md) | U6c/C4L — sealed historical `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | introduces dormant prospective `NON_PRODUCT_MAINTENANCE`, closes operator-provenance states, and repairs exact sealed-C4J consumer compatibility without product authority | commit `32911730afb7cc717b8e0a164b9ea0038d83f3fa`, tree `b71bbed9ca6044fa57fbb6c24379f3383f49689a`, 13/13 `TREE-EXACT`, strict exit `0`; frozen v25/27-row authority |
| [P07B-C C5 HTTP execution](status/P07B-C-C5-HTTP-SCOPE.md) | U6c/C5 — sealed `SOURCE_FULL` | completes standalone HTTP execution, exact scope closure, recovery parity, bounded evidence, and the 56-case qualification contract | commit `240060f018d5b0e86914bd27361c5899ac9ba1c0`, tree `4906ff4af407fa4e48cbf569f7e456694660ca06`, 79/79 `TREE-EXACT`, strict exit `0` |
| [P07B-C C5V verifier-preflight hygiene](status/P07B-C-C5V-VERIFIER-PREFLIGHT-HYGIENE.md) | U6c/C5V — sealed `SOURCE_FULL` `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR` | makes final-runbook residue checks no-follow and fail-early, adds terminal Finder-artifact revalidation, and binds exact sealed-C5 ancestry before C6A | commit `e7f51c0a8fbd2d6fbe8eacb4a7f0d46f010af7ba`, tree `65bdfc39b48c3b9b1b832a12cc9be5e0083ea5c4`, exact nine-path roster `sha256:42d2961220c8a2d06d70fe5fa9627f220569a74b3b6d2df9a26858f770481031`, 12/12 `TREE-EXACT`, strict exit `0` |
| [P07B-C C6A cumulative evidence](status/P07B-C-C6-EVIDENCE.md) | U6c/C6A — sealed `SOURCE_FULL` | closes cumulative architecture, verifier, and developer-facing evidence over the already implemented C1–C5 behavior without adding product/runtime behavior | commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`, 11/11 `TREE-EXACT`, strict exit `0`, `secrets_override: true` |
| [P07B-C C6F historical authority-fixture maintenance](status/P07B-C-C6F-HISTORICAL-AUTHORITY-FIXTURE-MAINTENANCE.md) | U6c/C6F — sealed `SOURCE_FULL` `DEFECT_REPAIR` | makes the historical C3Q/C3B replay explicitly total over four descendant C6 authority paths, keeps B live through explicit C1, isolates exactly two frozen C6A architecture rows behind raw sealed-tree replay, and pins an independently derived C6F note contract | commit `dd2a011edff20d65c43934fc7c3e5a09b5829d2e`, tree `b0141ac2671df75e51c4fdb5b71c35cee5d0f4ea`, exact twelve-path roster `sha256:6f4d8c5b02df2d2e0df6e8f4b91b66c9f1465bce427939e8ccc1442d64b85c9c`, 10/10 `TREE-EXACT`, strict exit `0` |
| [P07B-C C6G C3U historical fixture maintenance](status/P07B-C-C6G-C3U-HISTORICAL-FIXTURE-MAINTENANCE.md) | U6c/C6G — sealed `SOURCE_FULL` `DEFECT_REPAIR` | makes the real C3U-absent renderer apply and validate the four-path future-C6 absence projection after ambient current state, with a nine-case integration oracle and exact sealed-C6F/C6A ancestry | commit `3677b45d75204ce2ef7242abb5edd11cc049934e`, tree `520ad2cbf2d3da8354b02afd1cd2a7cd5df82d7d`, exact seven-path roster `sha256:c2b754a773ed5f1a17a3da9ca61f13525fea7235fb7e09422582c3ab2fa14724`, 10/10 `TREE-EXACT`, strict exit `0`, `secrets_override: true` |
| [P07B-C C6M sealed-source authority adapter](status/P07B-C-C6M-RECEIPT-ADAPTER-MAINTENANCE.md) | U6c/C6M — sealed exact-five-path `SOURCE_FULL` data-only boundary | derives sealed C6A's exact commit/tree/parent/subject, didrun note, eleven claim contracts, and position-specific hermetic argv projections into canonical `countershape/p07b-c-c6a-source-authority/v1`; it changes no product, checker, verifier, phase table, C6 evidence, or receipt and validates exact `C6M → C6G → C6F → C6A` ancestry | commit `b0de83dca3510102af4d1cddae2b2023bc88a87d`, tree `3d80a02443209f5d5649fcba92b79b72a2a04a15`, 10/10 `TREE-EXACT`, strict exit `0`, `secrets_override: true` |
| [P07B-C C6N note-redaction maintenance](status/P07B-C-C6N-NOTE-REDACTION-MAINTENANCE.md) | U6c/C6N — sealed exact-nine-path `SOURCE_FULL` defect repair | made the GOCACHE preview representation an exact source-era boundary input, pinned exact sealed C6M identity, replaced per-blob sealed-replay Git fan-out with two bounded batches, and closed plan-self-test throughput while preserving every case, deadline, raw preview, Git/in-process object parity, and immutable sealed history | commit `096842954c9e6ae72b66010fff81e90cf0bd7a22`, tree `69845d09f85abf76b275b16c0d941fbd568ee907`, 10/10 `TREE-EXACT`, strict exit `0` |
| [P07B-C C6R note-consumer maintenance](status/P07B-C-C6R-NOTE-CONSUMER-MAINTENANCE.md) | U6c/C6R — historical sealed exact-seven-path `SOURCE_FULL` defect repair | preserved C6N's frozen prospective projection while binding the suffix-preserving form observed in its exact immutable note through two independent validators | exact roster `sha256:3f815ac91f817fba618f24d307919dd9e97aec0f142a5cee1c9e86b284e06169`; subject `fix: bind sealed C6N note redaction projection`; C6R sealed before the now-sealed C6B continuation |
| [P08 — U7 CLI/reference studies](prompts/P08-U7-CLI-REFERENCE-STUDIES.md) | U7/U7N-amended | safe-next-action CLI, complete domain study workflows, one exact enrolled CLI official execution, and bounded mixed-authority closure | HTTP direct-process facts; CLI exact-stimulus execution; contamination/flake/projection/shape/freshness; HTTP target/run/classification and full resources remain unreceipted |
| [P09 — U8 secure studio](prompts/P09-U8-SECURE-STUDIO.md) | U8a | authenticated loopback decision bench, complete blind presentation, and any newly implemented derived-status or successor services | auth/Host/Origin/CSRF/CAS/blind/injection gates; no human-authenticity claim; no stale/defer/refine transition without durable authority |
| [P10 — U8 visual build loop](prompts/P10-U8-VISUAL-BUILD-LOOP.md) | U8b | reference-quality responsive/accessibility loop and real different-model critic | three visual passes plus final regression; all findings dispositioned |
| [P11 — U9 export/package/hardening](prompts/P11-U9-EXPORT-PACKAGING-HARDENING.md) | U9 | minimized local export, embedded binary, public docs, adversarial/performance closure | clean-root package, three studies, export warning/injection gates |
| [P12 — final evidence/S6/handoff](prompts/P12-FINAL-EVIDENCE-S6-HANDOFF.md) | closure | real-agent didrun S6, final comprehensive audit, receipt ledger, strict HTML, honest human tail | final commit sealed; strict exit 0; HTML bound to exact commit |

## Whole-system done condition

The run is complete only when the narrow implemented label matches the evidence; every completed slice has a sealed strict-clean commit; the two decisive studies prove the machinery prevents concrete false questions rather than decorating obvious output; U8 and U9 meet their exercised human-surface gates or are cut honestly; the final receipt table maps every claimed capability to a verbatim didrun grade or `UNRECEIPTED`; P12 records live S6 behavior, including bugs; and the exact final commit has a generated, unedited `didrun verify --strict --html` report with exit 0.

Even then, the result is a near-finish-line reference draft, not a finished or market-validated product. The final handoff must leave adoption, comparative user study, production hardening, independent security review, release/signing, cross-platform receipts, legal/name clearance, governance, and maintainership with the human owner.

## Sealed C4J bounded C5 verifier-timeout maintenance — frozen v24 snapshot

C4J sealed at commit `c12c927d94e6c56529a2fb90148674b4b4e731a5`,
tree `c99c1f394e02f17f8cf9de6b18f1a949bcaa07a8`, note blob
`f76ca73665f8cd3f19fdb9c7d4537462390a2200`, and note-body SHA-256
`a6b6191547efccf168d18932d559cba5a896c8dcde1f64ede72a27844f495cf1`,
with 13/13 `TREE-EXACT`, strict exit `0`, and `secrets_override: true`.
The owner-tense and `UNRECEIPTED` statements below are frozen v24 pre-seal
history; they do not supersede active C4L.

The permanent prerequisite edge is now `C3B → C3D → C4V → C4M → C4N → C4P → C4 → C4H → C4I → C4K → C4J → C5`. Exact sealed C4K is the direct parent; C4J is active under `OWNER_OUT_OF_BAND`, and C5 remains blocked until C4J seals. The live authority is `countershape/p07b-c-unit-paths/v24` with 26 ordered phase rows at authority SHA-256 `30530a2e05fa6b7fb74983756c24f22a898f4a6da6df75d447df17278b0152db`. Its plan transition matrix is 14 accepted and 422 rejected; the independent scope matrix is 14 accepted and 409 rejected. The active Markdown surface is 69 paths at `sha256:a53acdeef0e86b4865caab7822ea06cfa62742794ff1aaa5816023ef172cf901`, producing 1065 positive-form rejections and 355 controls.

Sealed C4K predeclared restoration of the exact C4J checkpoint in prose, but v23 did not machine-declare C4J: `predecessor-predeclared = true (sealed prose only)` and `machine-predeclared = false`. No external signed authorization artifact exists, and C4J inherits or rewrites no C4K grade.

The frozen C4J claim labels and `--verify-c4j-sealed-c4i-note` command spelling remain byte-exact compatibility names. The implementation first proves exact sealed C4K as C4J's direct parent, then proves the named sealed-C4I and lower chain transitively; the spelling does not assert that C4I is C4J's direct parent.

C4J owns exactly these 19 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4J-C5-VERIFIER-TIMEOUT-MAINTENANCE.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/01-empirical-failure.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/02-runtime-authority.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/03-governance-phase-machine.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/04-receipt-runbook-impact.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/05-synthesis.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/06-red-team.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/07-executive-briefing.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/verify-current-selftest.mjs`, `tools/verify-current.mjs`, and `tools/verify-runtime-authority.mjs`. Their sorted-newline roster digest is `sha256:c0920287c9dc9998f0cae11f8beced9d134c74da786a10f1886b03b2a1a50239`.

Commit subject: `fix: bound C5 architecture verification`. The runtime default remains 1,200,000 ms, while only `architecture-p07b-c-c5` and `architecture-p07b-c-c5-selftest` receive the exact 1,800,000 ms parent policy. The C4J 13-event ledger has ten tests and three terminal commands. It qualifies the bounded mechanism with targeted hostile coverage and three cumulative passes; it does not receipt C5 or prove that 30 minutes is sufficient under all future load.

## Active C4L non-product maintenance authority bootstrap

The permanent prerequisite edge is now `C3B → C3D → C4V → C4M → C4N →
C4P → C4 → C4H → C4I → C4K → C4J → C4L → C5`. C4L is active after
exact sealed C4J, and C5 remains blocked until C4L seals. The live authority
is `countershape/p07b-c-unit-paths/v25` with 27 ordered phase rows at digest
`189e1fe1a69e3937a769867d2e0d82c7b8b432da1de6c78382e97edd6aa5a796`.

C4L is a 13-exact-path, no-prefix `SOURCE_FULL` boundary with subject
`fix: bootstrap non-product maintenance authority`. It introduces
`NON_PRODUCT_MAINTENANCE` as dormant grammar and partial scope machinery; no
v25 row consumes it, and it has no generic runbook/preseal consumer. A later
`SOURCE_FULL` authority migration must predeclare and fully implement a
concrete child before the profile can be used. C4L itself runs the complete
three-pass cumulative battery.

The operator-authority token is `OWNER_OUT_OF_BAND`. C4L records provenance
as `UNEVIDENCED` with disclosure
`OWNER_ATTRIBUTED_SESSION_INSTRUCTION_ONLY_NO_QUALIFYING_PREEXISTING_ARTIFACT`,
authentication `NOT_ESTABLISHED`, and signed authorization
`NOT_IMPLEMENTED`. Citation is not authentication. Product behavior is
`INHERITED_UNREPROVEN`; no parent event, result, or grade is reused.

The 13-event ledger contains ten `tests-pass` events and three terminal
`command-succeeded` events, including three complete cumulative passes.
Render its only authoritative command sequence with `--print-final-runbook
C4L`. Any nonzero command or staged-tree drift leaves permanent failed
evidence and requires a repaired tree plus a fresh attempt from command 1.
