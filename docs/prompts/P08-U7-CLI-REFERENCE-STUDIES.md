# P08 — U7 reference CLI and falsifiable two-domain studies

Implement Countershape U7 from the active Mode C handoff. Read `docs/CONCEPT_BRIEF.md`, `docs/SEMANTICS.md`, `docs/PROJECTION_ALGEBRA.md`, deep-dive reports `06`–`08`, the checked-in study specs, and the U7 authority/status documents. Exact sealed C6B is the parent; its archived note/ledger authority is a prerequisite, not a transferable U7 grade.

## Execution decomposition

The original one-boundary U7 wording is superseded by the parent-frozen `countershape/u7-unit-paths/v1` execution chain `C6B -> U7P -> U7A -> U7B -> U7C -> U7D -> U7R`:

- `U7P` freezes the topology, exact rosters and claims, independent transition validators, architecture oracle, study-harness protocol/self-test, runbook, runtime epoch, and cumulative-verifier enrollment. It has no product authority and every claim begins `UNRECEIPTED`.
- `U7A` implements only the reference CLI/application foundation.
- `U7B` implements the HTTP falsification study and closes that domain's three-run process/artifact protocol and byte-repetition gate before sealing.
- `U7C` implements the independent CLI decision/contract study and closes that domain's three-run process/artifact protocol and byte-repetition gate before sealing.
- `U7D` performs the exact combined cross-domain process/artifact closure run and records the structured source evidence. It leaves the U7D receipt absent.
- `U7R` owns only the exact three-path parent-relative receipt projection. It binds already-sealed U7D evidence and cannot change or re-run product authority.

Every source child must remain a direct child of its exact sealed parent and satisfy the parent's exact roster, subject, product/receipt ceiling, command order, event indexes, pathspecs, and terminal evidence closure. A later unit cannot repair bytes owned by an earlier sealed domain unless a separately authorized repair boundary is declared. This decomposition changes execution topology, not the product objective or acceptance standard below.

## Objective and product milestone

Deliver a monochrome CLI and two checked-in, dependency-free end-to-end studies. This milestone must show the machinery preventing or refusing false questions—not merely printing outputs and generating an approval assertion.

The studies must run on exact committed candidates through fresh worlds and finish with disclosed budgets. The HTTP study is the decisive falsification case; the CLI study proves a second typed adapter reuses the same identity, eligibility, batch classification, exact-map comparison, tri-valued reduction, Choicepoint, ruling, and residue services without domain coercion.

Claim only runtime facts actually exercised on the named Darwin machine. Linux, Windows, unrun Node majors, broad imported repositories, hostile containment, network denial, comprehension, review compression, adoption, and maintainability remain `UNRECEIPTED`, explicit nonclaims, or human/market bets.

## Locked boundaries

Candidates are trusted local Node-core fixtures materialized from regular/executable Git blobs only. No setup, package install, registry, Docker, external API, TLS, redirects, cookies, compression, WebSockets, browser, database, or model. Every evidentiary trial gets a new root, private `HOME`/`TMPDIR`/state, process attempt, and captured evidence. No execution cache, prepared root, reset hook, snapshot, or reflink.

Every reducer and confirmation step binds the full typed `CandidateOutcomeMap`. It first checks plan, envelope, stimulus-independent comparison basis, roster, eligibility, and exclusions; only comparable maps compare the exact sorted eligible `candidate_execution_key -> projection_fingerprint` digest. Candidate order, nickname, producer, support count, partition shape, cluster ordinal, and naked digests never decide preservation. Evaluation remains `PRESERVES | CHANGES | UNRESOLVED`; unresolved blocks `ONE_MINIMAL_UNDER`.

Comparison envelopes state measured equalities, tolerated dimensions, rejected variance, and uncontrolled dimensions. They do not prove compatible worlds or that normalized roots/ports did not affect control flow. Use `OBSERVED_STABLE(k/k)`, never deterministic or stable without counts.

## Owned paths and CLI shape

Own the composition root under `cmd/countershape/**`, narrowly required application services, `examples/` or `testkit/reference/**` fixtures/specs, deterministic study drivers, end-to-end tests, and operator documentation. Do not build the React studio or report exporter.

Expose scriptable commands, following existing conventions, for study validation, preflight cost/warning, run, status, evidence/projection/reduction inspection, blind Choicepoint display, digest-CAS ruling preview/reveal/finalize, contract emit/run, and JSON. “Resume” starts new incomplete work, never a process. Print a safe next action for every partial, error, eligibility, decision, and resolved state.

Before first execution print verbatim:

> Countershape will run trusted repository code with your user permissions and host network access. The temporary directory is for repeatability, not security isolation.

Monochrome and `NO_COLOR=1` output must remain legible. Human output is non-authoritative; JSON exposes typed server/core facts without recomputing them.

## Pass 1 — deterministic fixture repositories

Create reproducible fixture-repository builders with fixed Git author/committer metadata and committed candidate refs. Resolve refs once to exact commit/tree OIDs, then move a display ref to prove pinning. Commit no nested `.git` directory.

HTTP uses four candidates:

- A returns observed-stable `403`;
- B returns observed-stable `404`;
- C returns observed-stable metadata-bearing `200`;
- D alternates `404` and `200` across newly invoked trials and becomes `UNSTABLE`.

Every response also contains a volatile request ID and scratch-root path. `CapturedObservation` retains them; a visible versioned projection removes exactly those declared noise fields while retaining status and disclosure-bearing metadata. Fixture-owned invocation evidence is recorded outside the selected predicate.

CLI uses three candidates with different precedence orders over conflicting config-file, environment, and argv values. Preserve ordered argv, sparse bindings, `Absent` versus `Present("")`, bounded fixture files, stdout/stderr, and exit-versus-signal. A private `HOME` contains no ambient user config. Include irrelevant inputs that typed reduction can remove while the three conflicting sources needed for the split remain.

All candidate-native tests must pass before Countershape compares them. A candidate failure in its own test is a fixture defect, not an interesting Choicepoint.

## Pass 2 — decisive HTTP negative proof

Implement and visibly report each of these gates:

1. A deliberately naive **negative fixture**, never admitted as product evidence, reuses a shared state root. B contaminates state so a later C invocation appears equal to B. Fresh Countershape roots eliminate the equality and recover the `403 | 404 | metadata-200` split. The output must label why the naive result is invalid.
2. Three or more fresh repeats classify A/B/C as `OBSERVED_STABLE(k/k)` and D as `UNSTABLE`; D never becomes a blind outcome card or confirmed-map member.
3. Captured-to-Projection operations are inspectable. Mutations that remove status or disclosure-bearing metadata must change/reject the proof; hidden normalization is forbidden.
4. The brief's named bad reducer proposal removes tenant seed facts and yields `200 | 500 | 500`; it changes the complete labeled map and is rejected. Also exercise a true same-membership-shape/different-fingerprint trap (for example three singleton outputs with changed candidate fingerprints) so a shape-only implementation cannot pass accidentally.
5. A later typed reduction removes only irrelevant facts and preserves the exact map. One checked-in run intentionally has an unresolved direct neighbor and ends `BEST_KNOWN`; one complete durable final sweep across either study earns `ONE_MINIMAL_UNDER(reducer_set_digest)`.
6. A separate confirmation batch creates new attempt artifacts before spawn, new roots/processes/captures, a rotated schedule, and fixture-observed invocation evidence. It reproduces the exact map; copied discovery/shrink captures with new metadata must fail.
7. Blind ruling allows `404`, selects status only, shows the metadata body as `Context only`/not asserted, then reveals provenance before finalization. A deliberately weak field selection that collapses an allowed and disallowed observed tuple returns `AMBIGUOUS_SCOPE` and emits nothing.
8. The six-file bundle runs in the already-defined absent-Countershape harness. B conforms; A and C contradict through eligible observations. D's new current run is classified only from new current evidence and never rewrites historical instability. A custom status `401` expectation makes all eligible stable candidates contradict. `REJECT_ALL` emits no executable files.

If contamination, instability, visible projection, full-map reduction, physical confirmation, separation, or absence does not materially change/refuse a result, label the milestone `observation/approval prototype` and stop the full-system claim.

## Pass 3 — independent typed CLI proof

Run the complete workflow on the three precedence candidates without routing CLI values through HTTP/JSON-shaped internal truth. Show that ambient parent `HOME`, config files, `Present("")`, ordered argv, and sparse environment remain distinct. The reduced witness retains only the three sources necessary to expose config/env/argv precedence. Select the exact strict stdout JSON field such as `effectiveMode` and, in a separate case, exit code. Unselected stderr/diagnostics may change without contradiction.

Prove a normal CLI exit `2` can be eligible and conform when selected, while signal termination, timeout, malformed selected JSON, output limit, or escaped fixture write is typed ineligible and cannot equal exit `2`. Exercise allow-many as a canonical set of complete selected tuples and verify no cross-product. Emit and run the standalone CLI contract under the same absence boundary as P07.

Add an architecture test: generic identity/observe/compare/reduce/choice packages contain no HTTP branches and no CLI-to-HTTP coercion. If the second adapter requires a second truth model, stop at the coherent one-domain instrument.

## Exact reproduction and acceptance gates

Run each full study three times from clean fixture repositories. Deterministic source specs, compiled `WorldPlan`, supplied fixture ruling, `DecisionRecord`, and `ContractBundle` bytes must match across runs. Structural historical `WorldInstance` identities, attempts, measurements/process receipts, captures, and confirmation must be newly allocated and linked. Each later conformance run must also publish a distinct capability-constructed `ContractExecutionTarget`, its exact target-bound `FinalizedContractRun`, and the matching classification-only `ContractExecution`; none may reuse WorldInstance authority or dirty working-tree bytes. Physical freshness is established only by lifecycle/invocation evidence, never by structural identity alone.

The U7P-frozen harness independently records native OS/kernel/architecture, Git/Go/Node versions, available CPU/memory facts, exact logical phase trial counts/budgets, raw Git/worktree fixture authority, direct prepare/compile/execute process argv and executable identities, artifact bytes, and subject-process wall/RSS observations. It derives byte equality and digest freshness from that observed surface. It does not independently recompute product-semantic linkage, attribute wall/RSS to each logical search/confirm/contract phase, or measure the total parent-harness process. Product tests remain responsible for the semantic workflow; the receipt must retain the harness semantic ceiling rather than relabel process/artifact evidence as independently observed semantic reproduction.

The exact machine gate budgets the sum of each domain's three directly observed prepare/compile/execute subject-process spans at 15 minutes and their maximum observed subject-process RSS at 4 GiB on the named reference machine. These are not total full-study resource measurements: harness orchestration, Git inspection, artifact walking/hashing, and parent Node memory are outside them, so `FULL_STUDY_RESOURCE_BOUND_UNVALIDATED` remains explicit. If a subject-process budget is exceeded, report the measured result and revise the metric; never reuse evidence, hide retries, or relax freshness to hit the number. Imported repositories inherit only explicit budgets, not this local observation.

Add end-to-end tests for every safe next action and state; malformed specs; moving refs; unsupported Git forms; unstable/uncomparable/incomplete candidates; budget arithmetic; JSON and `NO_COLOR` output; stale ruling CAS; noncompilable actions; exact receipt round-trip; both reduction grades; selected/unselected field mutations; conformance versus ineligible execution; and three-run deterministic/new-evidence separation.

## didrun discipline and handoff

Wrap every candidate-native test, fixture build, Go test/race/vet/lint, mutation suite, complete study run, timing run, standalone execution, and reproduction comparison in `didrun run --`. Do not treat a script's internal green lines as receipts unless the load-bearing script itself is run through didrun. Use `didrun show --session` to bind narrow claims to exact successful event indexes and paths. Name Darwin and the exact executed Node major in claim labels; do not generalize them.

At every verified U7 source boundary, review/stage only the exact parent-predeclared roster, commit intentionally without an AI co-author trailer, attempt the mandatory plain seal, inspect the didrun note, then loop on explicit-commit strict verification before emitting the exact HTML witness and rotating the live ledger into its commit-addressed archive. The generated runbook is authoritative for exact argv, environment, event indexes, pathspecs, and the sole-writer lifecycle. One root-owned writer owns repository, didrun, claim, Git, seal, recovery, and archive mutation for the whole attempt. Do not enter the next fence or mutate state until every yielded wrapper and relevant child is process-terminal; empty or yielded app output is not terminal evidence. If terminality or recorded interval non-overlap cannot be established, stop normal progression, make no mutation while a process may remain live, and after terminality run only the matching generated recovery fence. Any nonzero result permanently ends that ledger: preserve it, repair the defect from the declared parent, rerun affected verification through didrun, re-claim, commit, reseal, and reverify. Never erase old receipts, weaken tests, or promote an unrun environment.

Do not hand-clean a failed run. The generated runbook's normal rotation accepts only an exact clean committed workspace, no failed-attempt marker/ref, and a restartable same-device ledger move. Before commit, use its unique UTC/reason recovery command; at or after commit, use its exact-full-commit recovery command. Both preserve the ledger and hermetic final root. Postcommit recovery pins the failed commit under the unit's failed-attempt ref and compare-and-swaps HEAD to the declared parent while keeping the failed tree staged. Rerun the same recovery command after interruption; never copy, delete, reuse, or relabel the failed evidence.

Update the Mode C handoff with exact fixture commits, study commands, timings, trial counts, deterministic/new digests, native environment, event indexes, verbatim grades, strict verdict, commit, and every `UNRECEIPTED` item. Hand off to the secure studio prompt only if the CLI product milestone is green. Otherwise name the exact honest fallback: truth kernel, observation-only, one-domain, DecisionRecord-only, or observation/approval prototype.
