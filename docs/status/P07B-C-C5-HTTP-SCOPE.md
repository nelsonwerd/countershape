# P07B-C C5 — HTTP-profile physical execution and full standalone scope

- **Boundary:** active pre-seal `SOURCE_FULL` child of exact sealed C4L.
- **Parent:** sealed C4L commit `32911730afb7cc717b8e0a164b9ea0038d83f3fa`, tree `b71bbed9ca6044fa57fbb6c24379f3383f49689a`, direct parent C4J `c12c927d94e6c56529a2fb90148674b4b4e731a5`.
- **Parent receipt:** C4L subject `fix: bootstrap non-product maintenance authority`; didrun note blob `1c03b8ff2cca98bc46aea1990945b8bfeeae2ec9`; note-body SHA-256 `ae41dda6bc1f570b67332763dcb88a892b690b797115275822361d28484925f6`; `13/13 claims recorded-exact`; every grade `TREE-EXACT`; strict exit `0`; `secrets_override: true`.
- **Parent local evidence:** `.countershape/evidence/p07b-c-c4l-final-32911730afb7.html`, SHA-256 `7ce724953c5d43252a83989cb0723d0daea1513aef40fe8a5875a44d572160db`; final ledger archive `.didrun-history/p07b-c-c4l-final-32911730afb7/.didrun/`.
- **Commit subject:** `feat: complete standalone contract execution`.
- **Receipt state:** C3P `PRESENT`; C3 `PRESENT`; C6A `ABSENT`.
- **Exact scope:** 12 exact paths at sorted-newline `sha256:d16ba74582e53dc5c791b8c34fbcd6200338dc066ec13230c2908eb6ba257bf5`, plus required realized prefixes `internal/contractexec/http/`, `internal/contractexec/scope/`, and `testkit/contractexec/http/` at `sha256:a9212680066fdbddc8f25eef04a41c264ac9148a506238dd92c39978e3e35f84`. The prefix realization is exactly 23 regular files: 13 HTTP runner files, eight scope files, and two public fixture files.
- **Final evidence root:** `.countershape/p07bc-c5-final`.
- **Final ledger:** exactly 79 commands and 79 immediate claims: 76 `tests-pass`, then three `command-succeeded`.

Every intended C5 grade remains `UNRECEIPTED` until one exact staged tree completes the parent-frozen final runbook, commits, seals, exposes a readable Git note, and passes `NO_COLOR=1 didrun verify --strict`. Focused development passes and stress receipts do not receipt this status or the final boundary.

## Implemented HTTP slice

`internal/contractexec/http` is a capability-only owner for the child-bind HTTP profile. Its exported package edges, available only inside Countershape's Go `internal` import boundary, accept a live `contractexec.OfficialTarget`: `Execute` performs one physical subject execution and `ResumeClassification` converges only an already durable terminal run. This is deliberately not an external HTTP SDK or service API. Callers cannot supply raw argv, request bytes, expected result tuples, target digests, scope conclusions, or classifications.

The exact owner-acquirer inventory has three rows: HTTP owner `internal/contractexec/http/runner_darwin.go:1`, sealed CLI owner `internal/contractexec/runner/runner_darwin.go:1`, and store definition `internal/store/contract_run_bridge.go:1`. The first two are the only profile-specific execution owners; the third defines the acquisition edge and is not a third execution owner.

The physical chronology is fresh target reopen, complete preparation and private-evidence capacity admission, second target reopen, executable and boot revalidation, durable owner acquisition, detached bounded revalidation phase, start-adjacent target reopen, creation of a separate detached bounded closure phase before the first phase is checked and canceled, exact target equality, one-shot start consumption, durable spawn observation, exact readiness and one raw loopback exchange, process-group teardown, scope/receipt/inventory closure, terminal target reopen, private-manifest and finalized-run publication, durable release, and exact classification. Each phase keeps the same source-derived `readiness + probe + teardown + 30 seconds` budget. The handoff prevents the full reopen from spending closure time, creates no unbounded scheduler gap, and can raise the total post-owner envelope from one phase to roughly two. Revalidation or pre-consumption closure expiry produces no spawn and leaves the claim/interlock held; later expiry preserves progress-dependent evidence/state, and post-release classification absence is recoverable. Start errors close the same terminal graph without inventing a child PID. Recovery freshly reopens the target and requires the standalone HTTP profile before any terminal-store lookup. It may restore release and classification, but contains no source preparation, owner acquisition, start consumption, spawn, exchange, or subject-respawn edge; live target reopen may still run bounded owned Node runtime probes.

After validating root metadata and the declared seed topology, preparation acquires a nonblocking advisory lease on the no-follow, identity-checked attempt temporary-root descriptor before fixture-roster inspection or mutation, with no persistent lock path. A successful preparation transfers the lease to the complete execution attempt through receipt retirement, terminal persistence, and return. A concurrent cooperating preparation therefore receives typed admission refusal before it can interpret the holder's live receipt as residue; process death releases the lease. The holder admits a bounded descriptor-backed compatible-partial fixture roster, exclusively creates one fixed staging directory beneath the attempt temporary root even for an empty seed roster, writes and synchronizes each complete staged inode, publishes through a same-device atomic no-clobber hard link, proves destination/staged identity, and removes only that staged name. Staging must close exact-empty and absent, and the final fixture must contain exactly the declared `0700` directories and regular `0644` seed files with exact bytes. Without a live holder, existing staging or receipt is crash/foreign residue, refuses as a fixture defect before intended writes, and remains preserved. The public authority-race test requires one valid exact classification and one typed admission refusal.

## Raw exchange, evidence, and five-domain scope

Readiness accepts only the exact portable FD-3 frame followed by EOF. The child-reported IPv4 loopback endpoint is used for exactly one raw TCP exchange; Countershape does not infer listener ownership, service authenticity, proxy absence, or egress denial from that frame. Partial, malformed, withheld, reset-endpoint, decoy-endpoint, early-exit, malformed-response, partial-response, response-overflow, and timeout controls retain distinct bounded facts.

Raw response parsing and projection stay adapter-owned. A malformed or partial captured response can remain `CAPTURED_UNPROJECTED`; a response timeout can remain `NO_CAPTURE`. Projection resolution precedes canonical `PROCESS_RESULT` evidence so typed control facts and observation state cannot disagree.

Standalone closure has exactly five ordered domains: target inventory, child binding, import resolution, service binding, and named sentinel inheritance. The exact enrolled reference inventory may close clean. Source-mutated controls are intentionally not reference-enrolled, so a clean process and projected tuple cannot make them eligible. `VIOLATED` outranks `PARTIAL`; start-error closure preserves observed violations and marks unobserved non-inventory domains missing. Missing or malformed invocation evidence makes child binding missing, while an exact attempt/request contradiction produces the typed child-binding violation.

C5 admits at most 15 private evidence bodies: three framed bodies and twelve canonical summaries. The pre-owner byte formula includes both channel caps, the response-read limit, maximum encoded request, maximum readiness observation, the 3 MiB projection frame, twelve 1 MiB summaries, and two 1 MiB framing allowances, all beneath the unchanged 64 MiB store ceiling. The invocation receipt is canonical, at most 64 KiB, bound to the fresh attempt, stimulus, exact request bytes/digest, and invocation count one, then identity-checked, retired, and directory-synchronized. It remains cooperating candidate-written evidence, not hostile-process attestation.

`internal/contractexec/scope` places its probe beneath the attempt-private temporary root, uses a caller-owned short alias beneath the root-owned sticky Darwin temporary directory, reads direct rosters through no-follow directory descriptors, binds module/socket/root identities, and removes only retained leaves and proven-empty directories. Inventory refuses setuid, setgid, or sticky bits on its root or any nested directory or regular file before returning authority. Foreign residue and same-size module replacement become ambiguity and are preserved; cleanup never recursively traverses candidate-controlled residue.

The six parent-owned process-pipe endpoints use pointer-owned close-once wrappers. Once `Start` is attempted, start-error cleanup, successful-start parent-writer cleanup, and terminal-reader cleanup each have one fixed durable diagnostic. Those diagnostics are deduplicated in phase order, set teardown error, leave the primary process diagnostic and orphan-risk axis unchanged, and are never retried because a numeric descriptor may already have been reused. This is bounded phase-level observation that `Close` returned an error, not proof that a kernel descriptor remained open and not universal coverage of pre-Start cleanup, TCP, or other non-pipe cleanup.

## Durability, exact joins, and classification-only recovery

The C4-sealed target/run/execution store graph remains authoritative. C5 uses the same exact StartClaim-bound private manifest, finalized-run release relationship, and classification derivation. Cross-target target/run/execution pairing is refused even when semantic result bytes look similar. Terminal recovery reopens the live official target, validates the standalone HTTP profile before reading terminal-store state, may reconverge a missing release link, derives the same immutable `ContractExecution`, and cannot readmit or respawn the subject. A structurally valid CLI/non-HTTP target returns `UNSUPPORTED_HTTP_CONTRACT_RUN_PROFILE`; malformed HTTP terminal state remains `HTTP_CONTRACT_RUN_RECOVERY_REFUSED`.

The public reference, differing-403, custom-401, and allow-many fixtures exercise real emitted Node entrypoints through fresh official targets. The development-observed result matrix is profile-specific: reference observed 404 conforms; differing observed 403 contradicts; custom-401 against observed 403 contradicts; allow-many including 403/404 conforms; and allow-many excluding 200 contradicts. Here `CONFORMS` and `CONTRADICTS` are domain classifications produced by development fixtures, not test-pass grades or didrun receipt grades. These are exact fixture outcomes, not an arbitrary HTTP compatibility claim or a sealed receipt.

## Architecture and verifier authority

The five exact Go-JSON profiles are:

| Profile | Exact top-level passes | Skips | Race |
| --- | ---: | ---: | --- |
| `c5-http-behavior` | 4 | 0 | no |
| `c5-readiness-teardown` | 9 | 0 | no |
| `c5-scope-closure` | 9 | 0 | no |
| `c5-cross-profile-parity` | 22 | 0 | no |
| `c5-http-authority-race` | 5 | 0 | yes |

The checker validates exact `(Package, Test)` pairs across multi-package transcripts, run-before-terminal lifecycle order, admitted nested-test roots, one package pass per enrolled target, all package arguments in fixed order, exact coverage of all 28 C5 non-helper top-level tests, the `4/9/9/22/5` count vector, and the `false/false/false/false/true` race vector. Its independent hostile self-test moves a test between enrolled packages while preserving the flat name roster, injects unknown and foreign packages, duplicates, reorders, or removes test/package lifecycle events, mutates topology and chronology facts, and attacks status/runbook parity.

C5 preserves the C4-sealed B future-surface manifest and the phase plan/scope checkers byte-for-byte. The frozen qualification catalog has 56 cases at `sha256:08db7c338eb3ba900cf6df2545c04f27bed16889c81dd16e5fb18197c4d52958`, including `contract-http-readiness-50` and `contract-http-teardown-20`. The cumulative verifier keeps all inherited gates and adds the five focused profiles plus C5 architecture/checker self-test rows. The three new HTTP/scope packages raise the current whole-package sensitive roster from nine to exactly 12 because they exercise subprocesses, deadlines, loopback I/O, Unix sockets, and identity-bound cleanup. They occupy a separate `go-test-sensitive-c5-serial` row immediately after the inherited-nine `go-test-sensitive-serial` row and before package revalidation, rotating the internal verifier roster from 61 to 62 without changing the frozen outer 79-command ledger. Both rows retain `-p=1`, `-parallel=2`, `-count=1`, and `-timeout=20m`; their sorted, unique, disjoint exact union is checked before execution and revalidated afterward.

## Development evidence and S6

Development commands remain in the unsealed didrun session. Sandbox-denied cache and loopback attempts are environment-bound non-evidence, not product failures. Focused host runs closed the readiness, response, scope, receipt-state, start-error, exact-join, terminal-recovery, teardown, and one-winner authority-race cases. The first red-team found that concurrent seed creation exposed a destination before its bytes were complete; complete-inode no-clobber publication repaired that product defect.

A later red-team found four additional pre-seal defects: crash-left seed staging/foreign fixture residue could reach preparation; nested special mode bits were collapsed by ordinary permission projection; six process-pipe close errors were discarded; and recovery accepted a structurally valid CLI target. The repairs add exact fixture preflight/final rosters, root/nested setuid-setgid-sticky refusal, close-once phase diagnostics, and a shared profile gate before terminal-store lookup. Seed, scope, and close targeted physical didrun controls followed two permanent development reds—one stale Go import and one incorrect `ReadDir` EOF assumption—while recovery uses a nested pure CLI/HTTP constructor control plus static path chronology. The extended architecture syntax/static facts subsequently passed through didrun on the repaired source tree. The identical static check first refused a sandbox-injected sterile-Git warning and then passed outside that contaminated environment; the checker was not weakened. These are still development observations, not C5 receipts.

A fresh three-lane critic replay then found one remaining product edge and three proof gaps. A valid empty seed roster bypassed crash-staging refusal; `closeFailureRetention`, `profileBoundRecovery`, and `specialModeRefusal` were true for source-wide anchors that did not fully prove reachable nested controls or every claimed production site. Empty rosters now execute the same staging creation/retirement protocol, and the facts bind exact pointer fields, endpoint calls, function-specific evidence, recovery error propagation/pre-gate store absence, pure helper limits, reachable nested tests, the special-mode helper body, and the exact three-by-three matrix. The first strengthened static replay failed honestly because it counted only the two pre-entry `CodeInventorySpecial` returns and omitted the existing third default-special return; the corrected exact count is three, after which the static facts and revised targeted physical controls exited `0` through didrun. The earlier profile pass is stale by source change and will not be used as current evidence.

Confirmation-only critic passes found four narrower proof seams: the early-readiness test did not distinguish `stopReadiness`'s first close from later terminal reuse; the recovery fact bounded the profile call start rather than the completed error guard and described helper purity with a denylist; the special-mode fact proved required row inclusion without exact cardinality; and its nested-path anchor required the join call without requiring assignment to `hostilePath`. The test now asserts the first close immediately and cached error reuse afterward. The checker binds `stopReadiness` close-before-timer order, the complete recovery guard before every terminal token, the exact profile-helper body digest, exact three-row mode/location rosters, nested cross-product loops, and the full hostile-path assignment. The strengthened static fact gate and revised targeted physical controls exited `0` through didrun on the resulting source/checker tree. All earlier profile events remain stale after these proof-source changes.

The first cumulative `--c5` checker attempt exposed a separate orchestration defect: the C5-owned cumulative checker still described the live C1 package enclosure at the C4 topology, so the untouched inherited B checker correctly rejected the new `http/` and `scope/` packages. The repair admits only the exact current HTTP/scope topology and importer rows, while the existing defensive self-test now also removes each new row and injects a foreign row. Direct C1, inherited B, full C5 architecture, and the full C5 defensive self-test subsequently exited `0` through didrun on one development tree. Those passes preserve the permanent red events and remain development evidence rather than C5 receipts.

The first additive `-race -count=20` stress command exited nonzero under Go's default package timeout and remains permanent failed history. A diagnostic replay with explicit `-timeout=12m` completed five full authority-race iterations successfully in `633.527s`. The latter is valid development stability evidence for one exact tree; it neither erases the earlier red event nor upgrades a C5 grade. Physical fixture cleanup now affects the test-binary exit instead of silently discarding `RemoveAll` failures.

didrun continued to retain nonzero events, bind successful wrappers to exact tree identities, and suppress child output in its concise wrapper summary. Bounded diagnostics were captured separately under `/tmp` through didrun rather than by opening private event blobs. Final S6 judgment still requires the fresh sealed 79-event ledger, strict verification, and exact-commit HTML report.

The first repository-wide C5 development verifier on tree `a6bf94ae0442ff27706951b98a661b228af8de75` passed rows `1–22/61`, including the `699103 ms` general-package row and `1135259 ms` twelve-package sensitive row, then exited `1` at inherited `architecture-p07b-a2-2-selftest` after `245811 ms`. Its `example-generator-byte-drift` control had already recorded the expected raw-digest violation, but the checker redundantly sent all six JavaScript profiles through its nested AST worker; that worker reached the unchanged 60-second deadline and surfaced `P07B_A2_JS_AST_QUERY_FAILED: SIGTERM` before the known violation could be reported. A didrun-wrapped read-only benchmark subsequently measured the same all-profile worker at `118 ms` over `218577` input bytes and `462688` output bytes, so the deadline event is nondeterministic load sensitivity rather than an ordinarily expensive parse. Both events remain permanent unclaimed development history.

This was the recurrence under `p=1` anticipated by sealed C4V. C5 was checkpointed rather than replaying the forty-minute chain unchanged. The owner-authorized predecessor maintenance sealed as C4I: it optimizes AST inspection to drifted profiles, preserves every semantic hostile and the fixed 60/90-second deadlines, and distinguishes worker infrastructure failure from a surviving hostile while classifying the unchanged verifier/U5/U6/A1/A2 structural preflight before the load-heavy Go lanes. C4I changed no package partition or cumulative-row order. After C4J sealed the exact continuation requirement, resumed C5 itself split the twelve-package sensitive aggregate into exact inherited-nine and C5-three serial rows because the earlier combined row completed only `64741 ms` below its unchanged 20-minute child ceiling. Neither repair changes package coverage, assertions, fresh-cache policy, `p=1`, `GOMAXPROCS=2`, test `-parallel=2`, or the frozen 79-command final C5 ledger. The retained checkpoint has now been reapplied over exact sealed C4L after the intervening sealed C4K/C4J/C4L process boundaries, but every pre-C4I C5 development event is stale and nonreusable; C5 requires fresh verification on the reconciled tree.

The final-gate audit also exposed live S6 permission behavior: under the agent launcher's default umask, the development `.didrun` root existed at mode `0755`; didrun 0.1.0 did not force that secret-bearing root private. Development event 120 applied an identity-local recursive `go-rwx` repair, and event 121's recursive mode check exited `0`; the live root then measured `0700`. The final driver must therefore enter one stable launcher context with persistent outer `umask 077` before command 1 creates `.didrun`, and must recheck the live ledger recursively. This is an observed didrun operator-security defect, not a Countershape receipt or a reason to modify the frozen 79 claims.

The post-C4I reconciled-source audit first found that two valid `Execute` calls could both prepare before durable owner acquisition and collide at the fixed seed-staging directory. The initial repair held a nonblocking advisory lease only across seed roster inspection, staging, publication, retirement, and final roster proof. A final audit then found a later interleaving: after that short lease was released, a winner could publish its live receipt before a delayed loser reached evidence preflight, misclassifying the loser as fixture rejection. C5 now transfers the lease from successful materialization into `executionPreparation` and releases it only from `closePrepared` after the complete attempt returns. The nested deterministic control first proves raw contention, then writes a live receipt while a returned preparation retains the lease and requires `HTTP_CONTRACT_RUN_ADMISSION_REFUSED`; after releasing the holder it requires the same unowned receipt to remain `HTTP_CONTRACT_RUN_FIXTURE_REJECTED`, retires the residue, and proves preparation resumes. The public race still requires one exact classification and one admission-refused loser. The `serializedHTTPAttempt` architecture fact binds the ownership transfer, error cleanup, full-attempt defer, both classifications, and recovery. All earlier reconciled-tree development events are stale after this repair.

The same audit found a proof gap rather than a demonstrated capacity bug: the prior maximal-envelope test used fifteen one-byte synthetic bodies and a synthetic fifteen-byte allowance. The strengthened test constructs a real HTTP source, measures a physical inventory, invokes production `preflightEvidenceCapacity`, proves the exact formula, builds production frames populated from profile-maximum raw segments under conservative framing allowances plus twelve near-ceiling canonical bodies, proves all fifteen bodies are digest-distinct and admitted, then proves aggregate-minus-one and sixteenth-body refusal. The checker now binds those source-local anchors. This establishes bounded arithmetic for the enrolled source/profile-derived envelope capped by the inherited 64 MiB store ceiling, not arbitrary future evidence encodings or store-wide capacity.

The first post-repair architecture replay then exposed a separate test-authority defect under the required outer `umask 077`: `materializeC5ReferenceInventory` requested `0644`/`0755` but accepted the caller-masked `0600`/`0700` files, so the exact reference inventory correctly refused enrollment. The fixture now normalizes each newly written path to its declared mode and immediately `Lstat`-checks the regular-file/mode facts; the architecture checker binds that order. The failed architecture events remain permanent development history.

The first reconciled-tree cumulative verifier pass then completed rows `1–40/61` before direct row `41/61`, `architecture-p07b-c-c4`, rejected the current exact three-row owner-acquirer inventory: that standalone inherited checker still admitted only C4's sealed two-row projection. This was a descendant-compatibility defect in verifier authority, not a product failure. Direct C4 validation now admits exactly either the sealed C4 projection—CLI owner plus store acquisition edge—or the declared C5 extension—HTTP owner plus those same two rows. Every roster other than those two exact projections—including a partial extension, duplicate, or foreign site—still fails, and C5 validation separately requires and semantically checks the exact three-row HTTP extension. The nonzero cumulative event remains permanent development history; because checker and documentation bytes changed, C5's required three consecutive current-tree cumulative passes restart from zero.

Final-ledger attempt 1 completed and immediately claimed events `1–75`, then event `76`—the third complete cumulative verifier—failed at row `56/62`, `go-json-p07b-c-c5-http-authority-race`. The recovery subject reported terminal release nonconvergence and the concurrent test reported zero winners/two admission refusals. That 75-claim attempt and its event-76 diagnostic are permanent under `.didrun-history/p07b-c-c5-final-attempt-private-1-event76-red-686defe74acc/.didrun/` and `.countershape/p07bc-c5-final-attempt-private-1-event76-red/`; the 47,040-byte private replay log has SHA-256 `891ed0591d943aeba6118e661b3ed0a1998f68a6d8f35c637d4cb5ece632045c`. The replay passed rows `1–55` before the same authority row failed, so no final claim, commit, seal, or strict result exists.

A fresh focused diagnostic initially produced simultaneous Unix-socket `EPERM` and all-candidate pre-readiness failures. Recursive bounded error chains proved those were Codex sandbox denials, not product regressions; the exact unchanged five-test profile then passed under authorized host-local socket/loopback execution. Those eight diagnostic events remain archived at `.didrun-history/p07b-c-c5-race-diagnosis-1/.didrun/`, with private logs under `.countershape/p07bc-c5-race-diagnosis-1/diagnostics/`. The one host pass does not erase the accumulated-load failure. Source inspection found that C5 began its single detached 34.5-second context before the post-owner full reopen and reused it through consumption, execution, persistence, and release. The observed substage cause was hidden in the original archived assertion, so deadline attribution remains a strong source-and-symptom inference rather than a captured wrapped cause. The repair uses two continuously handed-off contexts with the same 34.5-second per-phase budget and adds bounded error-chain diagnostics.

The repaired worktree at didrun tree prefix `2db1e5d82f83` then passed syntax and diff guards, the focused phase-context independence test, complete C5 architecture, the complete C5 defensive self-test, three consecutive exact `c5-http-authority-race` profiles in three fresh Go test processes, and one complete `62/62` cumulative verifier. The cumulative pass re-executed the race profile inside full C5 architecture and again at its explicit post-load row, then completed plan, authority, cleanup, and lock-release guards. Its development ledger preserves all 20 events—including the earlier permanent red diagnostics—under `.didrun-history/p07b-c-c5-phase-repair-dev-2db1e5d82f83-20/.didrun/`. These are unclaimed development events, not reusable final-ledger claims. No C5 commit, seal, strict result, HTML report, or grade exists; the exact 79-command final ledger must restart at command 1 on the final staged tree, so current C5 qualification remains `UNRECEIPTED`.

A subsequent pre-final critic found that the implementation was correct but the static phase-context fact was not mutation-tight: a post-formula budget reassignment or an additive alternate-context operation could coexist with the expected anchors, and the self-test flipped the collected fact rather than deriving rejection from hostile source. The repaired checker now uses one pure source inspector to bind the exact three-statement phase-budget helper, exact context/cancel/call cardinalities, the continuous handoff, and exactly one executable nested independence registration. Five in-memory source hostiles reject post-formula budget mutation, context substitution, an additive alternate-context call, an expected anchor preserved only in a comment, and a nested-test registration preserved only in a comment without expanding the frozen top-level roster. That checker change makes the `2db1e5d82f83` pass prior-tree development evidence only; it is not reusable and does not relax the fresh 79-command final-ledger requirement.

## Validated boundary versus open bets

Exact sealed C4L, C4J, C4K, C4I, C4H, and C4 remain validated at their recorded `TREE-EXACT` grades and exact claim ceilings. C5 source and focused checks currently have development evidence only. The real bets remain arbitrary-target behavior, Node and OS tuples beyond the admitted Darwin/arm64/cgo environment, listener authenticity, hostile-code containment, same-UID race resistance, production security/readiness, adoption, comprehension, and long-term maintainership.

## Intended C5 claim map

| # | Claim | Type | Intended grade |
| ---: | --- | --- | --- |
| 1 | `P07B-C C5 candidate phase plan coherence` | `tests-pass` | `UNRECEIPTED` |
| 2 | `P07B-C C5 independent candidate transition authority` | `tests-pass` | `UNRECEIPTED` |
| 3 | `P07B-C C5 locked raw-HTTP behavior profile` | `tests-pass` | `UNRECEIPTED` |
| 4 | `P07B-C C5 child-reported readiness binding and teardown profile` | `tests-pass` | `UNRECEIPTED` |
| 5 | `P07B-C C5 five-domain scope closure and forbidden-positive profile` | `tests-pass` | `UNRECEIPTED` |
| 6 | `P07B-C C5 cross-target CLI HTTP and Go Node parity profile` | `tests-pass` | `UNRECEIPTED` |
| 7 | `P07B-C C5 exact HTTP authority race and boundary-fault profile` | `tests-pass` | `UNRECEIPTED` |
| 8 | `P07B-C C5 cumulative architecture boundary` | `tests-pass` | `UNRECEIPTED` |
| 9 | `P07B-C C5 architecture defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 10 | `P07B-C C5 predecessor U6 compatibility` | `tests-pass` | `UNRECEIPTED` |
| 11 | `P07B-C C5 predecessor U6 defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 12 | `P07B-C C5 predecessor B compatibility` | `tests-pass` | `UNRECEIPTED` |
| 13 | `P07B-C C5 predecessor B defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 14 | `P07B-C C5 repetition-profile defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 15 | `P07B-C C5 isolated qualification case processmechanics-output-caps-50` | `tests-pass` | `UNRECEIPTED` |
| 16 | `P07B-C C5 isolated qualification case processmechanics-output-independence-20` | `tests-pass` | `UNRECEIPTED` |
| 17 | `P07B-C C5 isolated qualification case processmechanics-simultaneous-overflow-20` | `tests-pass` | `UNRECEIPTED` |
| 18 | `P07B-C C5 isolated qualification case world-lifecycle-readiness-20` | `tests-pass` | `UNRECEIPTED` |
| 19 | `P07B-C C5 isolated qualification case contract-runner-admission-50` | `tests-pass` | `UNRECEIPTED` |
| 20 | `P07B-C C5 isolated qualification case contract-cli-standalone-closure-20` | `tests-pass` | `UNRECEIPTED` |
| 21 | `P07B-C C5 isolated qualification case contract-http-readiness-50` | `tests-pass` | `UNRECEIPTED` |
| 22 | `P07B-C C5 isolated qualification case contract-http-teardown-20` | `tests-pass` | `UNRECEIPTED` |
| 23 | `P07B-C C5 isolated qualification case compiler-generated-runtime-20` | `tests-pass` | `UNRECEIPTED` |
| 24 | `P07B-C C5 isolated qualification case program-lifecycle-20` | `tests-pass` | `UNRECEIPTED` |
| 25 | `P07B-C C5 isolated qualification case store-cross-process-cas-20` | `tests-pass` | `UNRECEIPTED` |
| 26 | `P07B-C C5 isolated qualification case cli-physical-reducer-01-of-20` | `tests-pass` | `UNRECEIPTED` |
| 27 | `P07B-C C5 isolated qualification case cli-physical-reducer-02-of-20` | `tests-pass` | `UNRECEIPTED` |
| 28 | `P07B-C C5 isolated qualification case cli-physical-reducer-03-of-20` | `tests-pass` | `UNRECEIPTED` |
| 29 | `P07B-C C5 isolated qualification case cli-physical-reducer-04-of-20` | `tests-pass` | `UNRECEIPTED` |
| 30 | `P07B-C C5 isolated qualification case cli-physical-reducer-05-of-20` | `tests-pass` | `UNRECEIPTED` |
| 31 | `P07B-C C5 isolated qualification case cli-physical-reducer-06-of-20` | `tests-pass` | `UNRECEIPTED` |
| 32 | `P07B-C C5 isolated qualification case cli-physical-reducer-07-of-20` | `tests-pass` | `UNRECEIPTED` |
| 33 | `P07B-C C5 isolated qualification case cli-physical-reducer-08-of-20` | `tests-pass` | `UNRECEIPTED` |
| 34 | `P07B-C C5 isolated qualification case cli-physical-reducer-09-of-20` | `tests-pass` | `UNRECEIPTED` |
| 35 | `P07B-C C5 isolated qualification case cli-physical-reducer-10-of-20` | `tests-pass` | `UNRECEIPTED` |
| 36 | `P07B-C C5 isolated qualification case cli-physical-reducer-11-of-20` | `tests-pass` | `UNRECEIPTED` |
| 37 | `P07B-C C5 isolated qualification case cli-physical-reducer-12-of-20` | `tests-pass` | `UNRECEIPTED` |
| 38 | `P07B-C C5 isolated qualification case cli-physical-reducer-13-of-20` | `tests-pass` | `UNRECEIPTED` |
| 39 | `P07B-C C5 isolated qualification case cli-physical-reducer-14-of-20` | `tests-pass` | `UNRECEIPTED` |
| 40 | `P07B-C C5 isolated qualification case cli-physical-reducer-15-of-20` | `tests-pass` | `UNRECEIPTED` |
| 41 | `P07B-C C5 isolated qualification case cli-physical-reducer-16-of-20` | `tests-pass` | `UNRECEIPTED` |
| 42 | `P07B-C C5 isolated qualification case cli-physical-reducer-17-of-20` | `tests-pass` | `UNRECEIPTED` |
| 43 | `P07B-C C5 isolated qualification case cli-physical-reducer-18-of-20` | `tests-pass` | `UNRECEIPTED` |
| 44 | `P07B-C C5 isolated qualification case cli-physical-reducer-19-of-20` | `tests-pass` | `UNRECEIPTED` |
| 45 | `P07B-C C5 isolated qualification case cli-physical-reducer-20-of-20` | `tests-pass` | `UNRECEIPTED` |
| 46 | `P07B-C C5 isolated qualification case http-physical-reducer-01-of-20` | `tests-pass` | `UNRECEIPTED` |
| 47 | `P07B-C C5 isolated qualification case http-physical-reducer-02-of-20` | `tests-pass` | `UNRECEIPTED` |
| 48 | `P07B-C C5 isolated qualification case http-physical-reducer-03-of-20` | `tests-pass` | `UNRECEIPTED` |
| 49 | `P07B-C C5 isolated qualification case http-physical-reducer-04-of-20` | `tests-pass` | `UNRECEIPTED` |
| 50 | `P07B-C C5 isolated qualification case http-physical-reducer-05-of-20` | `tests-pass` | `UNRECEIPTED` |
| 51 | `P07B-C C5 isolated qualification case http-physical-reducer-06-of-20` | `tests-pass` | `UNRECEIPTED` |
| 52 | `P07B-C C5 isolated qualification case http-physical-reducer-07-of-20` | `tests-pass` | `UNRECEIPTED` |
| 53 | `P07B-C C5 isolated qualification case http-physical-reducer-08-of-20` | `tests-pass` | `UNRECEIPTED` |
| 54 | `P07B-C C5 isolated qualification case http-physical-reducer-09-of-20` | `tests-pass` | `UNRECEIPTED` |
| 55 | `P07B-C C5 isolated qualification case http-physical-reducer-10-of-20` | `tests-pass` | `UNRECEIPTED` |
| 56 | `P07B-C C5 isolated qualification case http-physical-reducer-11-of-20` | `tests-pass` | `UNRECEIPTED` |
| 57 | `P07B-C C5 isolated qualification case http-physical-reducer-12-of-20` | `tests-pass` | `UNRECEIPTED` |
| 58 | `P07B-C C5 isolated qualification case http-physical-reducer-13-of-20` | `tests-pass` | `UNRECEIPTED` |
| 59 | `P07B-C C5 isolated qualification case http-physical-reducer-14-of-20` | `tests-pass` | `UNRECEIPTED` |
| 60 | `P07B-C C5 isolated qualification case http-physical-reducer-15-of-20` | `tests-pass` | `UNRECEIPTED` |
| 61 | `P07B-C C5 isolated qualification case http-physical-reducer-16-of-20` | `tests-pass` | `UNRECEIPTED` |
| 62 | `P07B-C C5 isolated qualification case http-physical-reducer-17-of-20` | `tests-pass` | `UNRECEIPTED` |
| 63 | `P07B-C C5 isolated qualification case http-physical-reducer-18-of-20` | `tests-pass` | `UNRECEIPTED` |
| 64 | `P07B-C C5 isolated qualification case http-physical-reducer-19-of-20` | `tests-pass` | `UNRECEIPTED` |
| 65 | `P07B-C C5 isolated qualification case http-physical-reducer-20-of-20` | `tests-pass` | `UNRECEIPTED` |
| 66 | `P07B-C C5 isolated qualification case parity-evaluator-20` | `tests-pass` | `UNRECEIPTED` |
| 67 | `P07B-C C5 isolated qualification case parity-framing-20` | `tests-pass` | `UNRECEIPTED` |
| 68 | `P07B-C C5 isolated qualification case parity-full-package-3` | `tests-pass` | `UNRECEIPTED` |
| 69 | `P07B-C C5 isolated qualification case cli-physical-full-package-3` | `tests-pass` | `UNRECEIPTED` |
| 70 | `P07B-C C5 isolated qualification case http-physical-full-package-3` | `tests-pass` | `UNRECEIPTED` |
| 71 | `P07B-C C5 sealed-C4 Git-note and C4P C4N C4M C4V C3D ancestry compatibility` | `tests-pass` | `UNRECEIPTED` |
| 72 | `P07B-C C5 unit-scope defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 73 | `P07B-C C5 cumulative verifier self-test` | `tests-pass` | `UNRECEIPTED` |
| 74 | `P07B-C C5 cumulative verification pass one` | `tests-pass` | `UNRECEIPTED` |
| 75 | `P07B-C C5 cumulative verification pass two` | `tests-pass` | `UNRECEIPTED` |
| 76 | `P07B-C C5 cumulative verification pass three` | `tests-pass` | `UNRECEIPTED` |
| 77 | `P07B-C C5 exact admitted staged scope and diff integrity` | `command-succeeded` | `UNRECEIPTED` |
| 78 | `P07B-C C5 scoped staged credential-pattern scan` | `command-succeeded` | `UNRECEIPTED` |
| 79 | `P07B-C C5 sealed-C4 predecessor C4P C4N C4M C4V C3D ancestry and preceding didrun chain integrity` | `command-succeeded` | `UNRECEIPTED` |

## Nonclaims and next gate

C5 proves no arbitrary-target inventory completeness, Node vendor authenticity, listener ownership, service authenticity, proxy or egress denial, host-wide absence, registry denial, same-UID containment, exactly-once execution, prior-survivor absence, confidentiality, secure deletion, process resume, power-loss recovery, production security/readiness, adoption, comprehension, or maintainership. The exact physical fixtures and admitted Darwin/arm64/cgo toolchain are narrower than a production support matrix.

C5 creates immutable nonhead target/run/execution evidence and does not advance or freshen the study head/current/latest selectors. C6A may begin only after C5's exact 79-claim commit is sealed, its didrun note is readable, strict verification exits `0`, and the exact-commit HTML/ledger handoff is complete.
