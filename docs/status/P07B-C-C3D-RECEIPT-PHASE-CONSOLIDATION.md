# P07B-C C3D — receipt-phase machinery consolidation

Classification: `DEFECT_REPAIR`

- **Boundary:** active pre-seal `SOURCE_FULL` checker maintenance after sealed C3B and before C4. Classification: `DEFECT_REPAIR`.
- **Parent:** sealed C3B commit `3b0b56d2bdd36531caf31e3dbad555aca061616d`, tree `15edcc92c5d1ed79885cf9caaa85b4be36918433`, is note-present with note blob `e447175c54e8fa864f540f09f8bf614962cd8e07`, note-body SHA-256 `7602ba5baa1e23fc1d309ea4d50c23b843dd87d6f05efe732e8cf656358b2d4b`, `7/7 claims recorded-exact`, strict exit `0`, and recorded `secrets_override: true`.
- **Commit subject:** `fix: derive receipt fixtures from phase table`

C3D owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3D-RECEIPT-PHASE-CONSOLIDATION.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:ac4436680c612ad206d2786ddd3146af747e1f8ea2bdedf898afca67d46fcd63`.

Every intended C3D grade remains `UNRECEIPTED` until one exact staged tree runs the declared commands through a fresh didrun ledger, commits, seals, exposes a readable Git note, and passes `NO_COLOR=1 didrun verify --strict`. This status cannot receipt C3D itself.

## Proposal disposition

- **Defect repair:** boundary-specific lifecycle tuples and fixture drivers repeatedly diverged from the actual phase, producing the distinct follow-on repairs now sealed as C3R, C3Q, C3T, and C3U. C3D makes `countershape/p07b-c-unit-paths/v16` the single topology-only forward phase table. Each row owns its boundary ID, exact parent, existing verification profile, and named `C3P`, `C3`, and `C6A` receipt states; the exact visible HANDOFF capsule's `Boundary` field is the separate explicit cursor, while the table JSON rejects a second `active_boundary` field.
- **Intentional hybrid:** exact visible-Markdown topology, canonical receipt payload adapters, the frozen historical refusal matrix, fresh validated mutation baselines, byte-exact inverse round trips, terminal-C3 preservation, full-plan caller controls, sealed Git-note/ancestry validators, and fixed regression anchors remain independent oracles. The new generic lifecycle runner dispatches four named executable regressions, but it does not pretend their family-specific semantics reduce to receipt-state rows.
- **Declined with reason:** wholesale replacement of the C3R/C3Q/C3T/C3U behavior suites is declined because a generic capsule cannot express C3Q's external-surface asymmetry, C3R's one-position note projection, or C3U's terminal-byte invariant without losing specificity. The generic fixture generator is not allowed to grade itself, one receipt boolean cannot identify a phase, aggregate counts cannot replace unique case identities, and the cumulative battery is not reduced. Frozen pre-capsule adapters may reconstruct sealed historical documents but cannot gain C3D, C4, or C5 columns or select the live phase.

## Data-driven contract

`receiptPhaseRows` projects the ordered phase table from the machine specification. Validation requires exact immediate-parent order, exact receipt-family keys and state vocabulary, HANDOFF ownership by every phase row, no `PRESENT` to `ABSENT` downgrade, zero receipt-state flips at a `SOURCE_FULL` boundary, and exactly one flip at a `RECEIPT_RECONCILIATION` boundary. The complete 18-row authority—including profiles, exact paths, prefixes, states, and receipt-claim manifests—is pinned at `sha256:91ace265208883f3df976cc6a58d4a9d816d9c7c9fd2ba58887ccab13c3561fc`. C3D alone owns the specification in the forward horizon; C4, C5, C6A, C6M, and C6B omit it from their exact rosters. C6M is predeclared after C6A as the exact five-path data-only source-authority boundary at `sha256:5501a7e8c2d8f6d44392d101110edafe90a0348ef77f5ea24539f6a8ba969803`. It introduces canonical `spec/verification/p07b-c-c6a-source-authority.json`, schema `countershape/p07b-c-c6a-source-authority/v1`, and owns neither checker; it cannot edit the C6B target prompt, C6 evidence surface, table, scope checker, plan checker, or verifier. C6B is separately frozen at `sha256:94501edc81cc170b220fb83873f70ba638a3f19a0a317e2e8df226057dc77a48` to exactly `docs/HANDOFF_MODE_C.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/DIDRUN_BUGS.md`, `docs/status/P07B-C-C6-EVIDENCE.md`, and `spec/verification/p07b-c-c6a-receipt.json`. `renderReceiptPhaseCapsule`, `classifyReceiptPhase`, and `rewriteReceiptPhaseCapsule` operate on one exact visible capsule beneath `## Current state`; its `Boundary` field is the explicit cursor and must select that same row.

`--check-candidate-phase <unit>` supplies the plan/corpus boundary oracle before corpus selection. It builds a strict full-index source snapshot from the pinned index tree, requires equality with the live stage-zero index, loads every tracked blob through its OID, and compares every tracked worktree file through bounded no-follow stable reads. `checkPlan()` cannot fall back to live bytes for a path absent from that snapshot. The gate pins exact parent, index, and didrun-notes trees, validates the authority digest, admits only the table-declared immediate-parent edge, then mechanically invokes the separately implemented immutable unit-scope oracle. For C4 and later, candidate specification bytes must also match the pinned parent. It finally rechecks HEAD, index, notes, specification, and worktree bytes. C6M owns neither checker. C3D alone bootstraps from exact sealed C3B commit/tree with no prior capsule and may introduce v16. Same-row replay, skipped or backward edges—including C6A directly to C6B—CLI/capsule mismatch, malformed modes, OID mismatch, stale bytes, and persistent authority drift fail. Six admitted forward edges and 37 hostile snapshot cases are cardinality-pinned by the plan gate; the scope oracle retains six admitted edges, 20 generic hostiles, and end-to-end C6 material branch cases. This detects persistent drift but is not same-user isolation: an ABA mutation restored between samples remains possible, so sole-writer discipline is load-bearing. The no-argument cumulative plan check remains live-coherence evidence only.

`generatedReceiptPhaseCases` and the generic fixture generator produce a deterministic 666-case phase catalog: 18 rows × 37 cases, comprising 342 hostile capsule/observed-state rejections and 324 accepted baseline/rewrite controls. Its digest is `479f21a8a86a8a6eb34056dabb471e8ceebf08c725008d5f204fa2294af9f83a`. For those 666 generic records, case IDs, fixture/output bytes, and observed states are digest-pinned; every rewrite is classified as its target row, and every inverse rewrite must recover the source bytes exactly. Four named executable fixed regressions extend that to a 670-case combined catalog with 328 accepted controls/regressions; combined digest `a56fc2d71b79a06ba151ec454e7ed9280b20140e41c7006e027091eeddcf13e7`. Those four records pin their ID, oracle name, sealed commit, checker blob, and checker-byte digest, then execute family-specific assertions; they do not claim generic fixture/output fields they do not contain.

All plan-authority Markdown must be an exact row path: a prefix may admit non-Markdown source/capture artifacts but cannot silently admit a case-insensitive `.md` or `.markdown` file. This closes the external-surface asymmetry for C6A's future capture prefix without scanning an unbounded namespace. The unreceipted implementation defines full `checkPlan()` controls for C4, C5, C6A, C6M, and post-transition C6B, including their exact Markdown surfaces and active-unit refusal extensions; none is described as passing until the C3D final ledger records the self-test. Each forward corpus accumulates every declared Markdown surface from C4 through its target, so C6M and C6B retain C6A's evidence authority even when their own exact rosters do not edit it. Historical self-receipt corpus bytes come from matching first-parent Git commits, the current boundary uses a separate complete live plan snapshot, and future/negative fixtures are explicitly normalized, so absent entries cannot fall through to descendant worktree content. C4 and C5 are topology-only table rows, not executable final-claim contracts; their source runbooks must be introduced by their owning boundaries before either unit starts.

C6A requires the source-authority manifest, receipt declaration, and both projections absent. C6M resolves preseal-parent HEAD=C6A or sealed-current HEAD=C6M, creates the canonical sealed-C6A manifest, and keeps the receipt declaration and both projections absent while its live row remains C6A=`ABSENT`. C6B resolves preseal-parent HEAD=C6M or sealed-current HEAD=C6B, inherits that manifest unchanged, adds canonical `spec/verification/p07b-c-c6a-receipt.json`, and requires one tracked sealed-C6A evidence summary plus byte-identical receipt projections in HANDOFF and C6 evidence. `--verify-c6a-source-receipt` validates the full declaration, sealed summary, source/note/strict fields, and projections; `--verify-c6a-local-evidence` separately reopens and hashes the declared HTML and ignored ledger. Private-availability fields are bounded tracked declaration state only: C3D opens no private blob and proves no confidentiality, integrity, or future availability. The independent scope checker owns only the portable source identity/manifest fields, exact five-path roster, and portable dual-projection constraints. Missing, one-sided, duplicate, malformed, wrong-identity, wrong-grade, altered transport, declared local-evidence mismatch, or attempted C6B self-receipt fails. The exact two-diagnostic C6B preactivation case remains a negative control, while the post-transition control requires the manifest, receipt declaration, both projections, and prompt reframing. C6A/C6M/C6B predeclare 11/10/9 claims ending `--verify-c6a-preseal-ledger`, `--verify-c6m-preseal-ledger`, and `--verify-c6b-preseal-ledger`; all remain `UNRECEIPTED` until each owning unit runs, commits, seals, and verifies strictly. Any boundary or receipt family beyond C6B requires an explicit schema/checker change.

## Independent fixed regressions

The generated catalog is not the sole oracle. These named anchors are byte-reopened from previously sealed checker authorities and executed through the combined catalog:

- `C3R_NOTE_PREVIEW` preserves the admitted Git-note preview projection while raw local-ledger argv stays byte-exact.
- `C3Q_EXTERNAL_SELF_RECEIPT` injects the exact external `docs/VERIFICATION.md` positive result through ordinary current-C3D overrides and the outer `checkPlan()` loading path, preserves its prohibition control, and separately replays the historical C3B scanner corpus plus exact historical active-unit matrix.
- `C3T_VALIDATED_ABSENT_BASELINE` preserves fresh baseline validation plus absent/present byte round trips.
- `C3U_TERMINAL_RECEIPT_LAYERING` preserves terminal C3 bytes through both the helper and real C0/C1/C2/C3P full-plan callers.

The frozen C3U/C3B historical subset remains exactly 1,320 required rejections, 440 core accepted controls, eight supplemental aliases, and two strict-zero controls. It is labeled frozen historical output, not the current active phase. C3D adds current-row and forward-declared coverage separately; it does not reconstruct or relabel that sealed-tree corpus.

## Intended claim map

| Intended claim | Type | Grade |
|---|---|---|
| `P07B-C C3D data-driven receipt-phase plan coherence` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D independent candidate transition authority` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D generated fixture and fixed-regression defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D sealed-C3B Git-note and ancestry compatibility` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D unit-scope defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D cumulative verifier self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D cumulative verification` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3D exact seven-path staged scope and diff integrity` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3D scoped staged credential-pattern scan` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3D sealed-C3B predecessor and preceding didrun chain integrity` | `command-succeeded` | `UNRECEIPTED` |

## Final command order

These are command tails, not bare receipt commands. Run each only through `didrun run --` with the checker-owned hermetic prefix and claim its matching label/type immediately; bare execution creates no admissible final receipt.

`--print-final-runbook <C3D|C6A|C6M|C6B>` deterministically emits the complete checker-owned `/usr/bin/env -i` argv, each immediately adjacent claim, exact commit subject, private-root plus mode-`0700` `.countershape/evidence` preparation, plain-seal-first checkpoint, unclaimed note inspection, strict restart rule, exact-commit HTML, and copy-with-live-ledger-retained archive order. Every shell fence is an isolated `set -eu` subshell that independently re-enters and asserts the physical canonical repository root and stops before a claim or later mutation after any failed command; every post-commit fence re-derives current immutable HEAD and slices exactly its first 12 characters under sole-writer control, so neither persistent shell state nor collision-lengthened Git `--short` output is an input. One path helper binds that emitted commit-addressed archive to the future C6A receipt declaration; the exact form is `.didrun-history/p07b-c-c6a-final-<12-hex-commit>/.didrun/`. It prints instructions only and confers no grade. It refuses C4 and C5 because they remain topology-only until their owning boundaries introduce executable contracts; it never stages paths, deletes evidence, spins on an unchanged failed strict result, or activates `--allow-secrets` automatically. Behavioral shell hostiles require a failed command and a false precondition to prevent the following claim analogue from producing any output.

A didrun-wrapped development shell probe permanently exited `127` because an intermediate renderer draft named absent macOS path `/usr/bin/test`. That red event supports no capability. The renderer now uses `/bin/test` exclusively; its exact-12 shell expression is part of the still-`UNRECEIPTED` renderer self-test, which also rejects any emitted `/usr/bin/test` token.

Future sealed-source notes are not admitted by command suffix. Both independently implemented validators require exact full hermetic argv length and order, allow bounded didrun redaction syntax only at the eleven historically observed prefix positions, and keep every visible prefix token plus the complete tail exact. The accepted projection is position-specific: five private-root values use the exact double marker, GOCACHE uses one marker plus its exact expected `.countershape/.../gocache` suffix, and five tool values use one bare marker. Leading-executable injection, prefix omission/substitution, wrong marker form, wrong visible suffix, over-redaction, and tail redaction are fixed hostile cases. A permitted redaction is positional evidence only and does not attest the hidden bytes.

1. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --check-candidate-phase C3D`
2. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --candidate-phase C3D`
3. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --self-test`
4. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c3d-sealed-c3b-note`
5. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --self-test`
6. `/opt/homebrew/bin/node tools/verify-current-selftest.mjs`
7. `/opt/homebrew/bin/node tools/verify-current.mjs`
8. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --unit C3D --source-final-gate`
9. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --unit C3D --credential-scan`
10. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c3d-preseal-ledger`

## Nonclaims

C3D changes checker/schema/documentation authority only. It proves no product behavior, process start, runtime freshness, executable authenticity, containment, confidentiality, general Markdown safety, general security property, C4 authority, production readiness, adoption, or maintainership. The active status and every intended grade remain `UNRECEIPTED` until the separately sealed final boundary exists.
