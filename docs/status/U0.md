# U0 status — controlling contracts

- **Unit:** U0 / P00
- **Branch:** `codex/countershape-autopilot`
- **State:** boundary candidate; accepted only by this tree's attached didrun manifest and strict exit 0
- **Commit:** the commit carrying this file (hash assigned at the boundary)
- **Strict didrun verdict:** never infer from this prose; run `NO_COLOR=1 didrun verify --strict`

## Locked by this unit

U0 turns the revised concept and red-team cuts into machine-checkable planning contracts. It owns architecture direction, threat and claim boundaries, explicit state machines, versioned schemas/examples/refusal vectors, and a dependency-free planning validator.

The run-level center is the narrowed Darwin reference instrument:

- regular/executable Git blobs only;
- fresh executed attempts with no execution-evidence reuse;
- declared comparison envelopes, never proven compatible worlds;
- Captured evidence followed by visible exact projection;
- one canonical candidate-to-projection-fingerprint OutcomeMap;
- tri-valued reduction and construction-safe local-minimality;
- exact-witness selected-field rulings with separation;
- distinct DecisionRecord, ContractBundle, and ContractExecution authorities;
- Go/Node parity and physical Countershape absence before a standalone claim;
- blind-first human surfaces and default-minimized local exports with confidentiality unknown;
- Wake and didrun boundaries preserved.

## Reconciled discrepancies

1. The synthesis permitted constrained relative symlinks; the later red team removed every symlink from this run. The red-team cut controls.
2. Earlier documents used world compatibility language. U0 uses `ComparisonEnvelope`: an admission policy over measured/tolerated/rejected/uncontrolled dimensions, not proof of behavioral equivalence.
3. Earlier briefs used `STABLE` and “smallest.” U0 permits only counted observed stability and the three named reduction grades.
4. Earlier plans called the HTML output shareable. U0 calls it a default-minimized local evidence export and requires `CONFIDENTIALITY NOT ESTABLISHED`.
5. Node 24 is a design target from research, not a receipt. Only a Node major physically executed later may be claimed.

## Explicitly unimplemented / UNRECEIPTED

No canonical runtime, Git materializer, process owner, adapter, reducer, artifact store, contract emitter, CLI, studio, export, package, or reference study exists at U0. All product capabilities remain `UNRECEIPTED`. Planning examples are inert contracts, not runtime evidence.

The prompt-pack authoring phase produced pre-commit didrun events, including one preliminary unclaimed structural failure that was corrected and rerun. U0 binds claims only to final validator, mutation-self-test, and staged-diff events recorded after every U0 file was locked. No earlier event is upgraded into a product claim.

## Verification ledger

The first shared pre-commit ledger is permanently rejected: concurrent writers produced duplicate indices and broke its chain. Its checksum, preservation path, impact, and the separate tree-digest/output findings are recorded in `docs/status/DIDRUN_BUGS.md`. None of those events supports a claim.

The replacement ledger is written serially. Its first two successful preflight validator events produced no tree because didrun 0.1.0 conflicts with a redundant repository-level `.didrun/` ignore rule; they remain unclaimed. The root rule was removed while didrun's generated self-ignore and hard-coded tree exclusion remain active.

The first final-boundary staged-diff check at event 4 exited 2 and exposed trailing whitespace plus extra EOF blank lines in research artifacts. That failed receipt is permanent and unclaimed. Event 5 records the mechanical whitespace normalization; because it changed the tree, it is also not a verification claim. The complete verification trio is rerun after this note and the normalized files are staged.

The authoritative receipt is the commit-bound didrun manifest, not an index copied into documentation before sealing. U0 requires exactly these claims over final-tree events:

1. `tests-pass` — planning contracts validate;
2. `tests-pass` — five independent mutation controls are caught;
3. `lint-clean` — the exact staged patch passes Git's whitespace/error check.

Only verbatim `tree-exact` or `scope-exact` grades plus strict exit 0 accept the unit. Any other result keeps U0 open.

## Next unit

`P01-U1-TRUTH-KERNEL.md` is the only next implementation unit, and only after U0 is committed, sealed, and `NO_COLOR=1 didrun verify --strict` exits 0.
