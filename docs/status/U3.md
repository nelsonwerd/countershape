# U3 status — typed CLI observation spine

- **Unit:** U3 / P03
- **Branch:** `codex/countershape-autopilot`
- **State:** sealed and strict-clean at commit `4a4ce4d6bba6` (`feat: add typed CLI observation`), tree `feb3c4988366`
- **Receipt boundary:** strict exit `0`; `ALL RECORDED-EXACT`; `16/16 claims recorded-exact`; each final claim below has verbatim grade `TREE-EXACT`
- **Seal disclosure:** sealed with `--allow-secrets (redacted export)` after 86 entropy-only findings in controlled argv and mutation-output identifiers. The staged credential-prefix scan is `TREE-EXACT`; neither fact is a complete security or secret-free finding.
- **Prerequisite:** U2 is sealed and strict-clean at commit `2ab4b6511838`, tree `3993c7fff93a`

## Locked boundary

U3 owns typed CLI stimulus, direct one-process execution binding, bounded CLI capture, a visible closed projection, sequential rotated repeated observation, and an exact N-way precedence study. It does not own HTTP, reduction, Choicepoint persistence/ruling, contract emission, a product CLI, server, report, or UI.

The adapter may expose CLI facts to generic observation only through sealed projection derivation or typed projection-rejection evidence. Generic packages own eligibility, complete-matrix admission, classification, and exact candidate maps; they do not import CLI types or field identifiers. A process receipt records trusted-local execution, not sandboxing or malicious-process attestation.

## Closed v1 stimulus and execution profile

- one logical `node` executable plus fixed ordered base argv and ordered stimulus argv; U3 admits no other executable profile, and Node requires one immutable repository-relative `.js`, `.mjs`, or `.cjs` entrypoint, so inline `--eval`, stdin-as-program, and unprofiled interpreter script strings are refused;
- tagged stdin: `ABSENT`, `PRESENT` with zero bytes, or `PRESENT` with bytes;
- sparse tagged environment: `ABSENT` differs from `PRESENT("")`;
- bounded strict-order regular fixture files with mode `100644`;
- working directory policy `MATERIALIZED_ROOT`;
- direct execution only, with runner-owned private HOME/TMP/XDG/state/evidence/fixture roots and no inherited ambient environment;
- opaque plan/stimulus execution binding; no candidate key or copied digest can substitute for it;
- declared argv/stdin/capture policy remains separate from tagged physically applied execution; a pre-process control records `NOT_APPLIED`, zero applied capture limits, and no executed argv or stdin delivery edge;
- a real `os/exec` Start failure records physical-entry and spawn-attempt truth, `started=false`, the exact capture limits handed to the runner, any allocated present-stdin pipe but no writer/handoff, `NOT_APPLIED` stdin delivery, and inspected invocation absence;
- candidate-written invocation evidence is reopened after teardown and must bind the exact attempt id and full logical argv.

The exact implementation boundary in this section is covered by the sealed U3 receipt map below. Broader nonclaims remain explicit.

## Closed v1 projection field registry

| Field id | Channel | Exact value | Missing/channel policy | Current grade |
|---|---|---|---|---|
| `cli.completion.kind` | exit | `EXITED` or `SIGNALED` string | reject incomplete completion | `TREE-EXACT` |
| `cli.exit.code` | exit | safe integer | tagged missing for signal | `TREE-EXACT` |
| `cli.exit.signal` | exit | exact signal string | tagged missing for normal exit | `TREE-EXACT` |
| `cli.stdout.bytes` | stdout | exact retained bytes | reject absent/truncated channel | `TREE-EXACT` |
| `cli.stderr.text` | stderr | exact UTF-8 string | reject absent/truncated/non-UTF-8 channel | `TREE-EXACT` |
| `cli.stdout.json.mode` | stdout | strict top-level JSON string | tagged missing; reject malformed/non-object JSON | `TREE-EXACT` |
| `cli.stdout.json.source` | stdout | strict top-level JSON string | tagged missing; reject malformed/non-object JSON | `TREE-EXACT` |

The registry admits no arbitrary dotted path, regex, tolerant comparison, implicit JSON coercion, custom projection code, or hidden operation. The precedence study selects only `cli.stdout.json.mode` and `cli.stdout.json.source`.

## Classification and admission matrix

| Trial evidence | Projection/admission result | Batch result | Current grade |
|---|---|---|---|
| clean exit `0`, complete channels, valid projection | eligible fingerprint | stable or unstable from all repetitions | `TREE-EXACT` |
| clean nonzero exit, complete channels, valid projection | eligible fingerprint | stable or unstable from all repetitions | `TREE-EXACT` |
| signal completion, complete channels, compatible selected fields | eligible fingerprint | stable or unstable from all repetitions | `TREE-EXACT` |
| complete empty stdout under bytes projection | eligible empty bytes | stable or unstable from all repetitions | `TREE-EXACT` |
| complete empty stdout under strict-JSON projection | typed `PROJECTION_REJECTED` | `UNCOMPARABLE` | `TREE-EXACT` |
| malformed strict JSON or selected-field type mismatch | typed `PROJECTION_REJECTED` | `UNCOMPARABLE` | `TREE-EXACT` |
| complete selected value exceeds its canonical/evidence limit | typed `PROJECTION_REJECTED` | `UNCOMPARABLE` | `TREE-EXACT` |
| timeout, cancellation, output cap, materialization/start/orphan/teardown failure | ineligible control, never projected | `UNCOMPARABLE` | `TREE-EXACT` |
| repeated identical eligible fingerprints | all exact trials retained | `OBSERVED_STABLE(k/k,h)` | `TREE-EXACT` |
| deliberate A/B eligible alternation | all exact trials retained | `UNSTABLE(histogram)` | `TREE-EXACT` |
| budget cannot begin a complete next matrix | no partial admission | `INCOMPLETE` run | `TREE-EXACT` |
| complete measurement matrix rejected by envelope | no tokens, trials, batches, or map for that matrix | `COMPARISON_REJECTED` run | `TREE-EXACT` |

U3 never says deterministic. A result cache, prior-attempt reuse, partial matrix, majority/last-trial shortcut, or adapter-created admission token is forbidden.

## Reference study

The dependency-free Node-core fixture defines three selected Git candidates with the same executable program and different candidate-owned precedence declarations:

1. config-first yields `{"mode":"config","source":"config"}`;
2. environment-first yields `{"mode":"env","source":"env"}`;
3. argv-first yields `{"mode":"argv","source":"argv"}`.

The shared stimulus supplies config=`config`, sparse `APP_MODE=env`, and argv `--mode argv`. Events `351` and `353` exercise three distinct stable fingerprints, fresh attempt/world/invocation identities for every repetition, rotation between repetitions, ambient HOME neutralization, visible projection operations/source links, and order/label/producer-metadata invariance of exact map identity. Fixture behaviors also cover constant, alternating, timeout, output-limit, signal, malformed projection, empty output, nonzero exit, incomplete budget, present-empty and present-byte physical stdin, and a one-candidate pre-process materialization control that remains an excluded `UNCOMPARABLE` member without forged physical application.

## Required exact mutation contract

`tools/mutate-u3.mjs` freezes an independent forty-three-ID/forty-three-tuple contract, a positive exact source manifest, one named no-subtest command per mutant, immutable source/seed manifests, exact one-file deltas, and three distinct private roots for A/B/A. Missing/duplicate/changed anchors, baseline or post-control failure, a wrong/skipped/missing named test, compile or tool failure, malformed Go JSON, source drift, root reuse, or a survivor closes the gate. The reviewed IDs are:

1. `reverse-stimulus-argv`
2. `collapse-absent-stdin-into-present-empty`
3. `materialize-absent-environment-as-present-empty`
4. `inherit-ambient-environment`
5. `allow-runner-owned-environment-namespace`
6. `bypass-plan-stimulus-prefix-binding`
7. `invert-fixture-reopen-rehash-check`
8. `accept-truncated-invocation-argv`
9. `treat-nonzero-exit-as-invalid-behavior`
10. `promote-output-limit-prefix-to-present-value`
11. `prefer-latched-exit-over-timeout`
12. `collapse-signal-field-to-missing`
13. `swap-stdout-stderr-projection-sources`
14. `hide-projection-operations`
15. `promote-projection-rejection-to-eligible`
16. `allow-prior-attempt-reuse`
17. `majority-classify-alternating-candidate`
18. `permit-partial-comparison-matrix`
19. `include-support-artifact-in-preservation-identity`
20. `drop-candidate-key-from-labeled-map-identity`
21. `miswire-stderr-capture-and-report-requested-limit`
22. `collapse-physical-present-empty-stdin`
23. `substitute-same-length-physical-stdin`
24. `allow-present-invocation-as-absent`
25. `forge-preprocess-physical-entry`
26. `allow-cross-paired-projection-rejection`
27. `omit-probe-transport-from-arbitration-receipt`
28. `bypass-reducible-direct-argv-grammar`
29. `bypass-resolved-capture-policy`
30. `bypass-resolved-adapter-projection`
31. `erase-declared-plan-rotation`
32. `bypass-observation-plan-schedule`
33. `bypass-observation-wall-admission`
34. `allow-reserved-git-fixture`
35. `defer-physical-entry-until-start-success`
36. `discard-late-overflow-earlier-primary`
37. `bypass-environment-aggregate-limit`
38. `trust-fixture-receipt-without-rehash`
39. `misclassify-projection-resource-limit`
40. `allow-node-inline-script-string`
41. `publish-rejection-after-wall-budget`
42. `forge-physical-entry-before-spawn-attempt`
43. `overwrite-earlier-control-with-late-output-overflow`

The fixture reopen mutant deliberately inverts the live good-byte comparison, while the separate receipt mutant keeps the recomputed values live but trusts only a superficially valid stored digest. Together they exercise both physical reopen/rehash and sealed-receipt self-validation; neither is by itself a fault-injection proof against a same-user race. The projection stdout/stderr mutant swaps both selected sources and is paired with exact per-field assertions, not merely a digest-inequality assertion. The physical stderr mutant both applies the wrong cap and forges the requested cap into the receipt; the receipt still cannot hide that mismatch because the physical-output test asserts retained bytes and overflow independently. The stdin mutants exercise a real pipe edge and writer-edge digest rather than only typed constructors. The pre-process and real-Start mutants separately defend `not entered` and `entered + attempted + not started` truth. The added authority mutants cover resolved capture/projection contracts, declared and observed schedule, whole-run wall admission, reserved Git metadata, late overflow, aggregate environment capacity, fixture-receipt self-validation, projection resource controls, and Node's fixed-program profile. Strict SourceSpec provenance is enforced by the architecture gate plus exact compile/recompile identity because identical direct construction is not dynamically distinguishable from a compiler product. Events `350` and `356` receipt the exact full A/B/A and fail-closed self-test boundaries.

## Sealed receipt map

The sealed manifest is authoritative. This table mirrors each final U3 claim's exact supporting event and verbatim grade.

| Event | Claim label | Capability | Verbatim grade |
|---:|---|---|---|
| 350 | `u3-exact-mutation-aba` | forty-three reviewed U3 semantic mutants with fresh-root A/B/A restoration | `TREE-EXACT` |
| 351 | `u3-cli-observation` | typed stimulus, binding, capture, projection, and focused CLI tests | `TREE-EXACT` |
| 351 | `u3-observe-orchestration` | rotated complete-matrix scheduling and four-way batch classification | `TREE-EXACT` |
| 351 | `u3-cli-precedence-study` | physical three-candidate precedence study and inspectable projection evidence | `TREE-EXACT` |
| 352 | `u3-cli-race` | CLI adapter and observe packages under the race detector | `TREE-EXACT` |
| 353 | `u3-cli-precedence-repeat-10` | repeated physical precedence study | `TREE-EXACT` |
| 354 | `u3-architecture-selftest` | hostile U3 architecture tripwires | `TREE-EXACT` |
| 355 | `u3-architecture` | generic/adapter/world authority boundary | `TREE-EXACT` |
| 356 | `u3-mutation-selftest` | mutation harness fail-closed tripwires | `TREE-EXACT` |
| 357 | `u3-world-engine` | fresh Darwin process and receipt boundary | `TREE-EXACT` |
| 358 | `u3-full-go-test` | every Go package on the final U3 tree | `TREE-EXACT` |
| 359 | `u3-go-vet` | complete Go vet pass | `TREE-EXACT` |
| 360 | `u3-full-diff-check` | full candidate whitespace check | `TREE-EXACT` |
| 361 | `u3-staged-diff-check` | exact staged U3 whitespace check | `TREE-EXACT` |
| 362 | `u3-staged-inventory` | exact staged path inventory | `TREE-EXACT` |
| 363 | `u3-tracked-credential-prefix-scan` | scoped staged-file credential-prefix scan | `TREE-EXACT` |

## Permanent negative history

Failed events `249`, `254`, `261`, `263`, `277`, `280`, `309`, `311`, `316`, `327`, `330`, `331`, and `336` remain permanent evidence of, respectively, compile/test-contract mistakes, projection/admission and physical-study expectation gaps, mutant survivors or non-semantic compile failures, architecture-checker false negatives, strict-canonical fixture mistakes, a wrong closed Go executable path, and the two final mutation-causality/harness defects. None supports a capability. Event `350` is the first complete green forty-three-mutant receipt on the exact sealed U3 tree.

## Explicit nonclaims

Security, sandboxing, confidentiality, malicious-candidate attestation, same-user path-race resistance, remote acquisition, Windows/Linux parity, non-Node executable profiles, HTTP observation, general workflow execution, arbitrary projections, global equivalence, determinism, behavioral correctness, reduction, Choicepoint authority, contract output, production readiness, adoption, and maintainership are `UNRECEIPTED` or out of scope.

The only next prompt after a strict-clean U3 boundary is `docs/prompts/P04-U4-HTTP-OBSERVATION.md`.
