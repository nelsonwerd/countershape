# P07B-C C3F — B future-surface admission maintenance

## State

- **Boundary:** active pre-seal `SOURCE_FULL` maintenance after sealed C3L and before the unchanged frozen C3 source unit. Classification: `DEFECT_REPAIR`.
- **Parent:** sealed C3L commit `efa918928246c7793f4ad7201c003020e3a42193`, tree `d9187c6449392c269a385b53c563ba4411112460`, is note-present with note blob `b21365e156719fbc84e480108a7e8672644f5a71`, note-body SHA-256 `d4721b344adb95442c5273e596c83ea9cb1b855d26655198a9803771f83e2e9d`, `12/12 claims recorded-exact`, strict exit `0`, and a logged redacted-export override.
- **Scope:** ten exact mode-`100644` paths, empty prefixes, sorted-newline digest `sha256:f64293895a1b824faafddf236757d381c813b59c40286225bb1c352d5c812c1a`.
- **Commit subject:** `fix: admit exact C3 target codec surface through B`
- **Preserved C3 work:** stash object `f7160bbbc09245a1026eb6396ac76282ab913192` contains the post-C3L implementation, exact C3L predecessor record, and source-shape repair. Earlier checkpoints `fa737661e61c36c10ce793f64852836edec20065` and `3e766715609680adaef4678fc9aeaa9e3a8bae09` remain retained. No stash supplies a C3F or C3 grade.

Every intended C3F grade remains `UNRECEIPTED` until one exact staged tree runs the declared commands through a fresh didrun ledger, commits, seals, exposes a readable Git note, and passes `NO_COLOR=1 didrun verify --strict`. This status cannot receipt C3F itself.

## Trigger and defect classification

After the first post-C3L baseline exposed and the in-scope C3 source corrected a source-shape issue, the next C3 architecture baseline was run through didrun and became the first to reach inherited B; it failed there. That three-event, zero-claim development ledger is preserved at `.didrun-history/2026-07-19-p07b-c-c3-build-loop-post-c3l-pre-c3f/.didrun/`. C3's exact composition calls the inert model authority by its exported codec names in two fixed owners: `internal/contractexec/target.go` constructs and parses `ContractExecutionTarget`, while `internal/store/nonhead_contract.go` parses the exact stored body before reissuing an inert typed record. The conservative B scanner inventories every word-shaped token containing a reserved C family in production Go. C3A had admitted only `internal/contractexec/target.go:ContractExecutionTarget`, so it rejected the three equally necessary exact pairs `internal/contractexec/target.go:NewContractExecutionTarget`, `internal/contractexec/target.go:ParseContractExecutionTarget`, and `internal/store/nonhead_contract.go:ParseContractExecutionTarget` as `P07B_B_PREMATURE_C_SURFACE`.

That failure is valid evidence that C3A's future-surface assumption was incomplete. Obscuring the identifiers, duplicating the codec outside `internal/contractexec/model`, accepting generic bytes, removing B from cumulative verification, or letting C3 edit its own prerequisite would weaken the architecture. C3F repairs the owning B policy at a separate review-visible boundary. C3's forty-path roster and source payload remain unchanged in the preserved stash.

## Exact repair

B retains its historical zero-C3 state, exact C1 model-prefix admission, and exact C2 storage/support pairs. The C3 target surface is now one all-or-none bundle of exactly four path/symbol pairs:

1. `internal/contractexec/target.go:ContractExecutionTarget`
2. `internal/contractexec/target.go:NewContractExecutionTarget`
3. `internal/contractexec/target.go:ParseContractExecutionTarget`
4. `internal/store/nonhead_contract.go:ParseContractExecutionTarget`

The bundle is admitted only when all four exact pairs are present in the complete production inventory. Zero members preserves the historical clean tree. One, two, or three members are all foreign. A complete bundle plus an extra C-family token admits only the exact four and leaves the extra foreign, so the outer B validation still fails. Relocated paths, basenames, prefixes, suffixes, alias-spelled lookalike identifiers, spelling variants, Unicode-suffixed identifiers, sibling packages, copied files, and other target/run/execution codecs receive no admission.

This is deliberately a lexical compatibility ceiling, not semantic proof. The B scanner conservatively inventories comments and literals as well as executable tokens. C3's compiler-visible public-surface test, architecture checker, Go-JSON execution profiles, runtime tests, and composition/recovery tests must separately prove that the pairs are real typed calls with the intended ownership and behavior.

The defensive B self-test keeps its historical direct and symlink-entrypoint clean controls, checksum-pins its metadata roster, and independently proves historical absence, the complete four-pair bundle, all fourteen nonempty proper subsets (including helper-only and old type-only states), relocated owners, renamed and Unicode-suffixed identifiers, and complete-bundle-plus-extra/foreign cases. Every case compares the exact admitted and foreign arrays rather than accepting a generic rejection.

## Exact C3F scope

C3F owns exactly these 10 paths: `docs/ARCHITECTURE.md`, `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3F-B-FUTURE-SURFACE-ADMISSION.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-b-architecture-selftest.mjs`, `tools/check-p07b-b-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:f64293895a1b824faafddf236757d381c813b59c40286225bb1c352d5c812c1a`.

Schema v9 orders `C3P -> C3V -> C3M -> C3PB -> C3A -> C3L -> C3F -> C3`. Prefixes are empty. C3 remains frozen to forty exact paths and digest `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`; C3F cannot absorb, edit, or relabel any C3 implementation path. C3 must bind sealed C3F as its direct predecessor while retaining sealed C3L as C3F's parent and sealed C3A as C3L's parent.

## Intended C3F claim map

| Exact claim label | Claim type | Pre-seal grade |
| --- | --- | --- |
| `P07B-C C3F B future-surface plan coherence` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F B future-surface plan defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F historical B architecture compatibility` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F B conditional future-surface defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F unit-scope defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F cumulative verifier self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F cumulative verification` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3F exact ten-path staged scope and diff integrity` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3F scoped staged credential-pattern scan` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3F sealed-C3L predecessor and preceding didrun chain integrity` | `command-succeeded` | `UNRECEIPTED` |

Only these ten labels may be claimed. Every command uses one exact `/usr/bin/env -i` prefix, pinned local tools, offline Go settings, disabled Node startup injection, and a dedicated private `.countershape/p07bc-c3f-final` root. The final chain gate binds the first nine events and its own live invocation/private-directory context; before C3F commits, it additionally requires current `HEAD` to equal sealed C3L and independently reopens C3L's exact `refs/notes/didrun` blob, body, ordered twelve-claim semantics, and 12/12 event coverage.

## Development-evidence note

The first syntax-check launcher used `path` as a zsh loop variable. zsh ties that name to `PATH`, so didrun could not locate Git and exited before recording an event. The empty attempt was preserved at `.didrun-history/2026-07-19-p07b-c-c3f-launcher-path-shadow/.didrun`; the corrected launcher used `source_file`, and both syntax checks then recorded exit `0`. This was an operator launcher defect, not a Countershape test failure or a didrun receipt.

## Nonclaims and gate back to C3

If the declared gates close, C3F can prove only exact historical/future lexical admission in B and the continued cumulative baseline. Its intended predecessor check is point-in-time local Git object/note binding; it does not authenticate C3L, rerun C3L, or prove C3F's future seal. It can prove no target semantics, parser correctness, durable target relation, Git materialization, runtime measurement, boot identity, attempt allocation, process spawn, classification, confidentiality, hostile same-user boundary, production readiness, adoption, or maintainership. Comments and literals remain conservatively inventoried; admission does not prove a token is executable code.

After the full C3F build loop, stage exactly the ten paths, archive the development ledger, create a fresh ten-claim final ledger and private run root, run and immediately claim only the declared rows, commit, seal, inspect the note, loop strict verification to exit `0`, emit the exact-commit HTML report, and archive the final ledger. Then apply stash `f7160bbbc09245a1026eb6396ac76282ab913192` by full object ID without dropping, replace C3's direct predecessor with sealed C3F, and resume the C3 multi-pass behavior loop.
