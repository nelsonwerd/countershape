# P07B-A2.2 recoverable compiler receipt

- **State:** implementation complete and corrected; the accepted source is the corrected descendant below, whose serialized didrun chain is intact, Git note exists, and strict verification exits `0`
- **Prerequisites:** sealed P07B-A1 source commit `1e56da3bfafc4cdb8abdca62f18fb3b1accea8c3`, sealed P07B-A2.1 compilation authority, and the sealed cumulative-verification maintenance boundary
- **Receipt ceiling:** deterministic pure compilation, strict recovery, generated Node-core direct-contract behavior, checked-in Go/Node parity, and the named local human-surface capture only; no residue, publication, materialization, target, run, execution classification, or product surface
- **Implementation checkpoint:** commit `32efaaf2f6d9fc58389d48d18219ce03995a2805`, tree `d01848700be32bc7f9458f6002e0f4b81673ca11`; Git note present; strict exit `0`; `20/20` claims recorded-exact
- **Accepted corrected source:** commit `6bf33350944dcdd56a4ceaa70f1d0836203ffb1f`, tree `d394b8a9af3f92521a13a00868109fdfe234de26`; Git note present; strict exit `0`; `11/11` claims recorded-exact
- **Export disclosure:** both manifests record `secrets_override: true` after plain seal was blocked by aggregate high-entropy findings; exact staged inventories and scoped full-index structured credential-pattern scans passed first, but neither scan nor override establishes secret absence or publication safety
- **Receipt-document rule:** this reconciliation is durable only after the commit containing it receives its own Git note and strict exit `0`; the two source Git notes remain authoritative for the verbatim grades copied below

## Implemented boundary

P07B-A2.2 consumes only the private authority-narrowed input owned by the sealed
A2.1 preparation and adds:

- a deterministic partial compiler that emits exactly six sorted nonempty mode-`100644` files under fixed aggregate, per-file, and canonical-body ceilings;
- a typed external `ContractBundle` digest, strict canonical parser, defensive model, exact six-file recovery, source/profile/predicate joins, and coherent alternate-bundle semantics without claiming self-authentication;
- fixed checked-in Node-core entrypoint and harness assets, with a strict five-companion manifest verified before the harness import;
- a pure result selector exhaustively checked across the closed result/reason precedence surface, plus an exact direct-result TAP diagnostic with zero exit only for conformance, controlled empty outer stderr, and no authority flowing into later target/run/execution objects;
- intact generated assets physically exercised over the naturally constructible CLI/child-bind HTTP surface using a source-only private execution copy, create-new fixture overlay, sparse environment, fixed readiness grammar, and bounded capture/timeout/cleanup behavior;
- copied or explicitly instrumented test-edge assets that exercise otherwise unconstructible mapping paths without claiming byte-identical production-asset behavior;
- input-only Go and Node semantic evaluators plus one strict checked-in corpus, literal expected results owned only by the driver, poisoned-expectation/order/duplicate-input properties, and exact parser/framing bounds;
- fresh-process compiler-to-parser recovery without the compiler input, repository lookup, or corpus path at the parser edge; and
- an evolved A2.2 architecture checker that retains A1 and A2.1 closure while prohibiting filesystem, process, Git, clock, randomness, runtime, environment, and network authority from the production compiler closure.

The compiler itself is pure. Test-edge materialization and execution are physical
evidence for the named generated artifact, not production materialization
authority.

## Exact runtime and human-surface scope

- The exercised local tuple is Node `v25.2.1`, Go `1.26.5`, macOS `26.5.2`, Darwin kernel `25.5.0`, arm64. Other tuples remain `UNRECEIPTED`.
- The checked-in capture's conforming, contradicting, and timeout-ineligible records run the intact generated program assets. Its malformed-data record uses repinned altered decision data, its tamper record uses an intact entrypoint over an unrepinned companion change, and its harness-failure record uses a repinned explicitly instrumented harness. The latter records are deterministic mapping/UX evidence, not a claim that an intact production asset naturally generated those injected conditions.
- README rendering is deterministic at 60, 80, and 120 Unicode-code-point columns under `countershape-terminal-capture/v1`; TAP records normalize only the closed stable fields under `countershape-node-tap-normalization/v1`.
- The README places the full-user-authority and host-network warning before invocation, states that confidentiality is not established, requires a separately prepared target root, fail-closes the quoted `cd` with `&&`, and says direct invocation establishes no Git target identity.
- The didrun-wrapped renderer/capture checks can receipt deterministic bytes and exercised normalized records. Manual screenshot judgment and the different-model critic are qualitative design observations only and remain `UNRECEIPTED` as didrun or semantic evidence.

## Implementation checkpoint receipt map

These grades are copied verbatim from the sealed Git note on implementation
checkpoint `32efaaf2f6d9fc58389d48d18219ce03995a2805`. This checkpoint is not the
accepted final source by itself; the corrected descendant below repairs the
runtime-generic README and platform-fact separation found by the final audit.

| Claimed capability | didrun claim label | Verbatim grade |
| --- | --- | --- |
| Repository-wide tracked Go formatting and checked-in Node syntax | `P07B A2.2 formatting and Node syntax` | `TREE-EXACT` |
| Focused compiler, model, parser, ceiling, and recovery suites | `P07B A2.2 focused compiler model parser suites` | `TREE-EXACT` |
| Go/Node corpus parity, framing, and oracle separation | `P07B A2.2 Go Node parity and oracle separation` | `TREE-EXACT` |
| Exhaustive pure direct/owner result-selector matrix | `P07B A2.2 pure result selector exhaustive matrix` | `TREE-EXACT` |
| Intact generated CLI/HTTP physical behavior matrix | `P07B A2.2 intact generated CLI HTTP physical matrix` | `TREE-EXACT` |
| Copied/instrumented lifecycle and otherwise-unconstructible mapping matrix | `P07B A2.2 instrumented fault mapping matrix` | `TREE-EXACT` |
| Deterministic compilation, cross-process recovery, and tamper behavior | `P07B A2.2 deterministic compile recovery and tamper matrix` | `TREE-EXACT` |
| Schema, runtime bundle, planning example, and generated-body parity | `P07B A2.2 schema runtime example and planning parity` | `TREE-EXACT` |
| Deterministic renderer self-test and exact human-surface mapping/UX capture | `P07B A2.2 human surface deterministic capture` | `TREE-EXACT` |
| Complete Go repository suite | `P07B A2.2 complete Go repository suite` | `TREE-EXACT` |
| Complete Go vet | `P07B A2.2 complete Go vet` | `TREE-EXACT` |
| Focused race-enabled compiler/runtime/parity coverage | `P07B A2.2 focused race suite` | `TREE-EXACT` |
| One-worker bundle-parser fuzzing, 10,000 iterations | `P07B A2.2 bundle parser fuzz 10000 iterations` | `TREE-EXACT` |
| One-worker corpus-line fuzzing, 10,000 iterations | `P07B A2.2 corpus parser fuzz 10000 iterations` | `TREE-EXACT` |
| Layered A1/A2.1/A2.2 architecture boundary | `P07B A2.2 exact architecture boundary` | `TREE-EXACT` |
| Clean controls plus 117 defensive architecture copies | `P07B A2.2 architecture 117 case defensive self-test` | `TREE-EXACT` |
| One-command cumulative current baseline | `P07B A2.2 cumulative verification baseline` | `TREE-EXACT` |
| Exact staged path inventory and staged diff integrity | `P07B A2.2 exact staged inventory and diff check` | `TREE-EXACT` |
| Scoped structured credential-pattern scan over staged blobs | `P07B A2.2 scoped staged structured credential-pattern scan` | `TREE-EXACT` |
| Serialized didrun ledger chain integrity | `P07B A2.2 didrun chain intact` | `TREE-EXACT` |

## Corrected descendant receipt map

These grades are copied verbatim from the sealed Git note on accepted corrected
source `6bf33350944dcdd56a4ceaa70f1d0836203ffb1f`.

| Claimed capability | didrun claim label | Verbatim grade |
| --- | --- | --- |
| Corrected Go/Node/JSON formatting and syntax | `P07B A2.2 correction formatting and syntax` | `TREE-EXACT` |
| Regenerated bundle, downstream planning examples, and human-surface capture | `P07B A2.2 corrected generated artifact chain exact` | `TREE-EXACT` |
| Runtime-generic generated README and receipt-only host tuple | `P07B A2.2 runtime generic README and platform receipt separation` | `TREE-EXACT` |
| Corrected compiler, model, parity, and generated-runtime suites | `P07B A2.2 corrected compiler model parity and runtime suites` | `TREE-EXACT` |
| Planning validator baseline plus 76 defensive mutations | `P07B A2.2 planning validator 76 case self-test` | `TREE-EXACT` |
| Corrected layered architecture boundary | `P07B A2.2 corrected exact architecture boundary` | `TREE-EXACT` |
| Corrected clean controls plus 117 defensive architecture copies | `P07B A2.2 corrected architecture 117 case defensive self-test` | `TREE-EXACT` |
| Corrected one-command cumulative current baseline | `P07B A2.2 corrected cumulative verification baseline` | `TREE-EXACT` |
| Corrected exact staged path inventory and diff integrity | `P07B A2.2 correction exact staged inventory and diff check` | `TREE-EXACT` |
| Corrected scoped structured credential-pattern scan | `P07B A2.2 correction scoped staged structured credential-pattern scan` | `TREE-EXACT` |
| Corrected serialized didrun ledger chain integrity | `P07B A2.2 correction didrun chain intact` | `TREE-EXACT` |

## Superseded source-rewrite design

The proposed recipe-level source-rewrite driver was never completed, checked
in, claimed, or sealed. It supports no capability and remains `UNRECEIPTED`.
A2.2 instead keeps independent black-box conformance/negative matrices,
oracle-separation properties, differential parity, coherent recovery/tamper
cases, bounded fuzzing, exact boundaries, deterministic subprocess checks, and
the hostile architecture self-test. This is a transparent pre-claim evidence
redesign and makes no source-rewrite or mutation-completeness claim.

`FuzzExactOpaqueBytesRemainExact` remains seed-executed by the complete suite.
No active 10,000-iteration fuzz receipt is claimed for that target in this
unit, so active fuzz coverage for it remains `UNRECEIPTED`.

## Permanent negative history

The unsealed development ledgers preserve nonzero build/test/checker events and
successful events against superseded tree identities. They support no final
capability. Two early expanded architecture-self-test attempts exceeded the
checker subprocess budget in different unrelated cases; the implementation
then reduced cumulative parser work without weakening the 117-case roster, and
the complete self-test passed on the later tree.

The implementation checkpoint was frozen before a final read-only audit found
two specification defects: its README embedded a host exercise statement even
though R7 requires runtime-generic generated source, and its status text
conflated the macOS product version with the Darwin kernel version. The accepted
corrected descendant removes the host tuple from generated bytes, regenerates
every digest-linked artifact, distinguishes macOS `26.5.2` from Darwin kernel
`25.5.0`, repins the exact architecture and planning-validator expectations,
and reruns their defensive matrices plus the cumulative baseline.

The corrective development ledger also preserves an operator shell-quoting
error, the expected stale architecture fingerprint, and the stale planning
validator digests caught by cumulative verification. Each nonzero result was
left unclaimed, the real cause was repaired, and the relevant didrun-wrapped
gate passed again on the accepted tree. Platform generation refusals while
drafting the abandoned source-rewrite design are session diagnostics, not
didrun evidence or caught faults. No nonzero event is relabeled as a pass.

## Explicit nonclaims

P07B-A2.2 does **not** establish:

- terminal `RESIDUE`, store publication, a study-head transition, stale-before-publication closure, retryable product materialization, or filesystem publication semantics;
- a pinned Git target, verified target inventory, runtime-admission capability, finalized run, execution classification, or execution evidence usable by P07B-C;
- Countershape absence from a future target, subject package/dependency absence, external-network denial, sandboxing, hostile containment, confidentiality, secret absence, authenticity, authorship, or coordinated-replacement resistance;
- entrypoint or manifest self-authentication, immunity to same-user verification/import races, or identification of a coherent alternate bundle as a forgery;
- Linux, Windows, other architectures, other Node/Go versions, framework portability, future runtime compatibility, production readiness, security review, adoption, legal clearance, release readiness, or maintainership;
- universal human comprehension from three deterministic widths or one qualitative critic; or
- any conclusion stronger than a sealed receipt's verbatim grade.

## Sole next boundary

After the receipt-document commit containing this reconciliation is sealed,
Git-note-present, and strict-clean, the sole next feature unit is P07B-B
terminal publication plus retryable materialization under the controlling
preimplementation contract in
`docs/prompts/P07B-B-TERMINAL-PUBLICATION.md`. P07B-C target/run/
execution and every product CLI, server, dashboard, report, or studio surface
remain later independently gated units.
