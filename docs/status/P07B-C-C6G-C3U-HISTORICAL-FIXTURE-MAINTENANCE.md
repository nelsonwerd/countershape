# P07B-C C6G — C3U historical future-C6 fixture maintenance

**State:** active pre-seal `SOURCE_FULL` defect repair. Every C6G grade is `UNRECEIPTED` until its exact staged tree completes its own ten-event didrun ledger, commit, seal, readable Git note, strict verification, HTML report, and ledger archive.

## Boundary and authority

C6G is the owner-authorized child of exact sealed C6F and the required parent of C6M. It repairs one ambient-input defect exposed by the real C3U-absent renderer after the reconciled C6M payload introduced its canonical future authority file. It changes no Countershape product behavior, production Go source, verifier roster, runtime setting, or sealed evidence.

- **Commit subject:** `fix: isolate C3U historical C6 fixtures`
- **Direct parent:** exact sealed C6F commit `dd2a011edff20d65c43934fc7c3e5a09b5829d2e`, tree `b0141ac2671df75e51c4fdb5b71c35cee5d0f4ea`
- **Parent note:** blob `677e7dda3fc445bda07a7b473b0a40b9a18cea00`, body SHA-256 `fdabc5c9518d1b0d14981cc8dc9c99815b8caf57c481b6fbd75edd58390665f3`
- **Sealed C6A ancestor:** commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`
- **Transition source:** `OWNER_OUT_OF_BAND`
- **Classification:** `OWNER_AUTHORIZED_AUTHORITY_MIGRATION / DEFECT_REPAIR`
- **Predecessor declaration:** `NONE`
- **Provenance:** `UNEVIDENCED`, disclosed as `OWNER_ATTRIBUTED_SESSION_INSTRUCTION_ONLY_NO_QUALIFYING_PREEXISTING_ARTIFACT`
- **Authentication:** `NOT_ESTABLISHED`
- **Signed authorization:** `NOT_IMPLEMENTED`

The owner instruction was real, but no qualifying instruction artifact pre-existed in the direct-parent tree. This candidate-owned status cannot authenticate or retroactively evidence it. Receipt C6A remains `ABSENT`, and the C6A source-authority manifest remains absent; C6G cannot receipt, transfer, relabel, renew, or strengthen any ancestor grade.

The current machine authority is `countershape/p07b-c-unit-paths/v28`, with 42 ordered unit rows and 30 receipt-phase rows at receipt-phase authority digest `sha256:f48f7eafe266fad4a6908bb9319f6adb9739f95a32276a7325c9ac28d7ab8e95`.

## Observed defect and permanent evidence

The reconciled C6M five-path payload reached a six-event development ledger before its unclaimed plan-self-test event failed. The exact refusal was `P07B-C C3 validated C3U-active absent fixture failed: docs/HANDOFF_MODE_C.md: receipt phase observed-state mismatch (receipt phase C3U C6A source-authority manifest must be ABSENT)`. The failed ledger remains permanent at `.didrun-history/p07b-c-c6m-development-c3u-fixture-red-870a690744a0/.didrun/`; its candidate payload remains parked in stash object `6a0f9bd03c706a2a8afd23a68bb0c18d42551896`. No event from that attempt supports a C6M or C6G grade.

The root cause was narrower than the phase model. `syntheticC3AbsentPlanFixture()` reconstructed a historical C3U-absent corpus and then spread `...current`; because only its HANDOFF and historical receipt bytes were overridden, every later C6 authority path could fall through to current descendant bytes. A candidate manifest was therefore interpreted as historical C3U state and correctly refused.

## Repair and defensive authority

C6G installs the already-defined four-path absence projection after `...current`, then validates that exact projection before any historical full-plan call. The ordered JSON-lines path digest is `36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218`. The helper's exact 11-case baseline, omission, substitution, foreign-key, and ordering manifest is `983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`. Every projected value must be the private explicit-absence sentinel; omitted keys may not reopen ambient bytes.

The repair also exercises the real renderer rather than treating the helper as a unit-test substitute. Its ordered nine-case oracle contains one passing baseline, four cases that omit one required projection, and four cases that replace one sentinel with a descendant value. All eight hostiles must refuse through the ordinary full-plan path. The manifest digest is `c591e1874545674f0579db04d731ce5c366c408e99d3cd27eb63ffbef3ad4bf1`.

Sealed C6F remains valid. Its third claim was explicitly bounded to the named C3Q override installation and its four-path/11-case authority, while its status stated that other historical `checkPlan()` inputs remained ambient and outside the claim. C6G repairs that separately discovered C3U renderer without rewriting, extending, or relabeling the sealed C6F fact.

Both generic checkers independently project the future C6G subject, ten labels/types, ten command tails, final-root identity, and position-specific didrun redaction policy. Their sealed-source contract digest is `sha256:3dfffad546f7fee36e2e51856cea98a754626f395f319b0c6264fe918df3ef00`. Synthetic note parity is a checker contract only; it is not evidence that a future note exists.

## Scope

C6G owns exactly these seven mode-`100644` paths:

1. `docs/HANDOFF_MODE_C.md`
2. `docs/PROMPT_PACK.md`
3. `docs/VERIFICATION.md`
4. `docs/status/P07B-C-C6G-C3U-HISTORICAL-FIXTURE-MAINTENANCE.md`
5. `spec/verification/p07b-c-unit-paths.json`
6. `tools/check-p07b-c-plan.mjs`
7. `tools/check-p07b-c-unit-scope.mjs`

The sorted-newline roster digest is `sha256:c2b754a773ed5f1a17a3da9ca61f13525fea7235fb7e09422582c3ab2fa14724`.

Prefixes are empty. C6F's source paths, all product/runtime source, the cumulative verifier, the C6A evidence files, the receipt declaration, and the source-authority manifest are outside this roster and remain byte-stable.

## Development recorder observation

A didrun-wrapped plan self-test opened a persistent terminal but appended no event while the terminal remained silent. After roughly eleven minutes, a separate read-only host process inventory did not surface the wrapper or checker child. Interrupting that apparently stale terminal handle returned a Python traceback showing didrun still blocked in `subprocess.communicate()` and terminated the handle with exit `130`; no didrun event was appended. The attempt therefore supplies no checker result and no claim evidence.

This is a live S6 observability finding, not a proven didrun correctness defect: the host process inventory and terminal state disagreed, and the operator interruption prevented a natural terminal result. The permanent development history and final handoff must disclose that ambiguity. A fresh run from the repaired staged tree is required; no mid-command continuation is possible.

## C6G final manifest

| # | Claim | Type | Intended grade |
|---:|---|---|---|
| 1 | `P07B-C C6G data-driven phase plan coherence` | `tests-pass` | `UNRECEIPTED` |
| 2 | `P07B-C C6G independent candidate transition authority` | `tests-pass` | `UNRECEIPTED` |
| 3 | `P07B-C C6G historical C3U future-C6 fixture isolation defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 4 | `P07B-C C6G sealed-C6F Git-note and sealed-C6A ancestry compatibility` | `tests-pass` | `UNRECEIPTED` |
| 5 | `P07B-C C6G unit-scope defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| 6 | `P07B-C C6G cumulative verifier self-test` | `tests-pass` | `UNRECEIPTED` |
| 7 | `P07B-C C6G cumulative verification` | `tests-pass` | `UNRECEIPTED` |
| 8 | `P07B-C C6G exact seven-path staged scope and diff integrity` | `command-succeeded` | `UNRECEIPTED` |
| 9 | `P07B-C C6G scoped staged credential-pattern scan` | `command-succeeded` | `UNRECEIPTED` |
| 10 | `P07B-C C6G sealed-C6F predecessor and preceding didrun chain integrity` | `command-succeeded` | `UNRECEIPTED` |

## Exact final command order

1. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --check-candidate-phase C6G`
2. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --candidate-phase C6G`
3. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --self-test`
4. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6g-sealed-c6f-note`
5. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --self-test`
6. `/opt/homebrew/bin/node tools/verify-current-selftest.mjs`
7. `/opt/homebrew/bin/node tools/verify-current.mjs`
8. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --unit C6G --source-final-gate`
9. `/opt/homebrew/bin/node tools/check-p07b-c-unit-scope.mjs --unit C6G --credential-scan`
10. `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6g-preseal-ledger`

Run each command once in the generated C6G runbook and claim it immediately; any nonzero command stops that ledger, supports no claim, and requires a repaired fresh boundary.

The first seven prove the phase transition, the real C3U renderer's exact four-path future-C6 isolation plus defensive oracle, exact sealed-C6F and sealed-C6A ancestry, both generic checker self-tests, and the unchanged cumulative verifier roster. The last three prove exact staged ownership, structured credential-pattern absence over the seven blobs, and final ledger/ancestry coherence.

## Claim ceiling and continuation

C6G may claim only the named C3U historical renderer repair, the bounded four-path/11-case helper authority, the nine-case real-renderer oracle, exact parent/ancestor compatibility, current checker/verifier coherence, exact staged scope, and exact didrun chain integrity after its own receipt exists. It does not claim a product change, a C6A receipt, renewal of C6F, portable private-evidence availability, secret absence, confidentiality, hostile same-UID isolation, security certification, production readiness, adoption, or maintainership.

The next permitted edge is C6M only after this boundary independently closes. C6M may then restore the parked five-path payload, bind the canonical C6A source-authority manifest, and validate the exact `C6G → C6F → C6A` ancestry. C6B remains a later separate receipt reconciliation through `C6M → C6G → C6F → C6A`; no grade crosses either edge automatically.
