# U7C CLI decision and contract flow

## Boundary identity

- **State:** active replacement source candidate; not committed, sealed, or receipted; eight permanently closed precommit attempts—seven gate failures and one operator-requested relocation interruption—and one failed postcommit attempt are retained below.
- **Namespace:** `countershape/u7-unit-paths/v3`.
- **Boundary:** `U7C`.
- **Declared parent:** exact sealed `U7N`.
- **Subject:** `feat: add U7 CLI decision and contract flow`.
- **Profile:** `SOURCE_FULL`.
- **Product authority:** `U7_REFERENCE_APPLICATION`.
- **Product behavior:** `CANDIDATE_UNRECEIPTED`.
- **U7D source receipt:** `ABSENT`.
- **Final private root:** `.countershape/u7c-final`.
- **Next boundary:** `U7D` only after this exact candidate closes its own fresh 12-command ledger, seal, note, strict HTML witness, archive, and terminal audit.

Exact parent U7N is commit `ca50d47b99c467b0b07a91fc30666664c345ff2c`, tree `6f2614071bc827c38fcdabaeca494259bb1ad7f6`, direct parent U7B `7ae0d4847b4479e3d85b979ab26825f49638db54`, and subject `fix: reconcile U7 CLI and milestone authority`. Its didrun note is blob `6fc5769303ee861b829d776411c0376787bd4f77`, 33,727 bytes, with note-body SHA-256 `8bf312b4a36d803f7f16546127d214f06fb0b870c456667609f0ce0a350debd5`. U7N closed `15/15` claims at `TREE-EXACT`, strict exit `0`, with `secrets_override: true`. Its local 9,478-byte HTML witness is `.countershape/evidence/u7n-final-ca50d47b99c4.html`, SHA-256 `eef4b83be7a8a3e3cba9a0d4e09610eaa3a0b5fca1b6ca16de8c63100afb7fde`; its commit-addressed archive is `.didrun-history/u7n-final-ca50d47b99c4/.didrun`.

Those identifiers admit U7N only as the exact predecessor. They transfer no U7C grade and do not make the local HTML or private ledger portable evidence. Every U7C row below remains `UNRECEIPTED` until one exact final attempt closes.

## Product boundary

U7C owns one independent CLI reference study plus the minimum shared composition surface. It must not route CLI values through the HTTP study, create app-local semantic truth, or reopen U7B's HTTP-only files. The intended flow is:

```text
outer CLI fixture -> immutable Git pins -> strict SourceSpec / WorldPlan
  -> fresh CLI worlds -> typed captures and projection -> exact labeled outcome maps
  -> tri-state reduction -> durable strong grade -> fresh confirmation
  -> blind Choicepoint and ruling -> portable six-file CLI bundle
  -> bounded four-way inert official-target prepublication
  -> ten strictly sequential sealed CLI executions, finalized runs, and classifications
  -> bounded four-way no-spawn terminal graph revalidation
  -> sealed completed-study projection -> exact 111-file harness evidence
```

The application layer owns parsing, admission, safe presentation, the exact four-field machine result, and the already-open evidence workspace. `internal/reference/clistudy` owns study semantics. `testkit/reference` owns the direct outer fixture admission. The driver prepares only the fixture and receives no evidence-root authority.

## Exact U7N semantic correction carried into U7C

The official reference execution is intentionally narrow:

1. it uses the exact runner-enrolled candidate inventory and exact enrolled reference stimulus;
2. that stimulus supplies no behavior override, so the fixture completes with normal exit `0`;
3. the separate exit-`7` fixture behavior remains substrate coverage rather than enrolled official-reference authority;
4. a clean signal is eligible with selected exit code `MISSING` and therefore contradicts the selected normal-exit tuple;
5. invalid or traversal overlay input is rejected before spawn and creates no official execution;
6. timeout, malformed selected JSON, and output-limit cases may prove adapter/world behavior but altered stimuli are scope-unenrolled and cannot become official reference classifications;
7. runtime escaped-write behavior is unproven; and
8. `CLI_OFFICIAL_EXECUTION_GENERALIZATION_UNVALIDATED` remains explicit.

U7C must not restore the superseded exit-`2` or signal-ineligible wording. It must not relabel process/artifact observation, a wrapper digest, or an error string as core target/run/classification authority.

## Load-bearing semantic gates

The candidate is acceptable only if its product tests independently establish all of the following:

- three exact precedence candidates preserve config, environment, argv, ordered argv, sparse bindings, private `HOME`, and `Absent` versus `Present("")` distinctions;
- every selected candidate is pinned once from the harness-owned repository, and later display-ref movement cannot change admitted commit/tree identity;
- comparison uses the full candidate-key-to-projection-fingerprint map after plan, envelope, roster, eligibility, and exclusion comparability, never partition shape, majority, display labels, or naked digests;
- an exact-map recovery with fresh evidence preserves, a same-shape/different-fingerprint mutant changes, and eligibility drift is unresolved;
- one intentionally incomplete sweep remains `BEST_KNOWN`, while a complete durable exact neighbor sweep alone earns `ONE_MINIMAL_UNDER`;
- confirmation allocates new attempts, roots, processes, captures, and invocation evidence and reproduces the retained preserving map under a rotated schedule;
- blind review precedes provenance reveal, ambiguous selected fields refuse, allow-many retains complete correlated tuples without Cartesian expansion, and noncompilable actions emit no executable files;
- the portable CLI source, ruling, decision, six-file bundle, residue, target, finalized run, and classification remain digest-linked through their existing sealed constructors;
- ten contract rows are ten distinct live official target/run/classification graphs: the exact selected commit is pinned once, inert target publication is bounded across four independent repository capabilities and joined at a full barrier, all ten `ExecuteCLI` subject runs remain strictly sequential, and only later no-spawn terminal graph revalidation uses bounded four-way scheduling; and
- no evidence file or `GREEN` result is published before the package-sealed completed value revalidates every join.

## Evidence protocol and ceiling

Each clean harness run requires exactly 100 logical trials and 111 evidence files:

- fixture `1`;
- compile `1`;
- search `80`;
- confirmation `8`;
- contract `10`;
- five deterministic artifact wrappers;
- eight fresh artifact wrappers; and
- ninety-eight phase receipts.

The U7P-origin/U7N-amended `countershape/u7-study-harness/v2` independently observes the outer fixture Git/worktree bytes, exact prepare/compile/execute argv and executable identities, artifact paths/modes/bytes, within-domain deterministic-wrapper equality, ordinal-bearing fresh-wrapper nonaliasing, and direct subject-process wall/RSS. It does not independently recompute product-semantic linkage, compare raw product-computed authority across runs, attribute resources to each logical trial, measure total parent-harness resources, or establish hostile containment.

The deterministic `ruling`, `decision-record`, and `contract-bundle` files are explicitly labeled `SEMANTIC_REGRESSION_PROJECTION`. They project invariant action, selected-field, exact correlated-tuple/predicate, record-validity, and six-file-bundle facts only after the strict typed authorities revalidate. They are not the canonical `DecisionRecord` or `ContractBundle` bytes or their raw authority digests; those remain fresh-lineage semantic authorities in `StudyInspection` and are not compared across harness runs.

The expected bounded harness verdict is `LOCAL_CLI_DECISION_AND_ENROLLED_REFERENCE_EXECUTION_PROCESS_ARTIFACT_GREEN_SUBJECT_RESOURCE_OBSERVED`. It does not imply portable correctness, general CLI execution authority, imported-repository safety, or full resource measurement.

Event 5 is conjunctive. Under the frozen top-level race command, the race-instrumented package process synchronously builds a current-tree `clistudy.test` child with explicit `-race=false`, rejects child build information carrying `-race=true`, and runs the complete exact 80/8/10 completion, choice, official-graph, inspection, and hostile-forgery closure there. Separately, the race-instrumented parent runs the real nine-trial CLI `Run` path plus the bounded application, workspace, publication, cancellation, fixture, concurrency, and remaining package tests. The child must return terminally inside the nested test-only deadline before the parent can pass. This topology does not establish full-workflow race instrumentation or race freedom, and its child timing is not event-7 harness or resource authority. Event 7 remains the separate compiled-process, exact-artifact, and three-run protocol observation.

The complete physical closure test is intentionally restricted to event 5's build-info-verified race-parent/non-race-child path. An unmarked ordinary non-race package process, including event 8's cumulative `go-test-general` row, reports that one test as skipped instead of executing a second 80/8/10 workflow. Event 8 compiles the closure and runs the remaining cumulative roster; it is not a second complete-workflow receipt and inherits no event-5 grade.

## Exact owned roster

All 18 paths are required mode-`100644` tracked blobs. No other staged path is authorized:

1. `cmd/countershape/main.go`
2. `internal/reference/app/command.go`
3. `internal/reference/app/envelope.go`
4. `internal/reference/app/render.go`
5. `internal/reference/app/workspace.go`
6. `internal/reference/clistudy/study.go`
7. `internal/reference/clistudy/reduction.go`
8. `internal/reference/clistudy/choice.go`
9. `internal/reference/clistudy/contract.go`
10. `internal/reference/clistudy/inspect.go`
11. `internal/reference/clistudy/study_test.go`
12. `internal/reference/clistudy/choice_test.go`
13. `internal/reference/clistudy/contract_darwin_test.go`
14. `testkit/reference/cli.go`
15. `testkit/reference/cli_test.go`
16. `tools/run-u7-cli-study.mjs`
17. `docs/HANDOFF_MODE_C.md`
18. `docs/status/U7C-CLI-DECISION-CONTRACT.md`

The HANDOFF edit must remain outside every frozen P07 marked block. U7C may not modify the U7 specification, validators, architecture oracle, harness, U7A-only app files, U7B-only HTTP files, protected P07/core files, runner enrollment, or sealed fixture bytes.

## Exact claim manifest

Every grade remains `UNRECEIPTED` in this source candidate:

| Event | Type | Exact claim label | Current grade |
|---:|---|---|---|
| 0 | tests-pass | U7C candidate plan and sealed-U7N parent authority | `UNRECEIPTED` |
| 1 | tests-pass | U7C independent candidate transition and exact staged authority | `UNRECEIPTED` |
| 2 | tests-pass | U7C plan contract defensive self-test | `UNRECEIPTED` |
| 3 | tests-pass | U7C independent staged-scope defensive self-test | `UNRECEIPTED` |
| 4 | command-succeeded | U7C exact-owned Go formatting | `UNRECEIPTED` |
| 5 | tests-pass | U7C Darwin Node 25 CLI product orchestration and race tests | `UNRECEIPTED` |
| 6 | tests-pass | U7C Darwin Node 25 CLI decisive substrate regressions | `UNRECEIPTED` |
| 7 | tests-pass | U7C Darwin Node 25 harness-observed three-run CLI process/artifact protocol and byte-repetition evidence | `UNRECEIPTED` |
| 8 | tests-pass | U7C cumulative verification | `UNRECEIPTED` |
| 9 | command-succeeded | U7C exact staged scope and diff integrity | `UNRECEIPTED` |
| 10 | command-succeeded | U7C scoped staged credential-pattern scan | `UNRECEIPTED` |
| 11 | command-succeeded | U7C sealed-U7N predecessor and preceding didrun chain integrity | `UNRECEIPTED` |

## Process and safety ceilings

Candidate code is trusted local code running with the user's permissions and host network access. Private directories provide freshness and repeatability, not isolation. Natural successful return must include the known runner's terminal closure; timeout, cancellation, output overflow, drain uncertainty, process-group uncertainty, terminal inventory drift, or context loss makes the attempt nonpublishable. Deliberately detached descendants, same-UID replacement, network denial, and a kernel-backed whole-tree containment boundary remain unproven.

The generic post-spawn app failure text remains a known excluded defect because U7C does not own `run_darwin.go` or `validate.go`. Any nonzero after possible spawn is an attempted, nonpublishable run regardless of that generic wording. No same-attempt retry or evidence reuse is permitted.

The harness's 15-minute and 4-GiB domain limits are post-hoc sums/maxima for directly observed prepare/compile/execute subjects. They exclude parent orchestration, per-logical-trial attribution, Git/artifact inspection, and parent Node memory. `FULL_STUDY_RESOURCE_BOUND_UNVALIDATED` therefore remains true even if event 7 passes.

## Verification and nonclaims

The exact 12-command final procedure comes only from `tools/print-u7-final-runbook.mjs --unit U7C`. Every command runs through the generated hermetic didrun fence and receives its matching immediate zero-based claim over all 18 paths. A nonzero evidentiary command, claim, commit/preflight step, non-entropy seal failure, or later closure permanently ends that ledger. The sole generated review-gated high-entropy exception still requires this candidate's own freshly claimed zero-finding scoped credential scan and deliberate exact-inventory review; any override must be disclosed and is not proof of secret absence.

U7C does not establish Linux or Windows behavior, other Node majors, external repository safety, hostile containment, network denial, full-study resource bounds, full-workflow race freedom, adoption, maintainability, production readiness, independent security review, HTTP target/run/classification authority, two-domain reproduction, or a U7D/U7R receipt. Human-facing output remains presentation only. A passing test or harness is bounded evidence of that exact execution, not proof of correctness.

## Permanent closed precommit attempts

The first U7C final attempt used staged tree `50ff809b73ef026c6d6a89fc35bfe730b2e2e709`. Events `0`–`6` exited zero and were immediately claimed; unclaimed event `7`, the exact three-run harness command, failed closed during CLI run 1 with `U7_STUDY_HARNESS_RUN_BUDGET: cli:1`. Its observer recorded `0.48 s` prepare, `1.32 s` compile, and `374.52 s` execute, or `376.32 s` of direct subject-process wall time against the frozen `300000 ms` per-run ceiling. This is a product/runtime-budget failure, not an artifact, semantic, or race result.

The attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. After wrapper and relevant-child terminality were established, the exact generated precommit recovery preserved its eight events/seven claims at `.didrun-history/u7c-precommit-failed-20260811T080033Z-harness-event7-exit1/.didrun`, preserved its private runtime root at `.countershape/u7c-final-precommit-failed-20260811T080033Z-harness-event7-exit1`, and left exact sealed U7N as `HEAD`. Every recorded green is permanent nonreusable failed-attempt history. The replacement candidate must change the tree and restart at event zero; no event, claim, timing, scan, or grade transfers.

The second U7C final attempt used staged tree `1876f6094bd2bd73ffa5d211a47feb38148ab765`. Events `0`–`7` exited zero and were immediately claimed. Unclaimed event `8`, the exact cumulative-verification command, returned exit `1` at the verifier's exact `1200000 ms` child boundary while the serial `go-test-general` child and its `clistudy.test` binary were still live. Static control flow plus the measured 681.640-second ordinary duplicate identified that closure as the added wall-time source. This is a cumulative wall-clock failure, not a Go semantic, product, harness, artifact, or race result.

That attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. After wrapper and relevant-child terminality were established, exact generated precommit recovery preserved its nine events/eight claims at `.didrun-history/u7c-precommit-failed-20260811T110648Z-cumulative-verification-failed/.didrun`, preserved its private runtime root at `.countershape/u7c-final-precommit-failed-20260811T110648Z-cumulative-verification-failed`, and left exact sealed U7N as `HEAD`. Every recorded green is permanent nonreusable failed-attempt history. The replacement candidate changes the tree and restarts at event zero; no event, claim, timing, scan, or grade transfers.

The third closed U7C precommit attempt used staged tree `fe5f5b46f933afa5623b8d0eb062d5284aa729f5`. At the owner's explicit request to shut down and relocate the device, execution stopped at a durable terminal boundary; the didrun wrapper and every relevant child were terminal before recovery. Exact generated precommit recovery preserved the incomplete ledger at `.didrun-history/u7c-precommit-failed-20260811T150857Z-operator-requested-device-relocation/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260811T150857Z-operator-requested-device-relocation`, and left exact sealed U7N as `HEAD`. It created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. Its operator-requested termination is not a product, test, race, harness, artifact, or semantic result, and every recorded result is nonreusable history.

The fourth closed U7C precommit attempt, and third permanent gate failure, reused staged tree `fe5f5b46f933afa5623b8d0eb062d5284aa729f5` after relocation. Events `0`–`6` exited zero and were immediately claimed; unclaimed event `7` failed closed during CLI run 1 with `U7_STUDY_HARNESS_RUN_BUDGET: cli:1`. Its observer recorded `0.48 s` prepare, `1.62 s` compile, and `316.28 s` execute, totaling `318.38 s` of direct subject-process wall time against the frozen `300000 ms` per-run ceiling. This is a run-budget failure: because the command exited `1` before returning a complete three-run harness verdict, it establishes no event-7 grade and neither proves nor disproves product semantics, artifact protocol, or race freedom.

That attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. Exact generated precommit recovery preserved its eight-event/seven-claim ledger at `.didrun-history/u7c-precommit-failed-20260811T162117Z-harness-event7-exit1-after-relocation/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260811T162117Z-harness-event7-exit1-after-relocation`, and left exact sealed U7N as `HEAD`. Every recorded green is permanent nonreusable failed-attempt history. The replacement candidate must change the tree and restart at event zero; no event, claim, timing, scan, or grade transfers.

The fifth closed U7C precommit attempt, and fourth permanent gate failure, used staged tree `1f32f46b86329dc0b1111577cd7f6b42b45094ea`. Events `0`–`7` exited zero and were immediately claimed; unclaimed event `8`, the exact cumulative-verification command, returned exit `1`. The ledger establishes that event-8 nonzero and no event-8 claim; it does not by itself establish the inner cause.

Only after the attempt was closed did a separate no-ledger focused development rerun isolate `internal/contractexec`'s `TestC3OfficialTargetPublicSurfaceAndSoleIssuer`. `go test` reported `517.215s` and the outer timer reported `real 533.49s`; the failure was attributed to `internal/reference/clistudy.publishOfficialTargetRoster` returning `[]officialTargetSlot`, a helper-owned holder containing `contractexec.OfficialTarget` and therefore violating the existing sole-issuer architecture check. This later diagnosis does not retroactively become event-8 receipt evidence or transfer a claim or grade.

The fifth attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. Exact generated precommit recovery preserved its nine-event/eight-claim ledger at `.didrun-history/u7c-precommit-failed-20260811T182142Z-cumulative-verification-event8-exit1/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260811T182142Z-cumulative-verification-event8-exit1`, and left exact sealed U7N as `HEAD`. Events and claims `0`–`7` are permanent nonreusable failed-attempt history. The replacement candidate must change the tree and restart at event zero; no event, claim, timing, scan, diagnosis, or grade transfers.

The sixth closed U7C precommit attempt, and fifth permanent gate failure, used staged tree `ef3b3e3035a6bcabdd436d45955982a0bf2b933b`. Events `0`–`8` exited zero and were immediately claimed; unclaimed event `9`, the exact staged-scope final gate, returned exit `1`. That ledger establishes only the event-9 nonzero and no event-9 claim. A separate no-ledger diagnostic then exposed `U7_SCOPE_COMMAND` at the checker's internal `/usr/bin/git --no-replace-objects write-tree`: Git returned status `128` with 128 stderr bytes because the managed execution sandbox denied creation of `.git/index.lock`. This is an execution-environment authority failure, not a candidate scope, diff, product, test, race, harness, artifact, or semantic result, and the later diagnosis does not retroactively become event-9 receipt evidence.

The sixth attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. Exact generated precommit recovery, run outside that sandbox without changing the checker, preserved its ten-event/nine-claim ledger at `.didrun-history/u7c-precommit-failed-20260812T062856Z-scope-event9-sandbox-git-write-tree-denied/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260812T062856Z-scope-event9-sandbox-git-write-tree-denied`, and left exact sealed U7N as `HEAD`. Events and claims `0`–`8` are permanent nonreusable failed-attempt history. The replacement candidate changes the tree and restarts at event zero under an execution context that permits the unchanged checker to exercise its internal Git authority; no event, claim, scan, diagnosis, or grade transfers.

The seventh closed U7C precommit attempt, and sixth permanent gate failure, used staged tree `5fffeb37a75a538d372bd6e5dceaaa9bdfe0ebd5`. Unclaimed event `0`, the exact candidate-plan command, returned exit `1` before any claim. The direct didrun executable token had been shell-quoted, so the saved full-access prefix did not match; the managed sandbox again denied the child checker's internal Git authority. This is another execution-environment invocation failure, not a candidate plan, product, test, race, harness, artifact, or semantic result. The live ledger directory was created as mode `0755` because that direct invocation also bypassed the generated wrapper's `umask 077`; only its directory mode was corrected to the required `0700` so generated recovery could preserve it, without changing event content or promoting evidence.

The seventh attempt created no claim, commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. Exact generated recovery preserved its one-event/zero-claim ledger at `.didrun-history/u7c-precommit-failed-20260812T155604Z-event0-sandboxed-quoted-didrun-prefix/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260812T155604Z-event0-sandboxed-quoted-didrun-prefix`, and left exact sealed U7N as `HEAD`. Its event is permanent nonreusable failed-attempt history. The replacement candidate changes the tree and restarts at event zero with the approved didrun executable as the literal command prefix; no event, diagnosis, or grade transfers.

The eighth closed U7C precommit attempt, and seventh permanent gate failure, used staged tree `0a90dd327ca437b226dfc124a239ba70dd00cf5b`. All events `0`–`11` exited zero and were immediately claimed, but three read-only generated-runbook inspection commands then ran after the final claim and before the unit commit. The generated protocol permits no unreceipted command in that interval and contains no read-only or administrative exception. This is an evidence-procedure failure, not a candidate plan, scope, formatting, product, test, race, harness, artifact, credential-scan, predecessor-chain, or semantic failure; the twelve greens cannot authorize a receipt after the boundary was contaminated.

The eighth attempt created no commit, seal, note, strict result, HTML witness, normal archive, or U7C grade. After the didrun wrapper and relevant children were terminal, exact generated precommit recovery preserved its twelve-event/twelve-claim ledger at `.didrun-history/u7c-precommit-failed-20260812T190743Z-postclaim-unreceipted-runbook-inspection/.didrun`, preserved its private root at `.countershape/u7c-final-precommit-failed-20260812T190743Z-postclaim-unreceipted-runbook-inspection`, and left exact sealed U7N as `HEAD`. Every event and claim is permanent nonreusable failed-attempt history. The replacement candidate changes the tree and restarts at event zero; after its event-11 claim, it must execute the already-inspected commit/preflight/plain-seal block immediately with no intervening command.

The first failed U7C postcommit attempt used commit `9ed68ae2d3c76f248421ba2999e5880c6f76d11c` and tree `a83759d766fd845ff55a27a6a0540369b9eb2156`. Events `0`–`11` exited zero and were claimed; the mandatory plain seal refused on the high-entropy heuristic, and the deliberately reviewed exact inventory plus the freshly claimed zero-finding scoped scan authorized the generated `--allow-secrets` checkpoint. The commit then sealed and explicit-commit strict verification reported `12/12` `TREE-EXACT`; the post-seal note inspection exited zero and the HTML witness was generated. Normal rotation nevertheless failed closed because event `11` had a different didrun environment fingerprint from events `0`–`10`: the resumed direct recorder invocations inherited the ambient control-plane epoch, while event `11` used the generated isolated epoch. This is an evidence-environment consistency failure, not a candidate plan, scope, formatting, product, test, race, harness, artifact, credential-scan, predecessor-chain, or semantic failure, and its strict result is not promoted.

Exact generated postcommit recovery preserved the failed commit at `refs/countershape/failed-attempts/u7c/9ed68ae2d3c76f248421ba2999e5880c6f76d11c`, its sealed thirteen-event/twelve-claim ledger at `.didrun-history/u7c-final-failed-9ed68ae2d3c76f248421ba2999e5880c6f76d11c/.didrun`, and its private root at `.countershape/u7c-final-failed-9ed68ae2d3c76f248421ba2999e5880c6f76d11c`, then restored exact sealed U7N at `HEAD` with the failed tree preserved in the index. Its note, strict result, HTML, events, claims, seal, and override remain permanent nonreusable failed-attempt history. The replacement changes the tree, restarts at event zero, and uses one generated isolated recorder epoch for every event.

## Development-only record

The pre-ledger build loop may run focused fixture, app, CLI study, race, Darwin, architecture, harness, and cumulative diagnostics. Those results are development evidence only. No timing, digest, trial result, or green is promoted here until it is produced by the final exact staged candidate and, where applicable, the declared event-7 harness run.

After the fourth closed attempt, staged tree `00ed9a228e069fef440a778d9c45b051179e625f` received one deliberately no-ledger development harness run. CLI run 1 recorded `0.47 s` prepare, `15.29 s` compile, and `301.78 s` execute, totaling `317.54 s` of directly observed subject-process wall time, then returned `U7_STUDY_HARNESS_RUN_BUDGET: cli:1` before a complete three-run verdict. Exact generated no-ledger recovery preserved its private root at `.countershape/u7c-final-precommit-failed-20260811T164158Z-development-harness-run-budget`. It created no ledger, event, claim, scan, grade, commit, seal, note, strict result, or HTML witness and was not itself a closed precommit attempt or permanent gate failure. The execute measurement improved from `316.28 s` to `301.78 s` but still lacked acceptable margin and motivated the bounded official scheduling repair.

A subsequent no-ledger diagnostic on staged tree `74caa99bc05571400d393edfb0d978234297ba49` completed all three fresh runs with the full 80/8/10 and 111-file protocol. Direct-subject timings were run 1 `0.48 s` prepare, `14.24 s` compile, and `248.46 s` execute (`263.18 s` total); run 2 `0.47/1.94/244.48 s` (`246.89 s` total); and run 3 `0.45/1.68/234.29 s` (`236.42 s` total). Aggregate peak direct-subject RSS was `292978688` bytes. Exact generated no-ledger recovery preserved its private root at `.countershape/u7c-final-precommit-failed-20260811T172238Z-development-harness-green`. These are development measurements only, are not event-7 evidence, transfer no claim or grade, and do not alter the requirement that the next final ledger begin at event zero.

The full physical-test sequence exposed and repaired two independent test/product boundary defects: an invalid cross-cohort minimized-stimulus nonalias rule failed after 329.01 seconds, and an impossible foreign-rejection hostile failed after 502.55 seconds because it crossed unequal projection definitions. A later sequential hostile suite exhausted the outer 15-minute alarm at 900.384 seconds while repeating completed-study validation; it emitted no semantic failure and supports no timeout or race claim. After removing only duplicate `Inspect` traversals, the complete ordinary test passed in 851.198 seconds. The final bounded four-worker form retained all 52 strict authority-rebound plus opaque-`Completed.Valid` hostiles and passed ordinarily in 681.640 seconds. Its targeted race-parent/build-info-verified-non-race-child diagnostic first passed in 683.319 seconds; after the wrapper added an owned-PGID check plus natural-return group-absence proof, the same targeted closure passed in 662.885 seconds and its focused surviving-descendant refusal passed under `-race`. The earlier child-wiring probe had failed closed in 6.26 seconds on a `-trimpath`-relative source path before any physical trial. Terminal process audits after these wrappers found no matching test, driver, fixture, completion, or official-revalidation descendant. None was the exact staged event-5 command, none is reusable receipt evidence, and none establishes full-workflow race instrumentation or race freedom.

The second final-ledger attempt then exposed a distinct gate-composition defect: event 8's 20-minute cumulative child redundantly entered that 681-second ordinary closure after event 5 had already run the authoritative verified child. The replacement tree makes this deduplication explicit and observable as one ordinary non-race skip; it does not remove or abbreviate event 5's full physical and hostile closure.
