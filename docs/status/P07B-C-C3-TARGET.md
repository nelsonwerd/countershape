# P07B-C C3 — exact contract-execution target publication

- **Source receipt:** C3 source commit `029c5cf43853eb3cb46520f6e0dea8431a3de5c0`, tree `de3e02a343f8ecfb344bdb28d014342f5b423959`, is sealed, note-present, and strict-clean; every C3 source grade below is `TREE-EXACT`. The independently active C3B descendant is outside these source grades.
- **Scope:** 40 exact mode-`100644` paths, no prefixes; sorted-newline roster `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`
- **Commit subject:** `feat: publish exact contract execution targets`
- **Parent:** sealed C3S commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, tree `fce8593ad1fe7af1474b4e113f03e82a9a052f79`, note blob `7dff66800e79dc5b1a149ffe16df4dbf9178836a`, note-body SHA-256 `b65f71e25dd53509f8df876a28fb4027559464f77861a34d1aa0c5854bfdbae6`, `10/10 claims recorded-exact`, strict exit `0`.
- **Retained ancestry:** C3S's direct parent is sealed C3F `63038644ba347d7b934a5557a490af95bb4428a4`; C3F's direct parent is sealed C3L `efa918928246c7793f4ad7201c003020e3a42193`; C3L's direct parent is sealed C3A `2b84d2841971568784d2ac955775b4a99ca7f0f6`. `spec/verification/p07b-c-c3-predecessors.json` binds their exact trees, subjects, note blobs/types/body digests, ordered claims, complete event coverage, declared scope digests, and parent chain.

## What this unit publishes

C3 is the only production issuer of an opaque pre-spawn `OfficialTarget`. Publication rejoins one reopened terminal residue with one independently opened, explicitly pinned and inspected single-target Git materialization whose objects are checked directly; one fresh durable conformance attempt; one stable Darwin `kern.bootsessionuuid` capability; one admitted absolute Node executable with a bounded owned runtime probe; and one typed store-private publication witness. C2 records remain mechanically inert when obtained outside this graph.

`TreeIdentity` is content-portable object-format/commit/tree identity. Repository fingerprint, Git version, and display ref are freshly measured local provenance rather than publication-origin or cross-clone identity. A separately created object database with the exact immutable commit/tree is therefore an intentional positive reopen case; an unrelated database, a closed handle, or an old materialization receipt paired with a different live handle is refused. Repository handles are opened independently, canonical paths and symlinks are rejected, and the pinned object is verified into a private materialization without accepting dirty working-tree bytes.

`OfficialTarget.Valid()` checks sealed in-memory structural snapshots plus shared close/repository-revocation cells only; it performs no durable-store, filesystem, probe, or runtime read. The returned `ContractTargetRecord.Valid()` remains a separate durable-store revalidation. `ReopenOfficialTarget` alone repeats the live Git, materialization, boot, executable, probe, runtime, attempt, target, and relationship checks; C4 must consume one fresh successful reopen immediately adjacent to physical admission. The fixed probe establishes the selected cooperating executable's reported profile, not Node vendor authenticity or hostile-emulator resistance.

The attempt-to-target link is create-once. If different valid targets race for one attempt, the first durable writer wins and later conflicts fail; C3 avoids semantic ambiguity by allocating a fresh attempt for each publication. Restart recovery rebuilds the complete live authority graph and refuses target, runtime, link, executable, materialization, or boot drift. A 24-phase unexported fault seam exercises recovery around every load-bearing publication step without becoming production authority.

## Exact source partition

The source boundary contains 20 added and 20 modified regular files. Added files introduce the target issuer, single-target Git capability, Darwin host-epoch owner, Node runtime owner, predecessor manifest, and their tests/status. Modified files integrate the inert store bridge, cumulative architecture and verifier profiles, exact unit scope, and the controlling product/semantic/security/runbook documentation. No prefix scope is admitted.

The predecessor parser recovers commit/tree/parent/subject, exact raw note identity and body digest, ordered note claims, complete event coverage, redacted argv shape, and ancestry directly from Git. Its argv parser compares `PWD` with the explicit historical sealed-note literal rather than the current checkout location; current C3 event bindings are validated separately. Each manifest `scope_digest` and each historical strict-exit statement is an explicit tracked declaration about an already sealed unit, not a field synthesized from the didrun Git note; the raw note authority does not claim to prove either value.

## C3 source receipt map
- C3 source strict exit: `0`
- C3 source strict claims: `18/18 claims recorded-exact`
This receipt binds only the already-existing C3 source commit. The active C3B descendant cannot grade itself here and remains `UNRECEIPTED`.
C3 source commit `029c5cf43853eb3cb46520f6e0dea8431a3de5c0`, tree `de3e02a343f8ecfb344bdb28d014342f5b423959`, is sealed, note-present, and strict-clean with `18/18 claims recorded-exact`.
The C3 seal records `secrets_override: true` after 293 local scanner findings with kinds `high-entropy`; this is not evidence of secret absence.
The separately claimed C3 structured staged credential scan reported `0` findings; its provenance is sealed supporting event `16`, not the Git note alone.
Local ignored C3 ledger archive `.didrun-history/2026-07-19-p07b-c-c3-final/.didrun/` contains `18` sealed events and `18` archived session events; post-seal events, if any, are outside the sealed manifest.
C3 ledger manifests use `sorted-relative-posix-path-tab-size-tab-sha256-newline/v1`; all-files SHA-256 is `3f09f030d1acb93f886759e99dd6c7808972f8da5a9b60ce9ee4d7abcebb801d` and objects-only SHA-256 is `3c1dba6f6f64e5cb2f24ef37b94715a1d6e17c704fcfb23155a85a69feeadb24`.
C3 archive core SHA-256 values are session `87c9dbb5e7058cd6e0a59f964999aa218efebf2e64a201660c83f91eff6a751a`, claims `354d5dadd029e2bb6d360c84eb15500d944b4ff05983a7ea25b0a2965ddeb2db`, seals `94d9432e2e96adafd440ece65c31f3e9b1f84a1d96c6afda8acc5842d079e99d`, and .gitignore `cdbcae15105d6b781e620813c79c7e868740d4e9cc53ce6f5fcbbc12387adf4b`.
The local C3 HTML snapshot is `.countershape/evidence/p07b-c-c3-final-029c5cf43853.html`, SHA-256 `6efd300874a963abbe20601af20c2927b7372731af9457ed20d3e9a9d2035ab0`, 10347 bytes; it is not a portable strict witness.
The C3 Git note blob is `9ad8a864911ca30914463c61d3447a88beeda927` with body SHA-256 `f5eaa66d0cae152a9edc7f4ff2d49ba66c0d1eb8f3459171a2888daaaaf80fa8`.

The rows below bind the sealed C3 source claims to their verbatim strict grades. They do not grade the active C3B descendant.

| Claim label | Claim type | Verbatim grade |
| --- | --- | --- |
| `P07B-C C3 plan, source scope, and C3B dual-phase coherence` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 plan and receipt-phase defensive self-test` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 exact official-target authority profile` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 direct single-target Git materialization profile` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 Darwin boot-session measurement profile` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 admitted Node runtime profile` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 inert attempt and target store-bridge profile` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 focused authority race suite` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 cumulative architecture boundary` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 architecture defensive self-test` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 predecessor B compatibility` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 predecessor B defensive self-test` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 unit-scope defensive self-test` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 cumulative verifier self-test` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 cumulative verification` | `tests-pass` | `TREE-EXACT` |
| `P07B-C C3 exact forty-path staged scope and diff integrity` | `command-succeeded` | `TREE-EXACT` |
| `P07B-C C3 scoped staged credential-pattern scan` | `command-succeeded` | `TREE-EXACT` |
| `P07B-C C3 sealed-C3S predecessor, C3F/C3L/C3A ancestry, and preceding didrun chain integrity` | `command-succeeded` | `TREE-EXACT` |

The focused race claim runs four exact deliberate-concurrency tests under `-race`, `-p=1`, and `-timeout=20m`: official-target close/reopen linearization, uncached host-epoch measurement, copied-runtime revalidation serialization, and attempt/target create-once convergence. The official-target profile machine-checks their exact direct `go`-statement counts, rejects any C3 `t.Parallel`, and rejects production `go` statements across the five owning packages. Every other C3 test remains in its complete non-race authority profile—including Git materialization—so the focused lane does not pretend that duplicating slow sequential behavior is additional race evidence.

## Intended C3B receipt reconciliation map

C3B is a later exact three-path `RECEIPT_RECONCILIATION` descendant at `sha256:3ef17156a2f3051171da70983f9428de2b9bdb5a179602407a36f34200012a67`. It may add only the C3 receipt declaration and reconcile this status plus the handoff. It must not edit source or verifier behavior, reuse a C3 event, or claim the cumulative suite, live runtime, process execution, security, or unchanged behavior.

| Claim label | Claim type | Verbatim grade |
| --- | --- | --- |
| `P07B-C C3B source receipt reconciliation` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3B receipt checker defensive self-test` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3B declared local source-evidence snapshot match` | `tests-pass` | `UNRECEIPTED` |
| `P07B-C C3B receipt-only Go build` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3B exact three-path staged scope and diff integrity` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3B scoped staged credential-pattern scan` | `command-succeeded` | `UNRECEIPTED` |
| `P07B-C C3B preceding didrun chain integrity` | `command-succeeded` | `UNRECEIPTED` |

## Nonclaims and next gate

C3 proves no subject process was spawned, no interlock or StartClaim was acquired, no `RunPermit` was minted, and no final run, classification, study-head transition, containment, confidentiality, hostile-code resistance, production readiness, adoption, or maintainership result. The runtime probe is deliberately narrower than vendor authenticity, and hash identity is not authorship.

Live S6 finding `C3-S6-DIDRUN-INTERRUPT-2`: after a stale cumulative success marker made the in-flight development verification deterministically obsolete, operator interrupt terminated the child verifier and left no surviving process, but didrun printed a Python `KeyboardInterrupt` traceback and appended no event. That interrupted command is therefore permanently `UNRECEIPTED`, supports no claim, and is not part of the final ledger. This is an interruption-capture defect in didrun behavior observed during a real agent session; C3 later replaced it with normal completion in the fresh sealed final ledger.

The sole-issuer AST policy is a cooperating-code API-shape check, not whole-program dataflow proof. Its import-qualified, module-wide named-type and function graphs are built only from files selected for the admitted Darwin/arm64/cgo profile, preserve source-ordered lexical block/function/receiver type-parameter scopes, and structurally substitute binding-sensitive instantiated arguments through output-bearing positions, including union constraints, repeated nested instantiations, and dependent/renamed receiver constraints. Known generic function call/value results and direct or shallowest-promoted generic method call/value/expression results are checked positionally; parenthesized callee and built-in forms normalize before resolution; omitted output type parameters fail closed; indexed module function/method call multi-results are tuple-positioned; and unresolved external generic calls with target-bearing type arguments remain conservatively refused. It rejects direct and recursively wrapped result types, constructed returns, function-local target-bearing wrapper declarations, exported target-bearing variables and nested/indexed/shallowest-promoted selected fields (including the admitted conversion/make/new/identifier initializer forms), non-holder composites, and struct/interface-held output factories across the current `internal/` production tree while allowing C4-style input parameters, input-only generic function/method consumers, consumer callbacks, package-declared request-holder shapes, and local values of those admitted holder types. The exact exported declaration/field inventory includes anonymous embedded fields, multiple dot imports cannot overwrite contract-package visibility, and lexical `OfficialTarget` shadows are not mistaken for the capability. Local value/function shadowing and non-call comma-ok tuple projection are not modeled exactly. Private state/seal ownership plus the frozen package topology remain the minting boundary; arbitrary local identifier/call flow through `any`, reflection, future production roots outside `internal/`, generated code, and arbitrary same-user code are not claimed closed by this test.

C3 changes `tools/verify-current.mjs` and its self-test only for roster-only verifier enrollment of seven exact C3 steps. It changes no cache lifetime, job count, package classification, child environment, lock semantics, or repetition-helper byte, but it materially increases sequential verification work. C3 explicitly supersedes rather than literally satisfies the sealed C3V blanket future-byte trigger. The 52-case C1V load-sensitive qualification matrix was not rerun. Its dedicated verifier self-test plus one complete cumulative verification re-establish only the evolved roster and one composition snapshot; an unclaimed post-repair development pass completed normally in `2908.769` seconds, which does not establish repeated load-flake stability, portable performance, or a new ceiling. A future execution-setting, sensitive-package classification, lock-semantics, cache-lifetime, or repetition-semantics change must rerun the complete C1V matrix.

C3 is independently sealed and its source grades are reconciled above. C3B is now the active receipt-only descendant and remains `UNRECEIPTED`; C4 remains blocked until C3B independently commits, seals, exposes its exact note, and passes `NO_COLOR=1 didrun verify --strict`. This file cannot receipt C3B itself.
