# Countershape semantic authority contract

- **Contract version:** `semantics-v1`
- **Status:** controlling model; runtime authority is receipted through P07B-B, P07B-C product authority through C4 and process/verification maintenance through C4L are sealed and strict-clean, and C5 HTTP-profile execution and full standalone closure are active and remain `UNRECEIPTED`.
- **Historical C3 checkpoint:** C3 exact-target publication is active describes the frozen C3 tree; the current repository boundary is C5.
- **Purpose:** separate structural identity, measured facts, comparison assessment, observed behavior, and storage/wire projection

Countershape does not treat a digest-shaped string, a status enum, or a JSON object as evidence merely because it has the right fields. Each stronger state is available only through the operation that can establish it. Some schemas describe exact canonical artifact bodies; others describe external storage or wire envelopes. Production constructors must rebuild the corresponding opaque authority from stricter inputs.

## Canonical body and wire envelope

Every persisted semantic artifact has a canonical body containing `schema_version`, `kind`, and its identity-bearing semantic fields. Its domain-separated digest is computed over that body and addresses it externally in the object path, head, typed reference, or an envelope that explicitly defines `artifact_digest`; the digest is not part of its own hash preimage. The U6 `choicepoint.schema.json` and `decision-record.schema.json` files describe exact canonical bodies and therefore intentionally reject a self-referential `artifact_digest` member. The corrected P07 `ContractBundle`, `ContractExecutionTarget`, `FinalizedContractRun`, and `ContractExecution` bodies follow the same rule; none may contain its own artifact digest.

Some envelopes also carry a separately derived companion digest. In particular, `preservation_map_digest` addresses the eligible labeled map derived from a `CandidateOutcomeMap`; it is not part of the enclosing outcome artifact's hash preimage and is never sufficient by itself to classify preservation.

Schema acceptance proves only that a wire value has the declared closed shape. It does not prove strict canonical parsing, semantic construction, source provenance, physical freshness, host truth, comparison admission, durability, or current-head authority. Strings and collections may have stricter UTF-8 byte caps in Go than JSON Schema's code-point-oriented `maxLength` can express.

U1 did not supply explicit runtime-to-wire codecs for every target schema. Sealed U6 adds strict runtime codecs, exact canonical body-field sets, and one valid runtime/example intersection for `Choicepoint` and `DecisionRecord`; the exact receipt boundary and grades live in `status/U6.md`. This is not equivalence between the JSON-Schema and strict-Go acceptance languages: schemas intentionally overapproximate some UTF-8 byte caps and semantic authority. For P07B-C C1, `internal/contractexec/model` is the semantic authority and the three checked-in target/run/execution schemas are closed syntax projections. They deliberately overapproximate cross-field rules such as evidence-kind/domain pairing, process/spawn/capture correlation, derived scope aggregate, violation precedence, and recursive witness ceilings. Schema-only acceptance is therefore never semantic acceptance. The checked examples must parse through the Go model and rebuild byte-for-byte; the source is sealed and strict-clean, but its tracked grades become authority only when the C1 receipt declaration, source note, status, and handoff agree and the separate C1B boundary is itself sealed. Studio and export schemas likewise remain planning authorities until their owning units implement and receipt their named parity gates.

## Projection definition and result authority

A `FieldRegistry` has its own domain-separated identity over the exact closed field IDs, paths, types, and missing/null policy. A full `ProjectionDefinition` is a different object. It commits the adapter domain, implementation and configuration digests, accepted channel set, ordered operation pipeline, exact comparator, and field-registry digest. Channel order is normalized; operation order is semantic and preserved.

Construction yields an opaque `ProjectionDefinitionBinding` that retains the declaration whose digest it exposes. `WorldPlan` consumes that binding and requires its adapter domain to equal the plan adapter. A source document may contain a projection-definition digest reference, but source compilation also requires the resolved binding and refuses a zero, mismatched, or differently domained capability. There is no compile-from-bare-digest path.

`ProjectionFingerprint` is only the domain-separated digest of exact canonical projection bytes and is used for equality grouping. `ProjectionResult` is a separate lineage-bearing artifact derived from those bytes plus world, finalized attempt, captured observation, capture policy, and full projection definition. Byte-identical results in different attempts share a fingerprint but never a result digest.

## Portable selected-field interpretation

Historical sealed U6 Choicepoints use `WHOLE_EXACT_CANONICAL_PROJECTION_V1`; their single field is the entire exact projection body. Those records remain valid parseable ruling artifacts but do not authorize a future emitter to assert that only a subset of HTTP or CLI fields matters. Sealed P07A fresh public construction instead uses only `ADAPTER_BOUND_PORTABLE_FIELDS_V1`.

P07A adds that mode without changing either adapter's projection bytes or the existing 27-member Choicepoint body. Construction first verifies the original exact projection proof against the confirmation roster, then resolves the complete adapter definition from the WorldPlan's exact `ProjectionDefinitionBinding`, then strictly translates the verified adapter wire into a complete portable tuple. The portable profile identity binds translator version, adapter domain, complete projection binding, and ordered adapter-owned field descriptors. It is a downstream interpretation digest, not another name for the adapter field-registry digest.

Legacy parsing reconstructs only the whole-projection registry. Fresh public construction uses only the adapter-bound profile. A legacy DecisionRecord offered to the store-bound portable-preparation seam returns `LEGACY_WHOLE_PROJECTION_NOT_PORTABLE`; canonical bytes are never migrated or reinterpreted in place.

The closed portable algebra distinguishes:

- tagged `MISSING` and `NULL`;
- `BOOLEAN`;
- safe `INTEGER` with canonical decimal spelling;
- exact `STRING` and `BYTES`;
- `ORDERED_STRING_LIST`, preserving order, duplicates, empty list, and empty members; and
- exact strict `CANONICAL_JSON` bytes.

These semantics do not imply that the historical CLI and HTTP wire codecs are identical. Their exact existing shapes remain fingerprint authority. The translator is the only versioned bridge. No caller-authored tuple plus a digest can substitute for verified projection bytes.

The sealed expectation domain validates existential model shape only: the selected values must be the restriction of at least one complete tuple admitted by the exact adapter projection model. It does not prove that a current source, candidate, fixture, or runtime can emit the values. That stronger source authority belongs to P07B.

A P07A portable ruling is selected-field semantic authority, not runnable-source authority. Historical stimuli intentionally retain some source content only by length and digest. Sealed P07B-A1 therefore adds a separate opaque `PortableSource` preimage witness. Its closed adapter constructor retains exact bounded bytes and rebuilds and exact-matches its supplied adapter stimulus, WorldPlan, projection binding/definition, portable profile, logical-Node runner/start/readiness/capture authorities, and execution binding. HTTP additionally requires the exact child-bind/pipe-readiness start authority.

### P07B-A1 authority ceiling

`PortableSource.Valid()` proves closed reconstruction integrity over the supplied A1 authorities only. It does not prove equality to a current store-bound `Ruling`, `Choicepoint`, or `FreshConfirmation`; it does not retain reopened projection proofs or ruling tuples; and it does not authorize compilation. Its `closed_facts` are construction declarations, not static analysis, dependency inspection, secret scanning, network denial, confidentiality, or containment.

The A1 child-bind lineage accepts exactly `COUNTERSHAPE_READY_V1 <port>\n` on inherited FD 3 followed by EOF, then uses the reported literal-loopback endpoint for the request. This yields a physical `Observation`, not a FreshConfirmation, portable Choicepoint, or ruling. `child_reported_port` is not kernel evidence that the reporting PID owns the listener; an arbitrary trusted script could report a decoy local service.

### P07B-A2.1 compilation-authority ceiling

Sealed A2.1 opens and revalidates the current ruling/Choicepoint/FreshConfirmation, freshly reinspects the strictly reparsed DecisionRecord, joins the exact retained portable-profile bytes rather than only its digest, compares the source execution binding with every retained schedule-ordered confirmation binding, independently retranslates the reopened confirmation projection proofs under the source-matched profile, and compares the result with the exact selected-tuple ruling partition. The source cannot change the ruling; the ruling cannot recover missing source. That application authority produces a sealed wrapper that privately retains the original preparation for later publication revalidation and separately owns a sanitized internal compiler input with no concrete candidate keys, refs, aliases, producer metadata, or support counts. The exact PortableSource still transitively contains opaque source/lineage identity digests, including candidate-set, plan, envelope, materialization, fixture/capture, and projection identities.

`PreparedCompilation.Valid()` proves construction integrity, not continued currentness. “Sanitized” means authority-narrowed rather than content-redacted. A2.1 produces no compiler output, generated file, bundle, residue, publication, materialization, target, run, or execution classification; those remain independently gated downstream units.

## Candidate reference and binding

`CandidateExecutionKey` is the exact `candidate:<64 lowercase hex>` wire and display reference. It intentionally excludes stimulus, display label, producer, branch spelling, order, support count, and group identity.

The key is not allocation authority. `CandidateExecutionBinding` is an opaque value produced from the full candidate execution identity:

- immutable tree identity;
- materialization policy;
- exact `WorldPlan`;
- adapter identity;
- runner identity; and
- projection definition.

Allocating evidence requires both the opaque binding and the actual `WorldPlan`. The constructor verifies that their plan and candidate references agree. A parsed key or a caller-authored collection of matching digest strings cannot substitute for the binding.

## World, measurement, and assessment chain

The v1 authority chain is:

```text
WorldPlan + CandidateExecutionBinding + stimulus + attempt identity
  -> WorldInstance

WorldInstance + ComparisonEnvelope + complete measured dimension row
  -> InstanceMeasurements

ComparisonEnvelope + one complete row for every candidate in the exact roster
  -> AdmittedComparison + per-row AdmissionToken
     or RejectedComparison
```

`WorldInstance` is structural declared-consistency identity only. It binds the exact plan, candidate binding, stimulus, attempt artifact, purpose, nonce, and schedule ordinal. It contains no roots, ports, PIDs, clocks, platform claims, tool claims, lifecycle result, teardown result, or admission enum. Its digest does not establish that a process ran or that any supplied host fact is truthful.

`InstanceMeasurements` is one complete row under one `ComparisonEnvelope`, bound to one `WorldInstance`. U2 edge code supplies the values and their measurement provenance. U1 establishes coverage, source tags, canonical identity, and policy linkage only; opaque construction does not prove an edge measured the host honestly.

`AssessComparison` is the sole admission operation. It yields:

- a persisted `AdmittedComparison` when the exact candidate roster is covered and every required-equal/reject-on-variance dimension agrees; or
- a persisted `RejectedComparison` with the exact measurement rows and reason codes.

An admitted result carries the plan, envelope, exact candidate roster, exact sorted measurement-digest matrix, and required/rejected equality basis. Every repetition therefore has its own concrete admission artifact. It also derives a `comparison_basis_digest` from only the plan, envelope, roster, and equality basis. That basis deliberately excludes stimulus, attempt purpose, measurement rows, and the per-run admission artifact, so baseline and neighbor studies can be comparable without pretending they are the same run.

Its per-row `AdmissionToken` is internal and ephemeral, deterministically derived from the per-run admitted comparison plus one exact measurement row, stimulus, and attempt purpose. Persisted comparison and measurement artifacts are sufficient to recompute it.

A rejected comparison is a pre-batch study refusal. It produces no admission token, `TrialFact`, `StableBatch`, or `CandidateOutcomeMap`. `ENVELOPE_REJECTED` is therefore not a behavior-shaped batch member. If a later product needs per-candidate pre-admission disposition, that requires a new sealed sum; it must not manufacture admission.

## Trial and batch authority

Every `TrialFact` has exactly one disposition:

- `BEHAVIOR_CAPTURED`, with a captured-observation digest, projection-result digest, and exact projection fingerprint; or
- `CONTROL_INELIGIBLE`, with one or more detected control reasons.

Both variants bind the structural world, finalized attempt artifact, and a deterministic admission-token digest. Captured behavior additionally binds the capture-policy and full projection-definition digests. Captured behavior and control ineligibility cannot coexist.

A `StableBatch` binds one candidate, plan, stimulus, comparison envelope, the exact ordered set of per-repetition admitted-comparison digests, stimulus-independent comparison basis, exact admission roster, attempt purpose, applied schedule digest, `ROTATE_START_BY_REPETITION_V1` policy, schedule ordinals, and tagged trials. Each trial names the admission matrix and row token used for that repetition. Within-batch attempt and world digests must be globally unique. U1 supplied structural ordinal checks; U3 adds the separate deterministic rotated-schedule evidence and refuses any mismatch with the plan-declared sequential policy. The wire fact `duplicate_evidence_within_batch:false` states only structural nonduplication inside that batch; it does not by itself establish physical freshness.

Peer candidate batches may form one `CandidateOutcomeMap` only when they carry the exact same ordered admission-digest set: one shared comparison matrix per repetition. Only an `OBSERVED_STABLE(k/k,h)` batch contributes an eligible map entry. `UNSTABLE`, `UNCOMPARABLE`, and `INCOMPLETE` batches remain explicit exclusions. A `CandidateOutcomeMap` covers the exact expected roster and also binds the plan, stimulus, envelope, admission set, comparison basis, phase, all contributing batches, and the globally unique attempt/world evidence set across those batches.

In U1, “expected roster” means the exact caller-declared structural roster supplied to the constructor. U1 proves entries plus exclusions cover that input; it does not prove Git candidate-set membership or equality with `Budgets.CandidateCount`. U2 must derive and bind the selected roster before any runtime study treats it as candidate-set completeness.

## Persistence and reconstruction

Opaque Go types are the in-process authority boundary. Schemas and examples are wire projections, never alternate constructors. When the sealed U6 store reopens a bounded semantic object it must:

1. verify the external artifact digest against the exact canonical body and kind domain;
2. parse through the strict canonical profile without information loss;
3. validate all semantic references and predecessor authorities; and
4. reconstruct the opaque type through the owning package.

A DTO Boolean such as a derived `compilable` display field, a string status such as `ADMITTED`, or a digest copied from another object is not accepted directly by an emitter, reducer, or state transition.

## Contract residue and execution authority

`ContractBundle` is a canonical semantic body whose external typed digest becomes the terminal `RESIDUE` reference. It embeds the exact recoverable bytes of all six generated files with fixed paths, modes, counts, and raw-byte digests. `manifest.json` covers the other five files and excludes itself; the outer bundle covers all six. Neither the manifest nor a generated file embeds the bundle digest. A didrun receipt or standalone execution fact cannot enter deterministic bundle source because it exists only after that source is built and run.

The store checks the complete expected `RULING` token under the exclusive transition before creating an object or temporary file. It durably publishes and reopens the bundle before advancing the head. A stale loser publishes nothing. A crash after immutable object publication but before head publication may leave an unreachable object; a head must never refer to a missing one. Final output materialization happens only after residue publication and is retryable from the reopened bundle. A post-residue materialization failure reports that durable publication already occurred.

The P07B-B publication/materialization paragraphs describe sealed source commit `eb06bdcf18f8e14db1257e73733afdc05cac045e`; only the exact commands mapped in its status document inherit their verbatim grades. The P07B-C paragraphs below retain sealed C0 authority and the sealed, strict-clean C1 source at commit `2fceacecbacb89fd7650f1570b2af33e6ea25ed3`; C1's tracked grades were reconciled by sealed C1B commit `46c48507fe998ea04e121470d6aa8ba0b38b3dae`. Tracked C1 grades are authoritative only through an agreeing source receipt declaration, Git note, status, and handoff plus a separately sealed C1B boundary. C1 values remain inert: they can be constructed, parsed, canonically encoded, hashed, compared, and classified from already-held values, but they confer no store, Git, materialization, runtime, process, publication, spawn, or study-head authority.

`ContractExecutionTarget` is a separate immutable nonhead pre-spawn object. It binds one reopened bundle/residue, one explicit Git-pinned inspected and verified private materialization, one fresh durably allocated conformance attempt, and one measured/revalidated explicit Node executable plus owned runtime probe. C1 records checkpointed path/content identity: the admitted absolute path, executable-byte digest, executable mode/count, measured `process.execPath`, and runtime tuple. `OfficialTarget.Valid()` rechecks structural joins only from sealed in-memory snapshots plus shared close/repository-revocation cells; it performs no durable-store, filesystem, probe, or runtime read. `ContractTargetRecord.Valid()` is a separate durable-store revalidation, and only `ReopenOfficialTarget` refreshes the complete physical graph, so the matching execution owner must consume a fresh reopen result immediately adjacent to physical admission. The fixed probe verifies the selected cooperating executable's exact reported tuple, not vendor provenance or resistance to a hostile emulator. Those facts do not bind the filesystem object that a later spawn actually executes and do not close a coordinated replacement race; every physical owner must revalidate at its boundaries and retain that residual risk. C2's exact canonical object, typed private witness, and attempt-to-target relationship are necessary durable storage facts but remain inert. If different valid targets race for one attempt, the create-once relationship preserves the first durable writer and refuses the rest; it does not choose a semantically preferred target. Only C3 may rejoin that reopened storage record with its live Git/materialization/runtime/attempt/boot prerequisite graph and return the pre-spawn `OfficialTarget` authority; generic CAS authority, a parsed body, copied OIDs/digests, or copied runtime strings remain inert. It does not reuse `WorldInstance`, whose authority remains closed over the historical 2–4-candidate comparison plan. Dirty working-tree bytes are never represented or executed.

The exact boot binding is profile `DARWIN_KERN_BOOTSESSIONUUID_V1`. Its production owner reads only the read-only Darwin `kern.bootsessionuuid` key through a bounded fixed-name system-call edge, validates each 36-character hyphenated nonzero UUID sample, lowercases each sample, compares the two normalized values, and hashes that canonical value as a typed `DarwinBootSessionIdentity`. There is no public constructor from raw UUID, digest, timestamp, or success flag. The legacy `DARWIN_KERN_BOOTTIME_V1` profile is invalid, and production never falls back to `kern.boottime`, wall clock, uptime, PID, filesystem or process-local state, a `sysctl` subprocess, or PATH. Unsupported, malformed, or unstable measurement refuses authority. Equality means only that the OS reported the same boot-session UUID at both checkpoints; inequality proves neither physical reboot nor child absence, VM non-rollback, kernel trust, collision impossibility, or safe reset.

Historical sealed-C4 topology wording is retained exactly: Only the C4 contract runner may acquire it from an opaque C3-issued official-target capability. That statement is scoped to the C4 tree and is not the live combined-tree owner roster.

Before spawn, one store-private `ExecutionInterlock` serializes subject admission for the measured Darwin boot session. It is an operational CAS, never canonical evidence, result discovery, or a process-status head. Only the matching profile-specific owner may acquire it from an opaque C3-issued official-target capability after independently remeasuring `DARWIN_KERN_BOOTSESSIONUUID_V1`; the current exact owners are the sealed CLI runner and active C5 HTTP runner. The owner then creates the target-keyed `StartClaim`; only a newly durable claim created under that same fresh lease may feed construction of one process-local `RunPermit`. An existing claim/held state, generic publish/read, C2 test fixture, losing caller, copied generation, restart, or ambiguous mutation can never mint or reissue it. A distinct `SpawnObservation` records `START_ERROR` or an observed child PID; missing observation is unknown and cannot enter an FCR. The exact claim ceiling is serialized cooperative at-most-once spawn admission per target, not exactly-once execution, child absence after crash, or safe lease takeover. The complete private manifest, exact FCR, and target-to-run relationship must be durable and reopened before `CLEAR`. An ambiguous operation returns no winner or permit and cannot itself reopen admission. Before `CLEAR`, ambiguity after StartClaim consumes the target and leaves the interlock `HELD`; after `CLEAR`, a missing or invalid receipt remains blocked but cannot undo the durable FCR. Only independently established exact receipt convergence may reopen admission. Proven-no-claim, known-no-effect preclaim reconciliation does not consume the target. A fresh target alone is insufficient retry authority. Reset otherwise requires explicit operator action plus a differently measured Darwin boot-session identity; without that fence P07B-C refuses every further subject spawn, and with it the system still makes no survivor-absence claim.

The C2 `interlockLease` is an internal non-expiring held-generation capability, not a TTL lease, PID-liveness assertion, takeover mechanism, or restartable authority. The first-ever absent interlock is the only receiptless bootstrap. Every persisted `CLEAR` must have one exact durable clear receipt reconstructing the exact previous HELD state and transition under a closed cause. Receiptless or invalid `CLEAR` is ambiguous and blocks admission. The receipt is a trusted-writer operational marker, not independent proof of terminal causation or child absence. Target, caller, HELD state, and StartClaim boot digests join exactly; a historical `CLEAR` boot remains transition history while C3 and the matching profile-specific owner own their respective live boot measurements.

`FinalizedContractRun` is a separate immutable nonhead physical-run authority. It references one exact official `ContractExecutionTarget`, its exact attempt, intent claim, and owner-produced terminal facts. One bounded canonical `ClosedRunWitness` carries typed summaries/refs. C2's private manifest is an operational target/attempt/StartClaim-bound map from each logical evidence reference to checked byte ranges and distinct private-byte digests; the matching profile-specific execution owner, not the store, owns the claim that those bytes substantiate each typed reference. Raw or large captures stay private behind explicit count/byte ceilings, retention-at-finalization metadata, and default omission. Retention at finalization records exact reopen at that boundary, not perpetual availability. Later `RETAINED | PURGED | MISSING_UNEXPECTED` state is nonsemantic and cannot mutate an FCR or classification; `PURGED` means a durable no-serve tombstone preceded cleanup, not secure erasure, and `MISSING_UNEXPECTED` is never repaired into retention. Private placement is not a canonical confidentiality field or result. One process-control owner preserves at most one primary cause plus independent `TEARDOWN_ERROR` and `ORPHAN_RISK` controls, so cleanup cannot overwrite the original cause. Standalone scope is a separate closed `COMPLETE | PARTIAL | VIOLATED` result over target inventory, child bindings, import resolution, service bindings, and named parent-secret sentinel inheritance. Only a clean lifecycle with no primary or cleanup control, exact `PROJECTED` observation, and standalone `COMPLETE` without violation yields `ELIGIBLE_CLEAN`; partial/violated scope is structurally ineligible and is not relabeled as a process `ControlReason`. Publication waits for materialization/runtime revalidation, process completion, teardown, drains, orphan inspection, scope closure, and finalization. A target or attempt mismatch refuses publication/classification.

`ContractExecution` is a separate immutable content-addressed classification object, not a study-head stage, mutable execution record, or current/latest selector. Its body binds one exact official target, that target's exact official finalized run, one literal classifier profile, and only the constructor-derived `CONFORMS`, `CONTRADICTS`, or `INELIGIBLE_EXECUTION` result. The tuple and process/scope reasons live only in the finalized run; classification accepts or echoes none of them. `ClassifierProfileDigest()` is the canonical derived C2 link key for the closed schema/kind/literal-profile declaration. It is not serialized inside `ContractExecution`, does not commit implementation bytes, and cannot be supplied as classification authority. The historical shorthand “classification-only retry and never reruns a process” means that recovery never starts or reruns the contract subject and never consumes subject admission. Recovery still performs a full live `ReopenOfficialTarget`, whose runtime admission and revalidation execute two bounded owned Node probe processes. A future classifier profile creates a distinct object and exact `(run, profile)` link rather than editing history. Repeated trials allocate distinct target/run/execution triples while the terminal study residue remains byte-identical. Nonfreshening is enforced by package topology and head evidence, not authored Boolean proof fields.

### Sealed historical C4 CLI semantics

C4 realizes that CLI-only chronology. The runner performs four full target reopens across preparation and closure. Its final reopen occurs after `AcquireContractRunOwner`, on a detached bounded terminal-closure context, in a contiguous guarded window immediately before permit consumption and `Prepared.Start`. Preparation freezes the first fresh target's inert canonical model, bundle, and physical-root identity. Every later reopen still validates its retained predecessor and reconstructs the complete live graph; the runner validates the newly returned capability and compares its canonical model plus seven roots with that frozen identity before replacement. Cached inert values are consumed only after this equality edge, so avoiding repeated deep getters removes no Git, materialization, runtime-probe, boot-session, or spawn-adjacent freshness check. Target drift at that point refuses without spawning, leaves the durable StartClaim and interlock held, and leaves RunPermit/start authority unconsumed. Before start and after closure, candidate inventory uses batched no-follow directory descriptors and streamed file hashing under exact ceilings of 20,000 entries, 32 MiB cumulative relative-path bytes, depth 128, 64 MiB per file, and 64 MiB aggregate. Limit, special-file, symlink, identity, or byte drift refuses rather than producing partial inventory authority. A repository-wide code-only inventory admits exactly the store definition and the runner's production `AcquireContractRunOwner` reference. That inventory is a cooperating-source property, not complete dataflow or hostile-code containment.

C5 retains the same owner-before-final-reopen and exact-equality rules but uses two phase-local detached deadlines. The revalidation phase owns only the start-adjacent full reopen. Before that phase is retired, the runner creates the closure phase, reads the first phase's terminal error, cancels it, and refuses on either reopen error or expiry. The fresh closure phase then covers equality, replacement, one-shot consumption, physical start, evidence closure, persistence, release, and classification. Each phase keeps the unchanged source-derived budget; neither retries nor refreshes within a phase. The total post-owner envelope can therefore approach two phase budgets. Revalidation or pre-consumption closure expiry produces no spawn and leaves the StartClaim/interlock held. After consumption, expiry may follow a real child and leaves progress-dependent held or ambiguous state; after durable release, a missing classification is recoverable and the interlock may already be clear.

C4 also closes private-evidence work before admission. Preparation reserves a conservative maximum of 48 MiB of unique bodies: two 16 MiB channel caps, 1 MiB capture framing, 3 MiB projection framing, and twelve 1 MiB canonical summaries. One binary length frame is the shared persisted body for stdout and stderr; `DRAIN_RESULT` and `CAPTURED_OBSERVATION` are distinct logical references to that one content digest. Every actual draft is capped at 15 logical kinds and 14 unique blobs and is re-counted before persistence; the full projected child draft reaches both caps, while controlled or start-error drafts may be smaller. Candidate-writable transient roots are inspected only through exact-cardinality-plus-one descriptor rosters. Clean closure removes named retained leaves and proven-empty roots nonrecursively; foreign evidence refuses, while foreign scope residue yields an ambiguous partial scope and remains untouched. Neither outcome establishes containment, secure erasure, or same-UID race resistance.

The CLI child writes one canonical invocation receipt bounded to 64 KiB. The runner consumes it before terminal reopen, compares the fresh random attempt ID and exact logical argv, retains a digest-backed validated summary, and retires the receipt. Scope evidence then closes the exact enrolled two-file inventory plus child binding, import/service canaries, and named sentinel omission. The historical real reference still classifies `CONTRADICTS`; a separate real selected-field ruling compiled over the same enrolled tree produces `CONFORMS` with the exact profile-ordered `argv` tuple. Target mutation is a hard refusal before classification, and sentinel coverage proves omission of named keys only; neither is a broad forbidden-positive detector.

The private manifest preserves logical-reference multiplicity while comparing its sorted exact sequence and enforces 16 blobs/64 MiB. Before `ContractExecution` persistence, the store reopens the exact StartClaim and revalidates the durable finalized-run release relationship after classification readiness. A stale in-memory release bit cannot bypass missing/corrupt durable release. Classification-only recovery still works after private evidence purge because semantic target/run/release authority is separate from current private availability.

The semantic ceiling remains narrow: CLI only, exact cooperating fixture inventory, candidate-written invocation evidence, and a cooperating executable probe tuple. It establishes no Node-vendor authenticity, same-UID replacement resistance, host-wide absence, network denial, listener ownership, containment, confidentiality, secure deletion, reboot/survivor proof, exactly-once execution, process resume, or study-head advancement. Direct Node TAP remains independent operator evidence rather than Go target/run/classification authority.

### Active C5 HTTP closure semantics

C5 retains emitted source as request, start, and readiness authority. The HTTP owner accepts only the exact portable FD-3 readiness frame followed by EOF, then sends one exact raw loopback request to the reported endpoint. Raw response capture precedes adapter-owned HTTP parsing and projection. Process control, observation state, and standalone scope remain independent axes: projection rejection does not erase a clean process fact, and clean process closure does not upgrade partial or violated scope.

The five-domain order remains target inventory, child binding, import resolution, service binding, and sentinel inheritance. `VIOLATED` outranks `PARTIAL`; start-error closure preserves every observed violation while converting unobserved non-inventory domains to missing. Missing or malformed invocation evidence yields missing child binding, whereas an exact contradiction yields a typed violation. A decoy endpoint may emit valid readiness and a clean projected response, but source-mutated inventory cannot become eligible merely because its service behaved.

C5 admits at most 15 private evidence bodies under a source/profile-derived envelope capped by the inherited 64 MiB store ceiling, retains raw readiness separately from parsed facts, and binds target, run, invocation receipt, five scope findings, terminal process facts, and classifier profile through exact joins. Cross-target target/run/execution pairing is refused even when selected response bytes match. `ResumeClassification` freshly reopens the target and revalidates the standalone HTTP profile before reading terminal-store state; a valid CLI/non-HTTP target is rejected with `UNSUPPORTED_HTTP_CONTRACT_RUN_PROFILE`, while a malformed HTTP terminal graph remains `HTTP_CONTRACT_RUN_RECOVERY_REFUSED`. Recovery converges only durable terminal release and immutable classification. It has no source preparation, owner acquisition, RunPermit, start, or subject-respawn authority, although the live reopen may run bounded owned Node runtime probes.

Seed preparation is an exact-roster operation, not best-effort fixture repair. After validating root metadata and the declared topology, a nonblocking advisory lease on the no-follow, identity-checked attempt temporary-root descriptor is acquired before fixture-roster inspection or staging. A successful preparation transfers that lease to the complete execution attempt, which retains it through invocation-receipt retirement, terminal persistence, and return. Live lease contention is `HTTP_CONTRACT_RUN_ADMISSION_REFUSED` even when a holder's receipt is visible; process death releases the lease without a filesystem artifact. Without a live holder, pre-existing receipt or staging residue remains `HTTP_CONTRACT_RUN_FIXTURE_REJECTED` and is preserved. After acquisition, a compatible-partial descriptor roster must pass before mutation; fixed attempt-temporary staging must be exclusively created, exactly emptied, and retired even for an empty seed roster; and the final fixture must contain only declared `0700` directories and exact `0644` seed files. Foreign names, wrong kinds, special mode bits, identity drift, or byte disagreement also refuse. These are point-in-time cooperating-code checks, not isolation from a hostile same-UID mutator.

After a `Start` attempt, process-pipe close failures remain cleanup facts. The fixed `HTTP_START_ERROR_DESCRIPTOR_CLOSE_FAILED`, `HTTP_PARENT_WRITER_CLOSE_FAILED`, and `HTTP_TERMINAL_READER_CLOSE_FAILED` diagnostics are deduplicated in phase order, set `teardown_error`, and leave the primary process diagnostic and orphan-risk axis unchanged. Close is attempted once: an error does not prove the kernel descriptor stayed open, and retrying could close a reused descriptor. Pre-Start cleanup, TCP, and other non-pipe closes are outside this durable diagnostic claim.

The development fixture matrix observes 404 as `CONFORMS`, differing 403 as `CONTRADICTS`, custom-401 against 403 as `CONTRADICTS`, allow-many containing 403/404 as `CONFORMS`, and allow-many excluding 200 as `CONTRADICTS`. Those finite fixture results and every other C5 capability remain `UNRECEIPTED` until the exact C5 unit commits, seals, and passes strict verification. They establish no arbitrary HTTP compatibility, listener authenticity, network denial, hostile-code containment, same-UID isolation, confidentiality, secure deletion, or production readiness.

C1 accepts only the three closed start-error codes and proves their canonical structural round trip with an exact ineligible witness. It does not prove that a reported error occurred, that its referenced evidence contains the asserted OS/materialization/runtime facts, or that the evidence was recorded in the required chronology. Those content and ordering authorities belong to the matching profile-specific physical owner.

The bundle declares only a runtime-generic Node-core source profile. It is not execution evidence. Exact Node path, version, major, OS, architecture, executable identity, and probe program belong to a physically admitted `ContractExecutionTarget`; physical result authority belongs to the matching `FinalizedContractRun`; classifier-profile-bound predicate classification belongs to `ContractExecution`; any didrun grade remains external. Outer materialization verifies all six bundle members. With an intact `contract.test.mjs` entrypoint, direct execution verifies the protected companion-file manifest before subject spawn, but its TAP remains independent black-box/operator evidence and never becomes Go target/run/classification authority. Neither layer establishes authorship, authenticity, confidentiality, containment, entrypoint self-authentication, or resistance to coordinated post-materialization replacement.

## Verification-profile and operator-transition semantics

Verification profiles describe what one repository boundary proves about its own staged change; they are not fields in Countershape's canonical product artifacts and confer no target, run, classification, or study-head authority. C4L is a `SOURCE_FULL` authority migration. It introduces dormant `NON_PRODUCT_MAINTENANCE` grammar and partial scope primitives but cannot consume them. V25 has no concrete narrow row, generic runbook, preseal contract, or end-to-end candidate/stage fixture. A later `SOURCE_FULL` migration must add those authorities before a future consumer can become eligible from an exact row already present in its sealed `SOURCE_FULL` direct parent; candidate-owned schema, checker, prose, or diff bytes cannot create that eligibility.

For a narrow consumer, `product_authority: "NONE"` denies new product authority. `product_projection: "PARENT_FROZEN"` is a prospective eligibility token whose future final gate must reopen the exact sealed parent and prove no tracked change outside the predeclared roster relative to it; v25 does not yet define an enumerated product-projection digest. `product_behavior: "INHERITED_UNREPROVEN"` says product behavior remains outside the child's proof. Parent commit/tree/note identity is an ancestry prerequisite only: no parent verifier result, didrun event or grade, product-correctness conclusion, or behavior proof is inherited, reused, or relabeled.

Operator-transition provenance is separately closed. `CITED_UNAUTHENTICATED` requires a literal canonical repository path, raw-byte SHA-256, role `OWNER_INSTRUCTION_ARTIFACT`, `DIRECT_PARENT_TREE` preexistence, and exactly one nonempty, at-most-64-KiB valid-UTF-8 mode-`100644` regular blob. `UNEVIDENCED` carries the exact disclosure `OWNER_ATTRIBUTED_SESSION_INSTRUCTION_ONLY_NO_QUALIFYING_PREEXISTING_ARTIFACT`. A predecessor's prose-only record and a same-unit companion file are not original instruction artifacts. Citation is not authentication: it proves only which bytes were named and establishes neither owner identity nor authorization. All v25 `OWNER_OUT_OF_BAND` rows—C4H, C4I, C4K, C4J, and C4L—remain `UNEVIDENCED`, with `authentication = NOT_ESTABLISHED` and `signed_authorization = NOT_IMPLEMENTED`.

## Explicit U1 nonclaims

U1 establishes strict structure and pure relationships only. It does not establish:

- that a repository object was materialized;
- that a host fact was honestly measured;
- that an attempt was physically new;
- that a process ran, was contained, or was cleaned up;
- that execution evidence is fresh across batches;
- that a semantic object was durably published or reopened;
- that a reduction grade was earned; or
- that a current study head authorizes a transition.

Those authorities arrive only in their named later units and remain `UNRECEIPTED` until exercised through didrun on the exact claimed tree.
