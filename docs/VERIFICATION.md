# Current verification baseline

From a fresh shell at the repository root, run:

```sh
/opt/homebrew/bin/node tools/verify-current.mjs
```

This is the single cumulative baseline for the current Darwin/arm64 reference tree. It exits nonzero on the first failed current step and prints `RESULT PASS` only after all current steps pass. In an evidence-producing unit, wrap that exact command with `didrun run --`, claim the successful event immediately, and commit. In this managed environment, seal only with Git-note write authority, confirm `git notes --ref=didrun show HEAD` succeeds, and then require `NO_COLOR=1 didrun verify --strict` to exit `0`. A direct run is useful local evidence, but it has no didrun grade.

## What the command runs

The runner executes, in order:

1. a repository guard that rejects any `.DS_Store` outside private evidence/cache directories;
2. admitted `go list ./...` exact-union partitioning, direct two-job `go build`/`go vet`, the complete general package complement at `-p=2`, the six whole sensitive packages afterward at `-p=1`, and exact package-closure revalidation after both lanes;
3. the verification-runner and focused Go repetition-runner hostile self-tests;
4. the U5, U6/P07A-B, and P07B-A1 architecture checkers and hostile self-tests;
5. exact regeneration checks for the runtime bundle and planning examples plus the planning-validator hostile self-test, including real conforming execution, pre-import companion-tamper refusal, and eight byte-exact schema-valid/runtime-invalid cases crossed through the Go semantic parser;
6. fresh-process compiler-to-parser recovery;
7. the deterministic human-surface renderer self-test and exact six-scenario capture check;
8. the P07B-A2.2 layered architecture checker and its 117-case hostile self-test;
9. the P07B-B terminal-publication/materialization architecture checker and its defensive self-test;
10. the P07B-C C1 inert-model architecture checker and self-test, including exact production/test files, imports, top-level test/fuzz symbols, required rosters, and Go-JSON positive-execution parsing;
11. the P07B-C C2 cumulative architecture checker and its frozen 46-case hostile self-test, followed by the exact 6-test typed-nonhead, 7-test interlock/StartClaim, 6-test private-evidence, and 2-test exported-surface Go-JSON profiles;
12. the P07B-C C3 cumulative architecture checker and defensive self-test, followed by the exact 10-test official-target, 7-test single-target Git, 8-test host-epoch, 12-test Node-runtime, and 6-test store-bridge Go-JSON profiles;
13. the evolved C0-receipt/C1/C2/C3P/C3V/C3M/C3PB/C3A/C3L/C3F/C3S/C3/C3R/C3Q/C3B plan self-test, the machine-readable v13 staged-unit-scope self-test, and the dedicated C3P-receipt/C3S/C3/checker-succession coherence gate plus its hostile self-test; and
14. final revalidation of every admitted tool authority, followed by one current finalization row that removes the private run root and releases the exact verifier lock before terminal `RESULT PASS` is emitted.

The current architecture chain is nested fail-closed in two directions from P07B-B: its sealed predecessor chain invokes P07B-A2.2, then P07B-A1, U6, and U5; its current compatibility edge invokes the complete C1 inert-model checker. C2 then composes the exact C2 boundary over B and C1 (`C2 -> B -> C1`). C1 never invokes B and neither predecessor invokes C2, so there is no recursion. C2, B, and C1 retain explicit top-level rows and self-tests even where composition is inherited transitively. The evolved A2.2 checker retains the sealed A2.1 authority closure internally; there is no duplicate top-level A2.1 row pointing at the same file.

U1–U4 architecture checkers are `HISTORICAL-ONLY NOT-RUN`. They freeze the topology and, by U4, exact all-file aggregates of their sealed unit trees. The boundary evolved in two explicit steps: U5 moved typed adapter neighbor providers onto the pure reducer, then P07A introduced adapter-backed Choice test fixtures and U6's production-only import lattice. Under the current lattice, `internal/projectiontranslate` is the sole production generic-to-adapter branch allowed to import the full CLI/HTTP adapter packages, tests may reconstruct historical projections, and production Choice code still cannot import adapters. Running U3/U4 against today's larger tree would test a contract they explicitly do not own.

All mutation gates are also `HISTORICAL-ONLY NOT-RUN`. Each current script snapshot has a positive manifest and mutant roster bound to its latest sealed owner. Some were deliberately expanded and re-receipted by later units: U2/U3 at U4, and U6 through P07A/P07A-B. The current unit rewrites and shrinks none of them. The baseline prints every historical architecture and mutation row, but never spawns one, and no historical row is represented as passed on the current tree.

The proposed A2.2 source-rewrite driver never became a checked-in executable, completed run, claim, or seal. It therefore has no current or historical row and remains `UNRECEIPTED` design history. A2.2's replacement evidence is independently scoped across black-box/property/parity/recovery/fuzz/boundary checks and the hostile architecture self-test; the two bounded fuzz targets are separate final-unit didrun receipts and are not silently executed by this cumulative command. The human-surface rows verify deterministic rendered/captured bytes and normalized runtime records; they do not establish universal comprehension, semantic correctness, or a didrun grade for manual or different-model taste review.

The P07B-B defensive self-test likewise uses one clean canonical execution plus cloned metadata/topology negatives. It now also requires the clean C1 checker, partitions the exact `internal/contractexec/model/` prefix from lookalikes, preserves historical zero-member admission, admits the future C3 target-codec surface only as one complete exact four-pair bundle, and refuses every partial or foreign C surface. It does not generate or rewrite production source copies into a recipe corpus and makes no mutation-completeness claim.

P07B-B source commit `eb06bdcf18f8e14db1257e73733afdc05cac045e`, tree `d95c907754f3347bae885a64c98b30ad1ea00256`, is sealed and strict-clean with `16/16` claims recorded-exact. Final B handoff commit `464e47adbf7f4497dfafa89938a9539239ffd41b`, tree `c7e80d6e6819d4e2c54aaedeb4dc3ef730de58c6`, independently verifies `5/5 TREE-EXACT` with its matching ledger. C0a commit `1c6d9fdef339314bfcb99d7d53f3a7c5040e5020` and C0b commit `47044e95f405b7252959415adb1aa0dbea8045ab` remain historical sealed authority. Narrow cumulative maintenance source `fa3d0c12b4c599744b666b2848e38a2499f33a89`, tree `c908c4e580144aa481f646090a1a21d92c86e1cf`, is independently sealed and strict-clean with `8/8 TREE-EXACT`; it removes a scheduler race from the stdout-overflow test premise by completing the exact sibling-channel fixture write before crossing the output cap, without changing capture or teardown behavior. Its one-file receipt reconciliation `3f31370a70979c4c5fe3a523be19e05f84271e96`, tree `29c5708b05c562984a0b7b66d6cfe3ac6e19c626`, is separately strict-clean with `6/6 TREE-EXACT`. C1 source commit `2fceacecbacb89fd7650f1570b2af33e6ea25ed3`, tree `573fcd0b5548f9f7368493afe801a4bbd2cc9a34`, is sealed, note-present, and strict-clean with `19/19` claims; it adds strict inert semantics and defensive projection gates only, with no store, Git, runtime, process, or publication authority. Its tracked grades are authoritative only when the C1B declaration, source note, status, and handoff reconcile exactly and C1B is separately sealed and strict-clean.

The evolved plan self-test preserves the two historical C0 receipt phases and two exact C1 receipt phases. While `spec/verification/p07b-c-c1-receipt.json` is absent, the C1 source is still recognized as sealed and strict-clean, all 19 tracked C1 status rows remain `UNRECEIPTED`, and no handoff or didrun-live receipt block may exist. When present, the closed declaration must bind the real sealed C1 source commit/tree, exact subject, 19 ordered claim labels/types/indices, verbatim `TREE-EXACT` grades, strict exit `0`, complete event coverage, and the source didrun note; status, a delimited handoff table, and a didrun live-session disclosure must agree. Local HTML and ignored-ledger fields are declarations until a separate receipt event reopens and hashes them; HTML is never a portable strict witness. C1 also pins the three closed schema/object projections, their required rosters, canonical example graph, and exact source and C1B path allowlists. `check-p07b-c-unit-scope --exact-staged` requires the complete empty-prefix C1B roster and reports its sorted-newline digest `sha256:f8d1fb96d36f7e0cfde99bb73c7726297763c66bafd21aeb5c1fb1e249bdd119`; plain `--staged` remains an admission ceiling. Hostile self-tests cover wrong C0/C1 commit, tree, grade, coverage, handoff, schema, documentation, and scope state. Receipt reconciliation still implies no physical capability.

C1B is admitted only after a separately sealed pre-enrollment and cumulative-stability maintenance boundary over exactly these 18 paths: `docs/ARCHITECTURE.md`, `docs/CONCEPT_BRIEF.md`, `docs/PROMPT_PACK.md`, `docs/SEMANTICS.md`, `docs/STATE_MACHINES.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/decisions/0002-u2-impure-boundary.md`, `docs/status/P07B-C-C0-AUTHORITY.md`, `docs/status/P07B-C-C1-CUMULATIVE-MAINTENANCE.md`, `docs/status/P07B-C-C1-SEMANTICS.md`, `docs/status/U2.md`, `internal/world/http_portable_negative_darwin_test.go`, `internal/world/process_darwin.go`, `internal/world/process_darwin_test.go`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:eb51c52657591df8faa5d3c4737291c071e40c07eb9c32bde88af1a36e68bf76`. The cumulative final-ledger rehearsal observed an initial Darwin process-group `(present=true, EPERM)` result consistent with a transient zombie-only state; that observation became permanently uncertain even though a later final probe observed the numeric group absent. The repaired owner retries only that exact observation within one quarter of the existing teardown budget, clipped to the overall deadline; begins no retry at or after that deadline; signals only after a later clean presence observation; accepts a later clean absence without signaling; and leaves persistent EPERM or every other error fail-closed. Scripted transitions, fake-clock deadline controls, a causally synchronized late-EOF proof, and live inherited-writer teardown preserve the original readiness classification and strict cleanup obligations without the old timer-controlled fd-holder closure premise. This maintenance boundary is a prerequisite gate for C1B but is not C1B, cannot grade C1B, and must seal and verify strictly before the four-file data-only receipt boundary begins.

## Sealed C1V qualification and current verifier profile

The now-sealed C1V verifier-throughput maintenance was a full source gate. It declined a persistent Go-only cache because Go 1.26.5 does not detect imported C-library changes and Countershape contains Darwin/cgo publication code. It added a fail-fast private single-verifier lock, proved an exact whole-package test partition before and after execution, narrowed two-job work to direct general commands, and introduced machine-declared `SOURCE_FULL` versus `RECEIPT_RECONCILIATION` profiles without changing C1B's exact four paths or digest. Its rejected p4 development candidate passed every Go package but produced two permanent later AST-timeout didrun events; p2 was accepted only after the documented repeated stability gates and three cumulative ceiling checks passed. C1V owns exactly these 11 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-VERIFICATION-THROUGHPUT.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/verify-current-selftest.mjs`, `tools/verify-current.mjs`, and `tools/verify-go-test-repetition.mjs`. Their sorted-newline roster digest is `sha256:6032cc515978947743e1bfea72340e1065d77473ccc27b3b0fc036ebd4ceb2a5`. This declaration was not a C1V self-receipt. Its pre-seal exclusion rule was that no C1B receipt declaration, receipt block, or reconciliation claim may be introduced until C1V seals and verifies strictly; sealed C1V satisfied that prerequisite before C1E began.

## Hermetic child environment

The runner accepts no arguments, root override, skip flag, or historical-run mode. It creates a private mode-`0700` run root below ignored `.countershape/verify-current/`, then gives children fresh `HOME`, `TMPDIR`, `GOTMPDIR`, `GOCACHE`, `GOPATH`, and `GOMODCACHE` directories. The Go build cache deliberately remains fresh: Go 1.26.5 does not detect changes to C libraries, and this Darwin tree contains real cgo code. It sets `GOENV=off`, `GOWORK=off`, `GOTOOLCHAIN=local`, `GOPROXY=off`, `GOSUMDB=off`, `GOVCS=*:off`, `GOFLAGS=-mod=readonly -buildvcs=false -p=1`, `CGO_ENABLED=1`, `GOMAXPROCS=2`, fixed locale/timezone/color values, and a sparse `PATH`. Ambient `GOFLAGS`, Node preload paths, proxy variables, and credentials are not copied.

The admitted package guard parses `go list ./...` as a unique exact roster. Direct build, vet, and the exact general-package complement override only package jobs to `-p=2`; the general test row also fixes `-parallel=2` and `-count=1`. `internal/emit/node/compiler`, `internal/emit/node/program/v1`, `internal/store`, `internal/world`, `testkit/studies/cli_precedence`, and `testkit/studies/http_invoices` are removed as whole packages and then run once by a separate `-p=1`, `-parallel=2`, `-count=1` row. Because the inherited `GOFLAGS` remains `-p=1`, every nested checker and nested Go command stays serialized. Malformed, duplicate, foreign, or missing frozen-sensitive rows fail before tests; every other valid admitted-module package is intentionally part of the dynamic general complement. After both tiers, a second admitted `go list ./...` must reproduce the exact `all`, `general`, and `sensitive` sets, so any during-run addition, removal, or reclassification fails before later rows. The output header reports `general_jobs=2 nested_jobs=1 gomaxprocs=2`; this local profile intentionally leaves host headroom and is not a cross-platform throughput promise.

The focused Go repetition runner is a separate evidence helper, not a cumulative-row shortcut. It accepts one full admitted-module package, enforces that package's declared `general` or `sensitive` classification, accepts a count from 1 through 50, one anchored test regex, and an explicit top-level expected Test/Fuzz/Example roster. It holds the same verifier lock, uses the same fresh private roots and admitted tools, retains the 20-minute Go timeout, parses `go test -json` as exact LF-delimited package, test, and subtest state transitions, and refuses package/entrypoint drift, skips, failures, invalid actions/transitions, or missing run/pass counts. A full-package repetition must enumerate ordinary tests, fuzz-seed entrypoints, and examples rather than silently narrowing `-run`. It removes its run root and releases the lock before printing its success marker. Its hostile self-test injects the complete lock/admission/private-root/argv/parser/finalization composition plus child, parser, finalization, and cleanup failures; it is part of the cumulative baseline, while each positive focused execution still requires its own didrun receipt.

Qualification uses only `node tools/verify-go-test-repetition.mjs --case <id>`. There are 52 ordered named executions across 14 families: the seven world/compiler/program/store cases; `cli-physical-reducer-01-of-20` through `cli-physical-reducer-20-of-20`; `http-physical-reducer-01-of-20` through `http-physical-reducer-20-of-20`; and the five parity/full-physical-package cases. Each physical reducer shard has count 1, so all 20 separately locked, fresh-root receipts are required for its family. The exported canonical matrix digest is `sha256:bf5803a752ffe6c2833e7869086ec6a4919e15c5e49e8cd012a5c8c16879fc37`. The exact package/profile/count/regex/expected-entrypoint definitions live in the tracked helper and are summarized in the C1V status. Explicit package/regex mode emits `qualification=false` and cannot satisfy this matrix. Every real run first emits ordered admitted Go/Node/Git/sh/CC/CXX path/SHA-256 rows and binds their aggregate digest into its terminal record.

The sharding is a fail-closed receipt repair, not a reduced repetition count. C1V final attempt 1 proved that the CLI physical reducer's old aggregate count-20 command reaches the unchanged 20-minute child deadline: didrun recorded `ETIMEDOUT`/`SIGTERM`, after a `166.32s` iteration passed and the next began. The permanent failed ledger is `.didrun-history/2026-07-18-p07b-c-c1v-final-attempt-1-physical-aggregate-timeout/.didrun/`. C1V does not lengthen the deadline or claim that partial aggregate event; it requires twenty complete count-1 events instead. The HTTP physical sentinel uses the same bounded receipt shape proactively.

C1V qualification was tree-exact in practice as well as intent. The freeze procedure completed independent code/docs review and every tracked edit, staged the exact eleven-path snapshot, archived development evidence, and then started one fresh final ledger for the helper self-tests, all focused repetitions, all three cumulative runs, exact staged-scope/diff integrity, the scoped named-pattern credential scan, and preceding didrun-chain integrity. Any tracked change after that freeze invalidates the entire qualifying matrix: the superseded ledger had to be archived intact and qualification restarted from event zero. A scope check from the archived development ledger is admission evidence only. Only the unchanged qualifying snapshot and its fresh-ledger boundary events were claimed, committed, sealed, note-checked, and strictly verified.

The historical C1V admission rule was: Every one of the three qualifying cumulative runs must finish in at most `878544 ms`. All three sealed qualifying runs satisfied that ceiling. The bound was derived from the faster sealed C1M cold baseline (`976161 ms`): the required improvement was at least `97617 ms`, at least 10 percent, and therefore also more than 60 seconds. Median, fastest-only, and slower-baseline comparisons were not admission rules.

Before plan validation, authority admission, or private-run-root creation, the runner exclusively creates `.countershape/verify-current/active.lock` as a mode-`0600` owned regular file. Its bounded canonical record binds the schema, PID, nonce, creation time, and repository-root digest. A live or indeterminate owner fails immediately with `VERIFY_ALREADY_RUNNING`; an absent PID fails with `VERIFY_STALE_LOCK`. The O_EXCL implementation never queues and never auto-deletes a stale file: automatic deletion has an unsafe two-contender replacement race. Acquisition-error cleanup unlinks only after re-proving descriptor/path identity. Normal checks read the exact bounded file and recheck descriptor/path identity, metadata, and canonical content before unlinking; replacement, disappearance, content drift, or mode drift is `VERIFY_LOCK_INTEGRITY` and the uncertain path is preserved. The final current row removes the private run root and releases this lock; only its success permits terminal `RESULT PASS`. Review the recorded PID before manually unlinking a stale lock. PID reuse can conservatively refuse availability but cannot authorize concurrent verification.

The locked Darwin defaults are:

- `COUNTERSHAPE_GO=/opt/homebrew/bin/go`
- Node authority equal to the canonical `process.execPath` already running the verifier
- `COUNTERSHAPE_GIT=/usr/bin/git`
- `COUNTERSHAPE_SH=/bin/sh`
- `COUNTERSHAPE_CC=/usr/bin/clang`
- `COUNTERSHAPE_CXX=/usr/bin/clang++`

An explicit Go/Git/shell/C/C++ override must be an absolute path. Node is not overridable after startup: invoke the verifier with the desired Node executable; a conflicting `COUNTERSHAPE_NODE` fails closed. Every admitted tool alias is resolved to a regular executable, opened without following the final component, checked for replacement during each admission/fingerprint read, SHA-256 fingerprinted, and printed as an `AUTHORITY` row. Every executable roster row declares its complete admitted tool set, including tools reached by a nested checker, and the verifier re-opens and re-fingerprints each declared authority immediately before and after the row's direct spawn. All authorities are revalidated again before the final result. A private `authority-bin` exposes only the admitted `go`, `node`, `git`, `sh`, `cc`, and `c++` aliases ahead of `/usr/bin:/bin`; admitting one tool does not add its sibling directory to `PATH`.

Each JavaScript checker must also emit its exact success marker; zero exit without that marker is failure. Child output is JSON-framed under `CHILD` rows so it cannot counterfeit the verifier's reserved `CURRENT` or `RESULT` records. These controls reduce accidental/path-replacement ambiguity but do not establish hostile same-user isolation or eliminate the residual same-user check-to-exec race between the last pre-spawn fingerprint and a nested process launch.

The cumulative command uses its resolved private `TMPDIR` for deterministic operation. Separately, the repository's tests must also pass twice under the login shell's stock macOS `TMPDIR`; test-only helpers canonicalize `t.TempDir()` before it enters a strict object-store or Darwin no-follow publication boundary. Production store/publication code still rejects symlinked ancestors and never canonicalizes a caller's root for them.

## Unit verification profiles

The historical C3 boundary used the unit-scope v11 specification, and C3R used v12. The current machine authority is `countershape/p07b-c-unit-paths/v13`, which inserts C3Q between sealed C3R and frozen C3B while preserving one closed `verification_profile` for every unit. `SOURCE_FULL` is mandatory for source, verifier, checker, schema, generated-artifact, or runtime changes and retains this complete cumulative command. `RECEIPT_RECONCILIATION` is admitted only for an exact empty-prefix roster containing Markdown and/or a named `spec/verification/*-receipt.json` declaration. It is never inferred from a diff, and it cannot support a cumulative, full-suite, runtime, security, or unchanged-behavior claim.

Historical C0B is machine-declared `SOURCE_FULL` because that is the larger battery it actually ran; its grade is not rewritten. C1B, C2B, C3PB, C3B, and future C6B use or may use the narrow profile. Each carries an exact ordered seven-claim label/type manifest in the specification, printed by `check-p07b-c-unit-scope.mjs --unit <unit> --receipt-manifest`; the cumulative verifier never silently dispatches this profile. A receipt unit must explicitly run and claim the existing source note/declaration/status/handoff agreement, its defensive checker self-test, any declared local snapshot as `LOCAL_SNAPSHOT_NOT_PORTABLE_STRICT_WITNESS`, a receipt-only Go build, exact staged scope/diff integrity, a named-pattern credential scan, and preceding ledger-chain integrity before commit/seal/note/strict closure. Receipt staged checks require each exact path to be one stage-zero mode-`100644` nonzero regular blob. The four C1B paths and digest remain `sha256:f8d1fb96d36f7e0cfde99bb73c7726297763c66bafd21aeb5c1fb1e249bdd119`; no receipt-only label may say `cumulative`, `suite`, `runtime`, `security`, or imply unchanged behavior, and pre-seal text cannot present a receipt unit as already graded. Any source, path mode, manifest, profile, or scope drift fails rather than silently falling back to a smaller battery. C3R and C3Q are `SOURCE_FULL`; C3B remains frozen to its exact narrow profile.

C2 is an exact 26-path, empty-prefix `SOURCE_FULL` unit with sorted-newline roster digest `sha256:a463e4e9ad9830cb420bbad1da494c26d55e3f68b983abfd683a0c8cb1c3a522`. It enrolls the plan checker, unit-scope checker, and this verification contract because those source authorities must know the later receipt phase before C2 freezes. Its final source ledger admits exactly fourteen index-specific argv identities. The exact stage gate also requires one stage-zero mode-`100644` blob per path, a clean staged diff, and no unstaged or untracked path; the adjacent scanner mode reads those same staged blobs across nine named credential patterns. After the C2 source independently committed, sealed, proved its note, verified strictly, emitted its exact-commit HTML report, and archived its ledger, C2B created only `docs/HANDOFF_MODE_C.md`, `docs/status/P07B-C-C2-PERSISTENCE.md`, and `spec/verification/p07b-c-c2-receipt.json`. That exact three-path roster has digest `sha256:5e30e009bb29672d585ece9893bd7c8db198915335a73ded3f3c790ad18f665b` and the `RECEIPT_RECONCILIATION` profile. The receipt authority required one exact C1B parent plus the raw source diff: seven added and nineteen modified mode-`100644` nonzero blobs over the exact roster, with no merge, rename, deletion, type change, unchanged `M`, extra path, or missing path. Git-note `argv_preview` values were checked exactly except that didrun-redacted hermetic path/tool assignments retained only a structural positional claim; `--verify-c2-local-evidence` closed that local-only gap by checking the raw archived event argv while reopening and hashing the declared HTML and ignored ledger. Declaration text alone is insufficient, and ignored local evidence is not a portable strict witness. C2B reconciled the C2 source grades to `TREE-EXACT` at sealed strict-clean commit `ab5e2dd5b66702a1d1cd13eb0047b57a48469487`; it did not edit checker/source behavior, claim the cumulative suite, or receipt itself. Historically C3 then became blocked on C3P, C3M, C3PB, C3A, C3L, C3F, and C3S rather than C2B; those prerequisites, C3, and C3R are now sealed, and active C3Q self-receipt checker maintenance is the sole current source block.

## C3P stable boot-session prerequisite

The delayed pre-C3 audit falsified the clock-derived boot profile before any production `OfficialTarget` existed. C3P is therefore a full exact-roster `SOURCE_FULL` boundary, not a documentation exception. Its 25 mode-`100644` paths have sorted-newline digest `sha256:8b047704df047cb96f1c5d19418c4cfe8502e4b4746bb4f24c0851dac8d018c0`. It changes the future target contract to exact profile `DARWIN_KERN_BOOTSESSIONUUID_V1`, rejects the retired clock-derived bytes, regenerates the target/run/execution example chain, freezes C3's exact forty-path, empty-prefix source roster at `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`, and enrolls the dedicated C3P/C3PB receipt checker plus hostile self-test in cumulative verification.

C3P's final ledger has exactly twelve claims, in order: planning/scope coherence; legacy-profile refusal; joined-example regeneration; contract-model profile; inherited C1 architecture compatibility; architecture hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; cumulative verification; exact 25-path staged scope/diff integrity; the scoped staged credential-pattern scan; and preceding-chain integrity. The first nine are `tests-pass`; the last three are `command-succeeded`. C3P sealed at commit `f7b6e6bda7a8864969415ab8636c495902e78dd9`, tree `2d5555db63d46c837cf4e12f2dab2d04a41f4654`, with `12/12 claims recorded-exact` and strict exit `0`. Its source grades became tracked authority only when the later C3PB receipt declaration bound the sealed note and exact local snapshots.

C3PB is that descendant. It owns only `docs/HANDOFF_MODE_C.md`, `docs/status/P07B-C-C3P-RUNTIME-EPOCH.md`, and `spec/verification/p07b-c-c3p-receipt.json`, with digest `sha256:531cbe600cf2747752749890ea9a15c3d0126e498da6838d4f6faeb1a0d7e885` and the standard ordered seven-claim `RECEIPT_RECONCILIATION` manifest. It reconciled only the already-sealed C3P source evidence and labels ignored ledger/HTML checks as local snapshot evidence rather than portable strict witness. C3PB closed at commit `13369122ba7d5557eba1949095c1135a41843070`, tree `d710d9b249786f3fb648f566ae542f1d9ddec180`, with `7/7 claims recorded-exact` and strict exit `0`; that receipt proves no target behavior. C3 is now active only after the separate C3A/C3L/C3F/C3S maintenance chain sealed.

## C3V verification-throughput proposal reconciliation

The post-C3P observer proposal was based on a pre-C1V snapshot. C3V therefore changes no verifier or repetition-helper behavior. It binds an item-by-item ruling to the current machine declaration and sealed C1V authority: commit `88e62acb3023dcd6ee51950c2ad24bbcc2ca8900`, tree `6c98b3c384f01b63d1a00a002303041576896609`, `10/10 claims recorded-exact`, strict exit `0`, the exact 52-case focused qualification matrix, and three complete cumulative passes at `814384`, `815886`, and `813933` milliseconds beneath the fixed `878544` millisecond ceiling.

Item 1 is `DECLINED_WITH_REASON`: `GOCACHE` remains fresh because a Go-authority-only persistent key omits imported C-library, clang, SDK/header, libSystem, and prior writable-cache authority in this real Darwin/cgo tree. Item 2 is `INTENTIONAL; P8_DECLINED_WITH_REASON`: direct build/vet/general work already uses qualified `-p=2`, the six frozen sensitive packages and every nested Go command remain serialized, and the rejected `p=4` candidate caused two permanent later AST-timeout events despite passing Go packages. Item 3 is `INTENTIONAL`: the fail-fast O_EXCL lock already refuses a second owner and preserves review-first stale-lock handling. Item 4 is `INTENTIONAL`: exact empty-prefix receipt units already use the explicit seven-claim `RECEIPT_RECONCILIATION` profile and cannot claim a full suite or unchanged behavior.

C3V owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3V-VERIFICATION-THROUGHPUT.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:6cc5d8fe929f0f1dfa6ffc223d2e8bcee5ba4596a09caa9dc032103bbb5bafbd`.

Its final ledger has exactly eight claims: proposal reconciliation, historical-ruling hostile self-test, unit-scope hostile self-test, cumulative-verifier hostile self-test, one complete cumulative verification, exact seven-path staged scope/diff integrity, scoped named-pattern credential scan, and preceding-chain integrity. The first five are `tests-pass`; the last three are `command-succeeded`. Any verifier-setting change invalidates this no-delta boundary and instead requires an expanded verifier-authority scope plus fresh load-sensitive qualification. C3V proves no new portable performance, warm-cache correctness, p8 stability, secret absence, security review, or runtime capability.

C3V sealed at commit `725933fa3c7826a281e84b51f729736cbcfac6ec`, tree `21f0e1bf46e3776e1b496a9c2783f549923c4510`, with `8/8 claims recorded-exact` and strict exit `0`. That historical result closes only the four proposal dispositions and frozen-verifier authority; it does not receipt C3P or repair a later receipt-phase fixture.

## C3P receipt-phase checker maintenance

A source-receipt-field-validated C3PB draft exposed a phase-fixture lifecycle defect before any C3PB staging, ledger, claim, commit, or seal. The ordinary checker correctly selected receipt-present mode, but its synthetic absent-receipt fixture reopened both status and handoff from the sealed C3P commit. Reopening the exact sealed C3P status is necessary; reopening the C3P-era handoff is wrong because it discards interposed C3V and C3M plan authority. Field reconstruction did not make the full draft valid or graded.

C3M keeps the ordinary live baseline and existing receipt-present declaration, parent/diff, Git-note, argv, grade, coverage, local-evidence, documentation, no-recursion, and self-receipt hostile suites. Its absent fixture instead combines the exact-tree-proved sealed C3P status with the current handoff after fail-closed removal of at most one exact C3P receipt block. Raw start/end cardinality must be `0/0` or `1/1`; admitted markers are standalone LF lines in forward order. Marker-free input is identity-preserving and one canonical block round-trips byte-exactly. Nested/duplicate, partial, reversed, inline, or trailing-content marker topology fails. Empty or malformed receipt JSON is never treated as absence.

At the sealed C3M boundary, the full absent-phase plan passed after stripping and receipt-present mode transitioned the synthetic handoff to explicit C3PB-active state. Its six operational transition fields were an exact tuple: all-C3M transitioned once, all-C3PB was identity-preserving on rerun, and mixed, missing, duplicated, or stale state failed. The extracted block had to equal one canonical ordered payload, and any C3P-specific receipt residue outside that block failed. C3V and C3M authority survived both synthetic phases, and verifier/repetition-runner bytes remained frozen at the C3V-pinned digests.

The historical checker-compatibility anchor “C3V and C3M authority survives” remains preserved; it describes carried authority, not current unit state.

That paragraph records historical C3M behavior, not the current live admission contract. With C3R sealed and C3Q active, receipt-absent reconstruction must preserve exactly one complete C3Q tuple in its canonical Markdown locations: exactly one exact line for each of four named metadata keys in `## Current state`, then the matching active subsection heading and receipt-presence paragraph. A validated C3 source receipt plus exact sealed C3R/C3Q ancestry atomically transitions all six slots to C3B; receipt-present rerun remains byte-identical across status, handoff, and declaration. Correct bytes relocated into quoted/history text, placed inside a contradictory duplicate key, or embedded as historical text on a noncanonical line fail. Complete C3M, C3PB, C3A, C3L, C3F, C3S, C3, or C3R tuples are recognized only for stale-state diagnostics and are rejected, as are missing, duplicated, relocated, embedded, mixed, or coherent whole-tuple downgrade states. Active-unit positive receipt forms remain forbidden across the exact plan-authority Markdown corpus.

C3M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3P-RECEIPT-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:58e1f6fba95cf3b4312136eea5bb5c64af89a6958711aca84181a1874aa03240`.

C3M was a full checker-authority `SOURCE_FULL` boundary, not a reduced receipt battery. Its final ledger ran plan coherence, the dual-phase checker self-test, unit-scope self-test, cumulative-verifier self-test, one full cumulative verification, exact seven-path source-final gate, named-pattern credential scan, and exact preceding-chain verification. C3PB's three paths, digest, labels, and narrow profile remained unchanged at that historical boundary; C3M and C3PB later committed, sealed, exposed readable notes, and verified strictly before C3A began. C3A then independently sealed before C3L; C3L sealed before C3F began.

## C3A cumulative-admission maintenance

The first preserved C3 source loop exposed two predecessor-contract defects before any C3 commit or seal. The cumulative B architecture checker allowed the C1 model prefix and three exact C2 storage symbols, but not its own authority contract's future sole C3 issuer. Separately, the target JSON Schema and live Node probe already enforce a 128-byte runtime-version ceiling while the Go canonical constructor/parser accepted any length matching the ASCII grammar. Neither defect can be repaired silently inside C3 because both owning files are outside C3's frozen forty-path roster.

C3A admits only `internal/contractexec/target.go:ContractExecutionTarget` as the new C3 pair through the inherited B partition. The collector conservatively inventories word-shaped raw-source tokens in non-test Go files under `internal/`, including comments and literals, for the `ContractExecutionTarget`, `FinalizedContractRun`, and `ContractExecution` roots. The exact partition also admits the established `internal/store/nonhead_contract.go:ContractExecutionClassifierProfile` support pair; outside the intentionally admitted C1 model prefix and exact C2/C3 pairs, the same token at another path and every tested prefix, suffix, or Unicode-suffix variant remain foreign. This is an internal production-Go token policy, not a repository-wide scan, Go-AST identifier claim, escaped/concatenated runtime-string detector, or general Unicode-confusable detector. The B checker's executable guard compares real paths, and the self-test requires the exact success marker plus empty stderr through both direct and temporary-symlink invocation. This is repository conformance evidence, not hostile same-user isolation.

The model correction rejects `len(runtime.Version) > 128` before the existing ASCII version regex. The focused test proves exactly 128 bytes construct and round-trip, while 129 bytes refuse both construction and a coherently rehashed parse. The schema, examples, generated planning bytes, and valid target digests remain unchanged because the implementation is converging on already-sealed syntax authority.

C3A owns exactly these 13 paths: `docs/ARCHITECTURE.md`, `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3A-CUMULATIVE-ADMISSION-MAINTENANCE.md`, `docs/status/P07B-C-C3P-RUNTIME-EPOCH.md`, `internal/contractexec/model/codec_test.go`, `internal/contractexec/model/target.go`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-b-architecture-selftest.mjs`, `tools/check-p07b-b-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:cc9bb98ceeb2f281336d3e235377d3cdb8920664f833baea830a84b847621188`.

Its final ledger has exactly eleven claims: plan coherence; plan/phase hostile self-test; the direct 128/129-byte model boundary; the cumulative B architecture gate; the B partition/entrypoint hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact thirteen-path staged scope/diff integrity; scoped named-pattern credential scan; and preceding-chain integrity. The first eight are `tests-pass`; the last three are `command-succeeded`. The declared manifest requires one exact `/usr/bin/env -i` prefix for all eleven commands with pinned Node/Go/Git/shell/compiler paths, disabled Node startup injection, offline Go settings, and one dedicated final root. The first ten events bind the exact repository cwd and one stable didrun wrapper-environment fingerprint as well as argv/tree/claim identity; the fingerprint is not a full environment attestation. The live eleventh gate validates its own cwd, Node realpath, invocation tail, effective child bindings, and owned mode-`0700` nonsymlink run directories, retains the C3P receipt declaration, and refuses any premature seal. Its didrun event is appended only after exit, so this self-check cannot prove event eleven's outer `env` argv; the later recorded event remains inspectable evidence without becoming part of the preceding-chain claim.

C3A sealed at commit `2b84d2841971568784d2ac955775b4a99ca7f0f6`, tree `b8d858033431fe51c262ce725b9d9b051ba9e8c9`, with `11/11 claims recorded-exact`, readable Git-note blob `3e516946f4b86e536ae9ea0124938bd534573983`, note-body SHA-256 `07a0d04dc667963ea16ec6bb458b929d3a7659637e705bb642b5267766609a8c`, and strict exit `0`. C3 remains frozen to forty exact paths and digest `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`. C3A claims no live Git, host, runtime, attempt, official-target, process, or classification behavior.

## C3L legacy-lattice maintenance

Restoring the preserved C3 source exposed a cumulative architecture defect before any C3 commit or seal. The cumulative verifier runs U6 before later P07B-B/C checks. U6's exact historical store import lattice therefore rejected C3's future `internal/contractexec/model` bridge edge before those later checks could assess its narrower semantics. Removing U6/B from cumulative verification, broadening a prefix, or replacing typed authority with generic bytes would weaken evidence, so C3L repairs the owning legacy boundary separately.

C3L admits two and only two store states. The historical tree retains the exact old import lattice. The future state uses a fail-closed top-level Go source-shape index that decodes interpreted/raw import literals, ignores import-like literal camouflage, and adds alias `contractmodel` only in the production file `internal/store/nonhead_contract.go`. Across the production `store` package, the four bridge types must be unique non-alias package-level structs owned by that file, the input must contain exactly four ordered untagged `domain.Digest` fields, and the four `ObjectStore` methods must be uniquely owned there with exact parameter/result type sequences while receiver and parameter identifier spelling remains inert. Absent shape, wrong alias, tagged/hidden/extra/reordered fields, field/parameter/result drift, raw or escaped foreign imports, local shadows, foreign aliases, duplicate types, or duplicate methods fail with `U6_STORE_C3_MODEL_IMPORT_NOT_ADMITTED`; every other import still fails the inherited exact lattice. The checksum-pinned self-test has 147 cases: seven clean/comment controls and 140 hostile cases. C3's compiler-AST and runtime tests remain responsible for deeper bridge behavior.

C3L owns exactly these 9 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3L-LEGACY-LATTICE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/check-u6-architecture-selftest.mjs`, and `tools/check-u6-architecture.mjs`. Their sorted-newline roster digest is `sha256:b3624e1337681e0909e6c074246103e066118a5924e8f790e7d1f42359da9dc0`.

Its twelve-claim final ledger ran, in order: plan coherence; plan hostile self-test; historical/current-tree U6 architecture compatibility; U6 conditional phase-admission hostile self-test; cumulative B compatibility; B hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact nine-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3A-predecessor plus preceding-chain integrity. The first nine were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and a private C3L final root. C3L sealed at commit `efa918928246c7793f4ad7201c003020e3a42193`, tree `d9187c6449392c269a385b53c563ba4411112460`, with `12/12 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `b21365e156719fbc84e480108a7e8672644f5a71`, note-body SHA-256 `d4721b344adb95442c5273e596c83ea9cb1b855d26655198a9803771f83e2e9d`, and strict exit `0`.

C3 remains frozen to forty exact paths. C3L proves only exact conditional repository conformance in the U6/B chain. It proves no live target, persistence behavior, Git materialization, host measurement, Node probe, attempt allocation, runtime stability, interlock, process spawn, classification, confidentiality, security boundary, production readiness, adoption, or maintainership. C3F and C3S subsequently closed as separate maintenance boundaries before C3 resumed.

## C3F B future-surface admission maintenance

The first resumed C3 architecture baseline to reach inherited B, after an in-scope source-shape correction, truthfully failed there: C3 requires the target type, its constructor and parser in `internal/contractexec/target.go`, and the store-owned parser use in `internal/store/nonhead_contract.go`, while C3A admitted only the type pair. The three-event, zero-claim trigger ledger is preserved at `.didrun-history/2026-07-19-p07b-c-c3-build-loop-post-c3l-pre-c3f/.didrun/`. Obscuring those identifiers, duplicating the codec, accepting generic bytes, removing B from cumulative verification, or letting C3 silently edit its prerequisite would weaken the architecture. C3F repairs only the owning B policy at a distinct boundary; the preserved C3 source stays frozen.

B retains historical zero-member admission. The future surface is admitted only as one complete exact four-pair bundle: `internal/contractexec/target.go:ContractExecutionTarget`, `internal/contractexec/target.go:NewContractExecutionTarget`, `internal/contractexec/target.go:ParseContractExecutionTarget`, and `internal/store/nonhead_contract.go:ParseContractExecutionTarget`. Any one-, two-, or three-member subset remains foreign. A complete bundle plus any extra reserved-family token admits only the exact four and leaves the extra foreign, so the outer architecture gate still fails. Relocated owners, renamed identifiers, suffix/prefix variants, Unicode suffixes, sibling packages, and copied files receive no admission. This lexical production-Go policy is not semantic call proof; C3's own compiler, architecture, and runtime gates still own that evidence.

C3F owns exactly these 10 paths: `docs/ARCHITECTURE.md`, `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3F-B-FUTURE-SURFACE-ADMISSION.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-b-architecture-selftest.mjs`, `tools/check-p07b-b-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:f64293895a1b824faafddf236757d381c813b59c40286225bb1c352d5c812c1a`.

Its ten-claim final ledger ran, in order: plan coherence; plan hostile self-test; historical/current-tree B architecture compatibility; B conditional future-surface hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact ten-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3L-predecessor plus preceding-chain integrity. The first seven were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and one private C3F final root. The live tenth gate validated the preceding nine events and claims, stable tree/fingerprint, exact private directories, its own effective bindings, and absent seal; it separately bound sealed C3L's exact commit/tree/parent/subject, note blob/body, ordered twelve-claim semantics, and complete event coverage. The final event was appended only after exit, so it could not prove its own outer wrapper.

C3F sealed at commit `63038644ba347d7b934a5557a490af95bb4428a4`, tree `3b4c39cfa907c2907d01dcd21399cafe52075872`, with `10/10 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `098958ba7a9b2d9d237cee901a34279424b68128`, note-body SHA-256 `a23982f84573505fc966378dbcc4f825daf11da8a08b2bfd24f7f71de588d315`, and strict exit `0`. Its exact-commit HTML is `.countershape/evidence/p07b-c-c3f-final-63038644ba34.html`; its final ledger is `.didrun-history/2026-07-19-p07b-c-c3f-final/.didrun/`.

C3F's proof ceiling is only exact historical/future lexical admission in B and the continued cumulative baseline. It proves no target semantics, parser correctness, durable relation, materialization, live host/runtime/boot/attempt authority, spawn, classification, confidentiality, hostile same-user boundary, production readiness, adoption, or maintainership. C3 remains frozen to forty exact paths.

## C3S cumulative U6 self-test maintenance

The restored C3 source passed production U6 but the full cumulative verifier failed at current row 13 of 46, `architecture-u6-selftest`. The self-test's `installC3StoreBridge` helper still assumed the historical import adjacency and always appended a synthetic bridge. Once the real C3 bridge existed, that anchor was absent and no hostile case ran. The nonzero receipt remains permanent; skipping the self-test, weakening U6, or broadening the frozen C3 source would be false closure.

C3S replaces the one-phase fixture installer with an exact two-phase ensure/require contract. Historical C3F source is admitted only when the model import and every C3 bridge surface are absent; the helper then installs the checksum-pinned synthetic bridge. A current C3 source is admitted only when the exact import, ordered four-field input, non-alias owner structs, and four typed `ObjectStore` signatures are each uniquely present; that source is validated in place and never duplicated. The clean bridge case calls the helper twice to prove install-then-present idempotence. Hostile field/signature/owner cases use unique current-source anchors. The 147-case roster and digest remain unchanged.

C3S owns exactly these 8 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3S-CUMULATIVE-SELFTEST-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, and `tools/check-u6-architecture-selftest.mjs`. Their sorted-newline roster digest is `sha256:1d5f793e70caea0ad7379e92bbf781da50846276c28a2aca2d3e7e2ed8c17a26`.

Its ten-claim final ledger ran plan coherence; plan hostile self-test; historical U6 architecture compatibility; the phase-stable U6 hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one full cumulative verification; exact eight-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3F-predecessor plus preceding-chain integrity. The first seven were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and private C3S final root. The last gate bound sealed C3F's exact commit/tree/parent/subject, note object/type/body, ordered ten-claim semantics, and complete event coverage.

C3S sealed at commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, tree `fce8593ad1fe7af1474b4e113f03e82a9a052f79`, with `10/10 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `7dff66800e79dc5b1a149ffe16df4dbf9178836a`, note-body SHA-256 `b65f71e25dd53509f8df876a28fb4027559464f77861a34d1aa0c5854bfdbae6`, and strict exit `0`. Its exact-commit HTML is `.countershape/evidence/p07b-c-c3s-final-47a65b45a50c.html`, SHA-256 `38a2264e5f7bcba72755717b882460651852f1ff4ece826a38d0b37be47ada9b`, 7941 bytes; its final ledger is `.didrun-history/2026-07-20-p07b-c-c3s-final/.didrun/`.

C3S proves only cumulative repository/test-fixture compatibility. It proves no C3 target composition, store behavior, Git materialization, host/runtime freshness, race freedom, process spawn, classification, security boundary, production readiness, adoption, or maintainership. C3 binds sealed C3S as direct predecessor while retaining C3F/C3L/C3A ancestry.

## C3 exact-target publication

C3 owns forty exact mode-`100644` paths, no prefixes, at sorted-newline digest `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`. The frozen source partition is exactly 20 added and 20 modified files. The final source gate admits no rename, deletion, type change, executable/symlink entry, unstaged tracked change, or untracked path. It includes the compiler-frozen store surface because the narrow C3 bridge must be explicit, and it materializes directly from opaque `gitobj.InspectedTree` rather than synthesizing a one-member comparison world.

The direct parent is sealed C3S commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, whose parent is sealed C3F `63038644ba347d7b934a5557a490af95bb4428a4`, whose parent is sealed C3L `efa918928246c7793f4ad7201c003020e3a42193`, whose parent is sealed C3A `2b84d2841971568784d2ac955775b4a99ca7f0f6`. `spec/verification/p07b-c-c3-predecessors.json` binds that exact chain, each tree and subject, `refs/notes/didrun` blob type/identity/raw-body digest, exact ordered claim/type/grade map, complete event coverage, declared scope digest, and redacted-seal disclosure. Scope digests and the historical strict exits documented here are tracked declarations, not fields synthesized from a didrun note. The live eighteenth preseal gate reopens the tracked manifest without following a symlink, rechecks all four Git commits and raw note blobs, and then verifies the preceding seventeen didrun events share one exact staged tree and hermetic wrapper fingerprint with no seal.

The behavioral battery has five authority profiles: exact official-target composition, direct single-target Git materialization, Darwin host-epoch measurement, admitted Node runtime, and the inert attempt/target store bridge. The official-target profile includes the public live-parent matrix: same physical object database after restart and an independent exact-content database are positive, while cross-store/residue/study/target joins, unrelated or closed repositories, and zero/absent locators issue no authority and preserve the exact scoped store trees, including names, modes, inode identities, ownership, link counts, sizes, and regular-file hashes. A focused race suite separately runs the four exact deliberate-concurrency tests that directly launch concurrent capability operations: official-target close/reopen linearization, uncached host-epoch measurement, copied-runtime revalidation serialization, and attempt/target create-once convergence. It uses `-race`, `-p=1`, and an explicit 20-minute bound over the four owning packages. A machine-checked C3 source inventory pins the exact direct `go`-statement counts for those tests, forbids `t.Parallel`, and forbids production `go` statements in the five owning packages; complete Git materialization and all non-concurrency behavior remain in the five normal authority profiles rather than being duplicated under race instrumentation. The cumulative C3 architecture gate and its defensive self-test freeze issuer topology, source ownership, predecessor authority, forbidden process edges, and the five profile rosters; its raw predecessor lane injects malformed Git framing, fatal UTF-8, note object/body/field-roster drift, exact argv-prefix/command-boundary changes, ancestry-result failures, and coverage mutations without changing sealed notes. The sole-issuer AST cases resolve qualified named types and functions through module-wide graphs over the current Darwin/arm64/cgo production-file selection; exercise source-ordered local shadows, block-scoped generic construction, union and dependent receiver constraints, binding-sensitive repeated instantiation, partial and fully inferred generic producers, generic function calls/values plus direct and promoted generic method calls/values/expressions, parenthesized callees and built-ins, tuple-positioned module function/method call results, nested/indexed/shallowest-promoted exported fields, multiple dot imports, and held factories; and include positive input-only type/function/method consumers plus shadowing and promotion controls. Non-call comma-ok tuple projection and local value/function shadowing are not modeled exactly. These cases remain a cooperating-code shape policy rather than dataflow or hostile-code proof. Inherited B compatibility and its hostile self-test remain current. The cumulative verifier then repeats all prior current units plus these C3 rows.

C3's verifier delta is roster-only verifier enrollment: seven exact C3 current-step definitions plus their self-test expectations. It changes no cache lifetime, job count, package classification, child environment, lock behavior, or repetition-helper byte, but it materially increases sequential verification work. C3 explicitly supersedes rather than literally satisfies sealed C3V's blanket future-byte trigger. The 52-case C1V load-sensitive qualification matrix was not rerun. For this unit, the dedicated verifier self-test and cumulative verification re-establish only the evolved roster and one complete composition snapshot; an unclaimed post-repair development pass completed normally in `2908.769` seconds, which does not establish repeated load-flake stability, portable performance, or a new ceiling. The complete C1V matrix remains mandatory for a later execution-setting, sensitive-package classification, lock-semantics, cache-lifetime, or repetition-semantics change.

One private mode-`0700` C3 final root carried a fresh eighteen-claim final ledger: plan coherence; dual-phase defensive self-test; five authority profiles; focused race suite; architecture and architecture self-test; B and B self-test; scope self-test; verifier self-test; cumulative verification; exact source-final diff; credential-pattern scan; and predecessor/preceding-chain integrity. Every command used the same exact `/usr/bin/env -i` bindings. C3 closed at commit `029c5cf43853eb3cb46520f6e0dea8431a3de5c0`, tree `de3e02a343f8ecfb344bdb28d014342f5b423959`, with `18/18 claims recorded-exact`, readable note blob `9ad8a864911ca30914463c61d3447a88beeda927`, note-body SHA-256 `f5eaa66d0cae152a9edc7f4ff2d49ba66c0d1eb8f3459171a2888daaaaf80fa8`, and strict exit `0`. Its historical pre-seal status intentionally keeps every C3 source grade `UNRECEIPTED` until the separate C3B receipt declaration reconciles them.

C3B is the later exact three-path receipt unit at `sha256:3ef17156a2f3051171da70983f9428de2b9bdb5a179602407a36f34200012a67`. Its separate seven-claim ledger may reconcile the sealed source declaration, run the receipt checker self-test, compare the declared ignored local snapshots, build without source edits, prove its exact staged diff, scan those three staged blobs, and bind its preceding chain. C3B changes no source or verifier behavior and cannot claim cumulative execution, runtime freshness, security, C4 authority, or unchanged behavior. C4 cannot start until C3B independently seals and verifies strictly.

None of these declarations proves a subject process ran or that a hostile executable is authentic. The OS-reported UUID profile remains explicitly weaker than physical reboot, survivor absence, VM non-rollback, kernel trust, clone resistance, or safe-reset proof; the fixed runtime probe proves only a cooperating executable's reported tuple. `OfficialTarget.Valid()` uses sealed in-memory snapshots only, the returned `ContractTargetRecord.Valid()` remains a separate durable-store revalidation, and only reopen refreshes the complete physical graph. C4 must consume a fresh successful reopen immediately adjacent to its physical boundary.

## C3R sealed-note preview compatibility maintenance

C3R owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3R-NOTE-PREVIEW-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:fe14d9f9985771cce0525edfca47c001b5912dacc4c2d6fef182458d3b44f58e`.

The first C3B plan event exposed a deterministic compatibility defect: sealed C3 claim eight's exported Git-note `argv_preview` contains the canonical four-test race selector with only its first long test identifier replaced by `«redacted:high-entropy»`, while the ignored ledger retains the complete raw selector. The prior note validator admitted exact redactions only in the common hermetic prefix and then required a raw byte-equal tail. It therefore rejected a valid sealed projection even though the note blob, note-body digest, commit/tree, claims, grades, and raw local evidence were intact.

`c3RaceNotePreviewPattern` names the one admitted tail projection. The validator accepts it only at C3 claim index seven and only as the entire otherwise-canonical selector argument. It rejects a second-alternative marker, whole-selector marker, suffix, other claim, unrelated tail, length drift, or any other argv change. The local-evidence checker continues to require byte-exact raw local-ledger argv and explicitly rejects the scrubbed projection there. `secrets_override: true` is disclosed as export behavior, not evidence of secret absence or generic redaction correctness.

C3R operator seal handling was outcome-dependent: the plain seal was attempted first, refused aggregate high-entropy-only findings, and the reviewed retry recorded `secrets_override: true` after the separately claimed exact-path structured scan reported zero named-pattern findings. Portable C3R authority does not prove plain-seal-first sequencing, refusal, or review: the Git note records only the resulting boolean. The later C3B ledger-profile line is rendered from that validated boolean and discloses the exact recorded seal disclosure with the same limitation. Neither value is evidence of secret absence, a general credential audit, or publication authority.

The historical C3R lifecycle was dual-phase and fail closed. While `spec/verification/p07b-c-c3-receipt.json` was absent, the six canonical handoff slots formed exactly `C3R_ACTIVE`; sealed C3 identity was legitimate predecessor evidence, but C3 source grades remained unreconciled. C3Q now interposes the repaired current phase, while C3R remains an exact sealed ancestor rather than the current `HEAD` requirement for C3B.

C3R's nine-claim final ledger covered plan coherence; note-preview and dual-phase defensive self-test; live sealed-C3 note compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped credential-pattern scan; and sealed-C3 predecessor plus preceding-chain integrity. C3R closed at commit `424273b000f87925a87f0b3041b2e1e8c5750d39`, tree `0e4694362d0242a3b3da0ed2f8f78971ee595f6c`, with `9/9 claims recorded-exact`, note blob `7d0c90aaec4f1a36d8603e5c1ff6195a02221314`, note-body SHA-256 `dcd8991ef2446da78137319d7069d89999eda9748338fbdb5f8927bb4460e09d`, and strict exit `0`. The final ledger is `.didrun-history/2026-07-20-p07b-c-c3r-final/.didrun/`; the exact-commit HTML is `.countershape/evidence/p07b-c-c3r-final-424273b000f8.html`. Its tracked status remains intentionally frozen pre-seal source-era text.

C3R changes no product, target, store, Git materialization, host/runtime measurement, verifier roster, cache/parallelism setting, repetition semantics, lock semantics, didrun installation, sealed C3 bytes, or frozen C3B three-path/seven-claim contract. It proves no process start, runtime freshness, security boundary, secret absence, production readiness, adoption, or maintainership.

## C3Q active-unit self-receipt maintenance

C3Q owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3Q-SELF-RECEIPT-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:f5f27e661c1ace6516aba4b9d5766f4c6ab50b6602dbcb101bff9b6a4ebfe6fc`.

The first post-C3R C3B defensive self-test exposed phase-asymmetric self-receipt enforcement. Its receipt-present branch inspected only the owned target status and handoff, while the hostile case deliberately placed a positive active-descendant result in an external verification authority. The case therefore escaped the branch that was supposed to reject it. That failed didrun event is permanently archived at `.didrun-history/2026-07-20-p07b-c-c3b-build-loop-pre-c3q/.didrun/`; it supports no claim and is not erased, relabeled, or treated as a transient infrastructure failure.

The repair applies one shared active-unit refusal function to a frozen 42-path plan-authority Markdown corpus with sorted-newline digest `sha256:16d63155a00c9e87b7eec79707ad6d47d6f5d70205d18243b8bc687e401e4b66`: every Markdown body loaded from the required-text maps and status checks, plus the separately read C1, C2, and didrun status authorities. In receipt-absent mode the active unit is C3Q; in receipt-present mode it is C3B. The hostile matrix covers positive commit/tree identities, closure/status words, zero strict-result forms, recorded-exact counts and verdicts, evidence/grade/result associations, positive claim-table rows regardless of column order, and receipt-marker topology. Its declared cross-product is 2 phases × 42 paths × 15 positive forms, or 1,260 required rejections, plus 420 pending/future/failed positive controls that must remain accepted. Eight supplemental natural aliases must reject and two explicit prohibition controls must remain accepted. This is a bounded lexical grammar, not arbitrary sentiment or paraphrase inference. End-to-end external-surface mutations retain the original C3Q and C3B regressions.

Run `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --print-plan-authority-corpus` to inspect the deterministic sorted corpus, its path count and digest, and its explicit bounded-scope footer. The report recomputes the digest and fails closed if the declared constant has drifted. The inventory mode is read-only and is not itself a final-ledger command or didrun grade. Rejections name a stable matched form such as `strict-zero` or `claim-table-grade` without echoing the matched authority text.

Lifecycle authority now requires `C3Q_ACTIVE` before the C3 receipt exists. Once the exact C3 receipt appears, the checker validates sealed C3R as the fixed direct child of C3, resolves exactly one C3Q direct child of C3R, validates C3Q's exact seven-path source diff, subject, note, nine ordered claims/argv values, grades, and coverage, and requires that C3Q child to be current `HEAD` before admitting `C3B_ACTIVE`. This preserves the full `C3 -> C3R -> C3Q` chain rather than weakening the old C3R-current-head test.

The C3Q status authority is projected through visible Markdown before its scope, boundary, subject, state, claim rows, and required rulings are accepted. The C3Q-to-C3B subsection transition uses that same visible projection to locate headings while preserving raw byte offsets, so headings hidden in comments or fences cannot truncate replacement and leave stale C3Q prose behind.

C3Q uses a nine-claim final ledger: plan coherence; dual-phase, all-surface/all-form defensive self-test; live sealed-C3R note compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped staged credential scan; and sealed-C3R predecessor plus preceding-chain integrity. The first six claim types are `tests-pass`; the final three are `command-succeeded`. A direct pass has no didrun grade. Every intended grade remains `UNRECEIPTED` until the exact staged tree runs through the fresh ledger, commits, seals, exposes a readable note, and passes strict verification.

C3Q changes no verifier roster, product, target, store, Git materialization, host/runtime measurement, process behavior, cache/parallelism setting, repetition semantics, lock semantics, didrun installation, sealed C3/C3R bytes, or frozen C3B contract. It proves only bounded positive-receipt refusal over the exact loaded Markdown corpus—not arbitrary repository prose, secret absence, security review, runtime correctness, production readiness, adoption, or maintainership.

## Historical C2 receipt-phase checker maintenance

The first C2B development self-test exposed a lifecycle defect: four hostile mutations that belong only to the absent-receipt phase were still applied to live receipt-present documents. The plan checker correctly changed branches, but the test continued to demand old phase errors; one digest mutation became invisible to that branch and the pending-grade replacement became a no-op. The failed development event remains unclaimed and permanent.

C2M preserved, rather than skipped, those tests. The ordinary top-level plan baseline checked the real working tree, including the receipt-present working-tree baseline during C2B. The existing synthetic receipt suite exercised declaration, Git/didrun authority, local evidence, documentation agreement, and self-receipt failures; a receipt-present synthetic full-plan baseline and hostile handoff-identity mutation proved that top-level `checkPlan()` wired that branch before C2M sealed. A second full-plan fixture made the C2 receipt path return a synthetic, `readFile`-equivalent `ENOENT` signal, reopened the exact sealed-C2 status blob from commit `19c90786d9832e21ec86daea96ca514cc7304836` after re-proving tree `1557f1e26762d717327a2ec770f8153daa0baf3e`, removed at most one exact C2 receipt block from the current handoff, and required a clean full `checkPlan()` baseline. It then ran the same four pre-receipt mutations with exact-one anchors and their exact phase-specific errors. Empty or malformed JSON was never treated as absence, and an absent or duplicated mutation anchor failed the test.

The v4 unit scope inserted C2M between C2 and C2B. Source-final diff integrity and the named staged credential scan were admitted only for an explicitly declared empty-prefix, exact-roster `SOURCE_FULL` unit; prefix-bearing source units and receipt profiles remained ineligible for these exact-roster gates. C2M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C2-RECEIPT-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:75dcb6a88a9faec8aa89cfbe67c0870e88d95709a1bf07e2fb494e92362688cd`.

This was a full checker-authority maintenance boundary, not a docs-only receipt exception. Its final ledger ran plan coherence, the dual-phase checker self-test, unit-scope self-test, cumulative verifier self-test, cumulative verification, exact seven-path source-final gate, scoped staged credential scan, and exact preceding-chain verification. C2 and its evidence stayed immutable; C2B's three paths, digest, labels, and narrow profile stayed unchanged and resumed only after C2M independently committed, sealed, proved its note, and verified strictly.

## Historical mixed didrun object namespace for C1 local evidence

The C1B development gate exposed a checker defect before freeze: the pinned ignored C1 ledger stores two intentional object namespaces under `.didrun/objects/`. Flat `objects/<64-lowercase-hex>` files are didrun stdout/stderr/transcript capture blobs named by SHA-256 of their raw bytes. Fan-out `objects/<2-lowercase-hex>/<38-lowercase-hex>` files are zlib-compressed Git loose objects redirected there while didrun computes working-tree identities in this SHA-1 repository. The existing objects-only SHA-256 manifest continues to bind the stored path, size, and raw bytes of both namespaces together.

Local evidence validation is reject-by-default and resource-bounded before content reads. Flat names must match raw SHA-256. Fan-out files must consume one complete zlib stream of at most 4 MiB inflated bytes without trailing input, expose a canonical Git object header and exact declared payload size, and match the SHA-1 object identity encoded by their path. The archive directory set must equal `objects/` plus the nonempty two-hex fan-out parents implied by admitted loose-object files; empty `pack`/`info`, alternates, and every foreign or extra-depth directory fail. Every other namespace, malformed stream, over-limit inflation, header/size mismatch, identifier mismatch, observed symlink, or nonregular entry also fails. Session stdout/stderr/transcript references must be valid flat identifiers, and their set must equal the flat capture-blob inventory. SHA-1 here is the repository's Git object-format identity; archive integrity remains the declared SHA-256 manifests. This exact C1 observation is a static local snapshot under Countershape's sole-writer protocol; it is not hostile same-user isolation against concurrent namespace replacement, a portable didrun layout, or a strict-witness guarantee.

The retained maintenance-only `--verify-sealed-c1-local-evidence` mode requires the C1B receipt declaration to be absent and crosses the already-pinned ignored C1 archive without enrolling it. C1B later used `--verify-c1-local-evidence`, which requires that declaration to be present, to bind the same local snapshot to its tracked declaration. Declaration-bound mode reads one bounded no-follow UTF-8 receipt snapshot and passes those exact bytes through plan/authority validation and evidence consumption; it never validates one receipt read and consumes a second. Neither mode makes the HTML or ignored archive portable, grades a receipt by itself, establishes security, or authorizes publication.

C1E owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C1-LOCAL-EVIDENCE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:9a6422b45c82a44e6171ae9351468df5f6ece2190d6ab47f26aec6f84fd7ccde`.

C1E was a full source-maintenance boundary between sealed C1V and unchanged C1B. It ran the mixed-namespace hostile self-test, the real sealed-C1 local archive check, unit-scope self-test, complete cumulative verifier, exact staged/diff gate, named-pattern scan, and preceding-chain audit from a fresh frozen-tree ledger before commit/seal/note/strict closure. Its successful source receipt was not reused as a C1B receipt.

## Output contract

The stable output classes are:

```text
COUNTERSHAPE_VERIFY_V1 ... general_jobs=2 nested_jobs=1 gomaxprocs=2 ...
AUTHORITY ...
ROSTER CURRENT ...
HISTORICAL-ONLY NOT-RUN ...
CURRENT [NN/NN] START ...
CHILD ...
CURRENT [NN/NN] PASS ...
RESULT PASS current=N/N historical_not_run=M
```

On failure, the failing row is `CURRENT ... FAIL`, no later current row is spawned, and the summary is `RESULT FAIL`. The terminal `RESULT PASS` is printed only after the current resource-finalization row has removed the private run root and released the verifier lock; cleanup or release failure therefore cannot leave an overall PASS marker. `HISTORICAL-ONLY NOT-RUN` means exactly that; it is not a current-tree compatibility claim.
