# Current verification baseline

## U7M successor-verifier maintenance boundary

The active repository boundary is U7M under `countershape/u7-unit-paths/v2`, directly after exact sealed U7P commit `05ade5ea9af2ededbc91118c8952631c68e43f36`, tree `372ac1b48b239f6680f8159b7155628d61faaded`. U7M is a `SOURCE_FULL` successor-verifier repair with product authority `NONE`, product behavior `INHERITED_UNREPROVEN`, no grade transfer, and an absent U7D receipt. Its exact 14-path roster and 13-command manifest are machine-owned by `spec/verification/u7-unit-paths.json`; `docs/status/U7M-P07-RECEIPT-SUCCESSOR-COMPATIBILITY.md` is the human-readable projection. Every U7M grade remains `UNRECEIPTED` until its own final ledger closes.

Both U7 transition validators independently reopen sealed U7P and then its exact C6B ancestry from commit-addressed archives. They require each declared commit/tree/subject/direct-parent edge, exact scope, canonical JSONL, didrun entry hash chain, object closure, exact claims, note preview/raw-argv relationship, exact seal, and terminal stable snapshot. Ancestor verification never swaps a live child ledger into place and never runs `didrun verify --strict` against an in-progress child ledger. Historical HTML remains a `LOCAL_SNAPSHOT_NOT_PORTABLE_STRICT_WITNESS` rather than portable Git authority.

U7 final commands run through a private unit root and a fixed `/usr/bin/env -i` recorder/control environment. The admitted local epoch binds Darwin/arm64; the exact Node/Go/Git/shell/gofmt/clang/clang++/env/time entrypoint bytes and reported versions where available; didrun's entrypoint shim, Python launcher, `pyvenv.cfg`, editable route, startup surface, recursive package bytes, repository Git config, and absent replace refs. Admission occurs before version/import probes and repeats terminally. This is local byte authority, not vendor authentication; dynamic libraries, SDKs, stdlib/transitive dependencies, same-user hostile replacement, and unrun platforms remain outside the claim.

The cumulative verifier derives exactly one of `U7P/U7M/U7A/U7B/U7C/U7D` from the visible operational HANDOFF capsule. Its U7 architecture row runs after the opening Finder-artifact guard and before `go list`, build, vet, or tests; malformed, unsupported, or mismatched phase tuples fail before Go execution. U7M is a real zero-governed-product phase rather than an alias for U7P. The terminal U7 architecture self-test reloads the capsule, requires the same phase, reruns the live phase oracle, and exercises the fixed hostile matrix. U7R is deliberately unsupported by this source-candidate roster.

The U7P-frozen study harness directly observes Git/worktree fixture authority, exact tool and executable identities, prepare/compile/execute process topology, timing transcripts, and the closed artifact-byte roster. It derives within- and cross-domain deterministic/fresh digest relations from those observed bytes. Its resource values are only the sum/max of directly observed subject processes, not per-logical-phase or total parent-harness resource measurements; its artifact parser does not independently recompute product semantics. `FULL_STUDY_RESOURCE_BOUND_UNVALIDATED` and the semantic ceiling remain explicit in the U7D/U7R receipt contract.

The cumulative roster has 70 current rows and 15 historical-only rows in the U7M epoch. Three adjacent historical-only rows preserve `tools/check-p07b-c-plan.mjs --self-test`, `tools/check-p07b-c-c3p-receipt.mjs --self-test`, and the ordinary ambient-HEAD `tools/check-p07b-c-c3p-receipt.mjs` entrypoint. The first two fixed historical replays conflict with C6B's sealed post-receipt projection. The ordinary entrypoint runs the full P07 plan but admits only a C6R-parent or C6B-current ambient subject, so it deterministically rejects every honest U7 descendant. U7M does not alter or relabel any of those bytes or outcomes.

Current row `architecture-p07b-c-inherited-compatibility` supplies the enforcement-equivalent descendant path. It brackets stable U7 HEAD/index/worktree/spec/notes authority; reruns current U7 candidate ancestry; binds all 11 visible P07 blocks, four protected P07 paths, and the C6A manifest/receipt/summary/evidence projection to exact sealed C6B; reopens exact C6A Git/note authority; explicitly requires the tracked summary to equal the sealed C6A tree; and runs the unchanged full P07 plan with explicit sealed C6A authority. Nested candidate narration is suppressed. The verifier accepts only the one exact success line, empty stderr, status zero, no signal, and terminally unchanged authority. This is current successor validation, not a historical grade or product receipt.

The first U7A final attempt is permanently unclaimed. On candidate tree `c65753ad3808c71eb8164b0761bf2dcf258226f7`, events 0–7 passed and were narrowly claimed; event 8 ran cumulative verification, rows 1–61 passed, and row 62 exposed the ambient-subject defect before any event 8 claim. Recovery preserved `.didrun-history/u7a-precommit-failed-20260809T181519Z-cumulative-verifier/.didrun` and `.countershape/u7a-final-precommit-failed-20260809T181519Z-cumulative-verifier`. Checkpoint ref `refs/countershape/checkpoints/u7a-candidate-pre-u7m` anchors commit `91a8a74fb4be80582c8fe6f8c09ab705a98a8806`, tree `c65753ad3808c71eb8164b0761bf2dcf258226f7`. After U7M seals, U7A restarts from event zero; none of the failed-attempt events or claims is reusable.

Final evidence rotation is itself machine-gated. Success accepts only the exact committed clean workspace, live-or-restartable normal ledger topology, absent failed archive/final-root/ref state, same-device no-follow renames, and a terminally revalidated commit-addressed ledger; the rotation command is the final command in its fence. Any nonzero permanently routes to the rendered recovery machine. Precommit recovery preserves an optional ledger and the unit final root under a unique UTC/reason slug. Postcommit recovery normalizes live or partially/fully normal-archived evidence into full-commit failed paths, creates an exact failed-attempt ref, and compare-and-swaps HEAD from that commit to its declared parent while preserving the failed tree in the index. These operations establish observed restartable transitions under the sole-writer model, not hostile same-UID containment or power-loss durability.

The generated runbook has six kinds of isolated fence: preparation; each command plus immediate indexed/path-bound claim; exact commit plus mandatory plain seal; deliberate commented `--allow-secrets` checkpoint after inventory review; note inspection; explicit-commit strict plus HTML; and terminal ledger move/closure. The prepare preflight uses the stable private `.countershape` recovery/control environment, never `HOME` or `TMPDIR` beneath the final root whose absence it checks; the unit root is created only after that preflight succeeds. A nonzero command permanently ends that ledger. Any post-commit closure failure is preserved under a failed-attempt ref and archive before the branch is restored to the declared parent; the failed note and ledger stay tied to that ref and are never reused.

The U7 plan and independent scope validators require finite, nonreversed, ledger-ordered, nonoverlapping recorded child-command intervals for every validated U7 session. Current preseal covers the declared command roster; archived U7 ancestry and U7D local evidence also cover the final unclaimed note-inspection event. This establishes only ordering of didrun's recorded wall-clock intervals around its top-level child commands. It does not establish wrapper, claim, Git, seal, recovery, archive, or detached-descendant nonoverlap; clock monotonicity or authenticity; a cross-process recorder lock; or same-UID ABA resistance. Archive enforcement applies only to declared U7 rows and does not retroactively reprofile sealed C6B. Operationally, one root-owned writer owns the whole attempt, and no next fence or mutation begins until yielded wrappers and relevant children are process-terminal.

Development exposed a didrun 0.1.0 cross-process writer defect: two concurrent successful writers received the same event index and broke the chain. The unmodified ledger is retained at `.didrun-history/u7p-development-concurrent-writer-chain-broken-20260808/.didrun`; it is unclaimed and unusable. A later app command cell yielded without returning the still-live long-command session; follow-on development writers overlapped it, and the delayed event recorded tree drift from `4386736aa9eb79f5dd55dae6abc070190adee6ac` to `d76919341fd5027fcfd12e6b7e79a247a39bdff6`. That otherwise linked ledger is also wholly unclaimed and retained at `.didrun-history/u7p-development-concurrent-tree-drift-20260808/.didrun`. Until the recorder itself is repaired in a later boundary, exactly one root-owned didrun writer may run at a time, and a yielded long command remains live until both its wrapper and relevant child process are confirmed terminal. Read-only critics may remain parallel.

The pre-interval-repair cumulative attempt on exact staged tree `036b1c97533bc2de21befba96e00e1892429452b` is preserved at `.didrun-history/u7p-development-cumulative-pre-interval-repair-036b1c97533b/.didrun`. Its cumulative event exited `1` after rows 1–62 of the then-72-current-row roster passed; row 63, `architecture-p07b-c-c3p-receipt-selftest`, exposed the second successor-incompatible entrypoint described above. That event and all earlier development events are unclaimed, nonreusable diagnostics and carry no U7P grade.

The first focused retry is separately preserved at `.didrun-history/u7p-development-red-verifier-selftest-syntax-c904201a7c30/.didrun`: four syntax events passed and the fifth exposed an invalid assignment target in the new verifier hostile. The corrected hostile swaps numeric array indexes; the failed diagnostic ledger remains unclaimed and nonreusable.

The following focused ledger is preserved at `.didrun-history/u7p-development-red-verifier-roster-hostile-order-b277173f233b/.didrun`: 15 events passed and event 15 exposed that the retired-row omission hostile reached adjacency before cardinality. The repaired self-test checks all row cardinalities first; the ledger remains unclaimed and nonreusable.

The repaired exact staged tree `39be44b29e59e045dfb17e8f0e8c7c12b49e483f` then passed a 16-event focused ledger and the complete cumulative verifier. The focused ledger is preserved at `.didrun-history/u7p-development-focused-green-39be44b29e59/.didrun`; the separate one-event cumulative ledger is preserved at `.didrun-history/u7p-development-cumulative-green-39be44b29e59/.didrun` and recorded `RESULT PASS current=71/71 historical_not_run=14`, exit `0`, in `8,205.490` seconds. Both are development-only and carry no U7P grade.

The first generated final preparation attempt created no didrun ledger, event, or claim. Its control environment pointed `HOME` and `TMPDIR` beneath the final root before the absence gate; the admitted Go version probe created `home/Library/Application Support/go`, and the preflight correctly refused the now-present root. The no-ledger residue is preserved at `.countershape/u7p-preflight-bootstrap-failed-home-probe-39be44b29e59`. The renderer now uses the stable `.countershape` recovery/control environment for prepare and self-tests that the forbidden final-root-prefixed prepare command is absent.

The repaired preparation then exposed a second no-ledger bootstrap defect: installing only the private child directories created their intermediate unit root with caller-default mode `0755`, while U7 recovery and evidence closure require the unit root itself to be exact mode `0700`. That unchanged residue is preserved at `.countershape/u7p-prepare-parent-mode-failed-9fe8c61cb68f`. The renderer now installs the unit root explicitly before its six private children, self-tests the exact command, and the archive/rotation preflight independently refuses any final root that is not a current-uid-owned, no-follow, exact-mode-`0700` directory.

The first focused rerun under the runbook's required caller `umask 077` then exposed that the study-harness self-test requested synthetic driver mode `0644` only at creation time; POSIX correctly produced `0600`, and the harness refused with `U7_STUDY_HARNESS_DRIVER_MODE`. The 13-event red ledger is preserved unclaimed at `.didrun-history/u7p-development-focused-red-caller-mode-driver-acadee5b3c99/.didrun`. Synthetic driver creation now normalizes the newly created files to exact `0644` before the existing wrong-mode hostile deliberately returns one to `0600`; caller `077` and the checker remain unchanged.

The next focused run passed events 0–16 and stopped at the credential scan because the former archive basename's adjacent characters matched the intentionally fail-closed OpenAI-key pattern. Its terminal ledger is preserved at `.didrun-history/u7p-development-focused-red-credential-doc-path-2e4347e68f3c/.didrun`; the earlier archive was renamed without byte or inode change to the caller-mode spelling above, and the scanner is unchanged. The same red-team pass found that private-directory admission compared only the low three octal mode digits. U7 now rejects sticky, setgid, and setuid variants by comparing `mode & 07777` to exact `0700`, with direct hostile cases for `0755`, `01700`, `02700`, and `04700` and one exact positive.

From a fresh shell at the repository root, run:

```sh
/opt/homebrew/bin/node tools/verify-current.mjs
```

This is the single cumulative baseline for the current Darwin/arm64 reference work. It exits nonzero on the first failed row and prints `RESULT PASS` only after all rows pass. Root authority is explicit: current product and governance checks are `LIVE`, while a row labeled `SEALED_C6A` is a bounded replay of that exact sealed source authority rather than a claim about descendant governance bytes. In an evidence-producing unit, wrap that exact command with `didrun run --`, claim the successful event immediately, and commit. In this managed environment, seal only with Git-note write authority, confirm `git notes --ref=didrun show HEAD` succeeds, and then require `NO_COLOR=1 didrun verify --strict` to exit `0`. A direct run is useful local evidence, but it has no didrun grade.

## C6R sealed C6N note-consumer compatibility maintenance

The current machine authority is `countershape/p07b-c-unit-paths/v30`, with 44 ordered unit rows and 32 receipt-phase rows at digest `sha256:cd7bd450bec6d891ea66dd48408788d38aa7bbcee778912eabce7f45e8538f3e`. `C6A → C6F → C6G → C6M → C6N → C6R → C6B` is the exact live tail. C6R is the active seven-path `SOURCE_FULL` repair after sealed C6N commit `096842954c9e6ae72b66010fff81e90cf0bd7a22`, tree `69845d09f85abf76b275b16c0d941fbd568ee907`, note blob `b66c7b38bad6c8ea94288f2cd8afa87e76f0c831`, and note-body SHA-256 `68abfb23fe35e2d2844e0a1622f7c4566f08789a29d16dd170c02b0e7046ef11`. Its roster digest is `sha256:3f815ac91f817fba618f24d307919dd9e97aec0f142a5cee1c9e86b284e06169`. Receipt C6A remains `ABSENT`; the canonical source-authority manifest is inherited byte-for-byte and no receipt declaration or visible receipt projection is introduced.

The frozen C6N source-era projection remains `sha256:500955d69b63da43516312788befa6642ecbdcb6e7777ac014f5beeeb5beb41b` under `LEGACY_DOUBLE`; it is historical authored authority, not an observation of the later note. The separately named observed projection of exact sealed C6N is `sha256:cc851cc157036b09ae84739220db65bdc6593d0f78644d5a61fe789d6a93ffc4` under `SUFFIX_PRESERVING`. C6R's prospective contract is `sha256:bc4dffd3b0a393eefbb7d3947a1ad9ca7a391a7a2a332113a893e0682a88cfde`. Both independent validators pin the sealed C6N identity before accepting raw exact or suffix-preserving previews and reject the frozen prospective form, a bare marker, foreign suffix, positional drift, or note-identity drift. This is a descendant-consumer correction; it neither amends C6N nor recovers hidden values.

The C6A/C6F/C6G/C6M/C6N/C6R plan self-test, the machine-readable v30 staged-unit-scope self-test, and the generated source runbook all derive the new edge independently. The C6R final ledger proves exact ancestry with `--verify-c6r-sealed-c6n-note`, runs the unchanged cumulative verifier plus sealed-C6A replay, binds exact staged scope, and closes with `--verify-c6r-preseal-ledger`. All ten C6R grades remain `UNRECEIPTED` until the exact tree commits, seals, carries a readable note, and passes strict verification; C6B remains blocked behind that boundary.

## C6N sealed C6M note-redaction compatibility maintenance

Historical sealed snapshot: this v29 section preserves C6N's source-authored contract and pre-seal evidence narrative. Exact C6N is sealed; its old active tense does not supersede the C6R section above.

The source-era machine authority was `countershape/p07b-c-unit-paths/v29`, with 43 ordered unit rows and 31 receipt-phase rows at digest `sha256:30ac9d08842915a5f3dfe5aca79c7fa94b02add8dc9785226ca1741411b66bb5`. `C6A → C6F → C6G → C6M → C6N → C6B` was that exact live tail. C6N was the active nine-path `SOURCE_FULL` repair after sealed C6M commit `b0de83dca3510102af4d1cddae2b2023bc88a87d`, tree `3d80a02443209f5d5649fcba92b79b72a2a04a15`, note blob `d08739e2f5bb3d761d5962081242924ae9cb7f36`, and note-body SHA-256 `1bcf1b4563561d6a07db9394bb6b25b2e7d2ca126df18b98f0c8596cc960a487`. Its roster digest is `sha256:012c7559efd28dce5dac7fb56881bbddd8f326f89eac4d32a6773ad8cfe856af`. Receipt C6A remains `ABSENT`; the canonical source-authority manifest is inherited byte-for-byte and no receipt declaration or visible receipt projection is introduced.

Both independent validators select one exact GOCACHE preview policy from the boundary contract. C6A/C6F/C6G remain `SUFFIX_PRESERVING`; exact sealed C6M and prospective C6N use `LEGACY_DOUBLE`. Raw exact previews remain admitted, either redacted form outside its named epoch is refused, and hidden bytes are not attested. The pinned sealed-source contract digests are `sha256:a7f3696229c63afe11911980d7b21873b21d5c06e50207f8d0883b2379e707ea` for C6M and `sha256:500955d69b63da43516312788befa6642ecbdcb6e7777ac014f5beeeb5beb41b` for C6N.

The first C6N final attempt is permanently unclaimed at `.didrun-history/p07b-c-c6n-final-attempt-1-sandbox-git-lock/.didrun/`; the restarted app sandbox denied `.git/index.lock` during command 1. The second is permanently retained at `.didrun-history/p07b-c-c6n-final-attempt-2-c6-hash-object-timeout/.didrun/`: commands 1–6 passed and were claimed, but unclaimed cumulative command 7 failed at row 57 when one per-blob `git hash-object --stdin` child reached 60 seconds. Neither attempt supports a C6N grade. The focused repair ledger is `.didrun-history/p07b-c-c6n-batch-repair-diagnostic-2dd3ee57e772/.didrun/`; its last two events exercise the exact real `--c6` and `--c6-selftest` rows successfully, but remain unclaimed development evidence.

The complete post-power-loss cumulative restart is separately retained at `.didrun-history/p07b-c-c6n-development-red-plan-selftest-timeout-0f2ce2b787a9/.didrun/`. It has 39 events on tree `0f2ce2b787a92c849a38d0f76b3cc2c6b64cad10`: rows 1–58 passed, while event 38 and row 59, `architecture-p07b-c-plan-selftest`, exited `1` after `1,200,016` ms with `spawnSync ... ETIMEDOUT` at the unchanged `1,200,000` ms child ceiling. The interrupted run preceding it had no terminal didrun event and is not evidence. The red restart supports no claim or partial cumulative result; rows 60–65 and all 12 historical rows were not run.

The repaired plan self-test keeps the 65-row cumulative roster, every checker case and console contract, and the `1,200,000` ms outer ceiling unchanged. Its first default plan check remains an ordinary uncached baseline. Only after that baseline passes does one invocation-scoped authority snapshot reuse immutable C0/C1/C2/C3P/C3 and C3R/C3Q/C3T/C3U Git/note results plus active C6 authority, guarded by exact canonical root, active boundary, source bytes, Git and Node executable identities, `HEAD`, index tree, and notes tree; configured Git must resolve to the `/usr/bin/git` used by sealed-C6 derivation. Explicit injections win; strict-index, byte/root/boundary mismatch, absence, or read failure takes the pre-existing validation path. A terminal full recapture detects persistent drift even when the body fails. Qualification analysis parses the same three modules and exact entry fixture once per exact ordered path-to-byte set and Node identity, checks full input equality after its typed digest lookup, and caches successes only. The wrapper is intentionally limited to the current C6M/C6N/C6B horizon; earlier phases keep the ordinary uncached path. There is no retry, deadline increase, case removal, case sharding, claim change, receipt change, or verifier-row change.

The first repaired diagnostic ledger is `.didrun-history/p07b-c-c6n-plan-throughput-green-26a91ed4bb6c/.didrun/`. Its event 2 completed the unchanged plan self-test on staged tree `26a91ed4bb6ca1a13b75d7934975f7e6e75a4859` in `164.872573` seconds with exit `0`, empty stderr, and the exact unchanged C3P, C3, and evolved-plan completion markers. That is approximately 86.3% below the 1,200-second outer ceiling. It remains unclaimed development evidence because adding this measured result changes the documentation tree; the final-tree plan row and complete cumulative battery must still run from command entry.

The later seven-event ledger `.didrun-history/p07b-c-c6n-development-red-bare-sealed-c6-tool-authority-c804f6ed8a00/.didrun/` is also permanently unclaimed. Its first six focused gates passed on staged tree `c804f6ed8a003a097499b3ed6295a9187fc39e45`; event 6 failed immediately with `SEALED_C6A_TOOL_AUTHORITY: COUNTERSHAPE_NODE` because the operator invoked the sealed runner bare, outside the cumulative verifier's required hermetic tool/root environment. Do not synthesize that environment manually and do not reuse the six green events. The corrected final-tree development pass must run `tools/verify-current.mjs` from command entry; that existing verifier constructs the qualified environment and exercises both real sealed-C6A rows.

The sealed replay runner now materializes the exact 567-entry tree using one bounded `git cat-file --batch` operation and proves Git parity with one bounded absolute-path `git hash-object --stdin-paths --no-filters` operation. The extraction parser requires exact ordered OID/type/size/binary framing, recomputes every blob SHA-1 in process, and enforces the existing 1,024-entry, 32 MiB-per-blob, and 256 MiB-total ceilings. The path-hash request is capped at 8 MiB; output is exactly one lowercase 40-hex-plus-LF record per entry, with no missing, extra, CR, uppercase, nonhex, reorder, or substitution. Snapshot inspection brackets Git parity and repeats after the frozen child. The unchanged 60-second per-Git bound remains; no retry or deadline extension exists. Status-zero stderr is admitted only through the existing 16 KiB, fatal-UTF-8, LF-only, three-form Darwin grammar; every other result or byte fails closed.

The C6N final ledger proves exact sealed C6M plus C6G/C6F/C6A ancestry with `--verify-c6n-sealed-c6m-note` and closes with `--verify-c6n-preseal-ledger`. Its full ten-command `SOURCE_FULL` structure remains unchanged: command 6 now explicitly claims the cumulative verifier plus sealed-replay runner self-test, command 7 claims cumulative verification with both real sealed-C6A replay rows, and command 8 binds exact nine-path scope. This repair narrows acceptance by explicit note epoch and exact batch framing; it does not reuse C6M's or C6A's grade. C6B may begin only after C6N seals, with preseal parent HEAD=C6N or sealed-current HEAD=C6B.

## C6G C3U historical future-C6 fixture maintenance

The heading above is frozen v28 source-era compatibility text. C6A → C6F → C6G → C6M → C6B was that source-era exact tail. C6A is sealed at commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`, with 11/11 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`. C6F is sealed at commit `dd2a011edff20d65c43934fc7c3e5a09b5829d2e`, tree `b0141ac2671df75e51c4fdb5b71c35cee5d0f4ea`, note blob `677e7dda3fc445bda07a7b473b0a40b9a18cea00`, with 10/10 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`. C6G then sealed as the seven-path `SOURCE_FULL` predecessor at commit `3677b45d75204ce2ef7242abb5edd11cc049934e`, tree `520ad2cbf2d3da8354b02afd1cd2a7cd5df82d7d`, note blob `df26876b86d89689d9125531a816b92b7db97afb`, and note-body SHA-256 `2d781bd6373dc0d91b0683a5a0e046ada58522451e0d9f58a71d4df1eb115bc9`, with 10/10 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`. C6M later sealed as its exact-five-path `SOURCE_FULL` data-only child. Receipt C6A remains `ABSENT`: C6M adds only the manifest without projecting a C6A receipt.

The current `countershape/p07b-c-unit-paths/v28` authority has 42 ordered unit rows and 30 receipt-phase rows at digest `sha256:f48f7eafe266fad4a6908bb9319f6adb9739f95a32276a7325c9ac28d7ab8e95`. The plan transition oracle admits 18 exact edges and rejects 610 generated invalid snapshots; the separately implemented scope oracle admits 18 and rejects 597. The generic phase catalog has 1,470 cases at `7ec1d04471f70e13b5082af04a78af2b6d09f35df65ea0fd2f5a00138b4e9b72`, split 900 accepted and 570 rejected; six fixed semantic regressions extend it to 1,476 cases at `5da15bb451143ce4d4c185bb9aa9777218e6a91b26c04870f1b01b8a803e3466`, split 906 accepted and 570 rejected.

The reconciled C6M development plan self-test exposed a real outer-driver gap. `syntheticC3AbsentPlanFixture()` reconstructed historical C3U state, then spread ambient `...current` bytes, allowing all four later-C6 authority paths to reopen. C6G applies the existing explicit absence projection after that spread, validates it before use, and preserves the four-path authority digest `36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218` plus its 11-case helper digest `983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`.

The ordinary real-renderer path executes a nine-case oracle: one passing baseline, four single-path omissions, and four reopened descendant values. All eight hostiles must refuse, and the ordered oracle digest is `c591e1874545674f0579db04d731ce5c366c408e99d3cd27eb63ffbef3ad4bf1`. Sealed C6F remains valid because its historical-fixture claim was explicitly bounded to C3Q installation and declared all other historical `checkPlan()` inputs outside its claim; C6G supersedes only that ambient-input limitation.

Both generic checkers independently froze the C6G subject, ten labels/types, command tails, final-root identity, and redaction positions at `sha256:3dfffad546f7fee36e2e51856cea98a754626f395f319b0c6264fe918df3ef00`. `--verify-c6g-sealed-c6f-note` reopened exact C6F and then exact C6A ancestry; `--verify-c6g-preseal-ledger` required the ten-event ledger, exact staged tree, absent C6A manifest/receipt, and unchanged preceding chain. The source-era commands did not receipt C6G before its own commit and seal; its strict-clean identity is recorded above.

## C6F historical C6 authority-fixture maintenance — sealed v27 snapshot

Under the sealed snapshot, `C6A → C6F → C6G → C6M → C6B` is the now-extended exact tail. C6A is sealed at commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`, with 11/11 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`. C6F was its twelve-path `SOURCE_FULL` child. Receipt C6A remains `ABSENT`: C6F contained neither the source-authority manifest nor receipt declaration and projected no C6A receipt.

The sealed `countershape/p07b-c-unit-paths/v27` authority had 41 ordered unit rows and 29 receipt-phase rows at digest `sha256:ba3e4836c50f6a4d7f4f5ef8563ba8fb8e1b0ab6d602bab6017656daa2d57faa`. The plan transition oracle admitted 17 exact edges and rejected 560 generated invalid snapshots; the separately implemented scope oracle admitted 17 and rejected 547. The generic phase catalog had 1,392 cases at `d0dfd9f73fc9b9fa3f834d018460cbeeeeb5ee41139cb67c085e7c1d28b3257e`, split 841 accepted and 551 rejected; six fixed semantic regressions extended it to 1,398 cases at `840e761790f7377bb92c1074c89a5c902cf7ed7b2add8ba62688e2f1cc6c7a4c`, split 847 accepted and 551 rejected.

C6F repairs one ambient-state seam in the fixed `C3Q_EXTERNAL_SELF_RECEIPT` replay. That C3B-era fixture previously replaced only HANDOFF while later C6 paths could fall through to the descendant worktree. The repair makes it an explicit-total historical fixture over exactly four ordered paths: `docs/captures/p07b-c/c6a-evidence-summary.json`, `docs/status/P07B-C-C6-EVIDENCE.md`, `spec/verification/p07b-c-c6a-receipt.json`, and `spec/verification/p07b-c-c6a-source-authority.json`. Their ordered JSON-lines digest is `36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218`; every value must be the exact absence sentinel. An 11-case defensive manifest—baseline, four omissions, four non-sentinel substitutions, one foreign path, and one ordering mutation—is pinned at `983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`. The C3Q integration additionally requires exact HANDOFF-plus-four installation before invoking historical `checkPlan()`, so removing the spread cannot leave only the isolated helper test green. The v27 catalog expands for C6F and this oracle without removing any prior hostile.

The first repaired cumulative pass exposed a second ambient-phase seam after product rows passed: live B called the C checker with no phase argument, so the frozen C6A contract read C6F's descendant HANDOFF and refused it. Editing the frozen checker would rewrite sealed evidence. C6F therefore changes live B to exact `--c1`, preserving B's current-tree scan, and routes exactly `architecture-p07b-c-c6` plus `architecture-p07b-c-c6-selftest` through `tools/check-sealed-c6a-architecture.mjs` with `root_authority=SEALED_C6A`. Every Go, B, C1–C5, plan, scope, receipt, and terminal row remains live.

The sealed runner pins C6A commit/tree, notes-ref commit `ba1f89377012e444d30b0520e297241eabacfb48`, and the 567-entry raw tree manifest `sha256:a6c028ab3302ba215d16c90f3a27cb0583e631584382f7a70bfaed59de8cfe52`. It reads literal blobs, checks every OID and exact `0644`/`0755` mode, rejects unsafe/colliding paths and object types, constructs a private detached Git control directory with a pinned notes ref and content-addressed object alternate, checks the materialization again after the child, and cleans it after every handled outcome. It never invokes `git checkout` or `git archive`, registers a linked worktree, or applies attributes or filters. It does create a private non-bare working directory; abrupt process termination or a failure before cleanup protection is established may leave that root behind for operator removal. Those two markers prove sealed-C6A compatibility under the current admitted toolchain; they do not prove current C6F governance-byte conformance.

Because C6F edits the phase specification, both generic checkers, B's inheritance seam, and the cumulative verifier, it cannot consume the narrower non-product profile. Its exact final contract runs the full 65-row verifier under the twelve-path `SOURCE_FULL` claim ceiling: 63 live rows plus exactly two labeled sealed replays. `--verify-c6f-sealed-c6a-note` reopens C6A's immutable commit/tree/parent/subject, exact diff, eleven-claim Git note, and hermetic argv previews. Before C6F seals, both checkers independently pin the future sealed-C6F subject, ten labels/types, ten command tails, final-root identity, and redaction positions at `sha256:84227e43b4534cc6977d5b5d274ee415f118af98c36e3e390c86d585d11aa5fb`; each passes a synthetic C6F note through its own generic validator and rejects tail drift. `--verify-c6f-preseal-ledger` then requires the exact ten-event ledger, exact staged tree, absent C6A manifest/receipt, and unchanged preceding chain. C6F proves the named fixture authority, checker-contract parity, live current-tree verification plus explicitly labeled frozen replay; it changes no product or C6A evidence byte and cannot relabel the replay or issue a C6A receipt.

The first C6F final ledger passed and claimed commands 1–7, including the complete 8,993,502 ms cumulative verifier, then stopped at unclaimed command 8 because its first Git inventory exited zero with 344 stderr bytes from exact Apple FSEvents and `DARWIN_USER_CACHE_DIR` fallback diagnostics. The permanent eight-event attempt is `.didrun-history/p07b-c-c6f-final-f6c30a8f641a/.didrun/` and supports no C6F grade. The repaired scope checker admits stderr only if it is empty or at most 16 KiB, has no leading UTF-8 BOM, is otherwise fatal UTF-8, has final-LF/no-CR framing, and has every line matching one of three exact Darwin forms already pinned by the C6 evidence reader. Any BOM, foreign line, altered framing, invalid encoding, over-bound payload, nonzero exit, signal, or spawn error still fails. The self-test crosses four accepted diagnostic layouts with BOM, missing-LF, CRLF, foreign-line, malformed-timestamp, over-bound, raw-byte, result-status, signal, and spawn-error refusals; this is bounded host-noise classification, not general stderr suppression. C6F later closed through a separate repaired ledger.

## C5V verifier-preflight hygiene

C5V is the now-sealed nine-path SOURCE_FULL repair after exact sealed C5 commit `240060f018d5b0e86914bd27361c5899ac9ba1c0`, tree `4906ff4af407fa4e48cbf569f7e456694660ca06`. Exact C5V is commit `e7f51c0a8fbd2d6fbe8eacb4a7f0d46f010af7ba`, tree `65bdfc39b48c3b9b1b832a12cc9be5e0083ea5c4`, with 12/12 `TREE-EXACT`, strict exit `0`, and recorded `secrets_override: true`. That flag discloses only that didrun produced a redacted export after its aggregate entropy gate was explicitly overridden; it is not a secret-absence, confidentiality, publication-authorization, or security-review claim. C5V's pre-seal contract changed generic final-runbook, phase-checker, cumulative-verifier, self-test, specification, and documentation authority, so it completed the full battery and three final cumulative passes before sealing.

Both generated preflight stages require any visible `.countershape/verify-current` parent to be a canonical, current-uid-owned, exact-mode-`0700` real directory, require its `active.lock` to be absent with no-follow `lstat` semantics, and reject every `.DS_Store` outside the five root-only private trees `.git`, `.didrun`, `.didrun-history`, `.countershape`, and `node_modules`. Before a final ledger mutates evidence, preparation additionally requires live `.didrun` and the exact boundary final root to be absent. Before archival, the separate preflight requires live `.didrun` and every final directory to be canonical private directories and requires only the exact commit-addressed archive root to be absent. A regular file, wrong object type/owner/mode, symlink, dangling symlink, or intermediate alias refuses where the corresponding contract requires absence or a real directory. The generator never deletes or rotates residue.

The cumulative roster performs the same bounded `.DS_Store` scan twice: at row one and again immediately before authority revalidation and resource finalization. Traversal applies `lstat` to each visible entry, never follows directory symlinks, detects the forbidden basename regardless of entry type, and treats nested directories named like an ignored root as ordinary scanned paths. The terminal row closes the observed C5 late-mutation gap; the generated preflight closes the C5 shutdown-left lock gap.

These are point-in-time observations under sole-writer discipline, not hostile same-UID isolation. On `active.lock` refusal, stop the ledger and inspect the no-follow object type first. Only a valid regular lock carries a PID that can be checked against process liveness and the current file holder; a malformed regular file, directory, symlink, or dangling symlink has no trusted PID. Remove only that reviewed lock object after confirming no live verifier owns it, and preserve all `run-*` residue, failed ledgers, and archives. On `.DS_Store` refusal, stop and remove only the named visible Finder artifact rather than weakening the scan or touching ignored/private evidence.

## C6A cumulative evidence and expert-surface gate

C6F descendant correction: the next two paragraphs preserve sealed-C6A roster semantics for historical replay. They do not describe the sealed C6F verifier routing above; live B now uses explicit `--c1`, and the exact two C6 rows are labeled `SEALED_C6A`.

C6A is the sealed `SOURCE_FULL` child of exact sealed C5V. Exact C6A is commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`; its note blob is `b110b998a56c89d77bec3f87a48f64f1f21a700e`, with note-body SHA-256 `096e9ae74e9a9ccfb984018f792a59bac3eb8ae24c550d83bb273e6b5ff064c7`. The current verifier adds two contiguous static rows after the seven C5 rows and before the plan/scope/receipt tail: explicit `--c6` architecture conformance and its explicit `--c6` defensive self-test. Both historical C1 rows now use `--c1`. Exactly two intentional callers use the no-argument checker: the sealed-B compatibility edge and C6A's frozen final-runbook architecture command. It executes the cumulative nonrecursive C6 implementation while emitting the exact legacy C1 marker; every other current cumulative-verifier architecture row uses an explicit mode. C6 never invokes B, so the inherited `B -> C(no args)` edge terminates.

The current roster is exactly 65 stages at roster digest `4d253e4baa8650cfd27e7655cbccd4a9b959d25e26eb5e47810469960eb670f6`. C6 adds no dynamic profile row to the cumulative verifier. The expensive exact evidence rerun remains a separate explicit C6A final-ledger command, so the static architecture baseline does not silently execute it twice.

The evidence entrypoint has four modes:

- `--write` reopens exact sealed C5V authority, reruns selected physical profiles, binds the fixed source roster, and transactionally replaces exactly five tracked artifacts;
- `--verify` rebuilds that authority and requires byte-equivalent tracked canonical evidence;
- `--self-test` exercises closed JSON/rendering rules, no-follow ancestry, transaction rollback/commit, and exact file boundaries; and
- `--render 60|80|120` prints only the tracked canonical diagnostic projection for the named width.

The selected `c5-cross-profile-parity` and `c5-http-behavior` transcripts contain exactly 26 top-level tests: CLI 4, HTTP 5 across the selected profiles, and generated Node 17. The Go-JSON parser consumes raw `Buffer` bytes, requires fatal UTF-8 plus exact LF framing, and enforces admitted package/test lifecycle order, exact tests, one terminal package pass, zero skips, and no post-terminal events. Slash-bearing test names are opaque Go identities: because one `t.Run` name may itself contain slashes without separate intermediate lifecycle events and Go-JSON carries no parent identifier, every nested identity must be enclosed by its exact admitted top-level root lifecycle, but textual segments are never invented as parents. The defensive clean control contains a two-segment slash-bearing child with no intermediate lifecycle; hostiles retain child-before-root, child-across-root-terminal, child-after-root-terminal, nested skip, post-terminal output, and unmatched pause refusal. Prefix-colliding intermediate relationships are not proved beyond that root enclosure. A pure shared authority module fixes their complete argv/package/test descriptors; same-count name substitution is invalid. The evidence layer additionally pins C5V's exact commit/tree/parent/subject/note, HTML digest/size, complete ledger manifest, core files, event/claim/seal semantics, and exact referenced-object closure. Its source closure includes Go compiled/test/embed members, the runtime-read choicepoint example and parity corpus, exact evidence/runtime modules, and required `go.sum` absence. Git is fixed to `/usr/bin/git`, `--no-replace-objects`, and a sterile environment.

The tracked evidence schema is `countershape/p07b-c-c6a-expert-evidence/v4`; the compact receipt-consumer summary remains `countershape/p07b-c-c6a-evidence-summary/v1`; the terminal grammar is `countershape/p07b-c-c6a-terminal-evidence/v2`. Expert v4 places an early closed `artifact_state` object with boundary `C6A`, document epoch `C6A_SOURCE_CANDIDATE`, receipt `ABSENT`, and state `UNRECEIPTED`; the terminal banner derives from that source-era object. Every exact argv profile also carries closed replay policy: `copy_paste_safe=false`, `standalone_argv=false`, and `requires_environment_contract=true`. The summary preserves the already-predeclared C6B six-key `{id,status}` consumer shape while its existing bounded note begins `C6A source-era state is UNRECEIPTED` as a compatibility disclosure, not lifecycle authority. A seventh summary root key is declined because it would require changing parent-owned C6B machinery outside every remaining unit's authority. Rich didrun finding details are carried only by expert evidence. The five-file capture root contains those two canonical JSON documents and ASCII-only diagnostics at exact 60, 80, and 120 column ceilings. The evidence admission digest binds the exact sealed-parent descriptor, admitted/revalidated front-door execution authority, pure exact profile descriptors, the frozen selected-profile present/absent source-closure descriptor, and every measured source byte descriptor. Each profile record supplies the admitted executable, exact argv and invocation digests, repository-root working-directory contract, and sterile fixed/private environment contract needed to replay its bounded command. “Execution authority” is deliberately limited to measured front-door executables and declared Go environment; it does not bind the complete Go distribution, standard library, compiler/linker closure, macOS SDK, dynamic libraries, or host provenance.

Capture publication uses a v3 four-state journal with exact old/new generation manifests and device/inode identities, including immediate identity checks on both sides of activation rename. Cleanup first quarantines only a journal-owned generation by rename and parent-directory sync; if cleanup is interrupted after deleting some files, recovery can continue only from the same journal-bound quarantine root identity. The defensive self-test injects second interruptions during stage and activated-generation cleanup both with and without a prior generation, and during committed-backup cleanup. Foreign or unjournaled residue and any root-identity drift refuse. A crash before the first durable journal or during its temporary-file replacement is a fail-closed operator-recovery case rather than an inferred state. These are sole-writer local-filesystem recovery guarantees, not hostile same-UID race resistance, secure deletion, or universal storage durability.

One exclusive writer lock and canonical four-state journal (`prepared`, `backed_up`, `activated`, `committed`) protect the five-file generation. The writer syncs the staged files and directory, rechecks the entire admission snapshot before activation, validates the active generation before durably recording `committed`, and syncs every directory transition. Recovery restores the prior generation for a pre-commit interruption, removes an uncommitted first generation, preserves a validated committed generation across cleanup failure, and rejects unjournaled or ambiguous residue. Every ancestor is no-follow and confined to the physical repository root. These are crash-recovery, correctness, and DX controls under sole-writer filesystem assumptions—not hostile same-user containment, universal power-loss durability, or security certification.

C6A development requires at least three genuine build/see/exercise/critique/rebuild passes over both JSON documents and all three text widths. Eight substantive passes completed before the final source freeze. The terminal projection now leads with its unreceipted boundary, open findings, retained negative, unavailable private evidence, and nonclaims before favorable profile detail; it deliberately omits shell-looking exact argv and points to machine-readable expert JSON instead. Each pass records its inspected bytes and resulting repair/disposition in `docs/status/P07B-C-C6-EVIDENCE.md`; unit tests are not a substitute. Fresh blind Terra-family critics received only sanitized tracked captures and a bounded rubric. After earlier reviews reached `0 High / 0 Medium / 0 Low`, successive source-frozen repeats exposed missing structured nonproof state, late state placement, and absent machine-readable replay safety. Expert v4 and the schema-compatible v1 summary-note disclosure repair the in-scope defects; the seventh summary root key is explicitly declined with reason. Every qualitative judgment and severity remains `UNRECEIPTED`, while each resulting source change passed the deterministic evidence self-test and regenerated the five-file generation. Because this documentation is itself an admitted source input, the freeze protocol reruns generation, all three render entrypoints, static width/schema/parity checks, and one fresh sanitized critic after the last edit, then avoids a self-invalidating source edit merely to narrate that final repeat.

The C6A final ledger is the frozen 11-command sequence generated by `--print-final-runbook C6A`: eight `tests-pass` claims, then exact staged-scope, credential-scan, and preceding-chain `command-succeeded` claims. It ran one cumulative verifier plus the separate evidence self-test and exact evidence verification. Two complete post-repair development passes preceded the final ledger; the sealed final result records all eleven grades `TREE-EXACT`. C6F, C6G, C6M, and C6N are sealed descendants; C6R is the active note-consumer repair before the later C6B receipt reconciliation. C6A contains neither receipt projection nor self-grade.

## C6M sealed-source authority adapter

C6M is the sealed exact-five-path `SOURCE_FULL` data-only source-authority boundary at commit `b0de83dca3510102af4d1cddae2b2023bc88a87d`, tree `3d80a02443209f5d5649fcba92b79b72a2a04a15`, note blob `d08739e2f5bb3d761d5962081242924ae9cb7f36`, and note-body SHA-256 `1bcf1b4563561d6a07db9394bb6b25b2e7d2ca126df18b98f0c8596cc960a487`, with 10/10 `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. Its direct parent is exact sealed C6G commit `3677b45d75204ce2ef7242abb5edd11cc049934e`, tree `520ad2cbf2d3da8354b02afd1cd2a7cd5df82d7d`; sealed C6F remains beneath C6G as a second transparent predecessor for the C6A source authority transported here, not a source receipt. C6M's roster digest is `sha256:5501a7e8c2d8f6d44392d101110edafe90a0348ef77f5ea24539f6a8ba969803`: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C6M-RECEIPT-ADAPTER-MAINTENANCE.md`, and `spec/verification/p07b-c-c6a-source-authority.json`. It owns neither checker, verifier, phase specification, product source, C6 evidence, C6B prompt, nor receipt declaration. Its exact subject is `fix: bind sealed C6A source authority`; the active cursor is now C6R.

The canonical manifest uses schema `countershape/p07b-c-c6a-source-authority/v1` and binds sealed C6A commit `3e9643f657248e0d5ff2c4bf0880218efbaeecd8`, tree `226a1e23da7eded933b3faabd4e8040576b2a2e0`, single parent `e7f51c0a8fbd2d6fbe8eacb4a7f0d46f010af7ba`, subject `test: close P07B-C cumulative evidence`, didrun note blob `b110b998a56c89d77bec3f87a48f64f1f21a700e`, note-body SHA-256 `096e9ae74e9a9ccfb984018f792a59bac3eb8ae24c550d83bb273e6b5ff064c7`, disclosed `secrets_override: true`, and all eleven source claim labels/types at `TREE-EXACT`. The plan and scope validators independently reopen those Git/note bytes, exact source diff, complete claim coverage, and position-specific redaction-tolerant hermetic argv previews before accepting the byte-canonical JSON.

That manifest is portable source authority, not a C6A receipt. C6M kept `spec/verification/p07b-c-c6a-receipt.json` absent, kept the visible C6A receipt state `ABSENT`, and added no `P07B-C-C6A-SOURCE-RECEIPTS` projection to HANDOFF or the inherited C6 evidence. C6N inherits the manifest byte-for-byte; C6B alone may later add its exact receipt declaration and project the source receipts. C6M's sealed ten-command ledger contains seven `tests-pass` claims followed by exact five-path staged scope, credential scan, and `P07B-C C6M sealed-C6G predecessor, sealed-C6F and sealed-C6A ancestry, and preceding didrun chain integrity` as the final `command-succeeded` claim. No C6A, C6F, or C6G event or grade was reused as C6M evidence.

## What the command runs

The runner executes, in order:

1. a repository guard that rejects any `.DS_Store` outside private evidence/cache directories;
2. admitted `go list ./...` exact-union partitioning, direct one-job `go build`/`go vet`, the complete general package complement at `-p=1`, the inherited-nine and C5-three whole sensitive package groups in two adjacent serial rows at `-p=1`, and exact package-closure revalidation after all three lanes;
3. the verification-runner and focused Go repetition-runner hostile self-tests;
4. the U5, U6/P07A-B, and P07B-A1 architecture checkers and hostile self-tests;
5. exact regeneration checks for the runtime bundle and planning examples plus the planning-validator hostile self-test, including real conforming execution, pre-import companion-tamper refusal, and eight byte-exact schema-valid/runtime-invalid cases crossed through the Go semantic parser;
6. fresh-process compiler-to-parser recovery;
7. the deterministic human-surface renderer self-test and exact six-scenario capture check;
8. the P07B-A2.2 layered architecture checker and its 117-case hostile self-test;
9. the P07B-B terminal-publication/materialization architecture checker and its defensive self-test;
10. the explicit `--c1` P07B-C C1 inert-model architecture checker and self-test, including exact production/test files, imports, top-level test/fuzz symbols, required rosters, and Go-JSON positive-execution parsing;
11. the P07B-C C2 cumulative architecture checker and its frozen 46-case hostile self-test, followed by the exact 6-test typed-nonhead, 7-test interlock/StartClaim, 6-test private-evidence, and 2-test exported-surface Go-JSON profiles;
12. the P07B-C C3 cumulative architecture checker and defensive self-test, followed by the exact 10-test official-target, 7-test single-target Git, 8-test host-epoch, 12-test Node-runtime, and 6-test store-bridge Go-JSON profiles;
13. the P07B-C C4 cumulative architecture checker and hostile self-test, followed by six exact Go-JSON profiles: process-mechanics parity `12`, admission/permit `7`, CLI closure `4`, finalized-run release `5`, classification recovery `2`, and authority race `4`, all with zero skips;
14. the P07B-C C5 cumulative architecture checker and its `14`-metadata/`60`-Go-JSON/`6`-command defensive self-test, followed by five exact Go-JSON profiles with counts `4/9/9/22/5`, zero skips, and `-race` only on the last profile;
15. the explicit `--c6` cumulative C6 architecture checker and its defensive self-test, covering inherited C1/C5 closure, exact evidence/summary/artifact/source rosters, documentation anchors, tool topology, and the frozen eleven-claim map;
16. the evolved C0-receipt/C1/C2/C3P/C3V/C3M/C3PB/C3A/C3L/C3F/C3S/C3/C3R/C3Q/C3T/C3U/C3B/C3D/C4V/C4M/C4N/C4P/C4/C4H/C4I/C4K/C4J/C4L/C5/C5V/C6A/C6F/C6G/C6M/C6N/C6R plan self-test, including the frozen `C6A/C6F/C6G/C6M/C6N plan self-test` projection, the machine-readable v30 staged-unit-scope self-test with its frozen `machine-readable v29 staged-unit-scope self-test` authority, and the dedicated C3P-receipt/C3S/C3/checker-succession coherence gate plus its hostile self-test; and
17. terminal no-follow Finder-artifact revalidation, final revalidation of every admitted tool authority, and one current finalization row that removes the private run root and releases the exact verifier lock before terminal `RESULT PASS` is emitted.

The current roster contains exactly 65 rows. The opening and terminal Finder guards have exact cardinality, order, and placement. The C5-three sensitive row is adjacent to the inherited-nine sensitive row before package revalidation. The seven-row C5 architecture/profile block remains contiguous after the sealed C4 block; the exact two-row C6 block immediately follows C5 and immediately precedes the plan/scope/receipt gates. Omission, reordering, duplication, interposition, subgroup drift, overlap, or union loss is a hostile self-test failure.

The frozen pre-C6 ruling remains: C2 then composes the exact C2 boundary over B and C1 (`C2 -> B -> C1`). The current architecture chain evolves that implementation without erasing the historical sentence: P07B-B's sealed predecessor chain invokes P07B-A2.2, then P07B-A1, U6, and U5; its compatibility edge invokes C with no arguments. That no-argument edge now runs the cumulative C6 implementation but returns B's exact legacy C1 marker. Current verification uses explicit `--c1` and `--c6` rows. C6 validates the C1 intersection plus cumulative C5/evidence facts without calling B; C2 still composes its boundary over B and the compatible C1 result (`C2 -> B -> C(no args/C6 without B)`). The graph therefore terminates without recursion. C2, B, C1, and C6 retain explicit top-level rows and self-tests even where composition is inherited transitively. The evolved A2.2 checker retains the sealed A2.1 authority closure internally; there is no duplicate top-level A2.1 row pointing at the same file.

U1–U4 architecture checkers are `HISTORICAL-ONLY NOT-RUN`. They freeze the topology and, by U4, exact all-file aggregates of their sealed unit trees. The boundary evolved in two explicit steps: U5 moved typed adapter neighbor providers onto the pure reducer, then P07A introduced adapter-backed Choice test fixtures and U6's production-only import lattice. Under the current lattice, `internal/projectiontranslate` is the sole production generic-to-adapter branch allowed to import the full CLI/HTTP adapter packages, tests may reconstruct historical projections, and production Choice code still cannot import adapters. Running U3/U4 against today's larger tree would test a contract they explicitly do not own.

All mutation gates are also `HISTORICAL-ONLY NOT-RUN`. Each current script snapshot has a positive manifest and mutant roster bound to its latest sealed owner. Some were deliberately expanded and re-receipted by later units: U2/U3 at U4, and U6 through P07A/P07A-B. The current unit rewrites and shrinks none of them. The baseline prints every historical architecture and mutation row, but never spawns one, and no historical row is represented as passed on the current tree.

The proposed A2.2 source-rewrite driver never became a checked-in executable, completed run, claim, or seal. It therefore has no current or historical row and remains `UNRECEIPTED` design history. A2.2's replacement evidence is independently scoped across black-box/property/parity/recovery/fuzz/boundary checks and the hostile architecture self-test; the two bounded fuzz targets are separate final-unit didrun receipts and are not silently executed by this cumulative command. The human-surface rows verify deterministic rendered/captured bytes and normalized runtime records; they do not establish universal comprehension, semantic correctness, or a didrun grade for manual or different-model taste review.

The P07B-B defensive self-test likewise uses one clean canonical execution plus cloned metadata/topology negatives. It now also requires the clean C1 checker, partitions the exact `internal/contractexec/model/` prefix from lookalikes, preserves historical zero-member admission, admits the future C3 target-codec surface only as one complete exact four-pair bundle, and refuses every partial or foreign C surface. It does not generate or rewrite production source copies into a recipe corpus and makes no mutation-completeness claim.

P07B-B source commit `eb06bdcf18f8e14db1257e73733afdc05cac045e`, tree `d95c907754f3347bae885a64c98b30ad1ea00256`, is sealed and strict-clean with `16/16` claims recorded-exact. Final B handoff commit `464e47adbf7f4497dfafa89938a9539239ffd41b`, tree `c7e80d6e6819d4e2c54aaedeb4dc3ef730de58c6`, independently verifies `5/5 TREE-EXACT` with its matching ledger. C0a commit `1c6d9fdef339314bfcb99d7d53f3a7c5040e5020` and C0b commit `47044e95f405b7252959415adb1aa0dbea8045ab` remain historical sealed authority. Narrow cumulative maintenance source `fa3d0c12b4c599744b666b2848e38a2499f33a89`, tree `c908c4e580144aa481f646090a1a21d92c86e1cf`, is independently sealed and strict-clean with `8/8 TREE-EXACT`; it removes a scheduler race from the stdout-overflow test premise by completing the exact sibling-channel fixture write before crossing the output cap, without changing capture or teardown behavior. Its one-file receipt reconciliation `3f31370a70979c4c5fe3a523be19e05f84271e96`, tree `29c5708b05c562984a0b7b66d6cfe3ac6e19c626`, is separately strict-clean with `6/6 TREE-EXACT`. C1 source commit `2fceacecbacb89fd7650f1570b2af33e6ea25ed3`, tree `573fcd0b5548f9f7368493afe801a4bbd2cc9a34`, is sealed, note-present, and strict-clean with `19/19` claims; it adds strict inert semantics and defensive projection gates only, with no store, Git, runtime, process, or publication authority. Its tracked grades are authoritative only when the C1B declaration, source note, status, and handoff reconcile exactly and C1B is separately sealed and strict-clean.

The evolved plan self-test preserves the two historical C0 receipt phases and two exact C1 receipt phases. While `spec/verification/p07b-c-c1-receipt.json` is absent, the C1 source is still recognized as sealed and strict-clean, all 19 tracked C1 status rows remain `UNRECEIPTED`, and no handoff or didrun-live receipt block may exist. When present, the closed declaration must bind the real sealed C1 source commit/tree, exact subject, 19 ordered claim labels/types/indices, verbatim `TREE-EXACT` grades, strict exit `0`, complete event coverage, and the source didrun note; status, a delimited handoff table, and a didrun live-session disclosure must agree. Local HTML and ignored-ledger fields are declarations until a separate receipt event reopens and hashes them; HTML is never a portable strict witness. C1 also pins the three closed schema/object projections, their required rosters, canonical example graph, and exact source and C1B path allowlists. `check-p07b-c-unit-scope --exact-staged` requires the complete empty-prefix C1B roster and reports its sorted-newline digest `sha256:f8d1fb96d36f7e0cfde99bb73c7726297763c66bafd21aeb5c1fb1e249bdd119`; plain `--staged` remains an admission ceiling. Hostile self-tests cover wrong C0/C1 commit, tree, grade, coverage, handoff, schema, documentation, and scope state. Receipt reconciliation still implies no physical capability.

C1B is admitted only after a separately sealed pre-enrollment and cumulative-stability maintenance boundary over exactly these 18 paths: `docs/ARCHITECTURE.md`, `docs/CONCEPT_BRIEF.md`, `docs/PROMPT_PACK.md`, `docs/SEMANTICS.md`, `docs/STATE_MACHINES.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/decisions/0002-u2-impure-boundary.md`, `docs/status/P07B-C-C0-AUTHORITY.md`, `docs/status/P07B-C-C1-CUMULATIVE-MAINTENANCE.md`, `docs/status/P07B-C-C1-SEMANTICS.md`, `docs/status/U2.md`, `internal/world/http_portable_negative_darwin_test.go`, `internal/world/process_darwin.go`, `internal/world/process_darwin_test.go`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:eb51c52657591df8faa5d3c4737291c071e40c07eb9c32bde88af1a36e68bf76`. The cumulative final-ledger rehearsal observed an initial Darwin process-group `(present=true, EPERM)` result consistent with a transient zombie-only state; that observation became permanently uncertain even though a later final probe observed the numeric group absent. The repaired owner retries only that exact observation within one quarter of the existing teardown budget, clipped to the overall deadline; begins no retry at or after that deadline; signals only after a later clean presence observation; accepts a later clean absence without signaling; and leaves persistent EPERM or every other error fail-closed. Scripted transitions, fake-clock deadline controls, a causally synchronized late-EOF proof, and live inherited-writer teardown preserve the original readiness classification and strict cleanup obligations without the old timer-controlled fd-holder closure premise. This maintenance boundary is a prerequisite gate for C1B but is not C1B, cannot grade C1B, and must seal and verify strictly before the four-file data-only receipt boundary begins.

## Sealed C1V qualification and current verifier profile

The now-sealed C1V verifier-throughput maintenance was a full source gate. It declined a persistent Go-only cache because Go 1.26.5 does not detect imported C-library changes and Countershape contains Darwin/cgo publication code. It added a fail-fast private single-verifier lock, proved an exact whole-package test partition before and after execution, narrowed two-job work to direct general commands, and introduced machine-declared `SOURCE_FULL` versus `RECEIPT_RECONCILIATION` profiles without changing C1B's exact four paths or digest. Its rejected p4 development candidate passed every Go package but produced two permanent later AST-timeout didrun events; p2 was accepted only after the documented repeated stability gates and three cumulative ceiling checks passed. That p2 result remains accurate sealed historical evidence. C4V later observed a second independent late A2 AST deadline under the live profile, firing C1V's explicit “any recurrence rejects tiering entirely” refusal; current direct build, vet, and general testing therefore revert to p1 without changing deadlines or assertions. C1V owns exactly these 11 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-VERIFICATION-THROUGHPUT.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/verify-current-selftest.mjs`, `tools/verify-current.mjs`, and `tools/verify-go-test-repetition.mjs`. Their sorted-newline roster digest is `sha256:6032cc515978947743e1bfea72340e1065d77473ccc27b3b0fc036ebd4ceb2a5`. This declaration was not a C1V self-receipt. Its pre-seal exclusion rule was that no C1B receipt declaration, receipt block, or reconciliation claim may be introduced until C1V seals and verifies strictly; sealed C1V satisfied that prerequisite before C1E began.

## Hermetic child environment

The runner accepts no arguments, root override, skip flag, or historical-run mode. It creates a private mode-`0700` run root below ignored `.countershape/verify-current/`, then gives children fresh `HOME`, `TMPDIR`, `GOTMPDIR`, `GOCACHE`, `GOPATH`, and `GOMODCACHE` directories. The Go build cache deliberately remains fresh: Go 1.26.5 does not detect changes to C libraries, and this Darwin tree contains real cgo code. It sets `GOENV=off`, `GOWORK=off`, `GOTOOLCHAIN=local`, `GOPROXY=off`, `GOSUMDB=off`, `GOVCS=*:off`, `GOFLAGS=-mod=readonly -buildvcs=false -p=1`, `CGO_ENABLED=1`, `GOMAXPROCS=2`, fixed locale/timezone/color values, and a sparse `PATH`. Ambient `GOFLAGS`, Node preload paths, proxy variables, and credentials are not copied.

The admitted package guard parses `go list ./...` as a unique exact roster. Direct build, vet, and the exact general-package complement now fix package jobs to `-p=1`; the general test row also fixes `-parallel=2` and `-count=1`. The inherited sensitive group is the exact ordered nine `internal/contractexec/runner`, `internal/emit/node/compiler`, `internal/emit/node/program/v1`, `internal/processmechanics`, `internal/store`, `internal/world`, `testkit/contractexec/cli`, `testkit/studies/cli_precedence`, and `testkit/studies/http_invoices`. The C5 group is the exact ordered three `internal/contractexec/http`, `internal/contractexec/scope`, and `testkit/contractexec/http`. Both groups are removed from the general complement and run in adjacent rows with identical `-p=1`, `-parallel=2`, `-count=1`, and `-timeout=20m` settings. Each group must be exact, sorted, unique, and present; the groups must be disjoint; and their union must be the complete 12-package sensitive set. Because inherited `GOFLAGS` remains `-p=1`, every nested checker and nested Go command stays serialized. Malformed, duplicate, foreign, missing, reordered, overlapping, or subgroup-swapped rosters fail before tests; every other valid admitted-module package is intentionally part of the dynamic general complement. After all three lanes, a second admitted `go list ./...` must reproduce the exact `all`, `general`, `sensitive`, and `c5Sensitive` sets, so any during-run addition, removal, or reclassification fails before later rows. The output header reports `general_jobs=1 nested_jobs=1 gomaxprocs=2`. `GOMAXPROCS=2`, test `-parallel=2`, fresh cache, lock, timeouts, total package coverage, and all assertions are unchanged. This is a conservative local Darwin/arm64 stability fallback, not proof that the historical two-job profile caused either late AST timeout or a cross-platform performance promise.

The focused Go repetition runner is a separate evidence helper, not a cumulative-row shortcut. It accepts one full admitted-module package, enforces that package's declared `general` or `sensitive` classification, accepts a count from 1 through 50, one anchored test regex, and an explicit top-level expected Test/Fuzz/Example roster. It holds the same verifier lock, uses the same fresh private roots and admitted tools, retains the 20-minute Go timeout, parses `go test -json` as exact LF-delimited package, test, and subtest state transitions, and refuses package/entrypoint drift, skips, failures, invalid actions/transitions, or missing run/pass counts. A full-package repetition must enumerate ordinary tests, fuzz-seed entrypoints, and examples rather than silently narrowing `-run`. It removes its run root and releases the lock before printing its success marker. Its hostile self-test injects the complete lock/admission/private-root/argv/parser/finalization composition plus child, parser, finalization, and cleanup failures; it is part of the cumulative baseline, while each positive focused execution still requires its own didrun receipt.

Qualification uses only `node tools/verify-go-test-repetition.mjs --case <id>`. There are 52 ordered named executions across 14 families: the seven world/compiler/program/store cases; `cli-physical-reducer-01-of-20` through `cli-physical-reducer-20-of-20`; `http-physical-reducer-01-of-20` through `http-physical-reducer-20-of-20`; and the five parity/full-physical-package cases. Each physical reducer shard has count 1, so all 20 separately locked, fresh-root receipts are required for its family. The exported canonical matrix digest is `sha256:bf5803a752ffe6c2833e7869086ec6a4919e15c5e49e8cd012a5c8c16879fc37`. The exact package/profile/count/regex/expected-entrypoint definitions live in the tracked helper and are summarized in the C1V status. Explicit package/regex mode emits `qualification=false` and cannot satisfy this matrix. Every real run first emits ordered admitted Go/Node/Git/sh/CC/CXX path/SHA-256 rows and binds their aggregate digest into its terminal record.

The sharding is a fail-closed receipt repair, not a reduced repetition count. C1V final attempt 1 proved that the CLI physical reducer's old aggregate count-20 command reaches the unchanged 20-minute child deadline: didrun recorded `ETIMEDOUT`/`SIGTERM`, after a `166.32s` iteration passed and the next began. The permanent failed ledger is `.didrun-history/2026-07-18-p07b-c-c1v-final-attempt-1-physical-aggregate-timeout/.didrun/`. C1V does not lengthen the deadline or claim that partial aggregate event; it requires twenty complete count-1 events instead. The HTTP physical sentinel uses the same bounded receipt shape proactively.

C1V qualification was tree-exact in practice as well as intent. The freeze procedure completed independent code/docs review and every tracked edit, staged the exact eleven-path snapshot, archived development evidence, and then started one fresh final ledger for the helper self-tests, all focused repetitions, all three cumulative runs, exact staged-scope/diff integrity, the scoped named-pattern credential scan, and preceding didrun-chain integrity. Any tracked change after that freeze invalidates the entire qualifying matrix: the superseded ledger had to be archived intact and qualification restarted from event zero. A scope check from the archived development ledger is admission evidence only. Only the unchanged qualifying snapshot and its fresh-ledger boundary events were claimed, committed, sealed, note-checked, and strictly verified.

The historical C1V admission rule was: Every one of the three qualifying cumulative runs must finish in at most `878544 ms`. All three sealed qualifying runs satisfied that ceiling. The sealed C1M ledger itself contained only the `992765 ms` cold baseline. The faster `976161 ms` value was a later same-tree observation in a C1B-attempt ledger whose following event failed; describing it as a sealed-C1M event was a provenance error. C1V nevertheless used the faster value, so the resulting threshold was stricter, not looser: the required improvement was at least `97617 ms`, at least 10 percent, and therefore also more than 60 seconds. This erratum changes no sealed timing, grade, threshold, or admission result. Median, fastest-only, and slower-baseline comparisons were not admission rules.

Before plan validation, authority admission, or private-run-root creation, the runner exclusively creates `.countershape/verify-current/active.lock` as a mode-`0600` owned regular file. Its bounded canonical record binds the schema, PID, nonce, creation time, and repository-root digest. A live or indeterminate owner fails immediately with `VERIFY_ALREADY_RUNNING`; an absent PID fails with `VERIFY_STALE_LOCK`. The O_EXCL implementation never queues and never auto-deletes a stale file: automatic deletion has an unsafe two-contender replacement race. Acquisition-error cleanup unlinks only after re-proving descriptor/path identity. Normal checks read the exact bounded file and recheck descriptor/path identity, metadata, and canonical content before unlinking; replacement, disappearance, content drift, or mode drift is `VERIFY_LOCK_INTEGRITY` and the uncertain path is preserved. The final current row removes the private run root and releases this lock; only its success permits terminal `RESULT PASS`. Review the recorded PID before manually unlinking a stale lock. PID reuse can conservatively refuse availability but cannot authorize concurrent verification.

The locked Darwin defaults are:

- `COUNTERSHAPE_GO=/opt/homebrew/bin/go`
- Node authority equal to the canonical `process.execPath` already running the verifier
- `COUNTERSHAPE_GIT=/usr/bin/git`
- `COUNTERSHAPE_SH=/bin/sh`
- `COUNTERSHAPE_CC=/usr/bin/clang`
- `COUNTERSHAPE_CXX=/usr/bin/clang++`

An explicit Go/Git/shell/C/C++ override must be an absolute path. Node is not overridable after startup: invoke the verifier with the desired Node executable; a conflicting `COUNTERSHAPE_NODE` fails closed. Every admitted tool alias is resolved to a regular executable, opened without following the final component, checked for replacement during each admission/fingerprint read, SHA-256 fingerprinted, and printed as an `AUTHORITY` row. Every executable roster row declares its complete admitted tool set, including tools reached by a nested checker, and the verifier re-opens and re-fingerprints each declared authority immediately before and after the row's direct spawn. All authorities are revalidated again before the final result. A private `authority-bin` exposes only the admitted `go`, `node`, `git`, `sh`, `cc`, and `c++` aliases ahead of `/usr/bin:/bin`; admitting one tool does not add its sibling directory to `PATH`.

Each JavaScript checker must also emit its exact success marker; zero exit without that marker is failure. Child output is JSON-framed under `CHILD` rows so it cannot counterfeit the verifier's reserved `CURRENT` or `RESULT` records. These controls reduce accidental/path-replacement ambiguity but do not establish hostile same-user isolation or eliminate the residual same-user check-to-exec race between the last pre-spawn fingerprint and a nested process launch.

The cumulative command uses its resolved private `TMPDIR` for deterministic operation. Separately, the repository's tests must also pass twice under the login shell's stock macOS `TMPDIR`; test-only helpers canonicalize `t.TempDir()` before it enters a strict object-store or Darwin no-follow publication boundary. Production store/publication code still rejects symlinked ancestors and never canonicalizes a caller's root for them.

## Unit verification profiles — frozen v25 bootstrap and live v30 cursor

The v25/C4L and active-C5 wording in the following frozen bootstrap paragraphs is retained to replay sealed authority; it is not the operational cursor. Current machine authority is `countershape/p07b-c-unit-paths/v30` with 44 ordered unit rows and 32 receipt-phase rows. Exact C5, C5V, C6A, C6F, C6G, C6M, and C6N are sealed; C6R is the active `SOURCE_FULL` note-consumer compatibility repair, while `NON_PRODUCT_MAINTENANCE` remains dormant. The live operational cursor is the exact visible capsule in `docs/HANDOFF_MODE_C.md` and the C6R section above, not a tense-sensitive sentence in this historical section.

The historical C3 boundary used the unit-scope v11 specification, C3R used v12, sealed C3Q used `countershape/p07b-c-unit-paths/v13`, sealed C3T used v14, and sealed C3U/C3B used v15. The sealed C3D machine authority was `countershape/p07b-c-unit-paths/v16`. It added ordered parent/profile/named-receipt rows from C3M through C6B while keeping the JSON topology-only; a second `active_boundary` root field was invalid. The complete 18-row forward-table authority—boundary, parent, profile, exact paths, prefixes, receipt states, and receipt-claim manifests—was pinned at `sha256:91ace265208883f3df976cc6a58d4a9d816d9c7c9fd2ba58887ccab13c3561fc`. C3D alone owns that historical specification in its sealed forward horizon; its declared C4, C5, C6A, C6M, and C6B rows omit it from their exact rosters.

Frozen v16 wording was: C6M is predeclared between C6A and C6B. Current v28 inserts sealed C6F and sealed C6G on that direct edge, so C6M is now predeclared after C6G and before C6B as an exact five-path `SOURCE_FULL` data-only source-authority boundary at `sha256:5501a7e8c2d8f6d44392d101110edafe90a0348ef77f5ea24539f6a8ba969803`: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C6M-RECEIPT-ADAPTER-MAINTENANCE.md`, and `spec/verification/p07b-c-c6a-source-authority.json`. It owns neither checker and cannot edit the table, C6 evidence, C6B prompt, or verifier. Its candidate transition validates exact `C6M → C6G → C6F → C6A` ancestry. C6B is separately frozen at `sha256:94501edc81cc170b220fb83873f70ba638a3f19a0a317e2e8df226057dc77a48` to exactly `docs/HANDOFF_MODE_C.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/DIDRUN_BUGS.md`, `docs/status/P07B-C-C6-EVIDENCE.md`, and `spec/verification/p07b-c-c6a-receipt.json`.

The exact visible HANDOFF capsule's `Boundary` field is the explicit cursor, and candidate closure supplies `--check-candidate-phase <unit>` before corpus selection; that plan gate mechanically invokes the immutable unit-scope transition oracle. Active phase is never inferred from receipt presence. `SOURCE_FULL` is mandatory for product/runtime source, generated product, product schemas/examples, product predicates, qualification catalogs, dependency authority, either generic phase/profile checker, the phase specification, every verifier/runtime implementation, every unpredeclared repair, and every boundary whose parent did not already seal its exact narrower consumer row from a `SOURCE_FULL` boundary. It retains the complete cumulative command, so sealed C4L, C5, C6M, and C6N were `SOURCE_FULL`, as is active C6R. `RECEIPT_RECONCILIATION` is admitted only for an exact empty-prefix roster and its exact receipt-only authority. `NON_PRODUCT_MAINTENANCE` may cover only tightly scoped parent-predeclared coordination/status/prompt/research documentation and boundary-specific maintenance-checker bytes under the narrower contract below; it is never inferred from a diff and has no current v30 consumer.

The current machine authority is `countershape/p07b-c-unit-paths/v25`: 27 ordered phase rows at `sha256:189e1fe1a69e3937a769867d2e0d82c7b8b432da1de6c78382e97edd6aa5a796`. C4L changed sealed C4J's v24 table only by inserting `C4J → C4L → C5`, adding the dormant maintenance-profile grammar and closed operator-transition provenance. C4L remained `SOURCE_FULL`; C5 and every later non-owner row inherit v25 byte-for-byte. The generated structural catalog has 1,242 cases at `a4cfe22a5f42b7cba5c3a60c39e609733452f8cc09d0cacda2e6b06f042f06c2`, split into 729 accepted controls and 513 hostile rejections; six fixed semantic regressions extend it to 1,248 at `1a0856522265a8d2d27c4b4f794c9109f84568c6ac75d0fa1fe8c474543e0ac9`, split into 735 accepted controls and 513 hostile rejections. The plan transition matrix is 15 accepted and 466 rejected; the independently implemented scope matrix is 15 accepted and 453 rejected. C4L's sealed Markdown authority corpus has 70 paths at `sha256:5894c9e05456cacff20e2f0095547b032befb13e7aea850583df3972f10fbc07`, with 1,080 positive-form rejections and 360 controls. The v24 and earlier values remain frozen historical evidence.

The third admitted token is dormant `NON_PRODUCT_MAINTENANCE`. C4L introduces grammar and partial scope primitives but remains `SOURCE_FULL`, runs the full three-pass cumulative gate, and does not predeclare a current narrow consumer. It is not yet an executable end-to-end profile: v25 has no concrete consumer, generic runbook renderer, profile-specific preseal reconciliation, or staged positive fixture. A later `SOURCE_FULL` authority migration must add those pieces and their end-to-end tests while predeclaring the exact child row before any boundary may use the profile. A future consumer is admitted only from an exact row already present in its sealed `SOURCE_FULL` direct parent. That row must bind `product_authority: "NONE"`, `product_behavior: "INHERITED_UNREPROVEN"`, `product_projection: "PARENT_FROZEN"`, empty prefixes, one exact sorted path roster, unchanged receipt states, and these ordered roles: `CANDIDATE_PHASE`, `INDEPENDENT_TRANSITION`, `PARENT_PLAN_SELF_TEST`, `SEALED_PARENT_ANCESTRY`, `PARENT_SCOPE_SELF_TEST`, `BOUNDARY_MAINTENANCE_SELF_TEST`, `STAGED_SCOPE_PRODUCT_PROJECTION`, `CREDENTIAL_SCAN`, and `PREDECESSOR_CHAIN`. The first six are `tests-pass`; the final three are `command-succeeded`.

The exact narrow allowlist is parent-predeclared coordination/status/prompt/research documentation plus boundary-specific maintenance checker/self-test tools. Product/runtime source, generated product artifacts, product schemas/examples, dependency manifests, qualification catalogs, verifier/runtime implementation, core truth/security/verification documents, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs` are forbidden. The candidate cannot authorize itself or edit the generic profile contract. Its eventual final gate may claim only exact stage-zero mode-`100644` scope and a clean diff with no tracked change outside the parent-predeclared roster relative to an exact parent reopened by the same gate. `PARENT_FROZEN` carries no enumerated product-projection digest, verifier outcome, didrun event or grade, product-correctness result, or behavior proof; those remain `INHERITED_UNREPROVEN`. No narrow command invokes `tools/verify-current.mjs`, and no narrow label may imply cumulative/full-suite/product/runtime/security/unchanged-behavior/inherited-grade/reused-witness authority.

`OWNER_OUT_OF_BAND` is phase-transition authority, not product or receipt authority. `CITED_UNAUTHENTICATED` is legal only for an exact canonical literal `OWNER_INSTRUCTION_ARTIFACT` path and raw-byte SHA-256 already present in the direct parent's tree. The artifact must be exactly one nonempty, at-most-64-KiB valid-UTF-8 mode-`100644` regular blob; executable blobs, pathspec interpretation, symlinks, trees, gitlinks, empty bytes, oversize bytes, and undecodable bytes refuse. `UNEVIDENCED` carries exact disclosure `OWNER_ATTRIBUTED_SESSION_INSTRUCTION_ONLY_NO_QUALIFYING_PREEXISTING_ARTIFACT`. C4J's exact C4K-status `PROSE_ONLY` predecessor record is not the original instruction and does not upgrade provenance. The v25 rows for C4H, C4I, C4K, C4J, and C4L are all `UNEVIDENCED`; citation does not authenticate, `authentication = NOT_ESTABLISHED`, and `signed_authorization = NOT_IMPLEMENTED`.

Historical C0B is machine-declared `SOURCE_FULL` because that is the larger battery it actually ran; its grade is not rewritten. C1B, C2B, C3PB, sealed C3B, and future C6B use or may use the narrow profile. Historical receipt boundaries carry an exact ordered seven-claim label/type manifest; C6B deliberately carries nine because portable source reconciliation, its defensive self-test, and declared local/private evidence reconciliation are separate claims. Manifests are printed by `check-p07b-c-unit-scope.mjs --unit <unit> --receipt-manifest`; the cumulative verifier never silently dispatches this profile. A receipt unit must explicitly run and claim the existing source note/declaration/status/handoff agreement, its defensive checker self-test, any declared local snapshot as `LOCAL_SNAPSHOT_NOT_PORTABLE_STRICT_WITNESS`, a receipt-only Go build, exact staged scope/diff integrity, a named-pattern credential scan, and preceding ledger-chain integrity before commit/seal/note/strict closure. Receipt staged checks require each exact path to be one stage-zero mode-`100644` nonzero regular blob. The four C1B paths and digest remain `sha256:f8d1fb96d36f7e0cfde99bb73c7726297763c66bafd21aeb5c1fb1e249bdd119`; no receipt-only label may say `cumulative`, `suite`, `runtime`, `security`, or imply unchanged behavior, and pre-seal text cannot present a receipt unit as already graded. Any source, path mode, manifest, profile, or scope drift fails rather than silently falling back to a smaller battery. C3R, C3Q, C3T, C3U, C3D, C4V, C4M, C4N, C4P, C4, C4H, C4I, C4K, C4J, C4L, C5, C5V, C6A, C6M, sealed C6N, and active C6R are `SOURCE_FULL`; C3B remains frozen to its exact narrow profile.

C2 is an exact 26-path, empty-prefix `SOURCE_FULL` unit with sorted-newline roster digest `sha256:a463e4e9ad9830cb420bbad1da494c26d55e3f68b983abfd683a0c8cb1c3a522`. It enrolls the plan checker, unit-scope checker, and this verification contract because those source authorities must know the later receipt phase before C2 freezes. Its final source ledger admits exactly fourteen index-specific argv identities. The exact stage gate also requires one stage-zero mode-`100644` blob per path, a clean staged diff, and no unstaged or untracked path; the adjacent scanner mode reads those same staged blobs across nine named credential patterns. After the C2 source independently committed, sealed, proved its note, verified strictly, emitted its exact-commit HTML report, and archived its ledger, C2B created only `docs/HANDOFF_MODE_C.md`, `docs/status/P07B-C-C2-PERSISTENCE.md`, and `spec/verification/p07b-c-c2-receipt.json`. That exact three-path roster has digest `sha256:5e30e009bb29672d585ece9893bd7c8db198915335a73ded3f3c790ad18f665b` and the `RECEIPT_RECONCILIATION` profile. The receipt authority required one exact C1B parent plus the raw source diff: seven added and nineteen modified mode-`100644` nonzero blobs over the exact roster, with no merge, rename, deletion, type change, unchanged `M`, extra path, or missing path. Git-note `argv_preview` values were checked exactly except that didrun-redacted hermetic path/tool assignments retained only a structural positional claim; `--verify-c2-local-evidence` closed that local-only gap by checking the raw archived event argv while reopening and hashing the declared HTML and ignored ledger. Declaration text alone is insufficient, and ignored local evidence is not a portable strict witness. C2B reconciled the C2 source grades to `TREE-EXACT` at sealed strict-clean commit `ab5e2dd5b66702a1d1cd13eb0047b57a48469487`; it did not edit checker/source behavior, claim the cumulative suite, or receipt itself. Historically C3 then became blocked on C3P, C3M, C3PB, C3A, C3L, C3F, and C3S rather than C2B; every source boundary through C6N is now sealed, and active C6R is the current note-consumer compatibility repair.

## C3P stable boot-session prerequisite

The delayed pre-C3 audit falsified the clock-derived boot profile before any production `OfficialTarget` existed. C3P is therefore a full exact-roster `SOURCE_FULL` boundary, not a documentation exception. Its 25 mode-`100644` paths have sorted-newline digest `sha256:8b047704df047cb96f1c5d19418c4cfe8502e4b4746bb4f24c0851dac8d018c0`. It changes the future target contract to exact profile `DARWIN_KERN_BOOTSESSIONUUID_V1`, rejects the retired clock-derived bytes, regenerates the target/run/execution example chain, freezes C3's exact forty-path, empty-prefix source roster at `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`, and enrolls the dedicated C3P/C3PB receipt checker plus hostile self-test in cumulative verification.

C3P's final ledger has exactly twelve claims, in order: planning/scope coherence; legacy-profile refusal; joined-example regeneration; contract-model profile; inherited C1 architecture compatibility; architecture hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; cumulative verification; exact 25-path staged scope/diff integrity; the scoped staged credential-pattern scan; and preceding-chain integrity. The first nine are `tests-pass`; the last three are `command-succeeded`. C3P sealed at commit `f7b6e6bda7a8864969415ab8636c495902e78dd9`, tree `2d5555db63d46c837cf4e12f2dab2d04a41f4654`, with `12/12 claims recorded-exact` and strict exit `0`. Its source grades became tracked authority only when the later C3PB receipt declaration bound the sealed note and exact local snapshots.

C3PB is that descendant. It owns only `docs/HANDOFF_MODE_C.md`, `docs/status/P07B-C-C3P-RUNTIME-EPOCH.md`, and `spec/verification/p07b-c-c3p-receipt.json`, with digest `sha256:531cbe600cf2747752749890ea9a15c3d0126e498da6838d4f6faeb1a0d7e885` and the standard ordered seven-claim `RECEIPT_RECONCILIATION` manifest. It reconciled only the already-sealed C3P source evidence and labels ignored ledger/HTML checks as local snapshot evidence rather than portable strict witness. C3PB closed at commit `13369122ba7d5557eba1949095c1135a41843070`, tree `d710d9b249786f3fb648f566ae542f1d9ddec180`, with `7/7 claims recorded-exact` and strict exit `0`; that receipt proves no target behavior. At that historical boundary, C3 became active only after the separate C3A/C3L/C3F/C3S maintenance chain sealed.

## C3V verification-throughput proposal reconciliation

The post-C3P observer proposal was based on a pre-C1V snapshot. C3V therefore changed no verifier or repetition-helper behavior. It bound an item-by-item ruling to the then-current machine declaration and sealed C1V authority: commit `88e62acb3023dcd6ee51950c2ad24bbcc2ca8900`, tree `6c98b3c384f01b63d1a00a002303041576896609`, `10/10 claims recorded-exact`, strict exit `0`, the exact 52-case focused qualification matrix, and three complete cumulative passes at `814384`, `815886`, and `813933` milliseconds beneath the fixed `878544` millisecond ceiling.

Item 1 is `DECLINED_WITH_REASON`: `GOCACHE` remains fresh because a Go-authority-only persistent key omits imported C-library, clang, SDK/header, libSystem, and prior writable-cache authority in this real Darwin/cgo tree. Item 2 is `INTENTIONAL; P8_DECLINED_WITH_REASON`: at sealed C3V, direct build/vet/general work then used qualified `-p=2`, the six frozen sensitive packages and every nested Go command remained serialized, and the rejected `p=4` candidate caused two permanent later AST-timeout events despite passing Go packages. C4V later superseded only the live direct-general setting after the declared recurrence clause fired; the sealed C3V ruling remains historical evidence. Item 3 is `INTENTIONAL`: the fail-fast O_EXCL lock already refuses a second owner and preserves review-first stale-lock handling. Item 4 is `INTENTIONAL`: exact empty-prefix receipt units already use the explicit seven-claim `RECEIPT_RECONCILIATION` profile and cannot claim a full suite or unchanged behavior.

C3V owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3V-VERIFICATION-THROUGHPUT.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:6cc5d8fe929f0f1dfa6ffc223d2e8bcee5ba4596a09caa9dc032103bbb5bafbd`.

Its final ledger has exactly eight claims: proposal reconciliation, historical-ruling hostile self-test, unit-scope hostile self-test, cumulative-verifier hostile self-test, one complete cumulative verification, exact seven-path staged scope/diff integrity, scoped named-pattern credential scan, and preceding-chain integrity. The first five are `tests-pass`; the last three are `command-succeeded`. Any verifier-setting change invalidates this no-delta boundary and instead requires an expanded verifier-authority scope plus fresh load-sensitive qualification. C3V proves no new portable performance, warm-cache correctness, p8 stability, secret absence, security review, or runtime capability.

C3V sealed at commit `725933fa3c7826a281e84b51f729736cbcfac6ec`, tree `21f0e1bf46e3776e1b496a9c2783f549923c4510`, with `8/8 claims recorded-exact` and strict exit `0`. That historical result closes only the four proposal dispositions and frozen-verifier authority; it does not receipt C3P or repair a later receipt-phase fixture.

## C3P receipt-phase checker maintenance

A source-receipt-field-validated C3PB draft exposed a phase-fixture lifecycle defect before any C3PB staging, ledger, claim, commit, or seal. The ordinary checker correctly selected receipt-present mode, but its synthetic absent-receipt fixture reopened both status and handoff from the sealed C3P commit. Reopening the exact sealed C3P status is necessary; reopening the C3P-era handoff is wrong because it discards interposed C3V and C3M plan authority. Field reconstruction did not make the full draft valid or graded.

C3M keeps the ordinary live baseline and existing receipt-present declaration, parent/diff, Git-note, argv, grade, coverage, local-evidence, documentation, no-recursion, and self-receipt hostile suites. Its absent fixture instead combines the exact-tree-proved sealed C3P status with the current handoff after fail-closed removal of at most one exact C3P receipt block. Raw start/end cardinality must be `0/0` or `1/1`; admitted markers are standalone LF lines in forward order. Marker-free input is identity-preserving and one canonical block round-trips byte-exactly. Nested/duplicate, partial, reversed, inline, or trailing-content marker topology fails. Empty or malformed receipt JSON is never treated as absence.

At the sealed C3M boundary, the full absent-phase plan passed after stripping and receipt-present mode transitioned the synthetic handoff to explicit C3PB-active state. Its six operational transition fields were an exact tuple: all-C3M transitioned once, all-C3PB was identity-preserving on rerun, and mixed, missing, duplicated, or stale state failed. The extracted block had to equal one canonical ordered payload, and any C3P-specific receipt residue outside that block failed. C3V and C3M authority survived both synthetic phases, and verifier/repetition-runner bytes remained frozen at the C3V-pinned digests.

The historical checker-compatibility anchor “C3V and C3M authority survives” remains preserved; it describes carried authority, not current unit state.

That paragraph records historical C3M behavior, not the current live admission contract. At the historical sealed-C3T/pre-C3B boundary, receipt-absent reconstruction had to preserve exactly one complete C3U tuple in its canonical Markdown locations: exactly one exact line for each of four named metadata keys in `## Current state`, then the matching active subsection heading and receipt-presence paragraph. A validated C3 source receipt plus exact sealed C3R/C3Q/C3T/C3U ancestry atomically transitioned all six slots to C3B; receipt-present rerun remained byte-identical across status, handoff, and declaration. Correct bytes relocated into quoted/history text, placed inside a contradictory duplicate key, or embedded as historical text on a noncanonical line failed. Complete C3M, C3PB, C3A, C3L, C3F, C3S, C3, C3R, C3Q, or C3T tuples were recognized only for stale-state diagnostics and rejected, as were missing, duplicated, relocated, embedded, mixed, or coherent whole-tuple downgrade states. Active-unit positive receipt forms remained forbidden across the exact plan-authority Markdown corpus.

C3M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3P-RECEIPT-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:58e1f6fba95cf3b4312136eea5bb5c64af89a6958711aca84181a1874aa03240`.

C3M was a full checker-authority `SOURCE_FULL` boundary, not a reduced receipt battery. Its final ledger ran plan coherence, the dual-phase checker self-test, unit-scope self-test, cumulative-verifier self-test, one full cumulative verification, exact seven-path source-final gate, named-pattern credential scan, and exact preceding-chain verification. C3PB's three paths, digest, labels, and narrow profile remained unchanged at that historical boundary; C3M and C3PB later committed, sealed, exposed readable notes, and verified strictly before C3A began. C3A then independently sealed before C3L; C3L sealed before C3F began.

## C3A cumulative-admission maintenance

The first preserved C3 source loop exposed two predecessor-contract defects before any C3 commit or seal. The cumulative B architecture checker allowed the C1 model prefix and three exact C2 storage symbols, but not its own authority contract's future sole C3 issuer. Separately, the target JSON Schema and live Node probe already enforce a 128-byte runtime-version ceiling while the Go canonical constructor/parser accepted any length matching the ASCII grammar. Neither defect can be repaired silently inside C3 because both owning files are outside C3's frozen forty-path roster.

C3A admits only `internal/contractexec/target.go:ContractExecutionTarget` as the new C3 pair through the inherited B partition. The collector conservatively inventories word-shaped raw-source tokens in non-test Go files under `internal/`, including comments and literals, for the `ContractExecutionTarget`, `FinalizedContractRun`, and `ContractExecution` roots. The exact partition also admits the established `internal/store/nonhead_contract.go:ContractExecutionClassifierProfile` support pair; outside the intentionally admitted C1 model prefix and exact C2/C3 pairs, the same token at another path and every tested prefix, suffix, or Unicode-suffix variant remain foreign. This is an internal production-Go token policy, not a repository-wide scan, Go-AST identifier claim, escaped/concatenated runtime-string detector, or general Unicode-confusable detector. The B checker's executable guard compares real paths, and the self-test requires the exact success marker plus empty stderr through both direct and temporary-symlink invocation. This is repository conformance evidence, not hostile same-user isolation.

The model correction rejects `len(runtime.Version) > 128` before the existing ASCII version regex. The focused test proves exactly 128 bytes construct and round-trip, while 129 bytes refuse both construction and a coherently rehashed parse. The schema, examples, generated planning bytes, and valid target digests remain unchanged because the implementation is converging on already-sealed syntax authority.

C3A owns exactly these 13 paths: `docs/ARCHITECTURE.md`, `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3A-CUMULATIVE-ADMISSION-MAINTENANCE.md`, `docs/status/P07B-C-C3P-RUNTIME-EPOCH.md`, `internal/contractexec/model/codec_test.go`, `internal/contractexec/model/target.go`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-b-architecture-selftest.mjs`, `tools/check-p07b-b-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:cc9bb98ceeb2f281336d3e235377d3cdb8920664f833baea830a84b847621188`.

Its final ledger has exactly eleven claims: plan coherence; plan/phase hostile self-test; the direct 128/129-byte model boundary; the cumulative B architecture gate; the B partition/entrypoint hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact thirteen-path staged scope/diff integrity; scoped named-pattern credential scan; and preceding-chain integrity. The first eight are `tests-pass`; the last three are `command-succeeded`. The declared manifest requires one exact `/usr/bin/env -i` prefix for all eleven commands with pinned Node/Go/Git/shell/compiler paths, disabled Node startup injection, offline Go settings, and one dedicated final root. The first ten events bind the exact repository cwd and one stable didrun wrapper-environment fingerprint as well as argv/tree/claim identity; the fingerprint is not a full environment attestation. The live eleventh gate validates its own cwd, Node realpath, invocation tail, effective child bindings, and owned mode-`0700` nonsymlink run directories, retains the C3P receipt declaration, and refuses any premature seal. Its didrun event is appended only after exit, so this self-check cannot prove event eleven's outer `env` argv; the later recorded event remains inspectable evidence without becoming part of the preceding-chain claim.

C3A sealed at commit `2b84d2841971568784d2ac955775b4a99ca7f0f6`, tree `b8d858033431fe51c262ce725b9d9b051ba9e8c9`, with `11/11 claims recorded-exact`, readable Git-note blob `3e516946f4b86e536ae9ea0124938bd534573983`, note-body SHA-256 `07a0d04dc667963ea16ec6bb458b929d3a7659637e705bb642b5267766609a8c`, and strict exit `0`. C3 remains frozen to forty exact paths and digest `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`. C3A claims no live Git, host, runtime, attempt, official-target, process, or classification behavior.

## C3L legacy-lattice maintenance

Restoring the preserved C3 source exposed a cumulative architecture defect before any C3 commit or seal. The cumulative verifier runs U6 before later P07B-B/C checks. U6's exact historical store import lattice therefore rejected C3's future `internal/contractexec/model` bridge edge before those later checks could assess its narrower semantics. Removing U6/B from cumulative verification, broadening a prefix, or replacing typed authority with generic bytes would weaken evidence, so C3L repairs the owning legacy boundary separately.

C3L admits two and only two store states. The historical tree retains the exact old import lattice. The future state uses a fail-closed top-level Go source-shape index that decodes interpreted/raw import literals, ignores import-like literal camouflage, and adds alias `contractmodel` only in the production file `internal/store/nonhead_contract.go`. Across the production `store` package, the four bridge types must be unique non-alias package-level structs owned by that file, the input must contain exactly four ordered untagged `domain.Digest` fields, and the four `ObjectStore` methods must be uniquely owned there with exact parameter/result type sequences while receiver and parameter identifier spelling remains inert. Absent shape, wrong alias, tagged/hidden/extra/reordered fields, field/parameter/result drift, raw or escaped foreign imports, local shadows, foreign aliases, duplicate types, or duplicate methods fail with `U6_STORE_C3_MODEL_IMPORT_NOT_ADMITTED`; every other import still fails the inherited exact lattice. The checksum-pinned self-test has 147 cases: seven clean/comment controls and 140 hostile cases. C3's compiler-AST and runtime tests remain responsible for deeper bridge behavior.

C3L owns exactly these 9 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3L-LEGACY-LATTICE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/check-u6-architecture-selftest.mjs`, and `tools/check-u6-architecture.mjs`. Their sorted-newline roster digest is `sha256:b3624e1337681e0909e6c074246103e066118a5924e8f790e7d1f42359da9dc0`.

Its twelve-claim final ledger ran, in order: plan coherence; plan hostile self-test; historical/current-tree U6 architecture compatibility; U6 conditional phase-admission hostile self-test; cumulative B compatibility; B hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact nine-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3A-predecessor plus preceding-chain integrity. The first nine were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and a private C3L final root. C3L sealed at commit `efa918928246c7793f4ad7201c003020e3a42193`, tree `d9187c6449392c269a385b53c563ba4411112460`, with `12/12 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `b21365e156719fbc84e480108a7e8672644f5a71`, note-body SHA-256 `d4721b344adb95442c5273e596c83ea9cb1b855d26655198a9803771f83e2e9d`, and strict exit `0`.

C3 remains frozen to forty exact paths. C3L proves only exact conditional repository conformance in the U6/B chain. It proves no live target, persistence behavior, Git materialization, host measurement, Node probe, attempt allocation, runtime stability, interlock, process spawn, classification, confidentiality, security boundary, production readiness, adoption, or maintainership. C3F and C3S subsequently closed as separate maintenance boundaries before C3 resumed.

## C3F B future-surface admission maintenance

The first resumed C3 architecture baseline to reach inherited B, after an in-scope source-shape correction, truthfully failed there: C3 requires the target type, its constructor and parser in `internal/contractexec/target.go`, and the store-owned parser use in `internal/store/nonhead_contract.go`, while C3A admitted only the type pair. The three-event, zero-claim trigger ledger is preserved at `.didrun-history/2026-07-19-p07b-c-c3-build-loop-post-c3l-pre-c3f/.didrun/`. Obscuring those identifiers, duplicating the codec, accepting generic bytes, removing B from cumulative verification, or letting C3 silently edit its prerequisite would weaken the architecture. C3F repairs only the owning B policy at a distinct boundary; the preserved C3 source stays frozen.

B retains historical zero-member admission. The future surface is admitted only as one complete exact four-pair bundle: `internal/contractexec/target.go:ContractExecutionTarget`, `internal/contractexec/target.go:NewContractExecutionTarget`, `internal/contractexec/target.go:ParseContractExecutionTarget`, and `internal/store/nonhead_contract.go:ParseContractExecutionTarget`. Any one-, two-, or three-member subset remains foreign. A complete bundle plus any extra reserved-family token admits only the exact four and leaves the extra foreign, so the outer architecture gate still fails. Relocated owners, renamed identifiers, suffix/prefix variants, Unicode suffixes, sibling packages, and copied files receive no admission. This lexical production-Go policy is not semantic call proof; C3's own compiler, architecture, and runtime gates still own that evidence.

C3F owns exactly these 10 paths: `docs/ARCHITECTURE.md`, `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3F-B-FUTURE-SURFACE-ADMISSION.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-b-architecture-selftest.mjs`, `tools/check-p07b-b-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:f64293895a1b824faafddf236757d381c813b59c40286225bb1c352d5c812c1a`.

Its ten-claim final ledger ran, in order: plan coherence; plan hostile self-test; historical/current-tree B architecture compatibility; B conditional future-surface hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one complete cumulative verification; exact ten-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3L-predecessor plus preceding-chain integrity. The first seven were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and one private C3F final root. The live tenth gate validated the preceding nine events and claims, stable tree/fingerprint, exact private directories, its own effective bindings, and absent seal; it separately bound sealed C3L's exact commit/tree/parent/subject, note blob/body, ordered twelve-claim semantics, and complete event coverage. The final event was appended only after exit, so it could not prove its own outer wrapper.

C3F sealed at commit `63038644ba347d7b934a5557a490af95bb4428a4`, tree `3b4c39cfa907c2907d01dcd21399cafe52075872`, with `10/10 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `098958ba7a9b2d9d237cee901a34279424b68128`, note-body SHA-256 `a23982f84573505fc966378dbcc4f825daf11da8a08b2bfd24f7f71de588d315`, and strict exit `0`. Its exact-commit HTML is `.countershape/evidence/p07b-c-c3f-final-63038644ba34.html`; its final ledger is `.didrun-history/2026-07-19-p07b-c-c3f-final/.didrun/`.

C3F's proof ceiling is only exact historical/future lexical admission in B and the continued cumulative baseline. It proves no target semantics, parser correctness, durable relation, materialization, live host/runtime/boot/attempt authority, spawn, classification, confidentiality, hostile same-user boundary, production readiness, adoption, or maintainership. C3 remains frozen to forty exact paths.

## C3S cumulative U6 self-test maintenance

The restored C3 source passed production U6 but the full cumulative verifier failed at current row 13 of 46, `architecture-u6-selftest`. The self-test's `installC3StoreBridge` helper still assumed the historical import adjacency and always appended a synthetic bridge. Once the real C3 bridge existed, that anchor was absent and no hostile case ran. The nonzero receipt remains permanent; skipping the self-test, weakening U6, or broadening the frozen C3 source would be false closure.

C3S replaces the one-phase fixture installer with an exact two-phase ensure/require contract. Historical C3F source is admitted only when the model import and every C3 bridge surface are absent; the helper then installs the checksum-pinned synthetic bridge. A current C3 source is admitted only when the exact import, ordered four-field input, non-alias owner structs, and four typed `ObjectStore` signatures are each uniquely present; that source is validated in place and never duplicated. The clean bridge case calls the helper twice to prove install-then-present idempotence. Hostile field/signature/owner cases use unique current-source anchors. The 147-case roster and digest remain unchanged.

C3S owns exactly these 8 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3S-CUMULATIVE-SELFTEST-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, and `tools/check-u6-architecture-selftest.mjs`. Their sorted-newline roster digest is `sha256:1d5f793e70caea0ad7379e92bbf781da50846276c28a2aca2d3e7e2ed8c17a26`.

Its ten-claim final ledger ran plan coherence; plan hostile self-test; historical U6 architecture compatibility; the phase-stable U6 hostile self-test; unit-scope hostile self-test; cumulative-verifier hostile self-test; one full cumulative verification; exact eight-path staged scope/diff integrity; scoped named-pattern credential scan; and sealed-C3F-predecessor plus preceding-chain integrity. The first seven were `tests-pass`; the final three were `command-succeeded`. Every command used one exact `/usr/bin/env -i` prefix and private C3S final root. The last gate bound sealed C3F's exact commit/tree/parent/subject, note object/type/body, ordered ten-claim semantics, and complete event coverage.

C3S sealed at commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, tree `fce8593ad1fe7af1474b4e113f03e82a9a052f79`, with `10/10 claims recorded-exact`, all verbatim grades `TREE-EXACT`, readable Git-note blob `7dff66800e79dc5b1a149ffe16df4dbf9178836a`, note-body SHA-256 `b65f71e25dd53509f8df876a28fb4027559464f77861a34d1aa0c5854bfdbae6`, and strict exit `0`. Its exact-commit HTML is `.countershape/evidence/p07b-c-c3s-final-47a65b45a50c.html`, SHA-256 `38a2264e5f7bcba72755717b882460651852f1ff4ece826a38d0b37be47ada9b`, 7941 bytes; its final ledger is `.didrun-history/2026-07-20-p07b-c-c3s-final/.didrun/`.

C3S proves only cumulative repository/test-fixture compatibility. It proves no C3 target composition, store behavior, Git materialization, host/runtime freshness, race freedom, process spawn, classification, security boundary, production readiness, adoption, or maintainership. C3 binds sealed C3S as direct predecessor while retaining C3F/C3L/C3A ancestry.

## C3 exact-target publication

C3 owns forty exact mode-`100644` paths, no prefixes, at sorted-newline digest `sha256:c0050817c739e6bf7505105113ebbfd2c2504674f93f14e34daf0609f7fad0fb`. The frozen source partition is exactly 20 added and 20 modified files. The final source gate admits no rename, deletion, type change, executable/symlink entry, unstaged tracked change, or untracked path. It includes the compiler-frozen store surface because the narrow C3 bridge must be explicit, and it materializes directly from opaque `gitobj.InspectedTree` rather than synthesizing a one-member comparison world.

The direct parent is sealed C3S commit `47a65b45a50c0f39fc39e1336fc7744a97a72b8f`, whose parent is sealed C3F `63038644ba347d7b934a5557a490af95bb4428a4`, whose parent is sealed C3L `efa918928246c7793f4ad7201c003020e3a42193`, whose parent is sealed C3A `2b84d2841971568784d2ac955775b4a99ca7f0f6`. `spec/verification/p07b-c-c3-predecessors.json` binds that exact chain, each tree and subject, `refs/notes/didrun` blob type/identity/raw-body digest, exact ordered claim/type/grade map, complete event coverage, declared scope digest, and redacted-seal disclosure. Scope digests and the historical strict exits documented here are tracked declarations, not fields synthesized from a didrun note. The live eighteenth preseal gate reopens the tracked manifest without following a symlink, rechecks all four Git commits and raw note blobs, and then verifies the preceding seventeen didrun events share one exact staged tree and hermetic wrapper fingerprint with no seal.

The behavioral battery has five authority profiles: exact official-target composition, direct single-target Git materialization, Darwin host-epoch measurement, admitted Node runtime, and the inert attempt/target store bridge. The official-target profile includes the public live-parent matrix: same physical object database after restart and an independent exact-content database are positive, while cross-store/residue/study/target joins, unrelated or closed repositories, and zero/absent locators issue no authority and preserve the exact scoped store trees, including names, modes, inode identities, ownership, link counts, sizes, and regular-file hashes. A focused race suite separately runs the four exact deliberate-concurrency tests that directly launch concurrent capability operations: official-target close/reopen linearization, uncached host-epoch measurement, copied-runtime revalidation serialization, and attempt/target create-once convergence. It uses `-race`, `-p=1`, and an explicit 20-minute bound over the four owning packages. A machine-checked C3 source inventory pins the exact direct `go`-statement counts for those tests, forbids `t.Parallel`, and forbids production `go` statements in the five owning packages; complete Git materialization and all non-concurrency behavior remain in the five normal authority profiles rather than being duplicated under race instrumentation. The cumulative C3 architecture gate and its defensive self-test freeze issuer topology, source ownership, predecessor authority, forbidden process edges, and the five profile rosters; its raw predecessor lane injects malformed Git framing, fatal UTF-8, note object/body/field-roster drift, exact argv-prefix/command-boundary changes, ancestry-result failures, and coverage mutations without changing sealed notes. The sole-issuer AST cases resolve qualified named types and functions through module-wide graphs over the current Darwin/arm64/cgo production-file selection; exercise source-ordered local shadows, block-scoped generic construction, union and dependent receiver constraints, binding-sensitive repeated instantiation, partial and fully inferred generic producers, generic function calls/values plus direct and promoted generic method calls/values/expressions, parenthesized callees and built-ins, tuple-positioned module function/method call results, nested/indexed/shallowest-promoted exported fields, multiple dot imports, and held factories; and include positive input-only type/function/method consumers plus shadowing and promotion controls. Non-call comma-ok tuple projection and local value/function shadowing are not modeled exactly. These cases remain a cooperating-code shape policy rather than dataflow or hostile-code proof. Inherited B compatibility and its hostile self-test remain current. The cumulative verifier then repeats all prior current units plus these C3 rows.

C3's verifier delta is roster-only verifier enrollment: seven exact C3 current-step definitions plus their self-test expectations. It changes no cache lifetime, job count, package classification, child environment, lock behavior, or repetition-helper byte, but it materially increases sequential verification work. C3 explicitly supersedes rather than literally satisfies sealed C3V's blanket future-byte trigger. The 52-case C1V load-sensitive qualification matrix was not rerun. For this unit, the dedicated verifier self-test and cumulative verification re-establish only the evolved roster and one complete composition snapshot; an unclaimed post-repair development pass completed normally in `2908.769` seconds, which does not establish repeated load-flake stability, portable performance, or a new ceiling. The complete C1V matrix remains mandatory for a later execution-setting, sensitive-package classification, lock-semantics, cache-lifetime, or repetition-semantics change.

One private mode-`0700` C3 final root carried a fresh eighteen-claim final ledger: plan coherence; dual-phase defensive self-test; five authority profiles; focused race suite; architecture and architecture self-test; B and B self-test; scope self-test; verifier self-test; cumulative verification; exact source-final diff; credential-pattern scan; and predecessor/preceding-chain integrity. Every command used the same exact `/usr/bin/env -i` bindings. C3 closed at commit `029c5cf43853eb3cb46520f6e0dea8431a3de5c0`, tree `de3e02a343f8ecfb344bdb28d014342f5b423959`, with `18/18 claims recorded-exact`, readable note blob `9ad8a864911ca30914463c61d3447a88beeda927`, note-body SHA-256 `f5eaa66d0cae152a9edc7f4ff2d49ba66c0d1eb8f3459171a2888daaaaf80fa8`, and strict exit `0`. Its historical pre-seal status intentionally keeps source-era text; sealed C3B supplies the separate descendant receipt.

C3B is the later exact three-path receipt unit at `sha256:3ef17156a2f3051171da70983f9428de2b9bdb5a179602407a36f34200012a67`. It sealed at commit `3b0b56d2bdd36531caf31e3dbad555aca061616d`, tree `15edcc92c5d1ed79885cf9caaa85b4be36918433`, with `7/7 claims recorded-exact`, every verbatim grade `TREE-EXACT`, note blob `e447175c54e8fa864f540f09f8bf614962cd8e07`, note-body SHA-256 `7602ba5baa1e23fc1d309ea4d50c23b843dd87d6f05efe732e8cf656358b2d4b`, and strict exit `0`. Its separate ledger reconciled the sealed source declaration, ran the receipt checker self-test, compared the declared ignored local snapshots, built without source edits, proved its exact staged diff, scanned those three staged blobs, and bound its preceding chain. C3B changes no source or verifier behavior and cannot claim cumulative execution, runtime freshness, security, C4 authority, or unchanged behavior. Historically, C3D rather than C3B became C4's next prerequisite; live C4 now has sealed C4P as its direct parent.

None of these declarations proves a subject process ran or that a hostile executable is authentic. The OS-reported UUID profile remains explicitly weaker than physical reboot, survivor absence, VM non-rollback, kernel trust, clone resistance, or safe-reset proof; the fixed runtime probe proves only a cooperating executable's reported tuple. `OfficialTarget.Valid()` uses sealed in-memory snapshots only, the returned `ContractTargetRecord.Valid()` remains a separate durable-store revalidation, and only reopen refreshes the complete physical graph. C4 must consume a fresh successful reopen immediately adjacent to its physical boundary.

## C3R sealed-note preview compatibility maintenance

C3R owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3R-NOTE-PREVIEW-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:fe14d9f9985771cce0525edfca47c001b5912dacc4c2d6fef182458d3b44f58e`.

The first C3B plan event exposed a deterministic compatibility defect: sealed C3 claim eight's exported Git-note `argv_preview` contains the canonical four-test race selector with only its first long test identifier replaced by `«redacted:high-entropy»`, while the ignored ledger retains the complete raw selector. The prior note validator admitted exact redactions only in the common hermetic prefix and then required a raw byte-equal tail. It therefore rejected a valid sealed projection even though the note blob, note-body digest, commit/tree, claims, grades, and raw local evidence were intact.

`c3RaceNotePreviewPattern` names the one admitted tail projection. The validator accepts it only at C3 claim index seven and only as the entire otherwise-canonical selector argument. It rejects a second-alternative marker, whole-selector marker, suffix, other claim, unrelated tail, length drift, or any other argv change. The local-evidence checker continues to require byte-exact raw local-ledger argv and explicitly rejects the scrubbed projection there. `secrets_override: true` is disclosed as export behavior, not evidence of secret absence or generic redaction correctness.

C3R operator seal handling was outcome-dependent: the plain seal was attempted first, refused aggregate high-entropy-only findings, and the reviewed retry recorded `secrets_override: true` after the separately claimed exact-path structured scan reported zero named-pattern findings. Portable C3R authority does not prove plain-seal-first sequencing, refusal, or review: the Git note records only the resulting boolean. The later C3B ledger-profile line is rendered from that validated boolean and discloses the exact recorded seal disclosure with the same limitation. Neither value is evidence of secret absence, a general credential audit, or publication authority.

The historical C3R lifecycle was dual-phase and fail closed. While `spec/verification/p07b-c-c3-receipt.json` was absent, the six canonical handoff slots formed exactly `C3R_ACTIVE`; sealed C3 identity was legitimate predecessor evidence, but C3 source grades remained unreconciled. C3Q, C3T, and C3U later interposed and sealed, while C3R remained an exact sealed ancestor rather than the then-current `HEAD` requirement for C3B.

C3R's nine-claim final ledger covered plan coherence; note-preview and dual-phase defensive self-test; live sealed-C3 note compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped credential-pattern scan; and sealed-C3 predecessor plus preceding-chain integrity. C3R closed at commit `424273b000f87925a87f0b3041b2e1e8c5750d39`, tree `0e4694362d0242a3b3da0ed2f8f78971ee595f6c`, with `9/9 claims recorded-exact`, note blob `7d0c90aaec4f1a36d8603e5c1ff6195a02221314`, note-body SHA-256 `dcd8991ef2446da78137319d7069d89999eda9748338fbdb5f8927bb4460e09d`, and strict exit `0`. The final ledger is `.didrun-history/2026-07-20-p07b-c-c3r-final/.didrun/`; the exact-commit HTML is `.countershape/evidence/p07b-c-c3r-final-424273b000f8.html`. Its tracked status remains intentionally frozen pre-seal source-era text.

C3R changes no product, target, store, Git materialization, host/runtime measurement, verifier roster, cache/parallelism setting, repetition semantics, lock semantics, didrun installation, sealed C3 bytes, or frozen C3B three-path/seven-claim contract. It proves no process start, runtime freshness, security boundary, secret absence, production readiness, adoption, or maintainership.

## C3Q active-unit self-receipt maintenance

C3Q owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3Q-SELF-RECEIPT-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:f5f27e661c1ace6516aba4b9d5766f4c6ab50b6602dbcb101bff9b6a4ebfe6fc`.

C3Q's sealed-tree machine authority was `countershape/p07b-c-unit-paths/v13`. The following 42-path matrix, counts, and lifecycle statements are exact historical evidence for that sealed tree; C3T's succeeding sealed-tree v14 authority and C3U's later sealed-tree v15 authority are documented separately below.

The first post-C3R C3B defensive self-test exposed phase-asymmetric self-receipt enforcement. Its receipt-present branch inspected only the owned target status and handoff, while the hostile case deliberately placed a positive active-descendant result in an external verification authority. The case therefore escaped the branch that was supposed to reject it. That failed didrun event is permanently archived at `.didrun-history/2026-07-20-p07b-c-c3b-build-loop-pre-c3q/.didrun/`; it supports no claim and is not erased, relabeled, or treated as a transient infrastructure failure.

The repair applies one shared active-unit refusal function to a frozen 42-path plan-authority Markdown corpus with sorted-newline digest `sha256:16d63155a00c9e87b7eec79707ad6d47d6f5d70205d18243b8bc687e401e4b66`: every Markdown body loaded from the required-text maps and status checks, plus the separately read C1, C2, and didrun status authorities. In receipt-absent mode the active unit is C3Q; in receipt-present mode it is C3B. The hostile matrix covers positive commit/tree identities, closure/status words, zero strict-result forms, recorded-exact counts and verdicts, evidence/grade/result associations, positive claim-table rows regardless of column order, and receipt-marker topology. Its declared cross-product is 2 phases × 42 paths × 15 positive forms, or 1,260 required rejections, plus 420 pending/future/failed positive controls that must remain accepted. Eight supplemental natural aliases must reject and two explicit prohibition controls must remain accepted. This is a bounded lexical grammar, not arbitrary sentiment or paraphrase inference. End-to-end external-surface mutations retain the original C3Q and C3B regressions.

Run `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --print-plan-authority-corpus` to inspect the deterministic sorted corpus, its path count and digest, and its explicit bounded-scope footer. The report recomputes the digest and fails closed if the declared constant has drifted. The inventory mode is read-only and is not itself a final-ledger command or didrun grade. Rejections name a stable matched form such as `strict-zero` or `claim-table-grade` without echoing the matched authority text.

On the sealed C3Q tree, lifecycle authority required `C3Q_ACTIVE` before the C3 receipt existed. Once the exact C3 receipt appeared, the checker validated sealed C3R as the fixed direct child of C3, resolved exactly one C3Q direct child of C3R, validated C3Q's exact seven-path source diff, subject, note, nine ordered claims/argv values, grades, and coverage, and required that C3Q child to be current `HEAD` before admitting `C3B_ACTIVE`. This preserved the full `C3 -> C3R -> C3Q` chain rather than weakening the old C3R-current-head test. Sealed-tree v14 authority later interposed C3T after that exact C3Q identity; sealed-tree v15 then interposed C3U after exact sealed C3T.

The C3Q status authority is projected through visible Markdown before its scope, boundary, subject, state, claim rows, and required rulings are accepted. The C3Q-to-C3B subsection transition uses that same visible projection to locate headings while preserving raw byte offsets, so headings hidden in comments or fences cannot truncate replacement and leave stale C3Q prose behind.

C3Q used a nine-claim final ledger: plan coherence; dual-phase, all-surface/all-form defensive self-test; live sealed-C3R note compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped staged credential scan; and sealed-C3R predecessor plus preceding-chain integrity. The first six claim types were `tests-pass`; the final three were `command-succeeded`. It closed at commit `818ed90ebbf99ae38916307d9f2342921d38ebbf`, tree `157241ac2ea7c390b2f67a788b26b9f7666b2893`, with `9/9 claims recorded-exact`, readable note blob `06cd81c05ce2e6f2348d0765298846ac048caf7e`, note-body SHA-256 `b61f53351614361f3646186827ed96e7aace8980bcd3be52f470a366a59fe855`, recorded `secrets_override: true`, and strict exit `0`. Its final ledger is `.didrun-history/2026-07-20-p07b-c-c3q-final/.didrun/`; its exact-commit HTML is `.countershape/evidence/p07b-c-c3q-final-818ed90ebbf9.html`.

C3Q changes no verifier roster, product, target, store, Git materialization, host/runtime measurement, process behavior, cache/parallelism setting, repetition semantics, lock semantics, didrun installation, sealed C3/C3R bytes, or frozen C3B contract. It proves bounded positive-receipt refusal over its exact loaded Markdown corpus—not arbitrary repository prose, secret absence, security review, runtime correctness, production readiness, adoption, or maintainership. The later post-C3Q red rehearsal narrowed the broader outer-driver claim: C3Q's production helper and direct renderer tests remain sealed evidence, but its absent-phase hostile driver was not phase-neutral. C3T owns that defect repair without rewriting any C3Q receipt.

## C3T receipt-phase self-test maintenance

C3T owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3T-PHASE-SELFTEST-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:496788860db6c42f5acc50de0de02be8a63bcb8e11f99fb24d59d9c0e016477e`.

The sealed-tree machine authority was `countershape/p07b-c-unit-paths/v14`. C3T is an exact empty-prefix `SOURCE_FULL` defect repair between sealed C3Q and frozen C3B; it adds one status file and modifies the other six scope/checker/documentation authorities. It changes no product code and C3T changes no verifier roster, execution setting, package classification, repetition semantics, lock behavior, or didrun installation. C3B remains an exact three-path `RECEIPT_RECONCILIATION` descendant with its seven labels and types unchanged.

The permanent post-C3Q C3B rehearsal ledger records the production plan event passing and the outer self-test failing at `P07B-C phase fixture C3 absent downgrade active state anchor count 0`. The driver had tried to find an absent-phase anchor in the live receipt-present handoff. C3T inverse-renders only the lifecycle-owned C3B subsection and metadata, the exact C3 status receipt projection, and the terminal receipt declaration/block, then runs the full plan checker on that validated C3T-active absent fixture. Every absent hostile mutation starts from a fresh validated copy.

The existing forward renderer then reconstructs the canonical receipt-present fixture. The driver requires absent → present, present → absent, and present → absent → present byte stability across the three controlled paths, with exact-one anchors and visible-Markdown topology. This repairs test-state construction; it does not relax C3Q's production refusal policy or discard any hostile case.

Receipt-present admission requires the canonical one-line receipt declaration and exact terminal handoff block. Pretty, reordered, duplicate-key, nonterminal, or trailing-content forms fail before they can be normalized. On the sealed C3T tree, the regenerated C3B status, handoff, and declaration had to equal the captured live bytes; ordinary sealed-descendant plan coherence admitted exact C3T ancestry, while that boundary's dedicated preseal gate required C3T at current `HEAD`. The succeeding sealed-tree v15 authority extended the same rule through exact sealed C3U, which became the direct predecessor required by the C3B preseal gate.

Adding the C3T status authority expanded its sealed-tree corpus to 43 exact Markdown paths at sorted-newline digest `sha256:52e78638d70f4e229e0b54c0a8311b6e0d1d2acc0d4e439cacf10cde65c62959`. That historical declared cross-product was 2 phases × 43 paths × 15 positive forms: 1,290 required rejections, plus 430 legitimate pending, future, failed-development, modal, and prohibition controls. The succeeding historical 44-path v15 corpus is declared in the C3U section below. Both reports remain bounded positive-receipt-form refusal rather than general prose interpretation or a security audit.

C3T binds sealed C3Q commit `818ed90ebbf99ae38916307d9f2342921d38ebbf`, tree `157241ac2ea7c390b2f67a788b26b9f7666b2893`, note blob `06cd81c05ce2e6f2348d0765298846ac048caf7e`, note-body SHA-256 `b61f53351614361f3646186827ed96e7aace8980bcd3be52f470a366a59fe855`, exact ancestry, source diff, subject, nine claims, grades, and complete coverage. Its preseal gate requires that exact C3Q identity to be current `HEAD`; later C3B authority must instead resolve one exact sealed C3T direct child.

C3T used a nine-claim final ledger: plan coherence; receipt-phase fixture defensive self-test; sealed-C3Q Git-note and ancestry compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped staged credential scan; and sealed-C3Q predecessor plus preceding-chain integrity. The first six claim types were `tests-pass`; the final three were `command-succeeded`. It closed at commit `9d8430aa70b1536a85dd7352a3d339667f2c1e88`, tree `3be4d7cde4d043b87438506483ffb8c256719df2`, with `9/9 claims recorded-exact`, readable note blob `d9e371310b360b183d890ed401cb36bd877becda`, note-body SHA-256 `05914bf85ce1d3bc48f5debf902babb526faf2500af318c35f08ffdf071ff735`, recorded `secrets_override: true`, and strict exit `0`. Its final ledger is `.didrun-history/2026-07-20-p07b-c-c3t-final/.didrun/`; its exact-commit HTML is `.countershape/evidence/p07b-c-c3t-final-9d8430aa70b1.html`. Its tracked status remains intentionally frozen pre-seal source-era text.

## C3U cross-phase receipt-fixture maintenance

C3U owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3U-CROSS-PHASE-RECEIPT-FIXTURE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:7af7456929b0db08abc2a31bd1cb62d84002c1a6bcdda3f4a8b4896f6b8c30cd`.

The sealed-tree machine authority was `countershape/p07b-c-unit-paths/v15`. C3U was an exact empty-prefix `SOURCE_FULL` defect repair between sealed C3T and then-frozen C3B. It added one status file and modified the other six scope/checker/documentation authorities. C3U changes no verifier roster, execution setting, package classification, repetition semantics, lock behavior, product code, or didrun installation. C3B remained an exact three-path `RECEIPT_RECONCILIATION` descendant with its seven labels and types unchanged.

The first post-C3T C3B build-loop rehearsal passed the live plan and then failed its receipt self-test at `docs/HANDOFF_MODE_C.md: C3 source receipt block must be the exact terminal handoff block`. The permanent zero-claim red ledger is `.didrun-history/2026-07-20-p07b-c-c3b-build-loop-post-c3t-c2-terminal-red/.didrun/`. The failure was in a legacy synthetic fixture driver: layering a historical C2 receipt after the already-canonical terminal C3 block violated the terminal-block invariant. It did not invalidate the live plan, sealed C3T authority, or C3B receipt content.

`insertLegacyReceiptBlockBeforeOptionalTerminalC3` is the one fail-closed composition primitive for synthetic C0, C1, C2, or C3P receipt insertion. It accepts either a marker-free handoff or one optional already-canonical exact terminal C3 block. If that block exists, the helper captures the complete terminal bytes, inserts the legacy block immediately before them, and reattaches the captured block byte-for-byte. It does not parse or regenerate the C3 receipt payload. Marker-free direct and trimmed-blank behavior stays byte-compatible with the historical renderer.

The defensive self-test exercises the actual C0, C1, C2, and C3P legacy fixture callers in both directions over a synthetic terminal-C3 plan. Each caller must pass a full `checkPlan()` baseline, removing its inserted receipt must recover the exact caller-absent prior handoff, and the exact terminal C3 block must remain byte-for-byte unchanged. Direct controls also cover marker-free outputs, while malformed, duplicated, reversed, inline, hidden, fenced, nonterminal, trailing-content, non-LF C3 marker/terminal framing, and invalid-mode inputs fail closed. These cases extend the existing suite; none is deleted or relabeled.

Adding the C3U status authority expanded the sealed-tree active-unit corpus to 44 exact Markdown paths at sorted-newline digest `sha256:1c5bf120f1ab948586bfa594e6ae634fc6acf7a5544febcccb2c4dc475a59a49`. Its frozen declared cross-product is 2 phases × 44 paths × 15 positive forms: 1,320 required rejections, plus 440 core accepted pending, future, failed-development, and modal controls. Eight supplemental natural aliases must reject, while two explicit strict-zero prohibition controls must remain accepted. The report remains bounded positive-receipt-form refusal, not arbitrary Markdown interpretation or a security audit.

C3U binds sealed C3T commit `9d8430aa70b1536a85dd7352a3d339667f2c1e88`, tree `3be4d7cde4d043b87438506483ffb8c256719df2`, direct parent `818ed90ebbf99ae38916307d9f2342921d38ebbf`, subject `fix: make C3 receipt self-test phase-stable`, note blob `d9e371310b360b183d890ed401cb36bd877becda`, note-body SHA-256 `05914bf85ce1d3bc48f5debf902babb526faf2500af318c35f08ffdf071ff735`, exact seven-path source diff, and nine ordered claims with complete event coverage. C3U preseal validation requires that exact identity to remain current `HEAD`. The later C3B preseal chain gate must resolve one exact sealed C3U direct child of C3T and require it at current `HEAD`; after C3B commits, ordinary plan coherence accepts the exact sealed descendant ancestry.

C3U used a nine-claim final ledger: plan coherence; legacy receipt fixture defensive self-test; sealed-C3T Git-note and ancestry compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped staged credential scan; and sealed-C3T predecessor plus preceding-chain integrity. The first six claim types were `tests-pass`; the final three were `command-succeeded`. It sealed at commit `6e3d837425248b9d9d414d801810dec37cb9ca0b`, tree `b77870b62b3aa29e85a365cef0af49184d2319a6`, with `9/9 claims recorded-exact` and strict exit `0`; its tracked status remains intentionally frozen pre-seal text.

C3U proves fixture composition and exact sealed-predecessor compatibility only. It proves no C3B receipt, process start, runtime freshness, executable authenticity, secret absence, general security property, C4 authority, production readiness, adoption, or maintainership. Those outcomes remain outside this historical maintenance boundary.

## Historical C3D data-driven receipt-phase machinery — sealed snapshot

This section preserves C3D's tracked pre-seal vocabulary and v16 design evidence. Statements below that call C3D current/unreceipted or C4/C5 topology-only are historical assertions about that sealed tree, not operational authority after later sealed migrations. The live authority is the v30/C6R section at the top of this file.

## C3D data-driven receipt-phase machinery

C3D owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C3D-RECEIPT-PHASE-CONSOLIDATION.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:ac4436680c612ad206d2786ddd3146af747e1f8ea2bdedf898afca67d46fcd63`.

The then-current `countershape/p07b-c-unit-paths/v16` specification was the topology-only forward phase table. `receiptPhaseRows` projected each ordered row's boundary, exact parent, existing verification profile, and named `C3P`, `C3`, and `C6A` receipt states. The exact visible HANDOFF capsule's `Boundary` field selected the live row; the specification rejected a redundant `active_boundary`, and no receipt-presence pattern identified the phase. The scope checker rejected parent/order drift, a phase row that omitted HANDOFF ownership, missing or unknown receipt keys, invalid states, downgrades, any receipt flip in a `SOURCE_FULL` row, and zero or multiple flips in a `RECEIPT_RECONCILIATION` row.

`--check-candidate-phase <unit>` is the plan/corpus transition oracle used by C3D and every declared successor. It inventories the entire pinned `git write-tree`, requires exact equality with the live stage-zero index, loads every tracked blob through its OID under explicit per-file and aggregate ceilings, and compares every tracked worktree file with no-follow stable reads before supplying `checkPlan()` a strict full-index source snapshot. Any read absent from that snapshot fails instead of falling back to the live filesystem. The gate pins `HEAD^{commit}`, its tree, and `refs/notes/didrun^{tree}`; classifies one visible canonical capsule matching the explicit unit argument; and requires exactly the table-declared parent. Sealed C3D used v16 digest `sha256:91ace265208883f3df976cc6a58d4a9d816d9c7c9fd2ba58887ccab13c3561fc`; C4V alone bootstraps v17 digest `sha256:11f96a7809d845ef21671d8f847bd3b9f324e84c539cd2f93ab2bc14929ecd46`; for C4 and later, staged/worktree specification bytes must equal the pinned C4V-sealed parent authority. The plan gate mechanically invokes the separately implemented immutable unit-scope command `--candidate-phase <unit>`, then rechecks HEAD, index, notes, and all sampled worktree bytes. C6M owns neither checker. C3D's historical bootstrap required exact sealed C3B commit `3b0b56d2bdd36531caf31e3dbad555aca061616d`, tree `15edcc92c5d1ed79885cf9caaa85b4be36918433`, with no prior capsule marker. C4V's only bootstrap requires exact sealed C3D commit/tree and an actual v16-to-v17 byte replacement. Same-phase replay, skip—including C6A directly to C6B—downgrade, argument/capsule mismatch, malformed modes, OID/byte mismatch, or persistent authority drift fails before acceptance. The plan self-test pins seven admitted forward edges and 41 hostile snapshot cases; the independent scope oracle retains seven admitted edges and 24 generic hostiles plus end-to-end C6 material cases. This is not an atomic worktree snapshot or same-user isolation: a same-user actor can attempt an ABA mutation restored between observations, so the claim depends on sole-writer discipline. The no-argument plan command used inside cumulative verification checks live coherence only; it is not independent transition evidence.

That paragraph is the frozen C3D/C4V-era contract. Sealed C4M alone bootstrapped v18 digest `sha256:dd134e870c11ed4e80eb8335680b59a0ed1553c45d04746fd2fdd722f3f975a4`. Sealed C4N alone bootstrapped v19 digest `sha256:8f237c7d883c212167e5a04e5dd24d6efdacf4a9ce5492215fd06892a8237c4a`; sealed C4P alone bootstrapped v20 digest `sha256:6d7e41d5ff22fc73118ceb9e27a2151e7121d3a91c8945a391b3f42868f7d`. C4 and later staged/worktree specification bytes must equal their pinned parent authority. At sealed v20 the plan matrix was ten admitted forward edges and 266 hostile snapshots, and the independent scope matrix was ten admitted edges and 253 hostile snapshots. Current v30 authority is stated only in the top C6R section.

`renderReceiptPhaseCapsule` emits one exact visible capsule under `## Current state`; `classifyReceiptPhase` requires standalone LF markers and an exact match to one table row; `rewriteReceiptPhaseCapsule` validates both source and target and proves inverse byte identity. `generatedReceiptPhaseCases` couples that capsule to a generic observed-state validator. The deterministic phase catalog contains 666 unique records: 18 rows × 37 cases, with 342 hostile capsule/observed-state rejections and 324 accepted baseline/rewrite controls. Its digest `479f21a8a86a8a6eb34056dabb471e8ceebf08c725008d5f204fa2294af9f83a` binds case IDs, expected outcomes, observed states, and fixture/output byte digests rather than counts alone. Four named executable fixed regressions extend the combined catalog to 670 records and 328 accepted controls/regressions at digest `a56fc2d71b79a06ba151ec454e7ed9280b20140e41c7006e027091eeddcf13e7`. Each fixed record binds its ID, oracle name, sealed commit, checker blob, and checker-byte digest, then executes a family-specific assertion; the combined digest does not imply generic fixture/output fields for those four records.

Receipt payload and sealed-authority parsing remain family-specific adapters. C3P and C3 file presence feeds the generic observed-state map. The then-unreceipted C3D implementation defined full-plan controls for C4, C5, C6A, C6M, and post-transition C6B with their exact Markdown surfaces; sealed C3D later receipted that design. Each target surface is cumulative from C4 through that target, preserving inherited C6A evidence authority in C6M/C6B even when the child roster does not edit the file. For a `HISTORY` target, the historical self-receipt corpus is reopened from its matching first-parent sealed commit; the complete then-current plan corpus is snapshotted separately. `LIVE` uses that complete snapshot, while the generic `FUTURE` and negative fixture generator uses explicit absence sentinels. That was the intended total-fixture invariant, but fixed regression `C3Q_EXTERNAL_SELF_RECEIPT` remained a hand-built exception whose later C6 keys could still fall through; C6F closes that exception without rewriting this frozen design history. Under sealed v16, C4 and C5 were topology-only placeholders; the succeeding sealed C4V v17 introduced their executable final-claim contracts before either product unit began.

Frozen v16 resolution wording was: C6M resolves either preseal-parent HEAD=C6A or sealed-current HEAD=C6M. The now-sealed v28 contract intentionally superseded that direct-parent edge through interposed sealed maintenance. C6A, C6F, and C6G require the source-authority manifest, receipt declaration, and both receipt projections absent. C6F freezes its sealed-note consumer at contract digest `sha256:84227e43b4534cc6977d5b5d274ee415f118af98c36e3e390c86d585d11aa5fb`; C6G freezes its future consumer at `sha256:3dfffad546f7fee36e2e51856cea98a754626f395f319b0c6264fe918df3ef00`. C6M derives and validates its direct sealed C6G predecessor, then sealed C6F, then sealed C6A's commit/tree/single parent/subject, exact diff scope, didrun note blob/body, and eleven claim labels/types/full hermetic argv previews/grades. It introduces only canonical `spec/verification/p07b-c-c6a-source-authority.json` schema `countershape/p07b-c-c6a-source-authority/v1`; the receipt declaration and both projections remain absent, so its live C6A receipt state is still `ABSENT`. The plan and scope validators independently require exact preview length and order, accept didrun's bounded redaction projection only at the historical hermetic-prefix positions, and retain exact visible prefix tokens plus the entire command tail. The form is position-specific: HOME/TMPDIR/GOTMPDIR/GOPATH/GOMODCACHE use the exact double marker; GOCACHE uses one marker plus the expected phase-root `.countershape/.../gocache` suffix; COUNTERSHAPE_GO/NODE/GIT/CC/CXX use one bare marker. A leading executable, missing or substituted prefix token, wrong redaction form or suffix, redaction outside those positions, or tail redaction fails. Redacted positions do not attest their hidden values. C6M resolves either preseal-parent HEAD=C6G or sealed-current HEAD=C6M. The frozen v28 sentence was: C6B resolves either preseal-parent HEAD=C6M or sealed-current HEAD=C6B. Under that historical contract, C6B validated `C6M → C6G → C6F → C6A`, inherited the manifest byte-identically without staging it, and added canonical `spec/verification/p07b-c-c6a-receipt.json`.

Live v30 inserts C6R between sealed C6N and C6B. C6N retains its sealed v29 rule—preseal-parent HEAD=C6M or sealed-current HEAD=C6N—and binds the exact representation epoch of the sealed C6M note. C6R resolves preseal-parent HEAD=C6N or sealed-current HEAD=C6R and binds the observed immutable C6N note projection; C6B resolves preseal-parent HEAD=C6R or sealed-current HEAD=C6B and validates exact `C6B → C6R → C6N → C6M → C6G → C6F → C6A` ancestry. The epoch selection is part of each exact source contract, never a global accept-either fallback.

`--verify-c6a-source-receipt` validates the declaration, sealed tracked `docs/captures/p07b-c/c6a-evidence-summary.json`, source/note/strict fields, and full byte-identical projections in HANDOFF and `docs/status/P07B-C-C6-EVIDENCE.md`. `--verify-c6a-local-evidence` separately reopens and hashes the declared local HTML and ignored ledger. The bounded tracked private-availability fields are declaration state only; C3D does not open private blobs or prove confidentiality, integrity, or future availability. The independent scope checker validates the portable source identity/manifest fields, exact five-path roster, and portable dual-projection fields without importing the plan checker. Missing, one-sided, duplicate, malformed, wrong-identity, wrong-grade, altered transport, declared local-evidence mismatch, or attempted C6B self-receipt fails. The exact two-diagnostic preactivation simulation remains a negative control, while the post-transition control requires the manifest, receipt declaration, both projections, and a non-self-receipting prompt transition. Prefixes may admit non-Markdown artifacts, but every case-insensitive `.md` or `.markdown` authority must be enumerated as an exact path, preventing C6A's capture prefix from recreating the C3Q external-surface asymmetry. Boundaries or receipt families beyond C6B require an explicit schema/checker change.

Ledger endpoints are immutable before their source commits exist. C6A has 11 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6a-preseal-ledger`; C6F has 10 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6f-preseal-ledger`; C6G has 10 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6g-preseal-ledger`; C6M has 10 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6m-preseal-ledger`; C6N has 10 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6n-preseal-ledger`; C6R has 10 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6r-preseal-ledger`; C6B has 9 claims ending `/opt/homebrew/bin/node tools/check-p07b-c-plan.mjs --verify-c6b-preseal-ledger`. C6A claim 11 proves the declared C5V parent edge plus its own preceding didrun chain; it does not claim a predeclared C5V note roster. C6F claim 10 proves exact sealed-C6A predecessor authority. C6G claim 10 proves exact sealed-C6F predecessor authority plus sealed-C6A ancestry. C6M claim 10 proves sealed-C6G predecessor plus sealed-C6F/C6A ancestry, while its source-authority gate separately validates the canonical manifest transport. C6N claim 10 proves exact sealed-C6M predecessor plus sealed-C6G/C6F/C6A ancestry and the unchanged preceding chain. C6R claim 10 proves exact sealed-C6N predecessor plus sealed-C6M/C6G/C6F/C6A ancestry and the unchanged preceding chain. C6B separates portable source-authority conformance, receipt-declaration reconciliation, receipt-checker self-test, and declared local-evidence reconciliation before its receipt-only build, exact five-path scope, credential, and chain gates.

The table does not validate itself. Four fixed sealed-source anchors are byte-reopened, manifest-digest pinned, and executed: `C3R_NOTE_PREVIEW`, `C3Q_EXTERNAL_SELF_RECEIPT`, `C3T_VALIDATED_ABSENT_BASELINE`, and `C3U_TERMINAL_RECEIPT_LAYERING`. C3Q's executable regression injects the external `docs/VERIFICATION.md` positive form through ordinary current-C3D overrides and the outer `checkPlan()` loading path, requires the singleton refusal and a passing prohibition control, then separately replays the historical-C3B scanner corpus. C6F bounds that replay's later-C6 absence authority to four ordered paths at digest `36d29096c536a24bd5882c4dd2c7e305bae19510f23033897a9ccb4a8cff1218`, with an exact 11-case baseline/missing/substitution/foreign/order manifest at `983fed85578bef0dc1eb9c3ce1b9b6622557083d71c0a767edd48bc5565c9683`; the actual C3Q override installation is checked before use. The existing behavior suites remain independent because wholesale generic replacement is intentionally declined: C3R's note-preview/raw-ledger distinction; C3Q's sealed-tree 1,260-rejection and 420-control matrix plus eight aliases and two strict-zero controls; C3T's fresh validated baselines and byte-exact absent/present round trips; and C3U's terminal-block helper plus real C0/C1/C2/C3P full-plan callers. The later frozen C3U/C3B matrix retains 1,320 rejections and 440 core accepted controls and is not regenerated from the table.

C3D declares a ten-claim final ledger: plan coherence; an independent real-tree candidate transition through the unit-scope authority; generated fixtures and fixed regressions; sealed-C3B Git-note/ancestry compatibility; unit-scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped staged credential scan; and sealed-C3B predecessor plus preceding-chain integrity. The first seven claim types are `tests-pass`; the final three are `command-succeeded`. Every C3D grade remains `UNRECEIPTED` until that exact staged tree commits, seals, exposes a readable note, and passes `NO_COLOR=1 didrun verify --strict`.

Historical C3D v16 authority froze `check-p07b-c-plan.mjs --print-final-runbook <C3D|C6A|C6M|C6B>` as the canonical operational renderer for four ledgers; under that historical contract C4 and C5 reject because they are topology-only. C4V v17 extended the exact registry through C4 and C5 without rewriting the frozen history; v21 admitted C4H, v22 admitted C4I, v23 admitted C4K, v24 admitted C4J, v25 admitted C4L, v26 admitted C5V, v27 admitted C6F, v28 admitted C6G, v29 admitted C6N, and current v30 admits C6R. The live registry is `<C3D|C4V|C4M|C4N|C4P|C4|C4H|C4I|C4K|C4J|C4L|C5|C5V|C6A|C6F|C6G|C6M|C6N|C6R|C6B>`. Every emitted shell fence is wrapped in its own `set -eu` subshell, independently enters and asserts the physical canonical repository root, and short-circuits before a claim or later mutation after any failed command; after commit, each fence re-derives current immutable HEAD and slices its exact first 12 characters under the same sole-writer boundary, so no persistent shell state or Git collision-lengthened `--short` output is required. Preparation and archive fences set `umask 077`, then invoke `--verify-final-runbook-parent-authority` through sterile `/usr/bin/env -i` before their first directory creation or copy. C4I's v22, C4K's v23, C4J's v24, C4L's v25, C5V's v26, C6F's v27, C6G's v28, C6N's v29, and C6R's v30 runbooks additionally set `umask 077` inside every evidentiary and post-commit isolated fence, so their claimed procedures do not depend on unbound persistent shell state; the other frozen boundary renderings retain their preparation/archive-only umask instructions. That operational guard requires the canonical repository root, `.countershape`, and `.didrun-history` to be existing real directories owned by the current uid and not group/other writable; an existing `.countershape/evidence` and any visible `.countershape/verify-current` must also be canonical exact-mode-`0700` real directories. Before mutation it no-follow refuses an active verifier lock, live ledger, occupied final root, archive collision, or nonignored Finder artifact. The renderer consumes the same exact expected-argv and claim arrays used by the preseal validators and emits deterministic full `/usr/bin/env -i` commands, immediate claims, exact subjects, private-directory preparation, mode-`0700` `.countershape/evidence` preparation, plain seal before any reviewed manual override, note inspection, strict restart semantics, exact-commit HTML, and copy-with-live-ledger-retained archival. The last claimed preseal command proves the staged tree equals the unchanged ledger tree, so the commit fence inserts no extra unreceipted verification event. Its executable existence/assertion primitive is macOS `/bin/test`; an intermediate `/usr/bin/test` draft produced a permanent unclaimed development exit `127`, and the renderer self-test now forbids that absent path. One helper derives both the emitted commit-addressed archive root and the receipt path; C6A is exactly `.didrun-history/p07b-c-c6a-final-<12-hex-commit>/.didrun/`, and cross-contract self-tests prevent the renderer, exact-12 derivation, per-fence state, parent-namespace guard, behavioral failure short-circuit, and declaration validator from diverging. Printing is not execution or evidence, and the renderer emits no staging, deletion, push, automatic `--allow-secrets`, or unchanged-commit verify loop. The separate guard remains bounded by sole-writer discipline; it is not an fd-bound defense against a malicious same-uid TOCTOU race and does not inspect macOS ACL grants.

C4M v18, C4N v19, C4P v20, C4H v21, C4I v22, C4K v23, C4J v24, C4L v25, C5V v26, C6F v27, C6G v28, C6N v29, and C6R v30 extend the current exact registry while retaining older renderer descriptions as sealed-history text. The legacy `--verify-c4-sealed-c4n-note` mode is historical compatibility surface only; sealed C4 used `--verify-c4-sealed-c4p-note`, and C5's unchanged `--verify-c5-sealed-c4-note` spelling traverses through exact sealed C4L, C4J, C4K, C4I, C4H, and the lower pinned chain. C5V reopens exact sealed C5 with `--verify-c5v-sealed-c5-note`; sealed C6F reopens exact sealed C6A with `--verify-c6f-sealed-c6a-note`; sealed C6G reopens exact sealed C6F and its C6A ancestor with `--verify-c6g-sealed-c6f-note`; sealed C6N reopens exact sealed C6M and the lower ancestry with `--verify-c6n-sealed-c6m-note`; current C6R reopens exact sealed C6N with `--verify-c6r-sealed-c6n-note`. C4J's frozen `--verify-c4j-sealed-c4i-note` spelling is likewise a compatibility identifier: it first proves exact sealed C4K as direct parent, then the named C4I-and-lower chain transitively.

C3D changes no product code, verifier roster, execution setting, package classification, cache lifetime, lock behavior, receipt payload, or didrun installation. Its bounded fixture evidence is not a general Markdown or security audit, and it proves no process start, runtime freshness, C4 authority, production readiness, adoption, or maintainership.

## C4M descendant historical receipt-fixture maintenance

Historical snapshot: C4M is sealed. The active, current, future-parent, and unreceipted wording below is frozen v18 source text retained for checker compatibility; it does not supersede the active C6R section at the top.

C4M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4M-HISTORICAL-RECEIPT-FIXTURE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:ec49dc68ce8fcfe92720fd2e8d6afa6d19d6d1c26aa9d27173dc2df736d06a87`.

C3D separated the historical semantic phase from the live self-receipt pointer, but an omitted descendant corpus still partially fell back to historical bodies.

A cross-boundary self-receipt scan consumes one complete captured live-boundary corpus; it never falls back to historical bodies or ambient descendant worktree bytes.

Explicit corpus keys must equal the complete declared Markdown roster: missing and foreign keys are rejected, explicit values take precedence, and ABSENT_FIXTURE_PATH means explicit absence rather than key omission.

The red was discovered only after checkpointed C4 passed its candidate and independent-scope development gates: the C3U historical fixture carried historical `bodies`, the dynamic self-receipt selector chose C4, and the descendant-only C4 status was unavailable. C4M captures the live corpus once before historical reconstruction and transports a frozen versioned snapshot envelope through every descendant fixture under a private checker-owned symbol. Live capture requires the canonical repository root, no-follow stable reads followed by a second byte pass for every declared Markdown path, and unchanged HEAD, index-tree, and didrun-notes-tree identities before and after. Five injected hostiles freeze the foreign-root, byte-drift, HEAD-drift, index-drift, and notes-drift refusals. Same-boundary checks use their already-loaded body map. Cross-boundary checks require an exact total override map and reject a missing key, a foreign key, a non-string/unavailable value, or a positive descendant receipt; an explicit-absence control places a detectable positive receipt in the historical body and proves that the exact absent key does not reopen it. The named C4V oracle executes an ordered 31-subcase manifest at `d7db6136c8c4bc4672bc6be374c5985ab4c5ad9990f48f4acde69f76d6bacc40`, including exact C3U→C4 trigger replay, cumulative C5 coverage, root/envelope/entry shape failures, alias isolation, omission, explicit absence, and positive-receipt refusal. No implicit fallback or borrowing can turn a partial fixture into apparently complete authority. These are bounded before/after observations under sole-writer discipline, not an atomic snapshot or protection from a same-user ABA mutation restored between observations.

The machine scope is `countershape/p07b-c-unit-paths/v18`: 20 phase rows at digest `sha256:dd134e870c11ed4e80eb8335680b59a0ed1553c45d04746fd2fdd722f3f975a4`. Only the C4M row and C4's parent edge are new. The plan transition matrix is exactly 8/45 accepted/rejected; the independent scope matrix is exactly 8/28. The generated table contains 780 cases at digest `9933fe888704b2a8aaac645e9a362a86b2e662b18efc2d632bf064858763b825`. The executable catalog adds the existing semantic anchors plus `C4V_COMPLETE_LIVE_SELF_RECEIPT_CORPUS` for 785 cases at digest `1dfd07994201ce139426bd6daad625dc40d4f32b60e4c5022fa8c8cd6fda1496`: 405 accepted controls and 380 hostile rejections. Its fixed manifest digest is `c2ab6de5fda27074dd1b9492fd10464c76f943c5913d07a037c6fe5fb58cbd78`.

The current refusal authority is 47 sorted Markdown paths at `sha256:3170c03ba6f8cda2910cd3a4f3437b88f6a30d0de991ca62484426d750af376e`. The active C4M extension exercises 735 positive-form rejections and 245 controls. Cumulative forward coverage exercises 5,355 rejections and 1,785 controls plus seven full-plan controls. Sealed C4V's v17 722/726 catalogs and 46-path corpus remain historical. C3U/C3B's separate literal 44-path roster is pinned at `sha256:1c5bf120f1ab948586bfa594e6ae634fc6acf7a5544febcccb2c4dc475a59a49`; its historical 15-form/five-control plus supplemental manifest is pinned at `ef4b8250cb0fbb666598b1926d3d17349b48ef913adced41831776821e99703c`. The exact 1,320/440/8/2 matrix executes only those frozen authorities and also proves the live 47-path authority is exactly the frozen 44 plus the C3D, C4V, and C4M status paths.

C4M's exact parent is sealed C4V commit `861fd49a2b35b46c101dd6f18707f9c3525c6c7f`, tree `c2a560bf4c7bed6f93078af93714f979d06cf997`, note blob `70c55639f4d0bbe031b3fd18166bde6162f35e3b`, and note-body SHA-256 `b5f56269be341db27f7a0af32f158607ec306762d82c2529425f24cc1880a061`. The predecessor gate validates all 68 recorded-exact claims, argv projections, grades, coverage, strict disclosure, source partition, and the C4V → C3D ancestry under one stable outer HEAD—the outer HEAD observed by both loads. C4's future parent becomes C4M, so its later chain is C4 → C4M → C4V → C3D; C5 later extends that chain through C4. These are validators, not future receipts.

C4M declares a ten-claim final ledger: candidate plan coherence; independent transition authority; generated fixture and descendant-corpus self-test; exact sealed-C4V/C3D ancestry; scope self-test; cumulative-verifier self-test; cumulative verification; exact seven-path staged scope/diff integrity; scoped credential-pattern scan; and preseal ledger/preceding-chain integrity. The first seven types are `tests-pass`; the final three are `command-succeeded`. Every intended grade remains `UNRECEIPTED` until the exact staged tree runs through a fresh ledger, commits, seals, exposes its note, and passes strict verification. C4 remains blocked until C4M seals.

C4M adds no product behavior, Go source, verifier roster, execution setting, external API, sandbox, or production claim. It repairs the fixture authority transport without deleting, narrowing, or relabeling an existing hostile case.

## C4P phase-aware B future-surface maintenance — sealed historical snapshot

C4P is sealed at commit `bbda662314fa3584859ad38f1baeb86ac1f09b5f`, tree `2819dfb90d23da0e89a17f92528338930b73f32c`, with direct parent `b915d43cced850936c46b52620654f7506bdb993`. Its didrun note blob is `0dbc8868d07eac3fa19a5cfab2a9b584d07209e8`, note-body SHA-256 `4a022906c3d4d4be4a6f32396537cd5c8fac0e2072f06b093b40f7fc8d0e3e75`, and it closed with `10/10 TREE-EXACT`, strict exit `0`, and `secrets_override: true`. These grades bind C4P only; the then-active C4 remained `UNRECEIPTED`.

C4P owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4P-FUTURE-SURFACE-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:5dc083fadb60003b0ba96516d0a01f91daa88b7298b142018b5eda00e48ab59b`.

The sealed machine scope is `countershape/p07b-c-unit-paths/v20`: 22 phase rows at digest `sha256:6d7e41d5ff22fc73118ceb0ae9f27a2151e7121d3a91c8945a391b3f42868f7d`. It inserts C4P between sealed C4N and checkpointed C4. The plan transition matrix has 10 accepted and 266 rejected cases; the independently implemented scope matrix has 10 accepted and 253 rejected cases.

The future-surface epoch is selected only from ordered phase-table anchors: `NONE` before C4V, `BASELINE` from C4V until C4, `C4` from C4 until C5, and `C5` from C5 onward. `checkPlan()` and `runBFutureSurfaceAuthoritySelfTest()` share that selector, but a separate frozen 22-record phase-to-mode oracle at `sha256:e042018eedc9ff7b78abc79361d6a824d0d4b4080e510f5b76336a3b39c8ff34` judges every selector result independently. Each of the 22 rows has a routed positive control; each enforcing row has a wrong-epoch negative; unknown phases fail; and explicit C5-without-runner plus HTTP-without-scope cases remain rejected. Every `BASELINE` forward fixture injects the exact frozen historical 27-row inventory and production-path projection instead of reading ambient C4 source. The strict synthetic sealed-C3D baseline and all 42 direct hostile cases are preserved unchanged, so phase awareness adds coverage rather than replacing it.

The future-note validator also preserves two observed, position-specific didrun GOCACHE redaction epochs without combining their authority. C4V/C4M allow only raw or the exact legacy double marker at prefix position 6; C4N/C4P/C4 allow only raw or one marker plus the exact expected `.countershape/.../gocache` suffix. Explicit cross-epoch hostiles reject a suffix projection for legacy validators and an opaque legacy projection for current validators. At GOCACHE position 6, a bare marker or foreign suffix also fails; other prefix positions retain their own fixed projections, and tail redaction remains rejected.

The v20 generic structural receipt-phase catalog contains 902 cases at `d52bdf89855bb306c844103851452a439534e3042e4741bed8787386da529770`, split into 484 accepted controls and 418 hostile rejections. Six fixed semantic regressions extend it to 908 executable cases at `5c2b8ecb64483ed999b817c74fe6751b2dbd689edf3c0740fbceb920c6f7f203`, split into 490 accepted controls and 418 hostile rejections. The separate nine-row phase-extension cardinality catalog is pinned at `6ac375496ed3a160375e5f55258299daad807d9047bc2af4b9be46d2058c4e8c`; its C4M/C4N/C4P/C4 anchors prove 47/48/49/50 paths without changing the frozen C4M digest. C4P's 49-path authority is `sha256:d901a41a7b01febb9bf6f0f9d4823056e9fdb1109746a5adf132877b5ae791be` with 765 rejections and 255 controls; C4 derives the 50-path authority at `sha256:3375ec014b2c977d8d27f052b0eb516b119e00b47c775afaf6c33ff757336c9e` with 780 rejections and 260 controls. The nine-phase aggregate is 7,020 rejections and 2,340 controls plus nine full-plan controls.

The receipt-fixture consolidation disposition is `DEFECT_ALREADY_REPAIRED` for the generic structural generator already delivered by sealed C3D/C4N, `INTENTIONAL_HYBRID` for generated structure plus fixed semantic regressions, and `DECLINED_WITH_REASON` for a pure four-field replacement. Fixed C3R/C3Q/C3T/C3U/C4V/C4M semantic regressions remain independent because their note projection, external corpus, validated-absence, terminal-byte, complete-corpus, and frozen-cardinality semantics cannot be represented honestly by a boundary/parent/profile/receipt-state tuple.

C4P changes no Go product source, B manifest schema, scanner inventory, verifier roster, execution settings, external API, or past receipt. Historical pre-seal contract: C4 remains blocked until C4P seals, and its ten intended grades remain `UNRECEIPTED` until its exact staged tree runs the final runbook, commits, seals, exposes a readable note, and passes strict verification. That gate was satisfied by the sealed identity above; C4, C4H, and C4I later sealed, and C5 later became operational in that historical sequence. The current operational cursor is C6R under v30 authority.

## C4N data-driven self-receipt cardinality maintenance

This is a sealed historical pre-seal snapshot. The owner-tense literals in this section, including `48-path active C4N` and “C4 remains blocked until C4N seals”, are retained as frozen checker history. C4N is sealed at `b915d43cced850936c46b52620654f7506bdb993`; sealed C4P, C4, C4H, C4I, C4K, C4J, and C4L intervened before the then-live C5 boundary under v25 authority. The current operational cursor is C6R under v30 authority.

C4N owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4N-SELF-RECEIPT-CARDINALITY-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:95de7e71ca2df4a57d0544c3197bb7d917359bb3d9ecfc51a2130973faf36e6e`.

The active machine scope is `countershape/p07b-c-unit-paths/v19`: 21 phase rows at digest `sha256:8f237c7d883c212167e5a04e5dd24d6efdacf4a9ce5492215fd06892a8237c4a`. It inserts only C4N between sealed C4M and checkpointed C4. Valid forward edges derive from validated phase rows; the independent forward-candidate allowlist derives from unit order; and every invalid candidate is generated against explicit absence plus every one of the 21 declared receipt-phase rows. C4V, C4M, and C4N are the post-C3D specification owners; C4, C5, C6A, C6M, and C6B each have a generated conformance case proving they cannot change parent specification bytes. The plan transition matrix has exactly 9 accepted and 232 rejected cases; the independently implemented scope matrix has exactly 9 accepted and 219 rejected cases.

The data-derived generic self-receipt catalog contains 840 cases at `a414d55463202a2edea6b132eb1259e83430ad10c0695ecbb729ac3441c3c19c`: 441 accepted controls and 399 hostile rejections. Six fixed semantic regressions extend it to 846 cases at `0aa97e46bd3a24c9238b6bd46a574234ea9b798d72175a46d4190049a5910595`: 447 accepted controls and 399 hostile rejections. The six-record manifest digest is `145421ce5d9b7eedbd852142febd89ecde5786e4224243bdbd7ea2e0e1e55ef5`. The separate eight-row phase-extension cardinality catalog is pinned at `c5e5d421e0dab5709ef8356dc8c25421a0effff8f2698efea0da9e9d74d23761`. C3R/C3Q/C3T/C3U/C4V continue to anchor note, outer-scan, lifecycle, terminal-receipt, and complete-corpus semantics; the sixth `C4M_FROZEN_SELF_RECEIPT_CARDINALITY` record pins sealed C4M's checker blob and historical cardinality.

The `47-path frozen C4M` corpus remains `sha256:3170c03ba6f8cda2910cd3a4f3437b88f6a30d0de991ca62484426d750af376e`, with 735 positive-form rejections and 245 controls. The `48-path active C4N` corpus is `sha256:b48c44457a30b93359e396c543432e897c2639b04c00de0d036f13f971b360af`, with 750 rejections and 250 controls. C4 reentry derives 765 rejections and 255 controls. The eight-phase forward aggregate is 6,180 rejections and 2,060 controls plus eight full-plan controls. This per-phase derivation deliberately preserves the sealed C4M baseline instead of leaking C4N's status path backward. The earlier 31-subcase complete-corpus oracle remains pinned at `d7db6136c8c4bc4672bc6be374c5985ab4c5ad9990f48f4acde69f76d6bacc40`.

C4N's predecessor gate requires exact sealed C4M commit `d87394799d2229bd6e6d342318f446272685a86f`, tree `afd0157303e9025a490b2b6ac991f5f15734137f`, note blob `af7eec1ff78c6ceec40e39dffebdfe97ed4da62c`, note-body SHA-256 `68fa26fcb59c9cb3c6066d28459b3f4ec4df596812f1d55ae7b23f3f5ea7be48`, ten ordered recorded-exact claims, and strict disclosure, then reopens exact C4V and C3D under the same outer HEAD. Historical v19 future ancestry is C4 → C4N → C4M → C4V → C3D; current ancestry is C4 → C4P → C4N → C4M → C4V → C3D, and C5 later extends that chain through C4.

C4N's ten-command final profile contains seven `tests-pass` claims followed by three `command-succeeded` claims. Its status and final runbook remain intentionally `UNRECEIPTED`; C4 remains blocked until C4N seals. C4N changes no Go product source, verifier roster, execution setting, external API, sandbox, performance claim, or past receipt.

## C4V executable execution-unit contract maintenance

This section is the sealed C4V pre-seal snapshot. C4V is strict-clean at commit `861fd49a2b35b46c101dd6f18707f9c3525c6c7f`, tree `c2a560bf4c7bed6f93078af93714f979d06cf997`, with direct parent `62422a2400edd702c82fb680c6ef0c6923eb6cfb`. Its didrun note blob is `70c55639f4d0bbe031b3fd18166bde6162f35e3b`, note-body SHA-256 `b5f56269be341db27f7a0af32f158607ec306762d82c2529425f24cc1880a061`, and records `68/68 TREE-EXACT` with strict exit `0`. Its `countershape/p07b-c-unit-paths/v17` figures and intentional `UNRECEIPTED` wording are historical source text. Live C4 has sealed C4P as its direct parent, inherits current v20 phase/specification bytes without editing them, and realizes the product/verifier rows originally declared by C4V.

C4V owns exactly these 11 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4V-EXECUTION-CONTRACT-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/verify-current-selftest.mjs`, and `tools/verify-current.mjs`. Their sorted-newline roster digest is `sha256:535a64fbb9f0d3c3672b4b8197fc7815da315117b1794a668e6565faa16df3f1`.

Its exact parent is sealed C3D commit `62422a2400edd702c82fb680c6ef0c6923eb6cfb`, tree `7573f57017c6f56b434d1fbd5642ac585f7789c2`, note blob `b9e6e4abe2e3e82a294f0cbfbb686b6a7d8d232e`, and note-body SHA-256 `b78873dbc59a3c81ec14f5894bee090ed3c3e8dc868669fd25e557ea4cf9ae6c`. That sealed note carries ten exact tree-bound claims and strict exit zero; C4V's predecessor gate reopens the commit/tree/single parent/subject, exact one-added-plus-six-modified regular-file diff, note identity/body, claims, argv, grades, coverage, and ancestry before accepting it.

The sealed v17 table extended C3D's generic phase machinery rather than replacing its semantic regressions. The observer's consolidation request was classified `DEFECT_ALREADY_REPAIRED` because C3D already made each normal future boundary a table row; the generic-plus-fixed design was `INTENTIONAL_HYBRID`; a pure four-field replacement for the four semantic oracles was `DECLINED_WITH_REASON`. C3R note-preview projection, C3Q outer/historical scanner asymmetry, C3T validated absent-state lifecycle, and C3U terminal legacy-caller bytes stayed separately executable. C3Q's ordinary current-surface positive and prohibition controls selected the then-active boundary dynamically; its historical C3B scanner replay remained fixed.

C4V's 19-row v17 generic catalog is exactly 722 cases at `c31ebebeac2fdc5d522b466ab082bcd60c5da5b488b7c5be96b7c6cfe1d15aac`; adding four fixed regressions produces 726 cases at `bb433b824bc306bb02138122e1e563f2584c4862766d9721b4881d5f1aa23132`. The exact 365/361 accepted/rejected split, 7/41 plan transition matrix, 7/24 independent transition matrix, and frozen 1,320/440 historical matrix are pinned. Explicit forward allowlists remain independent in both checkers so a staged table cannot authorize its own novel boundary.

C4V also repairs the live verifier setting after its second independent late A2 AST deadline triggered C1V's declared recurrence refusal. Direct build, vet, and general-package jobs are `p=1`; `GOMAXPROCS=2`, test `-parallel=2`, sensitive/nested `p=1`, fresh private cache, exclusive lock, all deadlines, package partition, ordering, and assertions remain unchanged. C4 and C5 inherit this direct `p=1` setting unless a later independently scoped and fully requalified verifier boundary changes it. The replacement C4V final ledger has exactly 68 claims: 65 `tests-pass`, then three `command-succeeded`. It runs seven preliminary gates, all 52 sealed C1V qualification cases separately, three complete unchanged A2 architecture self-tests, three complete 46-row cumulative passes, exact eleven-path scope, credential scan, and preseal chain integrity.

The first C4V final attempt is permanent red evidence at `.didrun-history/p07b-c-c4v-final-attempt-1-ast-sigterm/.didrun/`, with its private root retained at `.countershape/p07bc-c4v-final-attempt-1-ast-sigterm`. It contains seven events and six claims; unclaimed event 6 exited `1` at row `23/46`, `architecture-p07b-a2-2-selftest`, after `257205` ms with `P07B_A2_JS_AST_QUERY_FAILED: SIGTERM`. It produced no commit, seal, Git note, strict result, HTML report, or grade, and events 0–5 are not reusable. The development archive remains `.didrun-history/p07b-c-c4v-development-c63712d69852-134/.didrun/`; `.countershape/c4v-a2-retry` is unclaimed diagnostic state only. This evidence establishes a recurrent load-sensitive deadline manifestation and the policy response, not that the hostile mutant survived or that `p=2` was causal.

The C4V-sealed positive-receipt refusal corpus is exactly 46 sorted Markdown paths at `sha256:b480bb8e93d9ddd6075a57bdf8dbe99cb663eb1f0a11228af31698b54a5dd2f2`. It contains both the sealed C3D status and then-active C4V status. A 45-path development draft omitted C3D and failed the cumulative forward-surface self-test; the repair restored predecessor-plus-current coverage instead of deleting that test. The sealed historical C3U/C3B corpus remains 44 paths at `sha256:1c5bf120f1ab948586bfa594e6ae634fc6acf7a5544febcccb2c4dc475a59a49`.

The C4 source roster is 38 exact paths at `sha256:99bda0d3e5c2f2a2ee4f354945188bf2e5563716c9e26511211a4bfc9197bf56` plus `internal/contractexec/runner/`, `internal/processmechanics/`, and `testkit/contractexec/cli/` at prefix digest `sha256:f5f7b8cdfeba3581e8632c0d6686287f5afa00aef224bb4babe39ee1fdc7f4b2`. The exact roster includes the opaque store bridge/public API freeze, world process implementation and Darwin tests, B/U6 checkers and self-tests, U2/U3 mutation drivers, cumulative verifier/self-test, repetition helper, added `spec/verification/p07b-b-future-surface-authority.json`, and added `tools/verify-runtime-authority.mjs`. Prefixes do not waive compiler-parsed inventory/import checks or admit Markdown authority.

C4 is the final writer of the inherited B checker before C5. The named admitted states remain `C4-present/C5-absent` and `C4+C5-present`. C4V freezes an independent production-Go scanner and manifest schema at `sha256:91c4ca13a4b30a683b82066fdcdc1a7892de2679a2e5419a57142e36b22f5937`, canonical baseline policy at `sha256:5a1c3e2f33969143765612f4934ce4ca41cc1e3df95383b8d7780354461e3399`, and exact live-and-strict-scanned 27-row sealed-C3D historical inventory at `sha256:901d7cb5ca65cb9cb1ca7932ece1741fbfc82df5f9e375d43fadd6e057615960`. C4 creates canonical `spec/verification/p07b-b-future-surface-authority.json`, binds its real reserved-family delta to sorted unique `{boundary,path,symbol}` rows, and fixes seven nonempty C5 rows across `internal/contractexec/http/{evidence,runner,runner_darwin,runner_unsupported}.go`. The canonical 20-row instance is 2,248 bytes at `sha256:2e7c0e9fc530d75e5b8c6cd1cab5d270d5e8ce3e260cda37c4363b3e6c02f49a`; the sealed schema digest does not change. C4 matches only C4 rows with runner present and HTTP/scope absent; C5 preserves the manifest and B/plan checkers byte-for-byte, matches all declared rows against the real combined tree, and realizes runner plus HTTP and scope. C5-without-C4, partial/mixed topology, historical deletion, missing/extra/relocated/lookalike rows or tokens, and schema/digest/order drift are hostile. Private forward fixtures prove validator/state-machine behavior only, never future source existence or conformance. The one-known-row `sha256:4ebfb5ae2608f0cdaa2e316e087fd8c8e479a891e7e52d0a7aab7f9a45541807` is `TEST_VECTOR_ONLY/INCOMPLETE`.

C4's candidate inventory implementation is itself part of the evidence-scope gate. It forbids eager `WalkDir`/whole-file reads, traverses directory descriptors in batches of 128, hashes file streams through a bounded reader, and rechecks path/descriptor identity. The exact C4 ceilings are 20,000 entries, 32 MiB cumulative relative-path bytes, depth 128, 64 MiB per regular file, and 64 MiB aggregate. Tiny-limit controls cover every ceiling plus symlink/FIFO refusal and the exact boundary; the runner's real start-error test also closes and reopens an `OS_START_ERROR` run and classification with no child PID.

C4's future note validator is stricter than the historical suffix-only helper: every preview has the exact expected length; the full tail is byte-exact; prefix tokens are raw-exact or admit only didrun's position-specific projection—double redaction at zero-based argv indexes 2, 4, 5, 6, 7, and 8, bare redaction at zero-based indexes 31, 32, 33, 35, and 36. Added/missing/substituted prefix tokens, shifted/foreign redaction, control characters, appended tokens, or tail drift fail. `secrets_override` must be an actual boolean and may honestly be false after a successful plain seal or true after the explicit reviewed override path. The dynamic C4V authority self-test includes raw and redacted positive controls plus identity, ancestry, diff, note, argv, grade, coverage, and disclosure hostiles.

The live qualification authority remains sealed C1V's exact 52-case matrix through C4V. C4 changes sensitive package ownership, so its final tree must switch to an exact 54-case matrix at `sha256:3be703fae10155c83b19be54ae7cd79cfc5b1bbaddfcf86bb2c9781ad93f853a`. The three world output/capture cases move without semantic loss to `internal/processmechanics`; `world-lifecycle-readiness-20` remains on the world adapter; `contract-runner-admission-50` covers `TestConcurrentAdmissionProducesExactlyOneStart` and `TestRunPermitConsumptionIsSingleUseAndAdjacentToStart`; `contract-cli-standalone-closure-20` covers `TestCLIContractExecutionClosesStandaloneScope` and `TestCLIContractExecutionForbiddenPositiveControls`. Every other C1V case stays exact. Selection uses the actual live capsule, not a synthetic future fixture, so C4V can self-test future C4 projections while its current helper correctly remains 52-case.

The first C4 final-ledger attempt reached that standalone CLI case after 22 successful immediate claims, then the unchanged 20-minute child limit terminated the aggregate before claim 23. The failed event and unclaimed tail remain permanent development history. Diagnosis found repeated defensive `OfficialTarget` getters multiplying complete bundle validation around each of the four required reopens; the fix freezes one inert prepared identity and compares each newly reopened target against it while retaining every physical reopen and probe. The process-scoped test fixture also reuses only content-addressed immutable Git source objects; cached commit metadata derives from the same content digest, so caller order cannot change the cached commit identity. Every repetition still creates fresh materialization, runtime admission, attempt, target record, interlock, and three physical subject executions.

A later exact count-20 development attempt disproved the initial two-count warmup projection: it again reached the immutable 1,200-second child timeout, with the retained JSON tail showing passing closure tests around 57–60 seconds and passing forbidden-control tests around 28–29 seconds. A didrun timing probe then isolated the remaining cost at approximately 14.1 seconds per fresh public target publication, 1.9 seconds per full target reopen, 0.36 seconds per preparation, and 0.1 seconds per defensive getter. The temporary probe was removed; its event remains development history. An intermediate topology that overlapped only the reference and conforming subtests subsequently passed the exact count-20 helper in 1,162.043 seconds, but its 37.957-second margin below the immutable child deadline was not accepted as stable enough for finalization.

The current qualification topology keeps the unchanged reference, conforming, and forbidden workflows behind both Go's frozen `-parallel=2` ceiling and a test-only capacity-two semaphore. Closure and forbidden are parallel top-level tests; closure retains its two parallel subtests. This permits bounded within-iteration pairing but does not overlap separate `-count` iterations. Reference, conforming, forbidden, and ordinary hostile controls use pointer- and canonical-root-distinct stores. Control and forbidden residues must exactly match the reference bundle digest; conforming must differ because its real ruling selects the tuple that yields `CONFORMS`. The target-mutation control owns a fresh one-shot exact-reference store per invocation because its expected post-admission refusal intentionally leaves that namespace held. A selected two-count development run passed in 95.051 seconds, the complete four-test CLI package passed twice in 283.550 seconds including two independent mutation refusals, and the unchanged exact count-20 helper then passed in 1,022.843 seconds with 177.157 seconds of child-deadline margin. The checker freezes the capacity, scheduling topology, role routing, store/root separation, exact bundle relationships, hostile call-site roster, and all three qualification workflow bodies. No subject, publication, fresh attempt, four-reopen execution path, recovery edge, or assertion is deleted. These are development measurements, not claim 23: only the exact count-20 case on the final staged tree may receipt it.

One additive focused `-race` development run over the overlapping reference/conforming workflows emitted no race-detector report but exited nonzero in 272.937 seconds when both branches reached finalized-run persistence/reopen after the current pre-seal 32.3-second terminal-closure deadline had expired under instrumentation load. The nonzero event supports no race-free grade; full overlapping CLI-workflow race freedom remains `UNRECEIPTED`. The product deadline was not increased or refreshed to manufacture a green diagnostic. C4 continues to rely on its frozen targeted race profiles, architecture isolation, full-package lifecycle checks, and the unchanged exact count-20 semantic qualification for their separately scoped claims.

The second fresh C4 final attempt completed and immediately claimed runbook commands 1–77 on staged tree `aca5f714fc9aa6b29afffc58fe21c2a9bdeb883f`. Command 78 then failed before staged-scope evaluation because its first exact `/usr/bin/git` inventory child returned status zero with nonempty stderr. The gate correctly rejected that result, but the then-current failure message discarded the nested stderr bytes; therefore the archived event cannot establish the exact warning or a deterministic cause. Its shape is consistent with the already-observed managed-sandbox Apple `confstr()` warning, but that attribution remains an inference. The failed ledger and private root are preserved at `.didrun-history/p07b-c-c4-final-failed-aca5f714fc9a-attempt2/.didrun/` and `.countershape/p07bc-c4-final-failed-aca5f714fc9a-attempt2/`; all 77 green events are nonreusable. The scope checker is parent-owned and excluded from C4's frozen authority, so C4 does not alter it. A post-C4 `SOURCE_FULL` maintenance boundary must add bounded byte-oriented captured-stderr diagnostics while retaining fatal stderr semantics, directly test clean/error/status/signal/stderr result admission, cap rendered output, avoid stdout reflection, and disclose that an escaped prefix is terminal-safe rather than secret-safe. Final C4 evidence must restart at command 1 on the unchanged authority under approved host Git execution semantics.

The third fresh C4 final attempt exercised the frozen single-wrapper-context invariant on staged tree `ee87b43f51aa4f95a27e3a96abe37faf507bf84f`. Zero-based events 0–76 supplied successful commands 1–77 and claims 1–77 under `env_fingerprint` `3112b643b594e807`; after the Codex UI interruption, zero-based events 77 and 78 supplied successful, immediately claimed commands 78 and 79 under `588aa17f6ef49b56`. The command-80 preseal reconciliation was appended as zero-based event 79 under the resumed fingerprint, exited `1` with `P07B-C C4 live event wrapper context mismatch`, and received no claim. This is a correctly fail-closed receipt-boundary result, not a product-test failure or a completed chain receipt. The archived attempt has 80 events and 79 claims, but none is reusable because the required wrapper fingerprint is not stable across the ledger. The fingerprint is not a full environment attestation, and the interruption's chronology does not prove what caused its value to change.

The fourth fresh C4 final attempt proved another boundary of that environment model. Its detached driver held one fingerprint, `9d275669725f08c3`, across 75 events on tree `55c208c220f56aec6827f39884e6439fdaaf12df`; commands 1–74 passed and were claimed, while command 75's first cumulative verification pass exited `1` and remained unclaimed. The verifier passed its first 16 rows, then `planning-example-p07b-a2-2` returned `MALFORMED_CONTRACT|CONTRACT_DATA_INVALID`. The driver had selected inherited `umask 077`; the C1-owned generator supplies creation mode `0644` but no post-creation chmod, so POSIX reduced the materialized bundle files to `0600` and the intact entrypoint correctly rejected its manifest before importing the harness. A didrun-wrapped explicit-`077` diagnostic reproduced the same refusal and an explicit-`022` control passed conformance plus companion-tamper refusal on the same tree. The final-attempt archive is `.didrun-history/p07b-c-c4-final-failed-55c208c220f5-attempt4-cumulative-planning-example/`; the paired causal ledgers remain preserved under their original red and green caller-mode diagnosis archive directories. None is reusable final evidence. didrun's fingerprint binds its documented environment allowlist but not process umask, so identical fingerprints do not establish identical file-creation semantics. C4's sealed scope excludes both the generator and didrun installation: its replacement detached driver must place log/status under a precreated mode-`0700` attempt directory, run preparation first, create and enforce the mode-`0700` `.didrun` root under `077`, switch to explicit `022` before command 1 and every later manual gate, and retain the runbook's explicit `0700` evidence roots. A later verifier-authority maintenance boundary must make this fixture mode-exact under arbitrary caller umask and decide whether wrapper-context admission should bind umask.

The first attempt-5 detached launch returned before its screen worker became resident: the socket was dead, the precreated log remained empty, and no marker, status, final root, `.didrun`, runbook block, or event existed. Its no-ledger artifacts are preserved at `.didrun-history/p07b-c-c4-final-launcher-failed-709199497939-attempt5-no-ledger/`. The hardened relaunch requires a private worker-start marker plus a responsive live-screen handshake before the launcher may succeed. That attempt 5b retained fingerprint `9d275669725f08c3` for all 79 events on exact staged tree `7091994979392222d0c04fca82c157aa7f59c919`. Zero-based events 0–77 exited zero and were immediately claimed, including three complete cumulative verifier passes and the exact source-final gate. Zero-based event 78, runbook block 80 / evidentiary command 79, then exited `1` and remained unclaimed because the credential scan reported its OpenAI-key pattern in the same three amended documentation files. No credential existed: each file embedded the literal caller-mode diagnostic archive name whose `umask` basename ending immediately preceded the separate `-diagnosis-` suffix, forming a credential-shaped substring. This is a documentation false positive caught by an intentionally fail-closed gate; the scanner remains unchanged. The red ledger, runtime artifacts, and diagnosis are preserved at `.didrun-history/p07b-c-c4-final-failed-709199497939-attempt5b-credential-scan-path-substring/`, with the verifier root at `.countershape/p07bc-c4-final-failed-709199497939-attempt5b-credential-scan-path-substring/`. Its 78 green events are nonreusable; the next final ledger restarts at command 1 on a new staged tree.

C4's sealed 80-command final ledger contains 77 `tests-pass` claims and three `command-succeeded` claims. It covers candidate/independent transition gates; five focused functional profiles; exact race and boundary-fault behavior; C, U6, and B architecture plus self-tests; U2/U3 compatibility; repetition-helper self-test and 54 isolated qualification case receipts; sealed-C4P/C4N/C4M/C4V/C3D authority; scope/verifier self-tests; three cumulative passes; exact staged scope; credential scan; and preseal chain. C4's runbook is rendered by `--print-final-runbook C4`; it remains a frozen command contract and sealed historical evidence.

C5 is independently frozen to 12 exact paths at `sha256:d16ba74582e53dc5c791b8c34fbcd6200338dc066ec13230c2908eb6ba257bf5` plus `internal/contractexec/http/`, `internal/contractexec/scope/`, and `testkit/contractexec/http/` at prefix digest `sha256:a9212680066fdbddc8f25eef04a41c264ac9148a506238dd92c39978e3e35f84`. Every one of those prefixes must be realized by at least one staged regular source path; the prefix boundary admits no sibling spelling. C5 owns its C architecture checker and self-test, but owns neither `spec/verification/p07b-b-future-surface-authority.json`, the B checker/self-test, the phase plan/scope checker, the phase specification, `tools/verify-go-test-repetition.mjs`, nor `tools/verify-runtime-authority.mjs`; it may only satisfy the parent-sealed rows/topology and cannot alter its own admission rules or qualification primitives. C5, C5V, and C6A each own `docs/VERIFICATION.md` whenever they own and evolve `tools/verify-current.mjs`; a verifier-changing boundary may not leave this current-baseline manual stale.

C4 must pre-seal a dormant C5 case catalog. It extends the 54-case C4 catalog with `contract-http-readiness-50` in `testkit/contractexec/http`, count 50, exact test `TestHTTPChildReportedReadinessBindsExactService`, and `contract-http-teardown-20`, count 20, exact test `TestHTTPEarlyExitAndTeardownRetainCausalFacts`. The resulting ordered 56-case JSON projection is frozen at `sha256:08db7c338eb3ba900cf6df2545c04f27bed16889c81dd16e5fb18197c4d52958`. C4 moves lock/tool admission, private-root/environment, bounded-child, authority-revalidation, failure cleanup, and success finalization primitives into its added runtime module; that module has Node-built-in imports only and no import-time authority effects. `verify-go-test-repetition.mjs` imports only that C4-owned runtime, owns its separate sorted qualification-sensitive package roster, and must not read or import C5-owned verifier/checker/specification configuration. Its `qualificationMatrices` export contains recursively frozen literal C4/C5 records with ordered IDs, cases, and digests; the C4 identity aliases and stable `qualificationCaseForID(caseID)` remain exact.

The exported qualification route is parse → canonicalize → validate → build → run. `parseRunArguments(argv)` begins with the exact branch `argv.length === 2 && argv[0] === "--case"`, directly returns `qualificationCaseForID(argv[1])`, and follows it with the exact throwing ad-hoc preamble: length at least ten, even cardinality, and positions 0/2/4/6 equal `--package`/`--profile`/`--count`/`--run`. `validateExecutionSpecification(specification)` starts with the named branch, reselects through `qualificationCaseForID(specification.caseID)`, throws unless the result is the identical frozen object, and returns that canonical object. `buildGoTestArguments(specification)` returns one frozen 11-element argv: `test -json -mod=readonly -buildvcs=false`, `-p=2` only for `general` and `-p=1` otherwise, `-parallel=2`, the exact `-count`, `-timeout=20m`, `-run`, exact regex, and exact package. `runRepetition(specification, dependencies = {})` begins with `const executionSpecification = validateExecutionSpecification(specification)`, builds only from `executionSpecification`, and passes that exact argv to the dependency-bound child. `main()` ends with `await runRepetition(parseRunArguments(process.argv.slice(2)))`; both helper and verifier use an exact nonaliased `{ pathToFileURL }` Node import and a final awaited direct guard. That last sentence is immutable C4 source history. C4H leaves the helper unchanged and replaces only the cumulative verifier's entry route with an exact static-checked classifier: inert import returns `IMPORTED`, canonical execution awaits `main()`, and a same-file noncanonical spelling fails with `VERIFY_NONCANONICAL_ENTRY`.

The plan checker itself has no top-level repetition-helper import. Through C4V, the lazy loader reads mode-`100644` stage-equal bytes and requires the unchanged helper's exact SHA-256 `1321fb1381f1f74d0959b9ceb40b4dcc4abc637bb281ff2af759863ede3bfd5a` before dynamic import. At actual C4/C5 it reads pinned stage-equal helper, runtime, and verifier bytes, then uses a bounded sterile Acorn child to enforce static closure. At live C4/C5, `qualificationStaticAuthority` decodes the recursively frozen literal catalog into a checker-owned projection and never imports or evaluates future candidate helper bytes. The static gate does not execute candidate parser, selector, validator, or builder exports; actual execution evidence is the separate helper self-test, per-case receipts, and preseal reconciliation. Helper's one relative named-import edge is `./verify-runtime-authority.mjs`, runtime imports Node built-ins only, verifier's one relative named-import edge is the runtime, and re-exports, dynamic import, CommonJS/dynamic-evaluation loader forms including `process.getBuiltinModule`, side-effect/aliased imports, effectful module initialization, protected-binding drift, authority-wrapper forwarding, or noncanonical direct entry fail. This is source/import conformance, not confinement or a hostile-code sandbox.

C4's 80-command runbook contains 54 distinct `verify-go-test-repetition.mjs --case <id>` commands; C5's 79-command runbook contains 56. Each command is a separate didrun event and immediate unique claim. The helper therefore acquires a separate lock and admitted-tool snapshot, creates a fresh private-root/environment family, validates one child result and its Go JSON, and finalizes or cleans up before that event can pass. The C4/C5 preseal gate reopens every corresponding didrun stdout blob, verifies its content digest, the independently derived exact ordered `[go,node,git,sh,cc,cxx]` roster plus the SHA-256 digest of UTF-8 `JSON.stringify(roster)`, and the exact terminal case/package/profile/count/test result; an empty or decorative green event is not a qualification receipt. This exactly preserves the sealed C1V isolation shape: no shared session, root, process, or aggregate result may lend evidence across cases.

At preseal reconciliation, the checker independently derives the exact ordered `[go,node,git,sh,cc,cxx]` roster from frozen `COUNTERSHAPE_GO`, `COUNTERSHAPE_NODE`, `COUNTERSHAPE_GIT`, `COUNTERSHAPE_SH`, `COUNTERSHAPE_CC`, and `COUNTERSHAPE_CXX`; it realpaths each authority, requires an executable regular non-symlink file, performs a bounded no-follow raw-byte read of at most 512 MiB with before/after identity, SHA-256 fingerprints it, and requires the Node realpath to equal the invoking checker. Every one of the 54/56 event blobs must match that independently admitted roster exactly; the terminal digest is SHA-256 of UTF-8 `JSON.stringify(roster)`; the roster is reopened after all blobs; and a self-consistent foreign stdout roster is rejected. This is bounded preseal-time independent admission plus post-blob re-admission and recorded per-event output, not proof that executable bytes stayed continuously unchanged between event execution and reconciliation, across events, or against same-uid swap-and-restore/TOCTOU.

During C4, the helper self-test drives both dormant C5-only identities—`contract-http-readiness-50` and `contract-http-teardown-20`—through the actual exported parser, selector, `validateExecutionSpecification(specification)`, and `buildGoTestArguments(specification)`, asserts stable canonical identity plus exact frozen Go argv, and exercises the dependency-injected `runRepetition(specification, dependencies = {})` entry without acquiring runtime authority or spawning an absent C5 package. Unknown IDs, wrong flags, extra arguments, copied specifications, raw C4-alias lookup, and builder drift fail before child execution. C5 later executes all 56 parent-sealed IDs through the unchanged helper/runtime while retaining authority to evolve only its cumulative verifier.

C5 has an unchanged 79-command final ledger: 76 `tests-pass` claims and three `command-succeeded` claims. It covers candidate/independent transition gates; raw-HTTP behavior, child-reported readiness/teardown, five-domain scope closure, cross-target CLI/HTTP plus Go/Node parity, exact authority races and boundary faults; C/U6/B architecture and self-tests; repetition-helper self-test plus 56 isolated case receipts; exact sealed-C4L/C4J/C4K/C4I/C4H/C4/C4P/C4N/C4M/C4V/C3D ancestry; scope/verifier self-tests; three cumulative passes; exact staged scope; credential scan; and preseal chain. Its argv spelling remains `--verify-c5-sealed-c4-note`, but the validator starts from current one-parent C5 HEAD at dynamic sealed C4L, then traverses `C4L → C4J → C4K → C4I → C4H → C4 → C4P → C4N → C4M → C4V → C3D`. It validates C4L's exact `SOURCE_FULL` subject, source partition, 13 note claims, coverage, grades, and direct sealed C4J parent; validates C4J's exact 8-added/11-modified source partition and 13 note claims; then continues through exact C4K, C4I, C4H, C4, and the remaining pinned ancestry while every load observes the same outer C5 HEAD. `--verify-c5-preseal-ledger` couples that predecessor chain to C5's exact fresh 79-event ledger. The deterministic runbook is `--print-final-runbook C5`; none of this is evidence before execution.

C4V itself has 68 commands: 65 test claims followed by exact staged scope, credential scan, and chain integrity command claims. The test sequence includes all 52 C1V qualification identities, three unchanged A2 architecture self-tests, and three complete cumulative passes. `--verify-c4v-sealed-c3d-note` binds its fixed predecessor and `--verify-c4v-preseal-ledger` requires the exact live ledger/order, private mode-`0700` roots, visible C4V capsule, and unchanged C3P/C3-present/C6A-absent state. Every C4V grade remains `UNRECEIPTED` until its staged tree, fresh ledger, commit, seal, readable note, strict verification, HTML, and archive exist.

C4V changes no Go product source, repetition implementation, process behavior, target authority, or didrun installation. It changes `tools/verify-current.mjs` and its self-test only to make direct build, vet, and general-package jobs `p=1`; cache lifetime, lock semantics, `GOMAXPROCS=2`, test `-parallel=2`, timeouts, package partition, ordering, and the complete gate roster remain unchanged. It proves no spawn, permit, finalized run, classification, arbitrary-Node completeness, confinement, confidentiality, secret absence, general security, production readiness, adoption, or maintainership.

## Historical C4 CLI-profile execution boundary — sealed snapshot

C4 is the sealed `SOURCE_FULL` child of sealed C4P. It owns 38 exact paths at sorted-newline digest `sha256:99bda0d3e5c2f2a2ee4f354945188bf2e5563716c9e26511211a4bfc9197bf56` plus required realized prefixes `internal/contractexec/runner/`, `internal/processmechanics/`, and `testkit/contractexec/cli/` at `sha256:f5f7b8cdfeba3581e8632c0d6686287f5afa00aef224bb4babe39ee1fdc7f4b2`. Its receipt state is C3P `PRESENT`, C3 `PRESENT`, C6A `ABSENT`; its frozen subject is `feat: finalize CLI contract executions`. It sealed at commit `c4089ab31181c08dad880bf9e5d40c622c8c91c4`, tree `f427e23b6fe297a49a36a99fa037073b637d82ca`, with `80/80 claims recorded-exact`, all 80 grades `TREE-EXACT`, and strict exit `0`.

The cumulative C4 gate enforces the runner/mechanics/store topology, exact spawn-adjacent detached-context reopen, sole production owner-acquirer inventory, physical eligible `CONTRADICTS` and `CONFORMS` routes, private-manifest multiplicity, durable-release revalidation, recovery, and the C4-present/C5-absent B surface. Its defensive self-test contains 14 metadata cases, 42 Go JSON parser cases, seven command cases, and five owner-reference parser cases. The six separately runnable Go-JSON profiles contain `12/7/4/5/2/4` top-level tests respectively and refuse skips or roster drift.

The C4 architecture facts additionally pin the pre-owner 48 MiB evidence formula, raw length framing with one shared persisted Drain/Captured body, actual-draft ceilings of 15 logical kinds/14 unique blobs before persistence, bounded attempt/evidence rosters, descriptor-bounded scope rosters, identity-bound named cleanup, and deep-residue preservation tests. They also parse the CommonMark C4 status claim table and compare all 80 ordinal/label/type/`UNRECEIPTED` rows against a fresh runbook rendered by the parent-owned checker; parent identity and unchanged scope are enforced separately by the candidate/scope gates. The architecture snapshot covers the status, plan checker, its unit-scope import, phase specification, and handoff input across that render. The existing `execution-chronology`, `evidence-scope`, `scope-root`, and `profile-command` metadata cases independently falsify the added facts, so the 14-case roster and digest remain unchanged while hostile subcase coverage increases. Behavioral additions remain nested inside admitted top-level tests and therefore do not change the six profile counts, 54-case qualification catalog, or 80-command runbook.

The C4 build loop is multi-pass: first compile/targeted behavior, then cumulative checker plus defensive hostiles, then independent read-only code/test/authority and human-surface criticism followed by repair and rerun. Development reds remain permanent unclaimed didrun history. Only a fresh final ledger whose 80 commands all pass on one staged tree can support the declared claims.

C4's live qualification matrix contains 54 isolated cases at `sha256:3be703fae10155c83b19be54ae7cd79cfc5b1bbaddfcf86bb2c9781ad93f853a`. The same C4-owned helper freezes the dormant 56-case C5 catalog at `sha256:08db7c338eb3ba900cf6df2545c04f27bed16889c81dd16e5fb18197c4d52958`; dormant identities are parser/builder/runtime-lifecycle controls only and do not execute absent HTTP packages. The current verifier self-test digest is `0fab61318d55314e3accaeabbaf56cc049aae5755f3c3f41d6a36eba1a6c7af5`.

The exact final runbook is rendered by `node tools/check-p07b-c-plan.mjs --print-final-runbook C4`. It contains 80 commands and 80 immediate claims: 77 `tests-pass`, then three `command-succeeded`; 54 qualification cases remain separate events. That gate has now closed at the exact sealed commit above. The pre-seal `UNRECEIPTED` rows in C4's frozen status remain an immutable declaration rather than being rewritten by C4H.

The current throughput ruling is unchanged. A persistent Go-only cache remains declined because the admitted Go executable digest does not bind imported C libraries, clang/SDK headers, libSystem, or prior writable-cache authority. Direct/general and sensitive work remain serialized after the documented p4 and later recurrence evidence. The fail-fast O_EXCL single-verifier lock and explicit `RECEIPT_RECONCILIATION` reduced roster already satisfy the other observer proposals. Current v23 inserts C4K after sealed C4I and before C5; C4K changes only the exact nested C3 official-target Go deadline and its defensive self-test envelope: it tightens the clean C3 self-test ceiling from 1200 to 1080 seconds while leaving the 900-second profile ceiling and 1200-second cumulative-parent ceiling unchanged. It changes no verifier parallelism, cache, package partition, retry behavior, or order semantics.

Canonical verifier entry was a frozen C4 DX defect. Node resolves the main module URL to the real file while retaining a symlink spelling in `process.argv[1]`, so the old guard let a symlinked `tools/verify-current.mjs` invocation exit zero without executing. C4H is the explicitly scoped verifier-authority migration that replaces the silent no-op with deterministic nonzero `VERIFY_NONCANONICAL_ENTRY`; the canonical tracked invocation and inert import remain positive controls.

C4's receipt ceiling is CLI-only and fixture-specific. The exact enrolled cooperating two-file inventory, candidate-written invocation receipt, named socket canaries, and named sentinel omission do not establish arbitrary-target attestation, Node-vendor authenticity, host-wide absence, network denial, listener ownership, same-UID race resistance, containment, confidentiality, secure deletion, survivor absence, exactly-once execution, process resume, production readiness, adoption, or maintainership.

## C4H verifier hermeticity and operator-state maintenance

This section is sealed v21 pre-seal history. C4H closed at commit `4b4686ea30e5ddc5043eee11ba24725bcce8bb94`, tree `2d02d220961f3fc41b2372df5eb8dc3aac47a529`, direct parent C4 `c4089ab31181c08dad880bf9e5d40c622c8c91c4`, note blob `9532c545dd685046efd57e999613092f92e54e8d`, and note-body SHA-256 `e94c4b8b48978f0208757f7f7707bde5c02b0cb7c91eebaa16873e9aaf1b5041`, with 11/11 `TREE-EXACT` and strict exit `0`. Its local HTML is `.countershape/evidence/p07b-c-c4h-final-4b4686ea30e5.html`, SHA-256 `024cfb02a2ce19c7aa5b7dbfe87c7cb0d0a282903d9257e66488245e36de00c8`, 8,324 bytes. The owner-tense and `UNRECEIPTED` sentences below remain immutable historical declarations, not the live cursor.

C4H is the active owner-authorized `SOURCE_FULL` boundary between exact sealed C4 and unchanged C5. C4H owns exactly these 18 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C4H-VERIFIER-HERMETICITY-MAINTENANCE.md`, `research/deep-dive/p07b-c-maintenance-architecture/00-scope-and-method.md`, `research/deep-dive/p07b-c-maintenance-architecture/01-proof-semantics.md`, `research/deep-dive/p07b-c-maintenance-architecture/02-verifier-throughput.md`, `research/deep-dive/p07b-c-maintenance-architecture/03-maintenance-order.md`, `research/deep-dive/p07b-c-maintenance-architecture/04-synthesis.md`, `research/deep-dive/p07b-c-maintenance-architecture/05-follow-up-verification.md`, `research/deep-dive/p07b-c-maintenance-architecture/06-different-model-red-team.md`, `research/deep-dive/p07b-c-maintenance-architecture/07-executive-briefing.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/generate-p07-planning-example.mjs`, `tools/verify-current-selftest.mjs`, and `tools/verify-current.mjs`. Their sorted-newline roster digest is `sha256:1adc913036078ad4efdf418e78581d81369aa630d245a5b493c60a69a0478532`.

The live `countershape/p07b-c-unit-paths/v21` authority has 23 ordered phase rows. It inserts only `C4H` into `C4 → C4H → C5`; its plan transition matrix has 11 accepted and 302 rejected cases, and the independent scope matrix has 11 accepted and 289 rejected cases. C4H's exact Markdown authority surface has 59 paths, 915 hostile rejections, and 305 controls. Its authority source is `OWNER_OUT_OF_BAND`: C4 did not predeclare the new boundary, C4H inherits or rewrites no C4 grade, and the owner's instruction is not cryptographically authenticated.

The repair closes four observed operator-boundary defects without changing product behavior. First, fatal Git stderr diagnostics are byte-bounded: the message exposes only total byte count, the lowercase-hex encoding of at most the first 256 raw bytes, and an explicit truncation flag; it reflects neither stdout nor raw terminal controls and makes no UTF-8 assumption. Second, the planning-example exercise produces exact mode-`0700` directories and mode-`0644` files under caller umask `077` and `022`, validates descriptor identity before replacement mutation, and restores caller umask through nested failure paths. Third, canonical direct execution remains accepted and import remains inert, while every noncanonical/symlink spelling refuses with `VERIFY_NONCANONICAL_ENTRY`. Fourth, real subprocess tests prove same-repository contention rejects early with `VERIFY_ALREADY_RUNNING`, a different repository proceeds independently, a SIGKILL leaves a stale record classified as `VERIFY_STALE_LOCK`, and reviewed recovery removes the stale state.

C4H changed no Go or Node product code and no cache, parallelism, package-partition, deadline, or test-order semantics. At its historical tree, direct build, vet, general, sensitive, and nested execution remained at `p=1`; `GOMAXPROCS=2`, test `-parallel=2`, nine sensitive packages, fresh private caches, the O_EXCL production lock, and fixed timeouts were exact. Active C5 adds three sensitive packages in a second adjacent serial subgroup row without changing those execution settings. The sealed C1M provenance erratum is also tracked here: `992765 ms` is the only sealed-C1M cold baseline, while `976161 ms` was a later same-tree failed-attempt observation; using the faster value made C1V's threshold stricter and does not invalidate its sealed evidence.

C4H runs one C4H cumulative pass because it is a narrowly bounded verifier/operator maintenance boundary. This does not reduce or substitute for C5's three cumulative passes: C5's 79 labels, types, argv, order, 56 qualification cases, and terminal predicate remain byte-exact and monolithic. Proof sharding is `DECLINED_WITH_REASON` because the current C5 predicate requires one exact 79-event ledger and no authorized receipt transition can reuse a witness boundary without relabeling. Composite Proof Protocol v2 is `DEFERRED_WITH_REASON` until after C6B so it can be designed prospectively rather than retrofitted into frozen claims.

C4H's exact 11-command ledger completed on one staged tree, committed with subject `fix: harden verifier execution boundary`, sealed, exposed the readable note above, and passed strict verification. It proves only the bounded diagnostics, exact fixture modes, noncanonical-entry refusal, and real subprocess-lock/recovery cases described above. It does not establish secret absence, hostile same-UID isolation, universal process cleanup, arbitrary target containment, C5 HTTP behavior, production readiness, adoption, or maintainership.

## Sealed C5 HTTP-profile execution boundary — frozen pre-seal snapshot

This section retains C5's owner-tense and `UNRECEIPTED` pre-seal contract as frozen checker history. Exact C5 is now sealed at `240060f018d5b0e86914bd27361c5899ac9ba1c0`, tree `4906ff4af407fa4e48cbf569f7e456694660ca06`, with `79/79 TREE-EXACT`; the C6R section at the top is the live cursor.

C5 is a pre-seal `SOURCE_FULL` child of exact sealed C4L. Its 12 exact paths have digest `sha256:d16ba74582e53dc5c791b8c34fbcd6200338dc066ec13230c2908eb6ba257bf5`; its three required prefixes—`internal/contractexec/http/`, `internal/contractexec/scope/`, and `testkit/contractexec/http/`—have digest `sha256:a9212680066fdbddc8f25eef04a41c264ac9148a506238dd92c39978e3e35f84`. The frozen subject is `feat: complete standalone contract execution`. Every C5 grade is `UNRECEIPTED`.

The cumulative verifier adds seven contiguous C5 architecture/profile rows: architecture, its exact `14`-metadata/`60`-Go-JSON/`6`-command defensive self-test, and profiles `c5-http-behavior`, `c5-readiness-teardown`, `c5-scope-closure`, `c5-cross-profile-parity`, and `c5-http-authority-race`. Their counts are `4/9/9/22/5`, all have zero skips, and only `c5-http-authority-race` uses `-race`. Three new whole sensitive packages raise the sensitive package roster from nine to 12; one additional adjacent C5-three serial step rotates the verifier step roster from 61 to 62. The intentionally frozen spelling `--verify-c5-sealed-c4-note` begins its ancestry proof at sealed C4L, then traverses C4J, C4K, C4I, C4H, C4, and the fixed predecessor chain.

The C5 architecture facts additionally bind six red-team repairs without changing those frozen profile counts: `profileBoundRecovery` requires the complete standalone-HTTP error guard and exact pure-helper body before the first terminal-store token; `phaseLocalContexts` is derived by one pure source inspector shared with the defensive self-test and requires the exact three-statement detached-budget helper, exact total and context-specific call cardinalities, exactly two phase factories, continuous handoff from start-adjacent revalidation to closure, phase-one expiry inspection and cancellation, error joining and returned-target cleanup, exact closure-context consumption, and exactly one executable nested context-independence registration. Its five in-memory source hostiles cover post-formula budget mutation, context substitution, an additive alternate-context call, an expected anchor preserved only in a comment, and a nested-test registration preserved only in a comment. `serializedHTTPAttempt` requires a nonblocking no-follow temporary-root descriptor lease transferred from materialization into preparation and released only after the complete attempt, live-contention admission mapping ahead of receipt interpretation, exact unlock/close, and a nested physical live-receipt/residue/recovery control; `exactFixtureRoster` requires compatible-partial preflight under that lease, exclusive attempt-temporary staging even for an empty seed roster, atomic no-clobber publication, exact staging retirement, crash-residue preservation, and a final exact roster; `closeFailureRetention` binds pointer-owned close-once types, all six start-error endpoints, all three successful parent writers, all three terminal readers, early-readiness first-close/cached-reuse ordering, fixed diagnostics, and function-specific durable teardown projection; and `specialModeRefusal` binds the helper implementation, production refusal sites, exact three-mode and three-location roster cardinality, nested cross-product, and exact hostile-path assignment. `exactEnvelope` separately binds a real constructor-built HTTP source, a physical inventory snapshot, production preflight, exact formula equality, production frames populated from profile-maximum raw segments under conservative framing allowances, twelve near-ceiling canonical bodies, unique aggregate accounting, an aggregate-minus-one refusal, and a sixteenth-body refusal. Their hostile mutations live inside existing defensive-test cases. Seed, scope, close, envelope, and phase-context controls remain nested beneath the frozen top-level roster; the profile gate uses nested pure CLI/HTTP source/profile construction plus static recovery-path chronology. The exact `4/9/9/22/5` profile count remains unchanged.

The frozen qualification catalog contains 56 cases at `sha256:08db7c338eb3ba900cf6df2545c04f27bed16889c81dd16e5fb18197c4d52958`. The final ledger remains exactly 79 commands and claims: 76 `tests-pass`, then three `command-succeeded`, including three complete cumulative verifier passes. No C5 grade exists until the exact ledger passes on one staged tree, the commit and didrun note exist, strict verification exits zero, the exact-commit HTML report is generated, and the ledger is archived unchanged.

The final ledger is also an operator-security boundary. Its exact 35-path tree must be staged before command 1; the source-final/diff-integrity and credential-scan tail must be rehearsed in development after staging; all 79 wrapper events must come from one stable launcher context; and that launcher's persistent outer umask must be `077`. The generated preparation fence cannot set the umask for command 1's later `.didrun` creation because every fence is an isolated subshell. C5's reference-inventory test fixture therefore normalizes each freshly written file to its declared `0644`/`0755` mode and freshly `Lstat`-checks those facts rather than inheriting the caller umask. A nonzero strict result permanently fails that commit/note/ledger and requires repaired source plus fresh evidence, claims, commit, and seal; unchanged strict retries do not become green.

The first reconciled-tree cumulative pass completed rows `1–40/61`, then failed at direct inherited row `41/61`, `architecture-p07b-c-c4`: its owner-acquirer check admitted only C4's sealed two-row projection while the current C5 tree correctly adds the HTTP owner. The projection-compatible C4 check now admits exactly either that sealed C4 roster or the declared C5 three-row extension. Every other roster—including a partial extension, duplicate, or foreign site—remains hostile; the C5 checker separately requires the exact three-row extension and remains the authority for its HTTP semantics. The failed cumulative event is permanent development history, and the checker/documentation change resets C5's consecutive three-pass count to zero.

C4I subsequently sealed at commit `d784ace97dd03660c6c724e1cb8f838b869681ec`, tree `e38b702884b9df128b41bfa7710459dd92f901d7`, direct parent C4H `4b4686ea30e5ddc5043eee11ba24725bcce8bb94`, with subject `fix: stabilize verification surfaces`, note blob `f1633147e2943e7272abf3b9b903ac72ab274494`, and note-body SHA-256 `f221a99aaa1c0037d0de7ebe8c311bfd0ff1653434c4042f30b00a67d9c4e033`. Its exact result is `17/17 claims recorded-exact`, all grades `TREE-EXACT`, strict exit `0`, with recorded `secrets_override: true`. The exact-commit HTML is `.countershape/evidence/p07b-c-c4i-final-d784ace97dd0.html`, SHA-256 `122cfe225ea78d0498d12cafd16f18176104ad53a7ddc9bf6aa01882c182e1f5`, 10,102 bytes; the final ledger archive is `.didrun-history/p07b-c-c4i-final-d784ace97dd0/.didrun/`. The following owner-tense and `UNRECEIPTED` wording is frozen v22 pre-seal history retained for checker compatibility; the active C6R section above is the live cursor.

The owner-tense C4I section below is sealed v22 pre-seal history; its `UNRECEIPTED` sentences do not supersede the active C6R cursor.

## C4I verification-surface stability maintenance

Historical snapshot: C4I was the owner-authorized `SOURCE_FULL` maintenance boundary between exact sealed C4H and unchanged C5. C4I owns exactly these 14 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4I-A2-AST-STABILITY-MAINTENANCE.md`, `internal/world/process_darwin_test.go`, `spec/verification/p07b-c-unit-paths.json`, `testkit/processfixture/main.go`, `tools/capture-p07b-a2-human-surface.mjs`, `tools/check-p07b-a2-architecture-selftest.mjs`, `tools/check-p07b-a2-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:522af4209bde3f769a12c3943f46bd83e566977ad8f275165310962c1e697585`.

C4I's `countershape/p07b-c-unit-paths/v22` authority had 24 ordered phase rows at `sha256:3176e9a6764203afeed0ae836bd1a4974ae63887dbee8f0c1de1f66f2c93f580`. It inserted only `C4I` into `C4H → C4I → C5`; its plan transition matrix had 12 accepted and 340 rejected cases, and the independently implemented scope matrix had 12 accepted and 327 rejected cases. C4I's exact Markdown authority surface had 60 paths at `sha256:7bb2a0c222b4f568143c4bba9e3d99c8841ee0483fefc00ea3117d753be87dbc`, with 930 hostile rejections and 310 controls. Its authority source was `OWNER_OUT_OF_BAND`: C4H did not predeclare the new boundary, `predecessor-predeclared = false`, C4I inherited or rewrote no C4H grade, and the owner's instruction was not cryptographically authenticated.

The blocking recurrence occurred after the cumulative verifier completed its long Go lanes: one JavaScript digest drift sent all six profiles through one nested AST worker, including unchanged large profiles, and the worker reached its existing deadline before the already-detected expected violation could be reported. C4I still performs bounded stable reads and SHA-256 checks over all six profiles. Exact bytes skip AST analysis; only drifted profiles enter the worker. Generic import, binding, dynamic-edge, and export comparisons use only returned shapes, and profile-specific checks execute only for returned owners. The worker result is a closed ordered protocol: exact ordered AST rows must equal the requested path roster, with malformed, duplicate, missing, foreign, or out-of-order rows rejected. `P07B_A2_JS_AST_QUERY_FAILED` is fatal infrastructure failure before expected-hostile matching.

The defensive self-test preserves the complete 117-case roster at `sha256:832d98ce1e73056e3133a9b6cc31d1ef3da4c88a451f85f122d35ab38b05c80e` and pins 32 mutant-to-profile bindings at `sha256:16bf1f1a46090abc88d87d27726aa182c0cbe3977a52042db1c829bb58a85442`. Clean and non-JavaScript cases require zero AST rows; each relevant mutant requires its exact singleton row; one three-profile hostile requires one exact ordered batch; and two independent worker-infrastructure controls prove outer failure precedence and end-to-end invalid-JavaScript refusal. These are semantic and infrastructure controls, not a unit-test substitute for stability.

The second final attempt completed nine green claimed events, then failed its first cumulative pass at `human-surface-p07b-a2-2-check`: the fixture requested `0644`, inherited mandatory caller `umask 077`, created actual `0600`, and correctly refused mode drift. Those nine claims are permanently archived and nonreusable. The human-surface fixture creates below private mode-`0700` roots, then normalizes only newly created files to exact mode `0644` after exclusive creation and before exact type/link-count/mode/byte validation. Its synchronous self-test covers caller umasks `077` and `022`, hostile preexisting regular files, final symlinks, directories, induced partial failure, unconditional umask restoration, and root cleanup. The enclosing roots preserve cross-user privacy while the fixture is built; this path-based test helper does not establish hostile same-UID replacement resistance, a general materializer, or a product security boundary.

The third final attempt passed and claimed events `0–10`, then the unclaimed second cumulative verifier failed because the `setpgid` escape child did not publish its PID before original-group teardown. The repaired test-only fixture uses atomic, canonical, predecessor-digest-bound `PREPARED → AUTHORIZED → READY → RELEASE → RELEASED` records. Cleanup authority and an independent original-group observation exist before escape authorization; exact stdout/stderr markers plus independent post-escape PGID and `setsid` SID observations prove readiness; `RELEASED` binds the exact release request before voluntary exit. The `15 s` self-lease starts before `PREPARED` becomes visible, and bounded reap checks avoid identity-unsafe numeric-PID fallback. The unchanged `100 ms` product timer starts before the group-owned callback and is not reset, but the callback may delay physical teardown while it establishes the test precondition. This is causal test-fixture proof, not stronger product containment or wall-clock enforcement.

The final proof therefore requires one direct human-surface caller-umask pass, three standalone A2 passes, exact `world-lifecycle-readiness-20`, and three complete cumulative passes on one exact staged tree, after the plan/candidate/ancestry/unit-scope gates and before exact stage, credential, and preseal checks. The 17-event ledger has 14 `tests-pass` claims and three `command-succeeded` claims; render it with `--print-final-runbook C4I`. Every C4I fence sets `umask 077` inside its own isolated subshell before evidence or repository mutation, and the renderer self-test rejects a missing, duplicated, or misplaced C4I fence umask. Direct/general, sensitive, and nested Go work remain at `p=1`; `GOMAXPROCS=2`, `-parallel=2`, fresh private caches, the exclusive lock, the fixed 60/90-second verifier deadline hierarchy, package partition, all assertions, and cumulative verifier order remain exact. C4I changes Go test/test-fixture bytes but no production process implementation and establishes no HTTP, containment, confidentiality, production-readiness, adoption, or maintainership claim.

C4I later sealed at commit `d784ace97dd03660c6c724e1cb8f838b869681ec`, tree `e38b702884b9df128b41bfa7710459dd92f901d7`, with 17/17 `TREE-EXACT` grades and strict exit `0`. Those grades bind C4I only. C5 remained blocked through the later C4K, C4J, and C4L maintenance boundaries; all four boundaries, C5, C5V, C6A, C6F, C6G, C6M, and C6N are now sealed, while C6R is active under v30. C5's 79 labels/types/argv/order, 56 qualification cases, and three cumulative passes remain unchanged.

## Sealed C4K C3 Go-timeout maintenance — historical snapshot

C4K sealed at commit `8c1ee2df955055ee40eec1f14f0c4d96f97d5361`, tree `99fe7082d05c96647a144785e0ddc5af1a09f52f`, note blob `2d039109e269968e2314934fb4e2c4cccb31a097`, and note-body SHA-256 `cc276493b0d67bb9903d30591aaa99ed29df09d33154bed2f81faef717435766`, with `12/12 claims recorded-exact`, every grade `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. The owner-tense and `UNRECEIPTED` statements below are frozen v23 pre-seal history retained for checker compatibility; they do not supersede active C6R.

C4K owns exactly these 11 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4K-C3-GO-TIMEOUT-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-architecture-selftest.mjs`, `tools/check-p07b-c-architecture.mjs`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:9ab25f722aeaae583d08df271d6162c873254c51799b36b03fc85ff64d78567e`.

The `countershape/p07b-c-unit-paths/v23` authority contains 25 ordered phase rows at `sha256:6996cec6d83a380b82c912aebea52797984fadacdc0c9a053cf69c79f90e0a45` and inserts `C4I → C4K → C5`. Its plan transition matrix has 13 accepted and 380 rejected cases; the independent unit-scope matrix has 13 accepted and 367 rejected cases. The current self-receipt corpus has 61 Markdown paths at `sha256:8a2ffeb031c9073ae37163843cfe5e9fd8cf6b0dc0c280ff6839eed6a4bf0d5a`, with 945 positive-form rejections and 315 controls.

The generated structural catalog has 1,100 cases at `79e50d951dda94997c4cb26b48fbdc8225106bb9848a2f4db25f47aac7522390`, split into 625 accepted controls and 475 hostile rejections; six fixed semantic regressions extend it to 1,106 at `9463e6078487c74bc8bff2d60873b5529f50f0f7b59f17169fdf82312d4fff89`, split into 631 accepted controls and 475 hostile rejections.

Only exact `c3-official-target` receives `-timeout=12m`. The nested verification ceilings are `720 s → 900 s → 1080 s → 1200 s`: Go package deadline, outer C3/C4 profile process ceiling, clean C3 defensive self-test ceiling, then the unchanged cumulative-verifier child ceiling. The other four C3 profiles and all six C4 profiles receive no inner flag. Argument- and envelope-level defensive checks prove exact placement, nonleakage, all four values, and strict nesting margin. C4K changes no product Go byte, retry, package partition, parallelism, cache policy, assertion, or cumulative-row order.

Qualification is deliberately multi-pass: the focused C3 checker proves the historical profiles with the repaired official-target invocation; the C3 defensive self-test proves exact argv and parser behavior; one complete cumulative verifier proves the repair in the full current ordering. Those execute after candidate and ancestry gates and before exact staged scope, credential scan, and preseal reconciliation.

The exact C4K boundary uses a 12-event ledger with 9 `tests-pass` claims and three `command-succeeded` claims. Render it with `--print-final-runbook C4K`. C4J remains parked and C5 remains owner-blocked until this boundary closes; neither the v23 phase table nor this document grades the C4J checkpoint.

## Historical C2 receipt-phase checker maintenance

The first C2B development self-test exposed a lifecycle defect: four hostile mutations that belong only to the absent-receipt phase were still applied to live receipt-present documents. The plan checker correctly changed branches, but the test continued to demand old phase errors; one digest mutation became invisible to that branch and the pending-grade replacement became a no-op. The failed development event remains unclaimed and permanent.

C2M preserved, rather than skipped, those tests. The ordinary top-level plan baseline checked the real working tree, including the receipt-present working-tree baseline during C2B. The existing synthetic receipt suite exercised declaration, Git/didrun authority, local evidence, documentation agreement, and self-receipt failures; a receipt-present synthetic full-plan baseline and hostile handoff-identity mutation proved that top-level `checkPlan()` wired that branch before C2M sealed. A second full-plan fixture made the C2 receipt path return a synthetic, `readFile`-equivalent `ENOENT` signal, reopened the exact sealed-C2 status blob from commit `19c90786d9832e21ec86daea96ca514cc7304836` after re-proving tree `1557f1e26762d717327a2ec770f8153daa0baf3e`, removed at most one exact C2 receipt block from the current handoff, and required a clean full `checkPlan()` baseline. It then ran the same four pre-receipt mutations with exact-one anchors and their exact phase-specific errors. Empty or malformed JSON was never treated as absence, and an absent or duplicated mutation anchor failed the test.

The v4 unit scope inserted C2M between C2 and C2B. Source-final diff integrity and the named staged credential scan were admitted only for an explicitly declared empty-prefix, exact-roster `SOURCE_FULL` unit; prefix-bearing source units and receipt profiles remained ineligible for these exact-roster gates. C2M owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C2-RECEIPT-PHASE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:75dcb6a88a9faec8aa89cfbe67c0870e88d95709a1bf07e2fb494e92362688cd`.

This was a full checker-authority maintenance boundary, not a docs-only receipt exception. Its final ledger ran plan coherence, the dual-phase checker self-test, unit-scope self-test, cumulative verifier self-test, cumulative verification, exact seven-path source-final gate, scoped staged credential scan, and exact preceding-chain verification. C2 and its evidence stayed immutable; C2B's three paths, digest, labels, and narrow profile stayed unchanged and resumed only after C2M independently committed, sealed, proved its note, and verified strictly.

## Historical mixed didrun object namespace for C1 local evidence

The C1B development gate exposed a checker defect before freeze: the pinned ignored C1 ledger stores two intentional object namespaces under `.didrun/objects/`. Flat `objects/<64-lowercase-hex>` files are didrun stdout/stderr/transcript capture blobs named by SHA-256 of their raw bytes. Fan-out `objects/<2-lowercase-hex>/<38-lowercase-hex>` files are zlib-compressed Git loose objects redirected there while didrun computes working-tree identities in this SHA-1 repository. The existing objects-only SHA-256 manifest continues to bind the stored path, size, and raw bytes of both namespaces together.

Local evidence validation is reject-by-default and resource-bounded before content reads. Flat names must match raw SHA-256. Fan-out files must consume one complete zlib stream of at most 4 MiB inflated bytes without trailing input, expose a canonical Git object header and exact declared payload size, and match the SHA-1 object identity encoded by their path. The archive directory set must equal `objects/` plus the nonempty two-hex fan-out parents implied by admitted loose-object files; empty `pack`/`info`, alternates, and every foreign or extra-depth directory fail. Every other namespace, malformed stream, over-limit inflation, header/size mismatch, identifier mismatch, observed symlink, or nonregular entry also fails. Session stdout/stderr/transcript references must be valid flat identifiers, and their set must equal the flat capture-blob inventory. SHA-1 here is the repository's Git object-format identity; archive integrity remains the declared SHA-256 manifests. This exact C1 observation is a static local snapshot under Countershape's sole-writer protocol; it is not hostile same-user isolation against concurrent namespace replacement, a portable didrun layout, or a strict-witness guarantee.

The retained maintenance-only `--verify-sealed-c1-local-evidence` mode requires the C1B receipt declaration to be absent and crosses the already-pinned ignored C1 archive without enrolling it. C1B later used `--verify-c1-local-evidence`, which requires that declaration to be present, to bind the same local snapshot to its tracked declaration. Declaration-bound mode reads one bounded no-follow UTF-8 receipt snapshot and passes those exact bytes through plan/authority validation and evidence consumption; it never validates one receipt read and consumes a second. Neither mode makes the HTML or ignored archive portable, grades a receipt by itself, establishes security, or authorizes publication.

C1E owns exactly these 7 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/VERIFICATION.md`, `docs/status/P07B-C-C1-LOCAL-EVIDENCE-MAINTENANCE.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, and `tools/check-p07b-c-unit-scope.mjs`. Their sorted-newline roster digest is `sha256:9a6422b45c82a44e6171ae9351468df5f6ece2190d6ab47f26aec6f84fd7ccde`.

C1E was a full source-maintenance boundary between sealed C1V and unchanged C1B. It ran the mixed-namespace hostile self-test, the real sealed-C1 local archive check, unit-scope self-test, complete cumulative verifier, exact staged/diff gate, named-pattern scan, and preceding-chain audit from a fresh frozen-tree ledger before commit/seal/note/strict closure. Its successful source receipt was not reused as a C1B receipt.

## Output contract

The stable output classes are:

```text
COUNTERSHAPE_VERIFY_V1 ... general_jobs=1 nested_jobs=1 gomaxprocs=2 ...
AUTHORITY ...
ROSTER CURRENT ...
ROSTER ROOT_AUTHORITY live=... sealed_c6a=...:...
HISTORICAL-ONLY NOT-RUN ...
CURRENT [NN/NN] START ...
CHILD ...
CURRENT [NN/NN] PASS ...
RESULT PASS current=N/N historical_not_run=M
```

On failure, the failing row is `CURRENT ... FAIL`, no later current row is spawned, and the summary is `RESULT FAIL`. The terminal `RESULT PASS` is printed only after the current resource-finalization row has removed the private run root and released the verifier lock; cleanup or release failure therefore cannot leave an overall PASS marker. `HISTORICAL-ONLY NOT-RUN` means exactly that; it is not a current-tree compatibility claim.

## Sealed C4J bounded C5 verifier timeout maintenance — historical snapshot

C4J sealed at commit `c12c927d94e6c56529a2fb90148674b4b4e731a5`, tree `c99c1f394e02f17f8cf9de6b18f1a949bcaa07a8`, note blob `f76ca73665f8cd3f19fdb9c7d4537462390a2200`, and note-body SHA-256 `a6b6191547efccf168d18932d559cba5a896c8dcde1f64ede72a27844f495cf1`. Its 13/13 claims are `TREE-EXACT`, strict verification exited `0`, and `secrets_override: true`. The pre-seal tense below is frozen v24 history and does not supersede active C6R.

C4J owns exactly these 19 paths: `docs/HANDOFF_MODE_C.md`, `docs/PROMPT_PACK.md`, `docs/THREAT_MODEL.md`, `docs/VERIFICATION.md`, `docs/prompts/P07B-C-TARGET-RUN-EXECUTION.md`, `docs/status/P07B-C-C4J-C5-VERIFIER-TIMEOUT-MAINTENANCE.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/01-empirical-failure.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/02-runtime-authority.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/03-governance-phase-machine.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/04-receipt-runbook-impact.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/05-synthesis.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/06-red-team.md`, `research/deep-dive/p07b-c-c5-timeout-maintenance/07-executive-briefing.md`, `spec/verification/p07b-c-unit-paths.json`, `tools/check-p07b-c-plan.mjs`, `tools/check-p07b-c-unit-scope.mjs`, `tools/verify-current-selftest.mjs`, `tools/verify-current.mjs`, and `tools/verify-runtime-authority.mjs`. Their sorted-newline roster digest is `sha256:c0920287c9dc9998f0cae11f8beced9d134c74da786a10f1886b03b2a1a50239`.

Machine authority is `countershape/p07b-c-unit-paths/v24` with 26 ordered phase rows at authority SHA-256 `30530a2e05fa6b7fb74983756c24f22a898f4a6da6df75d447df17278b0152db`. The plan transition matrix is 14 accepted and 422 rejected; the independent scope matrix is 14 accepted and 409 rejected. The current Markdown authority corpus has 69 paths at `sha256:a53acdeef0e86b4865caab7822ea06cfa62742794ff1aaa5816023ef172cf901`, with 1065 positive-form rejections and 355 controls. The child runtime defaults to `1200000` ms and admits `1800000` only for `architecture-p07b-c-c5` and `architecture-p07b-c-c5-selftest`, selected from a parent-owned exact policy outside the step roster. Invalid shapes fail before spawn; valid execution retains one spawn, both tool revalidations, identical cwd/env/encoding/output ceiling, and no retry.

The exact historical C3 timeout-authority marker remains `const childTimeoutMS = 20 * 60 * 1000;`; C4J derives `defaultChildTimeoutMS` from that one constant and keeps the 30-minute value separate. This preserves sealed C4K's source-level C3 envelope inspection while making the default/extended policy distinction explicit.

Exact sealed C4K is C4J's direct parent. The frozen C4J claim labels and `--verify-c4j-sealed-c4i-note` command spelling remain byte-exact compatibility names: the implementation first proves exact sealed C4K, then proves the named sealed-C4I and lower chain transitively.

Exact sealed C4K has a boundary-local legacy double-marker GOCACHE projection in all 12 exported note previews. Its validator admits only the raw canonical token or that exact double marker at hermetic position 6, while the pinned note blob/body digest, full argv cardinality/order, other position-specific projections, command tails, labels, types, and grades remain exact. A suffix-preserving, bare, foreign, shifted, or tail-redacted form fails. The ignored raw final ledger corroborates the canonical `.countershape/p07bc-c4k-final/gocache` assignment but is not portable authority; the admitted marker is compatibility with the immutable exported note, not hidden-value attestation or a rule for C4J's future note.

Sealed C4K predeclared restoration of the exact C4J checkpoint in prose, while v23 did not machine-declare C4J. The live record therefore distinguishes `predecessor-predeclared = true (sealed prose only)` from `machine-predeclared = false`; no external signed authorization artifact exists, and no C4K grade is inherited or rewritten.

The final C4J evidence is a 13-event ledger: 10 `tests-pass` claims and three `command-succeeded` claims. It contains the targeted runtime/verifier self-test, repetition-runner self-test, and three complete cumulative passes before source-final, credential, and preseal gates. Render the exact sequence with `--print-final-runbook C4J`. Earlier C5 pass/timeout logs are diagnostic only and cannot substitute for these current-tree receipts.

Exact sealed C4J itself exported the legacy double-marker GOCACHE preview `«redacted:high-entropy».«redacted:high-entropy»`. The v25 consumer admits that projection only under C4J's pinned commit, tree, subject, note blob, note-body digest, source partition, claim roster, complete coverage, direct C4K parent, and lower ancestry. Generic, lookalike, and future notes retain their own declared position-specific redaction policy. This is immutable-note compatibility, not hidden-value attestation.

## C4L non-product maintenance and operator-provenance bootstrap

Historical snapshot: C4L is the now-sealed exact thirteen-path, empty-prefix `SOURCE_FULL` child of sealed C4J, with roster digest `sha256:76c8ecfba55dd8833333c0f6180bf1df38f1a7cba65373feaea281add963b1b3` and subject `fix: bootstrap non-product maintenance authority`. It sealed at commit `32911730afb7cc717b8e0a164b9ea0038d83f3fa`, tree `b71bbed9ca6044fa57fbb6c24379f3383f49689a`, note blob `1c03b8ff2cca98bc46aea1990945b8bfeeae2ec9`, and note-body SHA-256 `ae41dda6bc1f570b67332763dcb88a892b690b797115275822361d28484925f6`, with `13/13 claims recorded-exact`, every grade `TREE-EXACT`, strict exit `0`, and `secrets_override: true`. Its exact-commit HTML is `.countershape/evidence/p07b-c-c4l-final-32911730afb7.html`, SHA-256 `7ce724953c5d43252a83989cb0723d0daea1513aef40fe8a5875a44d572160db`; its final ledger archive is `.didrun-history/p07b-c-c4l-final-32911730afb7/.didrun/`. It changed no production Go, generated Node product, product schema/example, qualification catalog, runtime/verifier implementation, package partition, timeout, cache, parallelism, retry rule, or cumulative row. Its `OWNER_OUT_OF_BAND` provenance is `UNEVIDENCED`; authentication is not established and signed authorization is not implemented.

The v25 `NON_PRODUCT_MAINTENANCE` token is intentionally dormant. C4L
installs only its closed row grammar and partial scope primitives; no v25 row
consumes it, and there is no generic narrow runbook or preseal path. A later
`SOURCE_FULL` authority migration must predeclare the concrete child after a
`SOURCE_FULL` parent, bind its exact argv and claim manifest, add end-to-end
candidate/stage/preseal fixtures, and keep verifier/runtime implementation
outside the admitted roster before the profile is usable.

The final C4L ledger is a 13-event ledger: ten `tests-pass` followed by three `command-succeeded`. It covers candidate plan and independent transition authority, plan/profile/scope/verifier defensive self-tests, exact sealed-C4J legacy-note and lower ancestry compatibility, three complete cumulative passes, exact thirteen-path staged scope/diff integrity, credential scan, and preceding-chain integrity. Its historical runbook is rendered with `--print-final-runbook C4L`; ancestry is `--verify-c4l-sealed-c4j-note`, and the final root is `.countershape/p07bc-c4l-final`. Those exact C4L grades are sealed; they do not receipt sealed C5, sealed C5V/C6A/C6F/C6G/C6M/C6N, or active C6R.
